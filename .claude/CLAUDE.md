
## 复验协作节奏（用户亲定 2026-07-06·钉死）

**火力全开·多 agent 扇出（默认）**：大验收 / 复杂排查时，默认把一个任务拆成多个子代理并行啃（不同维度各一个：如代码审查/契约核对/某红线专盯/多维分析），同时跑再汇总。用户明确"不心疼 token·要又快又深"。
- 判据：单点确认(红/绿、某值对不对)→多进程跑测试即可、不必派 agent；需独立深度判断或多维覆盖(大 WO 验收、系统性排查、根因追查)→**默认扇出多个子代理**，一维一个，并行。
- 反例(仍单线)：纯机械 curl/跑测试、琐碎单文件改。
