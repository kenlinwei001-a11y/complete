# HANDOFF ① · 三板块对齐设计母版（驾驶舱/规划决策推演/项目决策推演 · 施工/评审合同）

> **三 HANDOFF 之一**（本份=跟 HTML 母版比对的板块对齐；另两份：②主题开关 ③可信溯源交互，分开）。
>
> **一句话目标**：把三板块从"各自已建、多为真算"补齐到**母版级**——差距集中在 ① 数据颗粒（八根因/毛利勾稽/provenance）② 母版的同源本体底座 ③ 母版有的母版级深度（三关联判/反事实排除/3案对比）。**融合优先：每条差距接现有后端（接/扩/换），禁新建并行前端。真推演 not 假推演（洛阳红色无数据=活体反例，红线）。**
>
> **读这两份再动手**：`AUDIT-three-boards-vs-design-master-alignment.md`（逐元素差距全表+母版数据值）、`SPEC-trust-traceability-interaction.md`（溯源交互+接/扩/换）。母版参照：上传的设计 HTML（5540 行,单文件 SPA）。

---

## 0. 先读什么（动手前）

1. **铁律0** `docs/SYSTEM-ONTOLOGY.md`：改了链路/事件/对象/不变量/门禁回写。本份触 R13(溯源)/R6(确定性)/G-3(presetContext→QOS)/G-10(规则一等)。
2. **AUDIT**（逐板块差距全表）+ **SPEC-trust**（溯源融合）。
3. **增量0 永远先做**：起 demo 真系统、**真浏览器逐板块走一遍 + 实拍定基线**（只看不改）——尤其亲手复现"洛阳红色→受影响订单暂无数据"，确认哪些推演是真/假。
4. `.claude/skills/fde-delivery`：完成=亲手用一遍能用+实拍,不是测试绿。

---

## 1.《母版 ↔ 现状 ↔ 设计》逐板块（融合优先 · 接/扩/换）

> Oracle=本表。每行带母版元素、现系统锚点、**融合决策（接现有/扩现有/换，禁新建并行）**、真值判据（钉 demo 真态）。详尽差距见 AUDIT。

### 1.A 经营驾驶舱（`DashboardView.tsx`）
| 母版元素 | 现状·锚点 | 融合决策 | 真值判据 |
|---|---|---|---|
| 待解决问题 **8 根源** | 根因种子 **4 条**(`battery.ts:1512-1517`) | **扩**：同结构补 4 条根因链(crm/push/frame/lta) | demo 问题面板出 8 类根源-问题 |
| 综合毛利率**归一勾稽**(Σ=−0.4 闭合) | 仅旧 `margin_attribution`(`service.ts:533`) | **先判接/扩/换**：能扩其输出做逐单贡献勾稽就扩，真不能才换；**不盲目另起** | 台账综合毛利率=逐单贡献求和勾稽闭合 |
| 八卡 **provenance**(公式/输入/来源/新鲜度/规则号) | `Provenance`/`RuleRef` **已建**未接全 | **接**`Provenance`+`RuleRef` 到每卡每数字 | 逐卡悬浮出公式+输入+来源+规则(可溯) |
| **AI 对话栏**(板块级,基于实时数据) | 驾驶舱无;但 **AgentCore QOS 已有**(路径A/B+SSE) | **接**：AI 栏=QOS 入口注入 presetContext(**补断点 G-3**),**不新建聊天** | 驾驶舱 AI 栏问"本月最大风险"→QOS 真编排出答 |

### 1.B 规划决策推演（`PlanAuditView`/`PlanGenerateView`/年/季）
| 母版元素 | 现状·锚点 | 融合决策 | 真值判据 |
|---|---|---|---|
| 根因 DAG **反事实排除层**(单价/BOM 反算达标→排除) | `KsfGraph` 3 层(问题→KSF→财务) | **扩**：根因求解器输出加"已排除因子+理由" | 点 miss KPI 出含"反事实排除"节点的 DAG |
| **项目级聚合勾稽表**(负+正=−0.4 闭合) | 缺 | **扩**(同驾驶舱毛利勾稽,同源) | 根因下出聚合表勾稽闭合 |
| 规划建议 **3 案**(五维雷达+取舍矩阵+外部敏感性+风险传播链) | `plan_generate` 在,渲染偏简 | **扩** `plan_generate` 输出+前端补对比 | 输入目标出 3 案,每案雷达+取舍矩阵 |
| 体检 X01-X05 命名校验+一键 fix+最终修正表 | `plan_audit` 出 H/M/S 三段 | **扩**：补命名校验 ID+fix action+修正规划表 | 改输入即重检,出 X01-X05+每项一键 fix |

### 1.C 项目决策推演（`ProjectSimView`/`RiskBoardView`）
| 母版元素 | 现状·锚点 | 融合决策 | 真值判据 |
|---|---|---|---|
| **订单驱动三关联判**(交期/齐套/财务→4 态 verdict) | ProjectSim 偏型号驱动;`OrderChainView` 有三判表但规则裸渲染 | **接**：三判=现有 `capacity_forecast`+MRP+credit+规则编排,**不重写判定** | 选一单出 ①交期②齐套③财务 三判+verdict+对冲条件 |
| **问题传播时序**(逐日传导→财务击穿) | `PropagationTimeline.tsx` **孤儿未挂载**+系数写死(`:60,96`) | **接**：挂载+财务系数取 `risk_timeline` 真值,**去写死** | 风险/规划页挂传导时序,系数来自求解器 |
| **🔴 风险卡红/黄越线日**(基地·因素) | `riskTimeline`(`risk.ts:224`)基线缺真数据退 `mockTightness`(哈希),**返回无 dataMode** | **扩**：riskTimeline 逐卡透 `dataMode/live`;**接**前端显"估算/无数据"标 | 见下红线 R1 真值判据 |

---

## 2. 增量（串行 · 每增量一 PR · 先证基线再动）

- **增量0（零代码·取基线·必先）**：demo 真跑三板块,**实拍 + 逐推演结果标"真算/半真/假"**(尤其复现洛阳红色无数据)。存 `docs/evidence/three-boards-baseline.md`。这是"补完没把真的改假/没新建并行"的标尺。
- **增量1（项目板块·真推演红线优先）**：修 1.C 三行——尤其 **R1 假推演**(riskTimeline 透 dataMode + 前端标 + 红色须真数据支撑或诚实降级)。先修这条,因为它是"可信"地基。
- **增量2（驾驶舱数据层）**：1.A 四行(八根因/毛利勾稽/provenance 接全/AI 栏接 QOS)。**高回归项(毛利勾稽求解器)独立 PR + FDE 逐值核**。
- **增量3（规划板块）**：1.B 四行(反事实排除/聚合勾稽/3 案/X01-X05)。
- **增量4（同源底座·可选升级）**：决策应用域 5 一等对象(AUDIT §4)——若做,**扩 A4 本体**让三板块同源,**不新建并行数据层**。牵动大,可拆独立 HANDOFF。

> 任一增量发现要"换"现有后端或牵动跨系统 → **停手报审核方**,别擅自重写。

---

## 3. 红线（破一条即打回）

1. **🔴 真推演 not 假推演（最高红线）**：mock/哈希(`mockTightness`)/写死**不得冒充真算**。凡推演结果(红/黄状态、紧张度、数字)：① 有真数据→真算；② 无真数据→**后端透 dataMode + 前端显"估算/无数据"诚实标**,**绝不渲染成与真算无差别的红/黄**。**洛阳红色却受影响订单空=当前活体反例,必修**。
2. **融合接/扩/换 · 禁新建并行**：每条差距先接现有(`RuleRef`/`Provenance`/`DagNodeDrawer`/现有求解器);扩须回写本体;禁第二套展示组件/数据层。
3. **无不可溯源数据 + 无下钻死路**(承 SPEC-trust)：规则号接 RuleRef、数字接 Provenance、下钻 modal/面包屑。
4. **R6 确定性 · RL5 零业务常数**：同 seed 字节一致,无 Date.now/random,换租户=换配置。
5. **FDE**：完成=真浏览器走一遍+实拍,数字能对到后端真值;不认测试绿。

---

## 4.《本体引用与影响》（回写本体）

- **不变量**：R13(溯源——provenance 接全+规则 provenance)/ R6(确定性)/ RL5(零业务常数)。
- **断点**：**G-3**(AI 栏注入 presetContext→QOS,推进其状态)/ **G-10**(规则一等→RuleRef 接全)/ 新识别"假推演"风险(riskTimeline 无 dataMode)→建议在 §8 记一条门禁待建 `genuine-sim:check`。
- **事件/对象**：决策应用域 5 一等对象(若做增量4)→回写 §2/§3。
- **求解器输出 shape 改动**(riskTimeline += dataMode；margin 勾稽；plan_audit/generate 扩)→回写 §3 求解器表。

---

## 5. 评审协议（我怎么验 · 不认口头）

- **两轴**：轴1 对 §1 表逐行(母版元素是否真补);轴2 对 demo 真态(真值判据)。
- **FDE 真跑**：我亲手起系统 → ① 复现洛阳:修后点红越线日**必须**要么有真受影响订单、要么诚实标"估算/无数据"(不再裸红) ② 全仓抽查推演结果:红/黄/数字能对到真数据或诚实标 ③ 任意 C0x 悬浮出规则详情 ④ 八根因 8 类 ⑤ AI 栏真走 QOS。
- **融合核**：抽查新增是否**接了现有**(没另起 RuleRef/求解器/数据层)。
- **判定**：任一红线破/真值判据不达=打回。**特别:再发现一处"假推演冒充真"=打回。**

---

## 6. 完成判据（FDE · 用户视角）

demo 真跑:三板块**所见即可信**——① 任何红/黄/数字**要么真算可溯、要么诚实标估算**(洛阳不再"红了却无数据") ② 八根因 8 类、毛利勾稽闭合、provenance 处处可溯 ③ 规划出 3 案对比+反事实排除 ④ 订单三关联判出 verdict ⑤ AI 栏接 QOS 真答 ⑥ 全程接现有后端、无并行新造。**母版级"一个事实一个出处"成立。**

---

## 7. 禁止清单

❌ mock/哈希/写死冒充真推演(不透 dataMode 就渲染红/黄) ❌ 新建并行展示组件/数据层(该接 RuleRef/Provenance/现有求解器) ❌ 下钻死路 ❌ 裸渲染规则号/数字 ❌ 盲目"另起求解器"不先判能否扩 ❌ 代码先行本体不回写 ❌ 业务常数硬编码 ❌ 拿测试绿冒充能用。

> 契约生效：从增量0 起逐增量提 PR,我逐 PR 按 §5 评审。**有疑义先问,越红线先停。**
