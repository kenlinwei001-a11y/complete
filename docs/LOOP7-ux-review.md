# LOOP 第 7 轮 · UX 产品经理评审 —— 决策台（`/v/decision-console`）

> **日期**：2026-08-28 · **角色**：UX 产品经理（交互便利性 + 设计品味）· **零代码改动**，本文件是唯一新增物
>
> **评审对象**：`apps/frontend-shell/src/views/sim/DecisionConsoleView.tsx`（1894 行）
> ＋ `DecisionConsoleView.module.css`（727 行）＋ `decisionConsoleModel.ts`（495 行），
> 分支 `origin/claude/handoff-wo-decision-console`。
>
> **取证方式（硬规矩：真实后端，零桩）**
> · datacore 内存模式 `SEED_DEMO=1 seed 42`，端口 **4777**（起服务前逐个探端口，
>   4131 / 4312 / 5199 当时被别的 agent 占着）
> · 前端 `pnpm --filter frontend-shell dev` + `VITE_DATACORE_URL=http://127.0.0.1:4777`，
>   **`VITE_MOCK` 一次都没设**，`env.mock` 全程为 `false`；探针抓到的每一条请求都打在 4777 上
>   （进页请求：`auth/refresh` · `me/workspace` · `history/watermark` · `data-health` ·
>   `sim/drill/catalog` · `sim/sessions`，全部 200）
> · Playwright headless Chromium（`/opt/pw-browsers`，`--no-sandbox`），视口 1600×900，
>   登录 `demo / admin / demo1234`
> · 探针 `lib/drive/p1–p9/m1–m19.mjs`、截图 `S01–S47`，全在
>   `/tmp/claude-0/-home-user-complete/3f5e96d7-59cd-5a3f-aa1a-9551fc6f8f15/scratchpad/l7/`
>
> **本文所有数字都是我今天亲手量的**，屏上原文逐字抄。凡引用别人的报告我会点名，且只当线索不当结论。
>
> **本体引用与影响**：本文只读不改，不新增/不改变链路 / 事件 / 对象类型 / 不变量 / 门禁，
> 不触发本体回写义务。禁令 3 遵守：**本文不新增任何门 / 棘轮 / 基线 JSON**。

---

## 一句话结论

> **像。这一屏可以拿出去。**
>
> 判据不是感觉，是三条可数的：**866 个 DOM 元素、526 个带文字元素，三套主题下小于 12px 的
> 一个都没有**；**全量对比度扫描只有 1 类元素低于本仓自己的门判据**（选中态的筛选钮，
> 三套皮全中）；**假控件 0 个、无名字的按钮 0 个、空表头 0 个**。
> 屏上最大的那个数是 `63 亿 · 这 30 天交不出去的货` —— **最大的数就是 COO 最需要的那个数**，
> 这是本仓九个月来第一次做到。
>
> **但它还不该现在就当主线放出去，有 4 条阻塞项**（见 §7），其中两条是**它自己写的规矩它自己没守**：
> 第一层出现机器编号 `pos_lfp_b2`；屏上出现 4 处没被渲染掉的 markdown 星号 `**…**`。
>
> **最大的风险不在这一屏里，在它隔壁。** 我今天顺手复测了 `/v/sim-optimize`：
> **263 个元素小于 11px、假英文菜单 `File Edit View` 还在、〔应用方案〕仍是
> `<div tabIndex=-1 cursor:auto>`**。演示时从决策台点两下走到那一屏，前面攒的信任当场清零。

---

## §0 · 先自证工具（铁律 0.6：报否定结论前必须先跑金丝雀）

本文要报四类否定结论：「0 个元素 < 12px」「0 个假控件」「0 个空表头」「没有撤销」。逐类金丝雀：

| 金丝雀 | 今日实测 | 撑住哪条否定结论 |
|---|---|---|
| **字号探针认得出字** | 主按钮〔算一下〕抓到 **16px**；同一次扫描抓到 42 / 20 / 16 / 15 / 14 / 13 / 12 / 12.5 共 8 档 | 「0 个 < 12px」不是探针瞎了 |
| **扫描分母非零** | `#root` 下 **866** 个元素、其中 **526** 个带自身文本节点 | 同上 |
| **对比度算法是对的** | 纯黑压纯白 = **21.00**（应 21）；纯白压纯白 = **1.00**（应 1） | 全部对比度数字 |
| **点击链路是活的** | 点模板「订单改交期」后本页 innerText **222 → 363** 字 | 「〔加进去〕点了没反应」之类都不是我没点到 |
| **词表探针能命中** | 同一个正则在同一段文本里数到 `红线`×36 · `订单`×14 · `客户`×8 | 「`tick` 0 次 / `扰动` 0 次 / `张力` 0 次」 |
| **Tab 探针真的在动** | 从页顶哨兵起连按 Tab，取到 **117** 个互不相同的 `activeElement` | 全部键盘计数 |

### 0.1 ⚠ 我自己的探针今天骗过我三次，逐条记在这里

铁律 0.5 说「grep 的结果不是结论」。我今天三次差点把**探针的 bug** 写成**产品的 bug**：

| # | 我先得到的（错的）结论 | 追一层之后的真相 |
|---|---|---|
| 1 | 「方案栏『多久见效』暗色皮 **1.49:1**，白字压白底，严重可读性事故」 | **是我的合成算法错了。** 我用 `over(fg,bg)` 把两层**都带 alpha** 的底色合成，公式假设下层不透明 ⇒ 算出一个页面上根本不存在的 `rgb(226,237,253)`。改成正确的 source-over 累积后是 **8.30:1，合格**。真正低于门判据的是**另一个**元素（`.sortBtn[aria-pressed=true]`，见 §1.2） |
| 2 | 「skip-link 对比度 **1.01:1**」 | `.skip { left:-9999px }` —— 它**没聚焦时本来就在屏外**，我量的是一个不可见元素。聚焦后实测 `rgb(255,255,255)` 压 `rgb(17,93,201)` = **6.12:1，合格** |
| 3 | 「有撤销（`hasUndo: true`）」 | 我的正则 `/撤销|取消|反悔/` 命中的是**左栏模板行「订单取消」** —— 那是一个事件类型，不是撤销按钮。**这一屏真的没有撤销**（确认条 `<button>` 数 = 0） |

三次都是同一个形态：**「我用 X 当作 Y 的证据，而 X 并不度量 Y。」**
下面所有数字都是**改正后**重跑的。

---

## §1 · 可读性

### 1.1 字号（截图 `S47-all-expanded-dark.png`）

**扫全 DOM 的 `getComputedStyle().fontSize`，六个折叠层全部展开后的最大态：**

| 主题 | `#root` 元素总数（金丝雀分母） | 带自身文字的元素 | **< 12px 的个数** |
|---|---:|---:|---:|
| 暗色（默认） | **866** | **526** | **0** |
| light（冷蓝） | 866 | 526 | **0** |
| warm（亮橙金融） | 866 | 526 | **0** |

字号分布（三套皮逐值一致）：
`12.0px×360 · 12.5px×5 · 13.0px×138 · 14.0px×12 · 15.0px×6 · 16.0px×2 · 20.0px×2 · 42.0px×1`

**判据来自本仓自己的门**：`required(S,w) = max(3.0, 72/(S×…))` ⇒ 12px 要 6.0 · 11px 要 6.55 ·
10px 要 7.2，而「10px 中文在本调色板内无解」。**这一屏一个 11px、一个 10px 都没有** ——
它整页站在地板之上，不是踩着地板。

> **对照组（同一次会话、同一套探针）**：`/v/sim-optimize` 今天 **263 个元素 < 11px**，最小 **8px**
> （`optimizer console` / `01` / `02` / `◔` / `◑`）。**同一个仓、同一天、差 263 个。**

**唯一的字号意见**：`12.0px × 360` = 全屏 68% 的文字挤在同一档。层级靠 **13/12** 一档之差撑着，
差 1px 在 1600×900 上肉眼几乎分不出。这不阻塞，但让整屏读起来"平"。

### 1.2 对比度（20 个采样点 × 3 套主题，共 60 次测量）

采样点覆盖派单点名的六类：主按钮 · 〔估〕角标 · 删除线文字 · 灰字说明 · 表格数字 · 引擎原文。

| # | 采样点 | 暗色 | light | warm | 门判据 |
|---|---|---:|---:|---:|---|
| ① | 主按钮〔算一下〕（16px/600 白字压 `--accent-solid`） | 6.12 | 6.07 | 6.05 | ≥5.54 ✅ |
| ② | 大数 `63`（42px/700） | 9.14 | 16.19 | 16.65 | ≥3.0 ✅ |
| ③ | **〔估〕角标**（12px/700） | 6.06 | 6.93 | 6.64 | ≥6.0 ✅（暗色仅富余 0.06） |
| ④ | **删除线本体 `——————`**（13px） | 6.09 | 6.95 | 6.64 | ≥5.54 ✅ |
| ⑤ | 「这次算不出来 ▸」链接 | 9.14 | 16.19 | 16.65 | ≥6.0 ✅ |
| ⑥ | **灰字说明**（`--muted`，12px） | 6.09 | 6.95 | 6.64 | ≥6.0 ✅ |
| ⑦ | **表格数字 `223.3`**（12px mono） | 9.14 | 16.19 | 16.65 | ≥6.0 ✅ |
| ⑧ | 表格表头「客户」（12px/500） | 7.32 | 9.37 | 9.08 | ≥6.0 ✅ |
| ⑨ | **引擎原文**「引用 710 条数据 …」 | 6.09 | 6.95 | 6.64 | ≥6.0 ✅ |
| ⑩ | 〔就这么办〕按钮（12.5px 白字压实心蓝） | 6.12 | 6.07 | 6.05 | ≥6.0 ✅ |
| ⑪ | **筛选钮·选中态**（`.sortBtn[aria-pressed=true]`，如「常州」「多久见效」） | **4.96** | **5.73** | **5.76** | ≥6.0 ❌ **三套皮全红** |
| ⑫ | 筛选钮·未选态 | 7.32 | 9.37 | 9.08 | ≥6.0 ✅ |
| ⑬ | 方案栏键「多久见效」 | 8.30 | 6.13 | 6.07 | ≥6.0 ✅ |
| ⑭ | 方案栏值「3 天估」 | 12.46 | 14.28 | 15.22 | ≥6.0 ✅ |
| ⑮ | 卡点正文 | 12.46 | 14.28 | 15.22 | ≥6.0 ✅ |
| ⑯ | 模板行标题 | 12.46 | 14.28 | 15.22 | ≥5.54 ✅ |
| ⑰ | 「还有一些事这里试不了 ▸」 | 7.30 | 6.48 | 6.45 | ≥6.0 ✅ |
| ⑱ | 时间线日期「第 2 天」 | 6.07 | 6.89 | 6.66 | ≥6.0 ✅ |
| ⑲ | 三段条下的分段字 | 6.09 | 6.95 | 6.64 | ≥6.0 ✅ |
| ⑳ | 「系统不给推荐」 | 6.09 | 6.95 | 6.64 | ≥6.0 ✅ |

**全量扫描**（526 个带文字元素逐个算，不只采样）：**每套主题只有 2 个元素低于门判据，
且都是同一个类 `.sortBtn[aria-pressed="true"]`。**

- 暗色 **4.96:1** = `rgb(162,198,247)` 压 `rgb(52,75,115)`（`--accent-txt` × `--primary-soft`）
- light **5.73:1**、warm **5.76:1**

**这一条是必修**（§7 阻塞项 4）。它不是装饰件：**它是「看哪个地方」那一排 8 个基地钮和
「按什么排」那 3 个排序钮里被选中的那一个** —— 全屏唯一告诉你"你现在看的是常州"的东西，
偏偏是全屏最难读的那一个。

> ⚠ 提醒排期的人：`.sortBtn` 那一对（`--accent-txt` × `--primary-soft`）是**全仓通用组合**，
> 不止这一页在用。改法应该落在令牌上，不该在这一页写死一个例外。

---

## §2 · 键盘可达（截图 `S31-focusring-dark.png`）

**方法**：在 `<body>` 首位插一个 1px 透明哨兵按钮并聚焦它，再从那里连按 Tab ——
这样「第 N 次」是从**文档顶**算的，不受上一次测量的残留焦点影响
（第一版没插哨兵，同一条链路量出 49 和 80 两个数，就是这个坑）。

| 问题 | 实测 |
|---|---|
| **到〔算一下〕几次 Tab** | **第 80 次**（未算态、已算态**同为 80**） |
| **到〔就这么办〕几次 Tab** | **第 117 次**（第一颗） |
| **首次进入本页内容区** | **第 80 次** —— 而第 80 站正是 skip-link |
| 前 79 站是什么 | 顶栏 5 站（全局搜索 / 🕐 / 健康度 / 🌙 / 用户菜单）+ 左导航 74 站（71 个链接 + 分组头） |
| **焦点环可见吗** | **可见。** 内容区去重后 38 个可聚焦元素，**35 个**是浏览器默认 `outline: auto 1px`，暗色皮下实拍是一圈亮白环（`S31`）；**3 个没有任何可见指示**：两个数字输入框（`outline: none 0px · box-shadow: none`）+ 全局搜索框 |
| 点击目标 | 38 个里 **21 个**至少有一边 < 32px：〔删〕**28×23** · 事件数字框 **74×22** · 「重算」**44×25** · 基地筛选钮 **44×25** · 「另外 13 处也一样 ▸」**105×17** · 「这次算不出来 ▸」**88×17** |

### 2.1 与上一轮的对照

LOOP5 实测「45 次 Tab 都进不了内容区、200 次到不了〔应用方案〕」。今天：

- **✅ 修好了「到不了」**：〔算一下〕80 次到、〔就这么办〕117 次到，**都是真 `<button>`，
  都能 Enter 触发**。上一轮那个 200 次都到不了的〔应用方案〕是个 `<div>`，这一屏没有这种东西。
- **✅ 新增了 skip-link**（「跳到〔算一下〕」），按 Enter 后焦点确实落进内容区。
- **❌ 但 skip-link 在第 80 站才出现，等于没有。** skip-link 的定义就是「文档的第一个可聚焦元素」;
  它现在写在 `DecisionConsoleView` 组件内部，所以排在整条顶栏 + 整条左导航**之后** ——
  **等你 Tab 到它，它要帮你跳过的 79 站你已经全按完了。**
  这是「做了一个正确的东西，装在了错的位置」，不是「没做」。

### 2.2 键盘上真正不好用的地方

- **未算态下〔算一下〕是 `disabled`** ⇒ 它**不在 Tab 序里**。纯键盘用户从第 80 站开始
  一路 Tab 到底，**找不到那颗按钮**，屏上也没有一句话解释它为什么不在。
  （鼠标用户看得到它灰着 + 下面一行「先加一件事，这颗按钮才有事可算。」；键盘用户什么都看不到。）
- **11 个模板行是 11 个 Tab 站，没有 ↑↓ 快选**。要试第 9 个模板（设备故障）得按 9 次 Tab。
- **20 条订单搜索结果是 20 个 `<button>`**，也就是 20 个 Tab 站，没有 ↑↓ + Enter。
- **时间线 12 个日期点不可聚焦**（它们不是交互元素，这一点是对的）；但**四栏方案之间没有 ←→ 切换**，
  只能一站一站 Tab 过去，从第一栏走到第四栏要 4 次。

---

## §3 · 视觉重心

**方法**：视口内（1600×900，未滚动）取所有带自身文字的可见元素，按字号 / 字重排序；
另取所有面积 ≥ 某阈值的实心色块按面积排序。

| 判据 | 实测 |
|---|---|
| 视口内可见元素 | **178** |
| 视口内出现过的字号 | **42 / 20 / 16 / 14 / 13 / 12**，只有 6 档 |
| **最大字号的元素个数** | **1 个** —— `63亿 · 这 30 天交不出去的货` |
| 第 2 / 第 3 大 | `0 / 2`（20px/600）· `5 / 5`（20px/600） |
| 第 4 大 | 〔算一下〕（16px/600） |
| **面积最大的饱和色块** | `360×49 = 17,640px²`，`rgb(17,93,201)` sat 0.92 —— **〔算一下〕，全屏唯一的高饱和实心块** |

### ⇒ **这一屏是 3 个视觉重心，且最大的那个就是 COO 最需要的那个。**

上一轮的病（`/v/dash` 12 个等大重心、最大的数是「在手订单 500」）**在这一屏没有复现**。
我今天顺手复测了 `/v/dash`：**12 个 28px 同权重数字仍在**（`758.20GWh` `78.31%` `91.3%`
`500` `375万套/年` `118.85亿` …），`在手订单 500` 还在，`RED`/`KSF` 药丸还在 4 个。
**病还在那一屏，没传染到这一屏。**

### 3.1 但第 2、第 3 重心是错的（截图 `S44-money.png`）

`0 / 2` 和 `5 / 5` 是全屏第二、第三大的字，**而它们各自单看是零信息**：

- `0 / 2` 底下要读 40 个字才知道它的意思是「你加的 2 件事里，有 0 件会直接改数」。
- `5 / 5` 底下要读 12 个字才知道是「这次问了 5 路算，全部跑通」。
- 更糟的是**读序**：眼睛从 `63 亿` 掉到 `0 / 2`，第一反应是「2 件里 0 件……成了？没成？」——
  它长得像一个**成功率**，实际是一个**分类计数**。
- 两块卡的面积（444×80 各一块）和「钱」那一块**同权重**，可它们不是钱。

**我的建议（不是重做，是降层）**：这两块降到 13px、去掉 20px 的大字，改成一行：
`这 2 件事里 0 件会直接改数 · 5 路算全跑通`。省下的高度还给「钱」那一块。

### 3.2 「什么都不做」是四栏里最抢眼的一栏（截图 `S42-plans.png`）

实测四栏**尺寸完全相同 216×618**，但：

| 栏 | 文字量 | 边框色 |
|---|---:|---|
| 工艺路线调整 | **47 字** | `rgba(226,235,245,0.14)` 常规 |
| 工序外协 | **45 字** | 常规 |
| 瓶颈工序扩容 | **47 字** | 常规 |
| **什么都不做** | **349 字** | **`rgb(234,189,93)` 琥珀高亮** |

三条真方案各自只有 47 个字，装在 618px 高的卡里 ⇒ **每栏约 440px 是空的**；
而唯一装满的、唯一带高亮边框的，是「**什么都不做**」。

**眼睛会先看第四栏，而第四栏是你最不希望他选的那个。**
这和上一轮 `/v/annual-scenario` 那条「可点性和重要性正好反了」是同一个形态，
只是这次反的是「视觉权重和推荐度」。

---

## §4 · 交互便利性 —— 我亲手走的主线

**路线**：加两件事 → 算 → 看钱 → 看卡点 → 看四栏 → 就这么办。

| 步 | 动作 | 点击数 | 滚动 | 走错路了吗 |
|---|---|---:|---:|---|
| 0 | 进页（**无导航入口，只能敲 URL**） | — | 0 | ⚠ 见 4.1 |
| 1 | 点模板「订单改交期」→ **就地展开** | 1 | 0 | 否 |
| 2 | 输「广汽」→ 20 条结果 → 选 SO-3391 | 2 | 0 | 否 |
| 3 | 填「提前天数 = 5」 | 1 | 0 | 否 |
| 4 | 〔加进去〕 | 1 | 0 | 否 |
| 5 | 点模板「设备故障」→ 选基地 → 选产线 → 填停机天数 | 4 | 0 | 否 |
| 6 | 〔加进去〕 | 1 | 0 | 否 |
| 7 | **〔算一下〕** | 1 | 0 | 否 |
| 8 | 看钱（区③） | 0 | **0**（在首屏内） | 否 |
| 9 | 看卡点（区④） | 0 | **1 格滚轮** | 否 |
| 10 | 看四栏（区⑤） | 0 | **4 格滚轮**（累计） | 否 |
| 11 | 〔就这么办〕 | 1 | 0 | 否 |
| **合计** | | **12 次点击 · 6 格滚轮 · 0 次走错** | | |

**滚动几何**：整个文档**不滚**（`scrollHeight = clientHeight = 900`）；滚的是 `main`
（内容 2435px / 视口 842px = **2.89 屏**）。右栏六区总高 **2337px**，
到底 **6 格滚轮**（300px/格）。左栏自己再滚 1.19 屏。**主线一次也没跳页、没弹窗。**

> 对照：LOOP5 记「今天要拿到一个钱数最少 9 步」，原稿目标 3 步。
> 今天实测**加一件事 5 步 + 算 1 步 = 6 步拿到钱**（加第二件再 +5）。
> 比 9 步好，比"3 步"多——但那 3 步是"只加一件、且主体是下拉"的理想路径，
> 今天订单走搜索多了一次输入，值得。

### 4.1 ⚠ 这一屏今天进不去（除非你知道 URL）

`ShellLayout.tsx:275` 里 `decision-console` 明写着「**本单只拿到「这一页」的逐案批准，
没拿到动导航的批准**」。实测左导航 71 个链接里没有它。
**这不是 bug，是刻意的**，但它意味着：**今天没有任何一个用户能自己找到这一屏。**
排期时要记住：这一屏的"上线"= 加导航入口那一刻，不是代码合入那一刻。

### 4.2 事件模板的就地展开 —— **好用，这是这一屏最对的交互**（截图 `S04` `S06`）

不跳页、不弹窗，点标题行原地长出表单，再点一次收起。加进去之后立刻出现在下方
「已经加了 N 件事」，每条带一个可直接改的数字框 + 一颗〔删〕。**改数字不用重新展开模板。**
这一条我完全没有意见。

### 4.3 但字段名不是业务话 —— **三个字段直接把后端的开发者 hint 印在了标签上**

我拉了 `GET /a/v1/sim/drill/catalog` 逐条比对：**屏上的字段标签 = 后端 `payloadKeys[].hint` 原文**。
11 个事件里有 3 个的 hint 不是业务话：

| 事件 | 屏上标签（逐字） | 问题 |
|---|---|---|
| 订单改交期 | `新交期 ISO 日期；给了则优先于提前天数` | **「ISO 日期」是工程词**；而且这是一句**规格说明**当标签用，还是个**纯文本框**（不是日期选择器） |
| 改交付地点 | `新交付地点对象 id` | **「对象 id」是机器词**，而且它要求 COO **手敲一个对象 id** —— 他从哪知道？这一格今天等于不可用 |
| 物料价格变动 | `涨跌百分比：+15 = 涨 15%，-8 = 跌 8% · 必填` | 一整段**使用说明**塞进标签，还带「· 必填」 |

**这不是文案没写好，是架构上把「后端给开发看的提示」直接当成了「给 COO 看的标签」。**
后端加一个事件，前端就自动多一个可能不是人话的标签 —— 屏上那句
「清单现读后台，后台加一件这里就多一件，不是这里写死的」正好说明了这条链路。

### 4.4 有两个事件把同一个问题问了两遍

| 事件 | 主体选择器 | payload 字段 | 后果 |
|---|---|---|---|
| **预测偏差** | 下拉「哪个型号」（6 项，实测 `2170 三元圆柱` / `4680 磷酸铁锂圆柱` / …） | 文本框 `型号（产能测算必需）` | **同一个型号问两次**，一次是下拉一次是要 id |
| **临时插单** | 下拉「哪个客户加单」（20 项） | 文本框 `型号（产能测算必需）` | 型号只能手敲，没有下拉 |

### 4.5 最长的下拉几行 —— **合规，且卡在上限上**

| 事件 | 下拉 | 行数（不含"请选一个"） |
|---|---|---:|
| **临时插单** | 哪个客户加单 | **20** ← 最长 |
| 设备故障 | 哪个基地 / 哪条线 | 13 / 10（两级） |
| 产能损失 | 哪个基地 | 13 |
| 物料到货延迟 / 短缺 / 价格变动 | 哪种料 | 8 |
| 预测偏差 | 哪个型号 | 6 |
| **订单类 5 个事件** | **不给下拉，走搜索** | 输「广汽」→ 后端返「**对得上 243 张，这里列前 20 张**」 |

**要求 ≤20 满足，订单走搜索也满足。** 但 20 正好压线：客户表今天就有 16 家，
再多两家这一格就要重做。搜索结果同样是 20 条一屏。

### 4.6 改了输入没重算 —— **压灰有，提示有，两样都到位**（截图 `S34-stale.png`）

我把已加事件的「提前天数」从 5 改成 9，一秒内：

- 结果区整块套上 `.stale`：**`opacity: 0.42` + `filter: grayscale(0.85)`** ——
  彩色标记全部转灰，那个 42px 的 `63` 变成灰的。**一眼就知道这不是现在的答案。**
- 顶部长出一条琥珀边框的提示条，**逐字**：`你改了左边的输入，下面这些还是上一次算的。`
  右边一颗 44×25 的〔重算〕。
- 删掉一件事同样触发。

**这一条做得比派单要求的还好。** 两点小意见：
1. **0.42 太狠**。灰下来之后旧数字实测已经读不动了 —— 而用户压灰时最常做的事恰恰是
   「对着旧结果改输入」。**0.6 左右就够表达"过期"，还留得住可读性。**
2. **同一个动作有两颗按钮**：提示条上的〔重算〕(44×25) 和左栏底部的〔算一下〕(360×49)。
   两颗都能用、文案不同、大小差 8 倍。留一颗，或者把左栏那颗也改成「重算」。

### 4.7 〔就这么办〕—— **给的是确认，不是静默成功**（截图 `S37-after-adopt.png`）

点完之后**就地**长出一条绿框（`rgb(98,190,119)`）确认，**逐字**：

> `已经生成一份待批的动作（常州 · 工艺路线调整），批了才会改真实数据。`

**这正是派单要的那句话，一字不差。** 0 弹层、0 跳页、就在按钮下方，在视口内。✅

**但三个问题：**

1. **没有撤销。** 确认条里 `<button>` 数 = **0**。（我第一次探针报"有撤销"是假阳性，
   命中的是模板行「订单取消」—— 见 §0.1。）
2. **连点三颗就真的生成三份，屏上只看得见最后一份 —— 我抓了网络包，这条是坐实的。**

   依次点三栏的〔就这么办〕，抓到 **3 条 `POST /a/v1/action-drafts`，全部 `201`**：

   | 点 | 请求体（逐字） | 回包 |
   |---:|---|---|
   | 1 | `{"actionTypeKey":"adopt_mitigation","payload":{"base":"changzhou","factor":"瓶颈工序","planKey":"reroute"},…,"submit":true}` | `{"draftId":"act_fneyagw7tjwr0azr","status":"PENDING_APPROVAL",…}` |
   | 2 | 同上，`"planKey":"outsource_step"` | `{"draftId":"act_p5mfn37x8y3b61wx","status":"PENDING_APPROVAL",…}` |
   | 3 | 同上，`"planKey":"debottleneck"` | `{"draftId":"act_02rpsm1wadwkseba","status":"PENDING_APPROVAL",…}` |

   **三条 `draftId` 各不相同、状态全是 `PENDING_APPROVAL`，而 `base` 与 `factor` 完全一样
   —— 也就是说：同一个瓶颈的三条互斥对策，同时排进了审批队列。**
   请求体里 `"submit": true`，所以它们不是草稿，是**直接进待批**。

   而屏上：确认条**始终只有 1 条**，永远显示最后那次；三颗按钮点完还是可点，
   **没有互斥、没有计数、没有清单**。更难看的是确认条那句话逐字写着
   「已经生成 **一份** 待批的动作」—— **从第二次点击起，这句话就是错的。**

   一个手抖点两下的人，会造出两份互相冲突的待批动作，而屏上一个字都不会告诉他。
3. **确认条是个死胡同。** 它说"生成了一份待批的动作"，却**没有任何链接**能去看那份动作
   —— 而左导航里明明有「Action 审批」。

---

## §5 · 设计品味

### 5.1 像不像专业产品 —— **像。** 这几条是它站得住的原因

- **一屏一个主动作。** 全屏唯一的高饱和实心色块就是〔算一下〕（`rgb(17,93,201)` sat 0.92，
  360×49）。不像 `/v/risk` 那样五个计数各用一个颜色。
- **表格数字走等宽 + 右对齐。** 实测客户表 7 个数值列全部
  `font-family: "JetBrains Mono"` + `text-align: right`，只有「客户」「要留意」两个文本列左对齐。
  **上一轮点名要的这条，做到了。**
- **金额只给到一位小数，而且同列一致**（`223.3` `88.2` `20.2` `19.3` `18.4` `18.4`）。
  对照 `/v/risk` 今天仍是 `35.5411` 与 `17.822` 混在一列 —— **这一屏没有继承那个病。**
- **区头全是数据或控件，一个解说都没有。** 逐条抄下来：
  「已经加了 2 件事」「客户与订单 · 这 30 天里，53 张单牵涉 63 亿、16 家客户」
  「哪儿会出事 · 一共看了 18 处」「有几条路 · 常州 · 这里最紧的是「瓶颈工序」」
  「这次算的时候做了什么 ▸ · 引用 710 条数据 · …」。
  **仓主那条「区头不许是『一共有哪些对策』这种解说」，这一屏守住了。**
- **口径打架被写在屏上而不是藏起来。** 区③ 给 `63 亿`，同屏一行小字：
  `含跨基地订单，各基地不可直接相加（相加会得到 90 亿，同一张单被算了两次）。`
  —— 而我今天实测 `/v/risk` 八张卡相加**正好是 89.5771 亿**。
  **上一轮我说"一个点开就露馅的勾稽入口比没有更危险"，这一屏是我见过最干净的解法：
  两个数都给，并且解释为什么不一样。**
- **假控件 0 个 · 无名字的按钮 0 个 · 空表头 0 个。**（`/v/order-chain` 上一轮实测有 5 个
  没有 `innerText` 也没有 `aria-label` 的按钮；这一屏 0 个。）
- **11 个事件模板，第一项是「订单改交期」**，不是上一轮那种 `（不输事件·只扫卡点）`
  这种带括号的否定项。

### 5.2 不像的地方 —— 逐处点名

#### （a）**机器编号上了第一层：`pos_lfp_b2`**（截图 `S43-impediments.png`）

「能动的 4 处」第一条逐字：

> `物料批次 pos_lfp_b2 现在 109天，红线 90天（超红线 21%）`

同一区其余三条是 `磷酸铁锂正极` `金华分切线` `自贡分容线` —— **人话**。
只有第一条把批次的机器 id 直接印上来。**这一条自己违反了这个文件头写的规矩**
（`DecisionConsoleView.tsx:47` 明写「机器编号 …一律进第二层」）。

#### （b）**4 处 markdown 星号没被渲染，`**…**` 直接印在屏上**

全文扫出 3 行、4 处：

```
…压力指数按百分点(pp)读；这是**推演投影**不是实测值…
收入行**故意不动**：…与 FinancePlan 收入行之间今天**没有任何传导规则**（（源码出处已按界面规范隐去）
…只比对快照与规则红线 95%，**未校验持续天数**；结论 dataMode 标 PARTIAL
```

同一行还带一个 **`（（` 双层括号**（把 scrub 出来的占位串又套了一层括号）。
上一轮 `/v/risk` 屏上原样打出 `**越线日序**` 是同一个病 —— **它跟着引擎原文搬到这一屏来了。**

#### （c）**「另外 13 处也一样 ▸」点开是 14 份同一句引擎话**（截图 `S46-nowhy-wall.png`）

展开后 innerText 从 5,555 → **8,390 字**（+2,835）。这 2,835 字里，**14 条**都长这样：

> `枚举已跑完，有效候选 0 个（探了 10 个杠杆锚点 / 34 次试算），不足 2 个 ⇒ 构不成多方案对比，
> 诚实不下发。缺口：LOCUS_PROP 够不着：对象类型 Base 在 CAPACITY_FACTOR_BINDINGS 上没有任何
> 可拨动落点 | RULE_GATE 够不着：规则 C34 不是任何可拨动因子的 ruleGate（该判据与产能因子册
> 今天没有共同的规则码）`

**同一句话复制 14 遍，只有对象名和规则码不同。** 里面有
`LOCUS_PROP` · `CAPACITY_FACTOR_BINDINGS` · `RULE_GATE` · `ruleGate` · **`落点`（这一页自己列的禁词）**。
一个 COO 打开这个折叠，是想知道"这 13 处为什么没救"；他得到的是**同一段编译器错误 14 份**。

**建议**：这一段的正确形态是**一句总结 + 一张 14 行的表**：
`这 13 处今天都卡在同一件事上：产能因子册里没有可以拨动的项。` 然后表格只列
`对象 · 现在 · 红线 · 超了多少`，把那段引擎原话收进每行的 ▸。

#### （d）第一层的机器话计数（词表扫描，金丝雀已验）

| 词 | 第一层 | 全部折叠展开后 |
|---|---:|---:|
| `扰动` / `tick` / `张力` / `敞口` / `状态变量` | **0** | **0** |
| `落点` | 0 | **15** |
| `世界` | 0 | 3 |
| `传导` | 0 | 2 |
| `求解器` | 0 | 2 |
| `推演` | 0 | 1 |
| 蛇形机器名 | **1**（`pos_lfp_b2`） | 16 |
| 全大写枚举（`NOT_APPLICABLE` `PARTIAL` `LOCUS_PROP` …） | 0 | 20 |
| 规则码 `C02/C05/C06/C09/C11/C28/C34` | 0 | 7 |
| **裸 JSON** | 0 | **1**（`{"bindingId":"BOTTLENECK.CAPACITY.line-utilization-redline","ruleKey":"C05",…}`） |

**第一层几乎是干净的**（就 `pos_lfp_b2` 一个漏网）。R-UI-4「机器话进第二层」大体守住了。
但第二层今天是**引擎原文直接倾倒**，包括一整段 JSON。第二层可以有机器话，
不代表可以是**未加工的机器输出**。

#### （e）**信息密度：第一层有 4 段共 216 字的解说型灰字**

第一层（不含折叠）≥26 字的整段说明有 16 条，其中 4 条是 `.greyLine` 类的解说：

| 字数 | 原文 |
|---:|---|
| 85 | `下面三条的「多久见效 / 代价 / 风险」是「瓶颈工序」这一类的通用档位估，不是这一次的试算 —— 这次真试算出来的那几条改法身上，代价 / 见效天 / 风险一个都没有` |
| 65 | `这次加的这几件事没有走到「产销缺口」那一路算上（只有物料与预测类的事才会问到它），所以这一屏没有那个数 —— 不是算出来是 0。` |
| 45 | `手上一共 500 张单：已完成 350 张 · 在产 100 张 · 已下待排产 50 张` |
| 40 | `含跨基地订单，各基地不可直接相加（相加会得到 90 亿，同一张单被算了两次）。` |

屏上总字符 2,291，其中数字字符 396（**17.3%**）。

**我的判断：这 4 段不是仓主说的那种「不需要的描述型文字」——** 它们每一段都是**本次运行的**
诚实位（"这次没走那条路"/"这个数不能相加"），不是"本区一共有哪些对策"那种解说。**该留。**
但那条 **85 字的** 太长了，它讲的是"下面三条的档位不是这次算的"——
这句话应该缩成一行 `下面三条的档位是通用估值，不是这次算的 ▸`，其余进第二层。

#### （f）时间线：同一批订单，两种格式（截图 `S43`）

区④ 底部时间线 12 行，逐字是 `第 2 天 / SO-900487 到期，预计晚 4 天〔估〕`；
而区⑤「什么都不做」栏里同一批订单写的是 `东风汽车 SO-3420 · 第 29 天到期 · 晚 4 天`。

**同一屏、同样的数据，一处带客户名一处不带。** 而客户名恰恰是 COO 分诊时唯一会用的字段
（"广汽的先救"）。时间线那 12 行今天占 **400px**，是区④ 里最高的一块，
而它的信息量比区⑤ 那一段少一列。另外「还有 41 件，都在这 30 天里。」**没有入口能看那 41 件。**

#### （g）「只能盯着的 14 处」这一栏 90% 是空的

区④ 左右两栏：左边「能动的 4 处」列了 **4 张卡**；右边「只能盯着的 14 处」只列了 **1 张卡 + 一个 ▸**。
**4 项占了 4 倍面积，14 项占了 1/4 面积。** 数量和版面正好反着。

#### （h）「物料平衡 磷酸铁锂正极 现在 492吨」这一条没有红线

同区其余三条都是 `现在 X，红线 Y（超红线 Z%）`；这一条只有 `现在 492吨`。
**行的形状不一致，读者无法判断 492 吨是好是坏**，而它却和另外三条并排在「能动的 4 处」里。

#### （i）加载态：**7–10 秒的空白 + 一句话**（截图 `S09-computing.png`）

实测三次〔算一下〕的端到端墙钟：**9.9s / 9.5s / 7.2s**（机器同时跑着另外 3 个 agent，
`load average` 4–9，这个数在闲机上会短些）。这段时间里右栏：

- 只有**一条不定量进度条 + 一行字**「正在算未来 30 天…」，**其余 ~700px 全空**。
- **没有骨架屏** —— 六个区一个占位都没有。
- **`SLOW_AFTER_MS = 10_000`** ⇒ 「还在算（已经 N 秒，通常 3 秒）」这句**几乎永远不会出现**：
  实测三次里 0 次触发，因为都在 10 秒之内回来了。**用户拿不到"已经等了几秒"这个数。**
- 而那句兜底话写的是「**通常 3 秒**」（`TYPICAL_SECONDS = 3`）。
  我直接压 `POST /a/v1/sim/sessions/{id}/drill` 实测 **2.4s / 3.5s** ——
  **drill 本身确实约 3 秒，慢的是它并发的另外 8 个请求**（页面自己的耗时表实测
  最慢那段 9,175ms 是「按客户汇总订单」）。所以"通常 3 秒"这句话，**度量的是后端的一段，
  不是用户等的那一段** —— 它一旦真的显示出来，就是在骗人。

**建议**：① `SLOW_AFTER_MS` 降到 3,000，让秒数尽早上屏；② `TYPICAL_SECONDS` 改成前端实测的
端到端中位数；③ 六个区各给一个灰条骨架，让人知道等下会长出什么。

#### （j）数汉粘连仍在

`63亿` · `109天` · `492吨` · `3,694套/日` · `90天` —— 数字和汉字之间没有空格。
上一轮点过一次，这一屏继承了。**不阻塞，但同一屏里 `53 张单`（有空格）和 `63亿`（没空格）并存**，
说明这是漏的不是选的。

---

## §6 · 诚实位好不好懂（截图 `S44-money.png`）

**我用的视角**：一个不懂技术的人，能不能分清「这里是 0」和「这里没算」。

### 三态今天在屏上长这样

| 态 | 屏上原文 | 我的判断 |
|---|---|---|
| **〔估〕** | 一个 12px/700 的琥珀圆角小徽章，紧跟在数后面。**`title="这个数是算出来的，不是量出来的"`** | **⚠ 半懂** |
| **删除线「这次没算」** | `毛利差多少` + 一条红色 `——————` + `这次算不出来 ▸` 三段一行 | **✅ 完全懂** |
| **「算了但一项没动」** | `0 / 2` + `你加的事里，没有一件会直接改数 —— 它们只决定去问哪几路算。这不是「没影响」。` | **⚠ 要读 40 个字才懂** |

### ✅ 删除线这一格：**满分，这是全案最好的一处设计**

那三行（毛利差多少 / 多花的成本 / 少收的钱）**没有填 0、没有写"无变化"、没有留空白**，
而是一条画掉的横线 + 「这次算不出来 ▸」。一个不懂技术的人看到一条被划掉的线，
不会读成"这里是零"。**而且展开给的是人话**：

> `① 这次的算是只读的，它算出来的那 30 天不留下来；而财务那一路只会读这次算例已经存下来的那一天
> —— 「你刚加的这几件事之后毛利是多少」今天没有那条线。`

**「今天没有那条线」** —— 这六个字比任何技术描述都清楚。

再加上区③ 顶上那句：

> `这次加的这几件事没有走到「产销缺口」那一路算上（只有物料与预测类的事才会问到它），
> 所以这一屏没有那个数 —— 不是算出来是 0。`

**「不是算出来是 0」这七个字，就是本条评审要问的那个问题的答案，而且是屏上自己写的。**

### ⚠〔估〕角标：**26 个，太多了，而且键盘用户拿不到它的意思**

- **全屏实测 26 个〔估〕。** 光区⑤ 四栏里就有 10 个（3 栏 × 3 维 + 1），
  区④ 时间线 12 行每行一个。
- 到这个密度，它**从"提醒"退化成"壁纸"** —— 满屏都是估，等于没有一处特别值得当心。
- 它的解释**只在 `title` 里**：`tabIndex = -1`、无 `aria-label`、`cursor: auto`。
  ⇒ **键盘用户和读屏用户完全拿不到这句话**；鼠标用户要悬停约 1 秒才等到原生 tooltip，
  而屏上**没有任何视觉暗示**说这个徽章能悬停。
- **屏上没有图例。** 第一次见到这个屏的人，要么猜，要么悬停 26 次里的某一次。

**建议**：① 同一个区里同一类数只标一次（区⑤ 那 9 个可以合并成栏头一句
`这一栏的三个档位都是估`）；② 给它 `aria-label`，或干脆在页脚放一行图例。

### ⚠「算了但一项没动」：意思对，形态不对

`0 / 2` 这个形态的问题在 §3.1 说过了：**数字最大、信息最少**。
底下那句 `这不是「没影响」` 是对的、也是必要的，但它现在是**注解**，
而**注解才是这一格真正要说的话**。把那句话提上来当主体、把 `0 / 2` 降成一个小标签，
这一格立刻就懂了。

---

## §7 · 必须修的（阻塞上线）

> 判据：**这条不修，会让用户看到坏东西 / 得出错误结论 / 造成不可逆后果。**

| # | 问题 | 精确位置 | 为什么阻塞 |
|---|---|---|---|
| **B1** | **第一层出现机器编号 `pos_lfp_b2`** | 区④「能动的 4 处」第 1 条；`DecisionConsoleView.tsx` 卡点行渲染（同区另三条已是人话，说明是漏的不是选的） | 这是这一屏自己写的规矩（`.tsx:47`）被自己破了。演示时第一条就是它 |
| **B2** | **4 处 markdown 星号 `**…**` 未渲染直接上屏 + 一处 `（（` 双层括号** | 区③「这次算不出来」展开层第 2 段；「5 处成色」第 3、4 条 | 屏上出现源码标记 = 一眼看穿是内部工具。上一轮 `/v/risk` 的 `**越线日序**` 是同一个病，它跟着引擎原文搬过来了 |
| **B3** | **〔就这么办〕可以连点，每点一次就真的 `POST /a/v1/action-drafts` 生成一份 `PENDING_APPROVAL`**（实测 3 次点击 → 3 个不同 `draftId`：`act_fneyagw7tjwr0azr` / `act_p5mfn37x8y3b61wx` / `act_02rpsm1wadwkseba`，`base`+`factor` 完全相同、只有 `planKey` 不同）；屏上只显示最后一份，确认条却写着「已经生成**一份**待批的动作」 | 区⑤ 三颗〔就这么办〕；确认条 `.confirm`（实测 `<button>` 数 = 0，即无撤销、也无入口去看） | 这是全屏**唯一会改真实世界**的按钮，`"submit":true` 直接进待批。手抖点两下 = 同一个瓶颈的两条互斥对策同时排进审批队列，用户不知情，屏上文案还在说"一份"。**不可逆 + 文案与事实相反 = 阻塞** |
| **B4** | **`.sortBtn[aria-pressed="true"]` 对比度三套主题全部低于本仓门判据**（暗 4.96 / 冷蓝 5.73 / 暖 5.76，12px 需 ≥6.0） | `DecisionConsoleView.module.css:643`（`--accent-txt` × `--primary-soft`） | 它是「你现在看的是哪个基地」和「按什么排」的**唯一指示**，全屏最难读的偏偏是它。⚠ 这对令牌是全仓通用的，**改法应落在令牌不落在本页** |

---

## §8 · 可以后修的

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| L1 | **skip-link 在第 80 次 Tab 才出现** | `.skip`（组件内，排在顶栏 + 左导航之后） | 移到 `ShellLayout` 的第一个可聚焦元素；它现在的位置让这个正确的东西完全失效 |
| L2 | **`0 / 2` / `5 / 5` 是全屏第 2、3 大的字，各自零信息** | 区③ 两块 444×80 的卡 | 降成 13px 一行文字：`这 2 件事里 0 件会直接改数 · 5 路算全跑通` |
| L3 | **「什么都不做」是四栏里唯一带琥珀高亮边框、唯一装满的一栏**（47 字 vs 349 字，边框 `rgb(234,189,93)`） | 区⑤ 第 4 栏 | 去掉高亮边框；或把它挪到三栏之外做成一条横幅，别和三条真方案等权并排 |
| L4 | **三个字段标签直接用了后端 dev hint**：`新交期 ISO 日期；给了则优先于提前天数` / `新交付地点对象 id` / `涨跌百分比：+15 = 涨 15%…· 必填` | `catalog.specs[].payloadKeys[].hint` → 前端 `fieldLabel` | 前端加一层「标签字典」，hint 降为 `fieldHint`；`新交期` 换成日期选择器；`对象 id` 那格改下拉或先隐藏 |
| L5 | **预测偏差 / 临时插单 把型号问了两遍**（下拉「哪个型号」+ 文本框 `型号（产能测算必需）`） | `FORECAST_BIAS` / `ORDER_INSERT` | 主体选了型号就自动填 `payload.modelId` |
| L6 | **「另外 13 处也一样」= 同一句引擎话 14 份（+2,835 字）**，含 `LOCUS_PROP` `CAPACITY_FACTOR_BINDINGS` `RULE_GATE` `落点` | 区④ | 一句总结 + 一张 14 行的表；引擎原话收进每行 ▸ |
| L7 | **加载态：7–10 秒空白、0 骨架；`SLOW_AFTER_MS=10000` 使秒数几乎不显示；`TYPICAL_SECONDS=3` 度量的是后端一段不是用户等的那段** | `.tsx:106/108/672` | `SLOW_AFTER_MS→3000`；`TYPICAL_SECONDS` 改成端到端实测中位数；六区各给灰条骨架 |
| L8 | **〔估〕26 个 + 无 `aria-label` + 无图例** | `.est` | 同区同类合并标一次；补 `aria-label`；页脚给一行图例 |
| L9 | **压灰 `opacity:0.42 + grayscale(0.85)` 太狠，旧数字读不动**；且同一动作两颗按钮（〔重算〕44×25 / 〔算一下〕360×49） | `.stale` / `.staleBar` | 0.42 → 约 0.6；两颗按钮留一颗或统一文案 |
| L10 | **时间线丢了客户名**（`SO-900487 到期` vs 区⑤ 的 `东风汽车 SO-3420`）；**「还有 41 件」没有入口** | 区④ 底部 | 补客户名；「还有 41 件」做成可展开 |
| L11 | **「只能盯着的 14 处」只显示 1 张卡**，4 项占了 14 项 4 倍的版面 | 区④ 右栏 | 至少列前 3–5 条 |
| L12 | **「物料平衡 磷酸铁锂正极 现在 492吨」没有红线**，行形状与同区另三条不一致 | 区④ | 无红线的项要么补一个参照，要么明说「这一项没有红线」 |
| L13 | **点击目标 21/38 小于 32px**：〔删〕28×23 · 数字框 74×22 · 基地钮 44×25 · 「另外 13 处 ▸」105×**17** | 全屏 | 至少把破坏性的〔删〕和高频的基地钮抬到 32px |
| L14 | **未算态下〔算一下〕`disabled` ⇒ 不在 Tab 序**，键盘用户找不到它也没有解释 | `.tsx:656` | 保留 `disabled` 但加 `aria-describedby` 指向那句「先加一件事…」 |
| L15 | **第二层出现裸 JSON**（`{"bindingId":"BOTTLENECK.CAPACITY.line-utilization-redline",…}`）与 `risk_timeline.dataMode = PARTIAL` 当作整句解释 | 「5 处成色」第 4、5 条 | 第二层可以有机器话，但不该是未加工的机器输出 |
| L16 | **数汉粘连**：`63亿` `109天` `492吨` `3,694套/日`，而同屏 `53 张单` 有空格 | 全屏 | 统一 |
| L17 | **68% 的文字挤在 12px 一档**（360/526），层级靠 13/12 一档之差撑 | 全屏 | 正文 13px、注解 12px，把差距拉开 |
| L18 | **这一屏今天没有导航入口**（`ShellLayout.tsx:275` 明写"没拿到动导航的批准"） | 导航 | **属产品决策，不是 UX 能定的**；但排期要记：上线 = 加入口那一刻 |

---

## §9 · 我实测判断「这条做对了」的地方

只挑毛病的评审没法拿来做取舍。下面每一条我都亲手验过：

1. **✅ 最大的数就是最该决策的那个数。** `63 亿 · 这 30 天交不出去的货`，42px，全屏唯一。
   对照同一天的 `/v/dash`：12 个 28px 等权数字，最大的仍是 `在手订单 500`。**病没传染过来。**
2. **✅ 三套主题 · 526 个文字元素 · 0 个小于 12px。** 对照 `/v/sim-optimize` 今天 263 个小于 11px。
3. **✅ 删除线诚实位是全案最好的一处设计。** 不填 0、不写"无变化"、不留空白；
   展开给的是「今天没有那条线」这种人话。**「不是算出来是 0」七个字直接写在屏上。**
4. **✅ 两个口径都给，并解释为什么不一样。** `63 亿` 旁边写 `相加会得到 90 亿，同一张单被算了两次`
   —— 而我今天实测 `/v/risk` 八张卡相加正好 **89.5771 亿**。
   **上一轮我说"一个点开就露馅的勾稽入口比没有更危险"，这是我见过最干净的解法。**
5. **✅ 事件模板就地展开**，不跳页不弹窗，加完立刻进"已经加了 N 件事"，数字可直接改。
6. **✅ 主线 12 次点击 · 6 格滚轮 · 0 次走错路。** 整页文档不滚，只有内容区滚 2.89 屏。
7. **✅ 结果过期会压灰 + 明说「你改了左边的输入，下面这些还是上一次算的。」** 派单要的两样都有。
8. **✅〔就这么办〕给的是确认不是静默成功**，逐字「批了才会改真实数据」，就地、绿框、在视口内。
9. **✅ 表格数字等宽 + 右对齐 + 同列同小数位。** 上一轮点名要的三条全做到。
10. **✅ 区头全是数据或控件，一个解说型区头都没有。** 仓主那条明确要求守住了。
11. **✅ 假控件 0 · 无名字按钮 0 · 空表头 0 · 假英文菜单 0。**
    （同一天 `/v/sim-optimize` 的 `File Edit View` 还在。）
12. **✅ 下拉最长 20 行，订单走搜索**，五个订单类事件全部搜索 + 后端回「对得上 243 张，这里列前 20 张」。
13. **✅ 焦点环 35/38 可见**（浏览器默认双色环，暗色皮下实拍是亮白圈）。
14. **✅ 键盘能走完主线**：〔算一下〕第 80 站、〔就这么办〕第 117 站，都是真 `<button>`。
    对照上一轮那个 200 次 Tab 都到不了的 `<div>` 版〔应用方案〕。
15. **✅ 耗时表把"并发"和"相加"分清楚了**：明写「相加会得到 70,558 毫秒，那个数不度量你真的等了多久」。
    **这是一处主动防止读者误读的设计，值得表扬。**

---

## §10 · 上一轮六条旧前提逐条复核

上一轮（`docs/LOOP5-ux-review.md` §0.2）我列了六条"原稿写的、我实测推翻的"前提。
**今天逐条重测**（`m14-old6.mjs`，真后端、同一套探针）：

| # | 上一轮记的 | 今天实测 | 判定 |
|---:|---|---|---|
| 1 | 原稿「三张方案卡 KPI 逐字相同 `741,226`」；LOOP5 更正为「四张卡四个不同值」 | `/v/sim-optimize` 全文 **搜不到 `741226`**；帕累托横轴今天是 `9187992.0 → 8854723.4 → … → 6855112.0` 共 **9 个不同值** | **原稿前提仍过期；LOOP5 的更正复现** |
| 2 | 原稿「横轴在 74 万的数上画 1 个单位」；LOOP5 更正为「跨度 233 万、递减、7 位数带 `.0`」 | 横轴 `9187992.0 → 6855112.0`，跨度 **2,332,880**，递减，**9 个标签全部 7 位数带 `.0`** | **原稿前提仍过期；LOOP5 的更正复现** |
| 3 | 落点下拉「共 11337 / 12740 个落点」 | **在决策台上这一格已经不存在了** —— 主体选择器换成"按事件类型限定范围"的 ≤20 行下拉 + 订单搜索（实测最长 20 行） | **已过期（被新设计取消）** |
| 4 | 「8 张基地卡 4 张 LIVE / 4 张 MOCK」；LOOP5 更正为「LIVE 0 · MOCK 0 · 合成·未接实测 8」 | `/v/risk` 今天：`LIVE` **0** 次、`MOCK` **0** 次、`合成·未接实测` **8** 次 | **原稿前提仍过期；LOOP5 的更正逐字复现** |
| 5 | 首屏 `31.1 亿` 与 `/v/risk` 八张卡相加 `89.5771 亿` 对不上 | `/v/risk` 今天八个金额 `35.5411 / 17.822 / 15.5991 / 11.3611 / 5.8529 / 1.4109 / 1.2522 / 0.7378`，**相加 = 89.5771 亿，逐字复现**。⚠ **但决策台已经把这个坑填了**：它给 `63 亿`（去重口径）并同屏写明「相加会得到 90 亿，同一张单被算了两次」 | **数据复现；病在决策台上已修** |
| 6 | 「同一列小数位不一致」（`35.5411` 四位 vs `17.822` 三位） | `/v/risk` **仍是 `35.5411` 与 `17.822` 混在一列**。⚠ **决策台没有继承**：客户表 6 行金额全部一位小数 | **在 `/v/risk` 复现；决策台未继承** |

### 10.1 上一轮的三条"不像产品"判据，今天在隔壁全部复现

上一轮我给的一句话判断是：「**一页 181 个元素小于 11px · 一个假英文菜单印在三个页面上 ·
一个长得像主按钮的东西点了没反应、键盘还够不着**」。今天在 `/v/sim-optimize` 逐条复测：

| 判据 | 上一轮 | 今天 | 判定 |
|---|---|---|---|
| 小于 11px 的元素 | 181 | **263**（最小 8px：`optimizer console` / `01` / `02` / `◔` / `◑`） | **复现且更差** |
| 假英文菜单 `File Edit View` | 有 | **仍在** | **复现** |
| 〔应用方案〕是 `<div>`、点了没反应、Tab 够不着 | 是 | **仍是 `<div>`，`role=null` · `tabIndex=-1` · `cursor:auto`** | **复现** |

**⇒ 这三条一条都没修，而它们和决策台隔着两次点击。**
决策台自己是干净的（0 个 <12px · 0 假控件 · 全是真 `<button>`），
**但「拿给同行看像不像产品」这个问题，问的是整条动线，不是一屏。**

同样复现的还有：`/v/dash` **12 个 28px 等权重心 + `在手订单 500` + `RED`/`KSF` 药丸 4 个**；
`/v/risk` **14 个 19px 重心，其中 9 个是 `T+1`（全屏 18 次）**。

---

## §11 · 一句话给排期的人

> **决策台本身是这个仓里第一个"能拿出去"的屏 —— 四条阻塞项（B1–B4）都是半天量级的活。
> 真正贵的不在这一屏：它今天没有导航入口（属产品决策），而它一旦有了入口，
> 用户点两下就会走到 `/v/sim-optimize` 那 263 个 8px 元素和那个假英文菜单上去。
> 排期时这两件事应该绑在一起，不该只发决策台。**
