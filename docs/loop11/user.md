# LOOP11 · 【使用者】视角 · 阶段① 第 1 轮发言（共 2 次，这是第 1 次）

## 报告头（三样）

| 项 | 值 |
|---|---|
| **base commit** | **`ec1e707c`**（PIN·未 rebase 未 merge）；本分支 tip `0d8b49e0` = 其上一个 `--allow-empty` 的 WIP 起点提交，**零源码改动** |
| **取证时刻** | `2026-09-07T03:42:58Z` 起（UTC），至 04:0x |
| **树龄探针** | `wc -l apps/datacore/src/synthetic/battery.ts` = **6618** |

**取证环境**：真 datacore（`SEED_DEMO=1`，内存模式），**自选端口 4457**（`VITE_DATACORE_URL` 对应值），
真登录 `demo / admin / demo1234` → 真 RS256 JWT（非 `X-Debug-User`），**禁 `VITE_MOCK`**。
**取证方式 = curl 打真后端 + 读前端渲染源**；按本轮 CPU 硬纪律（六角色同跑、4 核、实测 `load 8.25`、
派单原话「优先用 curl 打真后端取证，浏览器只在必须时开」）**未开浏览器**——
故「屏上有没有」这一类结论，我一律落在**前端渲染源的字符串扫描 + 金丝雀**上，不谎称是肉眼所见。

---

## 发言正文（≈200 字）

**跑得完，但「钱」这一步既空又错。**

砍 `SO-3391`（**1.61 亿**）→ 回 **621** 条结论；整个 99KB 回包「元」出现 **0 次**
（金丝雀：同一把尺子在 `/a/v1/objects` 上量到 `unitPrice` 6 处、实值 `161135282`）。
推演的输出词汇是 **41 个 0–100 压力指数**，没有一个是钱。

唯一给钱的 `quote_margin` 报毛利 **87.05%**，自带 `coherent:false`「价按套·BOM 按台·**绝对值偏高**」；
而种子自校验 `118.85/700.00` = **17.0%**，差 **5.1 倍**。前端渲染了 `verdict`（23 文件）
却把 `unitBasis` 丢了（**0** 文件）——屏上只剩「过线」。**这个数我看了会做错决策。**

比对是减法：收窄 `27.8−2.6802=25.1198`、`ticks=0`；`findingsChanged=0`（砍 1.61 亿，传导结论一条没翻）。

**长期那一半是只读的**：三条阈值全 `trigger.default`，覆盖需已发布规则 `trigger_thresholds`
（30 条已发布里 **0** 条），前端 `cfgRuleKey` **0** 文件 ⇒ 看得见「18.45>12 已触发」，**改不动那个 12**。

---

## 论据台账（不计入 200 字 · 全部真后端实测，命令可复跑）

### ① 扰动输入侧：**齐全，这一段没问题**
`GET /a/v1/sim/drill/catalog` → `specs` **11 条**，与仓主列的经营场景基本对得上：
`ORDER_INSERT` 临时插单 · `ORDER_CANCEL` 订单取消 · `ORDER_RESCHEDULE` 改交期 · `ORDER_REPRICE` 改价 ·
`ORDER_RELOCATE` 改交付地点 · `MATERIAL_DELAY` 到货延迟 · `MATERIAL_SHORTAGE` 短缺 ·
`MATERIAL_REPRICE` 料价变动 · `EQUIPMENT_FAILURE` 设备故障 · `CAPACITY_LOSS` 产能损失 · `FORECAST_BIAS` 预测偏差。

> ⚠ **金丝雀在这里救了我一次**：我第一版按 `items/drills/catalog` 解析，打印「drills: 0」，
> 差点报「预设场景一个都没有」。dump 原始 JSON 才发现键是 `specs`，真值是 11。
> **「我没找到」和「它不存在」是两个命题** —— 这条否定结论若发出去就是假的。

### ② 财务后果侧：**空**
真跑 `POST /a/v1/sim/sessions/sims_demo_seed_world/drill`，事件 `ORDER_CANCEL / SO-3391 / cancelPct=100`，`horizonDays=30`：

- HTTP 200，回包 **99,202 字节**，`worldCellsMoved` **412 / 7295**，`totalByKind` = 卡点 **296** · 脆弱点 **300** · 堵点 **25**。
- 全回包扫钱：`元`=0 `万元`=0 `亿`=0 `CNY`=0 `revenue`=0 `margin`=0 `unitPrice`=**0**。
- `"value"` 命中 180 次，逐条看上下文**全是压力指数**（`"stateVar":"queuePressure","value":96.171448`
  / `"shortageRisk",98.706768` / `"costPressure",99.822205`），**没有一处是金额**。
- **金丝雀（证明这把尺子是好的）**：同一段正则量 `/a/v1/objects?type=Order` 回包 →
  `unitPrice` 6 处、`value` 6 处，实值 `161135282 / 325464524 / 88104918`。⇒ 尺子可用，上面的 0 是真 0。
- 可推演状态变量全集 `GET /a/v1/sim/drill/state-var-layers` = **41 个**，
  含钱味的只有 `costPressure` / `priceShock` ——**仍是 0–100 指数，不是元**。

**这一条直接对上 TOPIC 的原话**：「预判可能发生的**财务指标**变化」——
今天扰动进得去、卡点出得来，**中间那个「财务指标」是缺的**。

### ③ 唯一的金额读数：**错 5.1 倍，且警示在屏上被丢掉**
`POST /a/v1/solvers/quote_margin/invoke` （真后端）：

```
margin 0.8705（87.05%） · floor 0.12 · verdict "过线"
breakdown: price 18613.5621 · bomCost 540.2012 · mfg 1861.3562 · logistics 8
scope.unitBasis.coherent = false
scope.unitBasis.gap      = G-QUOTE-BOM-PRICE-UNIT-SCALE
scope.unitBasis.note     = 「价按套、BOM 按台，二者间缺「台/套」换算常数（种子层欠账）——margin 绝对值偏高」
```

- 算式核对：`(18613.5621 − 2409.5574) / 18613.5621 = 0.8705` ⇒ 87.05% 确由**两个不同单位相除**得来。
- 对照真值：种子自带交叉验证 `battery.ts:1683` 逐字写 `Σ marginWan / Σ revenueWan = 118.85/700.00 = 17.0%`。
  **87.05% vs 17.0% = 5.1 倍。**
- **警示丢在最后一米**：前端源扫描 `unitBasis` **0** 文件 · `coherent` **0** 文件 · `绝对值偏高` **0** 文件；
  而 `verdict` **23** 文件 · `quote_margin` **5** 文件（**金丝雀，证明前端扫描可用**）。
  ⇒ 后端老老实实说了「这个数偏高、口径不自洽」，**屏上只把「过线」那两个字端出去**。

> 这是我这个角色最该报的一条：**它不是「建了没人用」，是「用了会把人带沟里」。**
> 一个 CEO 拿 87.05% 去定价或接单，比屏上什么都不显示更贵。

### ④ 多方案比对：**存在，但它是减法，且不接我刚输入的那个扰动**
`POST /a/v1/solvers/decision_play/invoke` 真跑：

- 确实给了 **一根因 → 3 方案 → 比对矩阵 → 推荐组合**：
  `缩短备份供应商认证周期`（closesGap .938 / cost 248 / 112 天 / risk .25）·
  `长协加价格联动条款`（1.7073 / 90 / 30 天 / .20）· `上游自采矿+战略储备`（2.1951 / 1730 / 180 天 / .55）。
  **这一段是真有价值的**，六维可排序。
- **但 `cost` 屏上没有单位**：契约里是注释 `cost: z.number(), // 代价（万元）`，
  而回包 `unit` 只有 `%|天|吨||电芯/日`，前端 `DecisionPlayPanel.tsx:639` 明写
  `return fmt(v); // cost / frac：不臆造单位（诚实）`，该文件 `万元` **0** 次、`代价` **8** 次（金丝雀）。
  ⇒ 我能排序 248 / 90 / 1730，**但读不出 1730 到底是 0.17 亿还是别的量级** —— 相对可用、绝对不可用。
- **「沙盘收窄」是减法不是推演**：`sandboxNarrowing = {beforeGap 27.8, afterGap 25.1198, narrowedPct 9.64, ticks 0}`，
  而 `recommendedPlan.totalClosesGap = 2.6802`。**`27.8 − 2.6802 = 25.1198` 分毫不差，且 `ticks:0`**
  ⇒ 这一步没跑任何一拍传导，是把三个方案的 `closesGap` 加起来减掉。
- **与扰动不连通**：演习回包里 `options` / `matrix` / `recommendedPlan` / `impedimentPlays` **各 0 次**
  （金丝雀：同一把中文尺子在同一回包量到「卡点」**52** 次）；`decision_play` 的入参是 `factorId`，不是扰动。
  深链 `fromImpediment` 确实存在（`sandboxConsoleModel.ts:874` 写 → `DecisionPlayPanel.tsx:672` 读），
  但契约写死 **`status==="JOINED"` ⟺ 传 `factorId`，「没对上就一个 factorId 都不传」** ⇒ 对不上时
  跳过去看到的是**通用根因**（实测默认 `cf-decision-gap`「价格预判缺失」），不是我刚砍的那张单的方案。

### ⑤ 长期那一半（防复发）：**看得见、改不动**
TOPIC 后半句「找到流程上需要调整的部分，避免类似情况再次发生」——**这一半今天是只读的**：

- `decision_play.triggers` 真读真算，**这是全平台最接近「流程」的东西**，且信号是真值：
  `licarb_pct_cum 18.45 > 12` → **fired** → 启动备份供应商认证；
  `li_carbonate_price 96000 > 90000` → **fired** → 长协重谈加价格联动条款；
  `usd_cny 7.18 > 8` → 未触发 → 汇率对冲。
- **三条的 `thresholdSource` 全是 `trigger.default`。** 引擎侧覆盖路径是真的
  （`solvers/service.ts:4013`：`rule.params[triggerId]` 存在则记 `rule.params`，否则 `trigger.default`），
  三条 `TriggerRule` 的 `cfgRuleKey` 实测**都是 `trigger_thresholds`**。
- **但那条规则今天不存在**：`GET /a/v1/rules?pageSize=200` → **30 条、全 `PUBLISHED`**，
  key 逐个是 `C01…C35`，**`trigger_thresholds` 0 条**（金丝雀：30 条已发布，字段可读）。
  ⇒ 照铁律 0.5 三分法，这是**「接了线没数据」**，不是没接线 —— 修法是补配置，不是造功能。
- **屏上也没有入口**：前端 `cfgRuleKey` **0** 文件（金丝雀：`thresholdSource` **5** 文件、`triggerId` **2** 文件
  —— 屏上**显示**得出「这个阈值是默认值」，却**没有任何一处**引用改它所需的那个 key）。

⇒ 使用者视角的结论：**我能看见「18.45 > 12 所以触发了」，但我改不动那个 12。**
下个季度同样的料价扰动，跑出来还是同一句话。**这正是 TOPIC 说的「避免类似情况再次发生」没有落点。**

### ⑥ 顺带一条：起点不是实测值
播种日志逐字：`cells 7295 · measuredCells **0** · derivedCells 7295`，
`origin.formula = round(hash01(\`${objectId}|${stateVar}\`) × 100)`，并自带一句
「**凡拿它当起点算出的差值，量级不可当实测读**」。⇒ 上面所有 Δ 的**方向**可信、**量级**不可当实测，
这一点后端已诚实披露，我照它的口径引用，不越界解读。

---

## 直接回答本轮三问

**问 1 · 今天能不能真的跑完一次「扰动 → 财务后果 → 卡点 → 多方案比对」？断在哪一步？**
**能跑完形式，断在第 2 步。** 扰动（11 类事件）✅ → 卡点（621 条）✅ → **财务后果 ❌（0 元）** →
多方案比对 ⚠️（引擎有 3 方案 + 六维矩阵，但**代价无单位**、**收窄是减法**、**不接我刚输入的那个扰动**）。

**问 2 · 哪几个数我看了会做出不同决策？哪几个看了也不会改变任何事？**

| 数 | 判定 |
|---|---|
| `decision_play` 三方案的 **closesGap / cycleDays / risk** 六维矩阵 | ✅ **会改决策**：三个方案 30 天 / 112 天 / 180 天、risk .20/.25/.55，能排序能取舍 |
| `triggers` 的 **fired + 信号真值**（96000 > 90000） | ✅ **会改决策**：告诉我现在该谈长协 |
| **`quote_margin` 的 87.05%** | ❌ **会改决策，但改错**——差 5.1 倍，且警示被前端丢掉。**这是负价值，不是零价值** |
| **621 条卡点/堵点/脆弱点** | ❌ 不改变任何事：`findingsChanged=0`、每类只回前 50、`trustworthy:false`、`dataMode:PARTIAL`。**一个 CEO 读不完 621 条，也没有一条告诉我该先动哪个** |
| **`worldCellsMoved` 412/7295** | ❌ 不改变任何事：契约自己写明它**不随幅度变**（`lossPct` 10→100，两次都是 210），拿它当「我改的数有没有用」必然误判 |
| **`cost` 248/90/1730** | ⚠️ 半个：相对可排序，绝对读不出量级 |

**问 3 · 有没有告诉我「这次的堵点是流程问题、该改哪条规则」？还是只能重跑推演？**
**有半个，且是只读的半个。** `triggers` 就是那个「流程」对象（阈值→行动，一等可编辑），
引擎侧覆盖路径真实存在（`rule.params[triggerId]`）。但**三条阈值今天全走默认、
承载覆盖的规则 `trigger_thresholds` 在 30 条已发布规则里一条都没有、前端零处引用改它所需的 key**。
⇒ 今天的答案是：**只能重跑推演**。

---

## 按「场景敞口 = 单次金额 × 年频次」排序（有出处的数）

**实测可查的真数**（本次 4457 端口真后端）：订单簿 **500 单** · **20 家客户** ·
`Σ Order.value` = **454.64 亿元** · 单均 **9092.9 万元/单** · `Σ qty` = **2,436,095 套**；
单张真实订单 `SO-3391` = **1.61 亿元**；碳酸锂现价 **96,000 元/吨**（`li_carbonate_price` 真信号）。

| # | 场景 | 单次金额（有出处） | 今天支不支撑 | 判定 |
|---|---|---|---|---|
| 1 | **料价冲击**（碳酸锂 96,000 元/吨，`licarb_pct_cum` 已累涨 **18.45%** 且**已触发**） | 波及全 **454.64 亿**在手单的成本侧 | 卡点✅ / **金额 ❌（唯一金额读数错 5.1 倍）** | **真缺口·第一优先** |
| 2 | **客户砍单 / 改期** | **单均 9092.9 万**，最大单 1.61 亿 | 事件✅ / 结论✅ / **财务后果 ❌ · `findingsChanged=0`** | **真缺口** |
| 3 | **临时插单** | 同上量级 | 事件✅ / 走 `portfolio` | ⚠️ 待第 2 轮补测 |
| 4 | **阈值/流程回写**（防复发） | 决定上面三项**明年还犯不犯** | **❌ 只读**（30 条规则里 0 条 `trigger_thresholds`） | **真缺口·被整轮忽略** |
| 5 | `worldCellsMoved` 波及面读数 | **挂不到任何有金额的场景**（自证不随幅度变） | ✅ 已做 | **屏位被占，建议降层** |

> **第三档的金丝雀（照硬要求跑）**：先证明我的「追场景」追法是好的 ——
> `Order.value` → `Σ = 454.64 亿` → 订单相关的卡点**追得到**「客户砍单」这个有金额的场景 ✅。
> 同一把追法用在 `worldCellsMoved` 上：它**不随幅度变**（契约自述 10→100 两次都是 210），
> 因此无法映射到任何「金额 × 频次」，**追不到**。⇒ 这个「追不到」是结论，不是我的工具坏了。

---

## 本轮我**没有**做的（诚实边界，供主持方判权重）

1. **未开浏览器**，未从登录点击走一遍动线（CPU 硬纪律 + 派单「浏览器只在必须时开」）。
   ⇒ **「几步能做完 / 在哪一步卡住」这类动线结论我一条都没下** —— 那是**第 6 视角·操作动线**的题，不是我的。
   我的「屏上有没有」全部落在**前端渲染源扫描 + 金丝雀**上，已逐条标注。
2. **年频次没有本仓出处**：上表只给了**单次金额**（全部实测可查），
   **频次一律留空而不是拍脑袋** —— 按题目边界，拍出来的数不算论据。
   ⇒ 建议主持方在阶段② 把「年频次」作为一个**待补槽位**，而不是当成我漏了。
3. `ORDER_INSERT` / `MATERIAL_REPRICE` 两类事件的**端到端演习未跑**（只跑了 `ORDER_CANCEL`），
   第 2 轮补 —— 但②③⑤ 三条结论**不依赖**它们（②是全回包扫描、③⑤ 是求解器与配置层）。
