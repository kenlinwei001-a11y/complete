# LOOP11 · 怀疑者 · 阶段① 第 1 轮（共 2 次）

- **base commit**：`ec1e707c`（HEAD `b0c44e98` = 其上的空提交）
- **取证时刻**：2026-09-07T03:43Z – 04:0xZ（UTC）
- **树龄探针**：`wc -l apps/datacore/src/synthetic/battery.ts` = **6618**
- **环境**：真后端 `SEED_DEMO=1` 内存模式，`PORT=4762`，禁 `VITE_MOCK`；全部读数经 curl 实测

---

## 发言正文

**① 财务数最可疑：`Customer.receivablePressure` 是按订单条数算的，与金额无关。**
实跑（三元正极 +15%，tick×4）：东风 4 单/**10.02 亿** 与零跑 4 单/**2.39 亿**，应收压力**都是 15.137**；
金额差 4.19×，读数一字不差。全表「压力÷亿元」跨度 1.226–7.722（**6.30× 口径失真**）——
按它排「哪个客户先催收」等于按单数排。病因：46 条边全 `combine:sum`，**43 条 `weightRef:null`**。
金丝雀：同一拍 `Model.costPressure=6.6257` 会随 BOM 占比动（`bom_cost_share` 在 3 条边上真生效）⇒ 引擎没坏，是这条边缺口径。
**对照实验**：给该边加入边归一后，东风必须 > 零跑约 4.19×，且两数不再相等。

**② 承诺没兑现：规则描述与真系数 9/13 不符。**
「客户应收 → 发票逾期」描述 `×0.8`、实际 `0.4`（**2 倍**）；「订单成本 → 客户应收」描述 `×0.6`、实际 `0.5`。
`GET /a/v1/sim/propagation-rules` 原样下发这段中文。金丝雀：4 条一致（含 `×0.65` 那条）。
同处：`coefficientRef` **0/46**、`cadenceNodeId` **0/46** ——「改规则即改推演」与节拍闸门两套机制在、零条边用。

**③ 长期那一半今天是查表，不是归因。**
`POST /a/v1/sim/chain-loss-matrix` 与 `/chain-loss-drill` **不收 sessionId**（源码自陈「本路由不在会话上下文里，也不消费模拟时钟」）。
实测：三元正极 `shortageRisk` 0→95、tick×3 后再调，回包 **md5 与扰动前逐字节相同**（`96109ffe…` / 30446B）；
金丝雀：只改 `so` 参数 md5 当场变（`c2e79d67…` / 19331B）⇒ 工具是好的。
**即：你在沙盘里推演出的任何东西，根因树一个字都不知道。**「避免类似情况再次发生」缺的不是页面，是这条接线。

**该扔的**：`cadenceNodeId` 全链闸门今天挂不到任何有金额的场景（0/46 在用），删掉没人会发现。
