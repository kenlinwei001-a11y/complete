✓ nav-group-coverage:check：后端 16 个 seeded 内置视图全部有 NAV_GROUPS 归属（或已收编）且全在 mock allViews 里；8 条专用 route 全部有 kind:"route" 入口（或已收编）且无悬空条目 —— 刻意不给导航 decision-play（仓主裁决（WO-IA-E2E5E6）：决策推演不该占导航位，已嵌入各决策点（订单链/链阻滞/壳布局三处共用 DecisionPlayPanel）；route 保留 = 深链 query 契约（fromImpediment/imp* 一族）不变）（mock allViews=26 · NAV_GROUPS view 键=28 · route 键=7）
  已收编进沙盘 chain-line-map（workspace.views·沙盘中栏画布**默认**模式「线路图」（进沙盘即在屏上）） · physical-topology（workspace.views·沙盘中栏画布模式条 →「物理拓扑」） · node-inspector（workspace.views·沙盘右栏常驻检视面板 → 页签「变量输入」） · transit-flow（workspace.views·沙盘线路图上的「在途批次图层」勾选框） · chain-impediments（workspace.views·沙盘主屏阻滞点统计条 + 逐条清单（残差见 AUDIT §2）） · cleanroom-attr（static-route·沙盘模式切换 →「归因」→ 档「净室归因」（沙盘关则回退为导航单列）） · what-if（static-route·沙盘模式切换 →「试一手」（沙盘关则回退为导航单列）） · optimize-whatif（static-route·沙盘模式切换 →「求最优」（沙盘关则回退为导航单列）） · disruption-radius（static-route·沙盘模式切换 →「影响半径」（沙盘关则回退为导航单列）） · process-wait（workspace.views·沙盘模式「归因」→ 档「流程等待态」（沙盘关则回退为导航单列）） · procurement-legs（workspace.views·沙盘模式「归因」→ 档「采购四段腿」（沙盘关则回退为导航单列）） · process-stuck（view-defs·沙盘模式「归因」→ 档「流程卡点」（暗发键关着时该档整个不出现·沙盘关则回退为导航单列）） · sim-conduction（view-defs·统一推演控制台顶部页签 →「传导识别」（`UNIFIED_MODE_SPEC.conduction.renderer = \"sim-conduction\"`，经 getRenderer 挂的就是本页组件）） · sim-attribution（view-defs·统一推演控制台顶部页签 →「损失归因」（`UNIFIED_MODE_SPEC.attribution.renderer = \"sim-attribution\"`，经 getRenderer 挂的就是本页组件）） · sim-optimize（view-defs·统一推演控制台顶部页签 →「方案寻优」（`UNIFIED_MODE_SPEC.optimize.renderer = \"sim-optimize\"`，经 getRenderer 挂的就是本页组件）） · sim-console（view-defs·统一推演控制台首档「指标态势」（= 本页首屏的合并去向：37 张指标卡墙取代旧首屏；旧版面 /v/sim-console 深链仍可直达））
✓ 判据⑨ 组收编承诺未被掏空：2 个组做了收编承诺 —— 「推演」→ sim.sandbox 开时剩 6 项 · 「归因与风险」→ sim.sandbox 开时剩 0 项
  （剩 0 项 = 空组自动隐藏 = 承诺兑现；剩 N>0 项且那 N 项在下面的豁免表里逐条有理由，也算兑现）
  组豁免 推演::sim-sandbox（它就是被收编进去的那个控制台本身；收编自己在逻辑上不成立（沙盘开着反而必须看得见入口）） · 推演::project-sim（独立场景：项目级推演有自己的求解器与整页流程，不是沙盘五问之一；收进去只会把沙盘撑爆） · 推演::global-sim（独立场景：全局推演有自己的求解器与整页流程，不是沙盘五问之一；收进去只会把沙盘撑爆） · 推演::risk（独立场景：风险看板有自己的数据面与整页流程，不是沙盘五问之一；收进去只会把沙盘撑爆） · 推演::order-chain（独立场景：订单全链有自己的求解器与整页流程，不是沙盘五问之一；收进去只会把沙盘撑爆） · 推演::sim-unified（它就是本组四个台被收编进去的那个控制台本身（/v/sim-unified）；收编自己在逻辑上不成立，合并壳开着反而必须看得见入口）
✓ 判据⑦ 渲染器可达：apps/frontend-shell/src/views/registry.ts 的 31 个 registerRenderer 键，每个都至少有一条路径渲染得到（后端派单 25 个 renderer · 专用 route 8 条 ⇒ 可达并集 33）—— 豁免棘轮 0/0，无豁免
