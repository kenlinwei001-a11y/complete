# GOALMAP-GAPFILL · 客户 COO 目标原文补漏取证

**base commit**：`fbfa88f1`（PIN）· 取证时刻 2026-09-11 · 树龄探针 `wc -l apps/datacore/src/synthetic/battery.ts` = **7053**

> ### ⚠ 派单前提有一条不成立，已处置并记录（必须先读）
> 派单写「你的 worktree 已在 `fbfa88f1`」。**实测 HEAD = `778cc589`** —— 正是 CLAUDE.md 铁律 3
> 点名的 LOOP10 陷阱树（06-15，`battery.ts` **1249** 行）。祖先测试：
> `git merge-base --is-ancestor HEAD fbfa88f1` → **YES ⇒ 落后**；`git rev-list --count HEAD..fbfa88f1` = **4665**，
> `git diff --shortstat` = **3039 文件 / +756,668 / −189,994**。
> 在那棵树上量，结论会系统性偏向「整条缺」。**已 `git checkout --detach fbfa88f1`（不建分支、不 commit、不 push）**，
> 树龄探针由 1249 → **7053** 行复核通过。下表全部读数取自 `fbfa88f1`。

**全局金丝雀**（证明 pathspec 写法与 grep 本身没坏）：`git grep -l "AnnualScenario" -- 'apps/*/src/*'` = **25 文件**。

---

## 判定表

| 编号 | 能力项 | 判定 | 实测证据（file + 符号 + 亲手跑出来的数） | 金丝雀 |
|---|---|---|---|---|
| **A1** | 产销匹配模型（模型本身） | **部分**<br>（**有两个，且互不相连**） | **「产销匹配」这个词不是模型**：全 src 命中 **2 处**，都在 `agentcore/src/dril/tag-taxonomy.ts`（`l3_scenario` 标签表 + `{tag:"产销匹配", keywords:[...]}`）——是**分类标签**。<br>**模型甲 · `SopVersion`（流程态，`datacore/src/sop.ts`，538 行，S1.8，状态机 DRAFT→IN_REVIEW→EXEC_MEETING→FINAL）＝真匹配**：`step3` 供应评审算 `sup = Σ_基地 Σ_{w=1..4} weeklyWan × certFactor × curveMult(params,w,maintWeek)` + `increments`，输入是 `computeRollup(c)`（真基地周产能）· `c.certByModel`→`certFactors`（认证）· `maintWeekOf(c,baseId)`（检修周）；`gap = dem − sup`，`gap > params.sop.gapRed(=2)` 即推 agenda「产销缺口」。**这条真把产（产能×认证×检修×爬坡）与销（需求）接上了。**<br>**模型乙 · `SopVersionRow`（本体对象，`battery.ts:3566`，displayName「S&OP版本演进」，domain `plan`）＝写死弧**：props `{verId,ver,date,demand,supply,note,isFinal}` + 派生 `gap = demand − supply`。种子只 **4 行**（`battery.ts:6417` `[1,3,5,7].map`），`demand = 375×(0.96+i×0.02)`、**`supply = demand×(0.9+i×0.03)`（供给由需求派生，不由产能派生）**。我按该公式算出的四行：<br>`V1 360/324 gap36(10.0%)`·`V3 368/342 gap26(7.1%)`·`V5 375/360 gap15(4.0%)`·`V7 383/379 gap4(1.0%)`，**Σ正缺口 = 81 万套/年**。缺口率 10.0→7.1→4.0→1.0 是脚本化收敛弧。<br>**接缝断裂（关键）**：`SopVersionRow` 全仓**只有一个写入方** = 种子 `synthetic/service.ts:812 putAll("SopVersionRow", g.sopVersionRows,"verId")`；`step3` 算出的 `sup` 只落 `repos.sopVersions`（`v.steps.s3`），**从不写进 `SopVersionRow`**。而求解器与驾驶舱读的是**乙**：`supply_demand_gap_attribution`（`G = Σ SopVersionRow max(0,demand−supply)`）、`cockpit_kpi`（`可供给V7 = 最终版 SopVersionRow.supply`，`ontology-signature.ts:187`）。⇒ **屏上那个数来自写死弧，不来自 step3 那次真计算。**<br>**与「集团产能模型」是两个东西**：产能侧实体 `CapacityPool`（一等对象，domain `capacity`，props `poolId/lineId/baseId/capacityCellsDaily/status`）+ 边 `has_capacity`(Line→CapacityPool) / `consumes_capacity`(WorkOrder→CapacityPool·量在边上)；产销侧实体 `SopVersionRow`（domain `plan`）。**两者之间无边、无公式引用。** | ①「产销匹配」报 2 处前，同法搜 `AnnualScenario` = 25 文件 ⇒ 搜法没坏。<br>② 判「乙的 supply 无产能输入」前：探针 `capacityPools\|capacityCellsDaily` 在 `battery.ts` 全文命中 **10**，在 S&OP 生成块（6415–6428）命中 **0**。<br>③ 判「只有一个写入方」前：`putAll("` 在种子里命中 **89** ⇒ 该探针找得到别的类型。 |
| **A2** | 「产销快速匹配」里的**快速** | **整条缺** | **① 时延指标/SLO：无。** 全仓 `SLO`（用 `SLO[^A-Z]` 排除 `SLOT` 污染——**第一次直接搜 `SLO` 被 `SLOT` 灌了一屏，这正是「X 不度量 Y」**）命中 **23 文件**，逐条看**全部**是同一件事：B↔A 缓存传播 `SLO ≤60s`（`agentcore/src/event-subscriptions.ts:10`「事件 60s / 配置 TTL 5min」·`llm/datacore-directory.ts:25`·`server.ts:2144`·`SYSTEM-ONTOLOGY.md:3194`）。**与匹配时延无关。**<br>**② 实测耗时埋点：匹配/求解路 = 0。** 探针 `elapsedMs\|durationMs\|tookMs\|performance.now` 在 `apps/datacore/src/solvers/*.ts` 命中 **0**。<br>**③ 一次匹配多久：读不出（需起服务）。** 唯一的时间控制 `solveBudgetDeadline`（`solvers/service.ts:703–707`）读 `process.env.SOLVER_INCUMBENT_BUDGET_MS`，**未配/非法 → 返回 `{}` → `incumbentDeadlineAt=undefined` → `portfolio.ts:562 budgetExhausted()` 恒 false**。该变量全仓 **9 处**命中，**docker-compose.yml / DEPLOY.md / deploy/ 里 0 处** —— 代码自己写着「运维必须把它设成 < AgentCore 的 `SOLVER_RUN_TIMEOUT_MS`」（`portfolio.ts:107`），**而它哪里都没被设**（典型「接了线没数据」）。另有 `SOLVER_RUN_TIMEOUT_MS` 默认 **15000ms**（`agentcore/src/config.ts:32`）——那是**放弃超时**（失败上界），不是 SLO，且同样不在部署配置里；因 DataCore 侧预算是关的，15s 到点时**没有可交的可行解**（`agentcore/src/server.ts:2550` 自述）。<br>⇒ **这条承诺今天无法证伪也无法证实。** | ② 报「solvers/ 零埋点」前，同一条探针在 `apps/datacore/src/sim/*.ts` 命中 **3 文件 4 处**（`agent-proposal.ts`·`disclosure.ts`·`pair-weights.ts`×2）⇒ 探针有鉴别力。<br>③ 报「配置里 0 处」前，`SEED_DEMO` 在 `docker-compose.yml:79` 与 `DEPLOY.md`（55/61/65）命中 ⇒ 我确实在搜对文件。 |
| **B1** | 辅助**产销策略**制定<br>（卖什么/卖给谁/什么价/什么量） | **部分**（**四问只答了「什么价」的一角**） | **什么价 ✅ 但只有一条、且未注册**：`frontend-shell/src/views/plan/OrderChainView.tsx:1253` 在 `!fin.marginOk && fin.priceUpPct>0 && unitPrice>0` 时推杠杆 `{objectType:"Order",prop:"unitPrice",value:unitPrice×(1+priceUpPct/100)}`，走 `plan_change`（`ACTION_WIRING` 标 **WIRED**，`actions.ts:55`）→ 真写属性 + `runDerivations`。**但 `unitPrice` 不在 `LEVER_PROP_SPECS`**（`solvers/lever-meta.ts:73–97`，共 **13 条**）⇒ 无 label/unit/kind，屏上走裸值兜底。<br>**卖什么 / 卖给谁 / 卖多少 ❌**：那 13 条杠杆**全是供给侧** —— `Equipment.oee_current`·`Line.utilization`·`Process.{yield_baseline,attendance,utilization,shifts,shiftHours}`·`MaterialBalance.coverage`·`Material.{onHand,leadTime}`·`Order.outsourceRatio`·`ChangeoverMatrix.minutes`·`Shipment.etaDay`。**零条价格、零条客户、零条品类。** `Customer` 在 `lever-meta.ts` 与 `capacity-factors.ts` 命中 **0**。<br>**品类结构：唯一发现问题的那条不给建议。** `plan_audit` 的 `R01 结构偏离`（储能占比偏离基线 `essDev>essShareTol`，`solvers/plan.ts`）是全文 **10 个 `fix:`** 之外**唯一没有 `fix` 的条目**，而建议清单 `S[]` 只收 `item.fix && Object.keys(item.fix.patch).length>0` ⇒ **结构问题永远不产生建议**。<br>**「经营方案比选」是查表不是推演**：`plan_generate`（场景卡 **S05**「经营方案比选·推荐哪个经营方案？」，`agentcore/src/scenarios-catalog.ts:65`）的本体读取签名是 **`plan_generate: []`**（`solvers/service.ts:507`）——**读 0 个本体对象类型**；三方案由 5 条写死系数元组算出（`battery.ts:1113–1119`：`A 保毛利型 rev×1.12 gm+0.014 share+6 capex0`…`C 扩产型 rev×1.2 capex27`…），base 是**归一化 `rev:100`**，`outcome.rev = base.rev × eff.rev`。仓库自己在三处点名：`app.ts:986`/`repo/repo.ts:378`「plan_generate 不读任何本体对象…只会冒充『可被推演关联的实体』」。 | ① 报「unitPrice 未注册」前，`outsourceRatio` 在 `lever-meta.ts:94` 命中 ⇒ 我确实在读对的注册表。<br>② 报「Customer 零杠杆」前，`"Customer"` 在 `battery.ts` 命中 **15** ⇒ 它确实是个存在的对象类型，不是我拼错了名字。<br>③ 报 `plan_generate:[]` 前，同一张签名表里 `risk_timeline` = **9** 个类型、`audit_timeline` = 5、`plan_audit` = 1 ⇒ 空数组是真的空，不是表没解析出来。 |
| **B2** | 辅助**产能策略**制定<br>（扩线/换产/外协/检修） | **部分**（**四类只真支持「换产」一类**） | **有一套真机制**：`solvers/impediment-options.ts`（**864 行**）阻滞点→候选方案枚举器，**两个生产调用方**（`chain-impediment.ts:1101` 沙盘 · `solvers/service.ts decisionImpedimentPlays` 决策路）；纪律写死：**零人工「阻滞点→方案」映射表**、**零业务/步长常数**（档位只取规则阈值本身或同侪对象真实取值）、族别由**拨完重算的结果**判定（`sensitivity===0` 即丢弃）。<br>**词表 `CAPACITY_FACTOR_BINDINGS`（`packages/contracts/src/capacity-factors.ts`）= 20 个因子，我逐行数出 `writable:true` **11** 条 / `writable:false` **9** 条。**<br>**逐条对目标原文点名的四类：**<br>· **换产 ✅** ⑤换型损失 `ChangeoverMatrix.minutes`（writable，ruleGate C08）<br>· **检修 ❌** ⑱设备检修 `MaintPlan.week` **`writable:false`** ⇒ 枚举器**永远拨不动检修排布**<br>· **扩线 ❌** 不在该词表（⑪在制WIP `Line.capacityDaily`、⑫产线平衡 `Line.max_capacity_day` 均 `writable:false`）；扩产走 `capex_scenario`，而它**读 0 个本体对象**（`solvers/service.ts:509 capex_scenario: []`）<br>· **外协 ❌** 不在该词表（在另一套 `LEVER_PROP_SPECS` 里作 `Order.outsourceRatio`）<br>**唯一把四类凑齐的地方没接线也没产品面**：`countermeasureCombo`（`solvers/extended.ts:578`）按成本贪心闭产销缺口，5 杠杆 = 认证解锁/换型/检修错峰/外协/扩产。但缺省 `release` 是 **gap 的固定比例**（`gap×0.3 / ×0.15 / ×0.1 / outsourceRedlineCap(gap) / ×0.5`）、`unitCost` 是写死常数（**0.5/0.6/0.7/1.4/2.2**）。注释称「编排时由对应求解器回填实算值」，**实测 `deriveExtendedArgs` 的 `case "countermeasure_combo"` 只派生 `gap`（`= Σ Order.qty × 0.15`，这一半是真数据），一个字都不回填 `levers`**（铁律 1.5 判据四：注释说的不算数）。且 `apps/agentcore/src` 与 `apps/frontend-shell/src` 内**零调用方**，`scenarios-catalog.ts` 里**无场景卡**。 | ① 报「无场景卡」前，同一文件里 `plan_generate` 有场景卡 **S05** ⇒ 该文件确有卡、我的搜法能命中。<br>② 11/9 的计数是我对 `grep 'mark: "'` 的 20 行逐行分类数出来的（总数 20 = 11+9 自洽）。 |
| **B3** | 「释放产能价值」这句结果宣称 | **部分**<br>（Δ 机器有且已上屏，**但没有一个金额口径成立的字段**） | **字面无**：`释放产能` 在 src 命中 **0**；`产能价值` 命中 **3**，全部是「**单位产能价值密度**」（`optimizer-client.ts:242`·`simSolvers.ts:975`·`MultiObjWhatifPanel.tsx:100`），用途是背包贪心的**排序键**，不是产出值。<br>**但确实有一个会动的 Δ，且已上屏**：`GlobalSimLeverDelta = { lever, before: GlobalSimKpi, after: GlobalSimKpi, provenance }`（`packages/contracts/src/global-sim.ts:258`），产出于 `solvers/portfolio.ts:989–1000,1113`，前端 `views/sim/GlobalSimLevers.tsx:238–242` 真渲染 before/after 七维 + 每值溯源（`drillType=Lever`·R13）。`GlobalSimKpi` 八项：`ontime / cost / changeoverHours / freight / fgInv / transitInv / margin`。<br>**口径缺陷（这才是判「部分」的理由）**：`margin = round(servedQty × avgUnitPrice − baseCost, 2)`（`portfolio.ts:819`），代码自己在 `:816–818` 标注「margin 是**毛利代理**…被减数与减数**两者不同源**，故只可作**方案间相对比较**，**不可当真实毛利读**」。<br>⇒ 「本方案多释放产能 X 套」有近似物（`ΔservedQty` / `Δontime`）；**「多产出 Y 元」没有任何一个字段的口径撑得住** —— 唯一的金额型字段自带「不可当真实毛利读」的作者声明。 | 报「释放类字段不存在」前，同族探针（`可释放\|releasable\|挖潜…`）**找到了** `releasableCash`（库存**可释放资金**，`solvers/extended.ts:324`，且 `service.ts:568` 声明在输出形状里）⇒ 探针对「可释放 X」这个概念有鉴别力，它报「产能没有」是真没有。 |
| **C1** | 「支撑管理层科学决策」＝推演过程可披露（六问） | **部分**<br>（**沙盘路六项全有且已上屏；求解/匹配路六项压根没有**） | **必须分两条路答，合并会盖住缺口。**<br>**路一 · 沙盘推进 `POST /a/v1/sim/sessions/:id/tick`（`app.ts:2533`）—— 六项全有**，契约 `SimRunDisclosureSchema`（`packages/contracts/src/sim-disclosure.ts:293`）：<br>①引用数据 → `.data`（含 `snapshotVersion`，真后端是 sha256 前 12 位）②本体切片 → `.slice`（`scopeReport{kind,target,**hops**,objects,links,droppedObjects,droppedLinks}`，`simSliceKey()` 是 sliceKey 唯一构造处）③命中规则 → `.rules`（**含 `coefficientSource`** = 内联还是配置）④约束来自哪条规则 → `.constraints.stateVarBounds[].{decayRef:"<ruleKey>.<paramKey>", decayRuleExpression}` + `ruleClamps` + `saturations` + `cadence`⑤是否调 LLM → `.agent{invoked,calls,provider,model}`，**做成必填非 optional**，契约注释写明「不许留白」，并另有 `adversary.selectors` 说明「还手是规则表选的不是模型选的」⑥各环节耗时 → `.timings[]`（`graph/shadow/engine/persist/total`，`PhaseTimer` 在 `sim/disclosure.ts`，「忘了停 = 不计入，不会记成 0 冒充很快」）。<br>装配处 `sim/disclosure.ts`（360 行）自述「**本文件一行都不参与计算**」，只重组已产生的中间量。<br>**已上屏**：`endpoints.ts:998 simTick(sessionId,n,disclose)` → `Console0828.tsx:468 readDisclosure(...)` → `rail/DisclosurePanel.tsx`。<br>**但默认不发**：`app.ts:2548 wantDisclosure` 需 `?disclose=1` 或 body `disclose:true`，理由写在旁边（快照指纹要遍历 12,745 对象 + 12,192 边，默认发等于每拍都付这笔账）。⇒ 这一条属于**「算得出且已露出，只是按需」**，不是缺。<br>**路二 · 求解器（60 个）—— 六项压根没有。** 全仓 **60** 个求解器（`catalog.ts` 去重 `{ key: "…"` 计数 = 60），输出里带 `disclosure:` 块的**只有 1 个**：`capacity_ledger`（`solvers/capacity.ts:937`），且该块只有 `formula/edgeAmountSource/units/demandMultiplier/agentInvolved:false/counts/totals` —— **无 sliceKey、无跳数、无 ruleKey+系数、无耗时**；代码自注「本求解器**今天没有任何前端消费方**」。<br>⇒ 六问在求解器路的答案：①部分（仅 `counts`）②无 ③无 ④无（只有 `ruleRefs` 列 key，无系数/无表达式/无来源）⑤仅 1 个求解器给 `agentInvolved` ⑥**无**（A2 已证 solvers/ 零计时埋点）。<br>**「算得出但没露出」vs「压根没有」的分开判**：沙盘 = 算得出且已露出（按需下发）；**求解器 = 压根没有**（没有计时器、没有切片回执、没有系数回执，不是算了不给）。 | 报「60 个里只有 1 个」前，同目录同写法探针：`summary:` 命中 **12 文件**、`provenance` 命中 **12 文件**、`disclosure:` 命中 **1 文件** ⇒ 探针有鉴别力，不是正则写坏了全报 0。 |

---

## 需起服务复核

1. **A2③「一次匹配实际多久」** —— 本单唯一真正需要起服务的问题。因为求解器路**零内部埋点**，只能从外部计时：起 `SEED_DEMO=1` 的 datacore（4001），对 `portfolio_optimize` / `supply_demand_gap_attribution` / `sop_reschedule` 各 invoke 一次，用**外部 wall clock** 记 p50/p95；同时分别在 `SOLVER_INCUMBENT_BUDGET_MS` 未设与设为若干值两种条件下各跑一轮，以确认 `budgetExhausted()` 那条分支**是否曾被进入过**（今天按配置推断是从未进入）。
2. **沙盘六项披露的真实读数** —— `handlers.ts` 注释里记了一次真后端实测（2026-09-03）：`12,745 对象 / 12,192 边 / 46 条规则 / 4,577 次饱和 / **517 毫秒**`。**这是注释里的记录值，我没有重跑**（铁律 1.5 判据四：注释说的不算数）。复验：`POST /a/v1/sim/worlds/:id/tick?disclose=1` 读回包 `disclosure` 六项。
3. **B2 枚举器的实际产出条数** —— `lever-meta.ts` 注释称「实测 `chain_impediments` 12 条候选里 `Process.utilization` 占 2 条」。枚举器是否在 demo 租户真枚举得出候选、`noCandidateReason` 命中率多少，必须真跑 `chain_impediments` / `decision_play` 才能给数。

## 我可能错在哪（≤3）

1. **我只量了落盘的内置注册表，没量运行期仓储。** 租户可经 `registerType` 自注册 ActionType（`ACTION_WIRING` 查不到的键默认 `NO_WRITE`，`actions.ts:188`）。若 demo 租户在运行期注册过价格/客户类动作，B1 的「零客户杠杆、价格只有一条」会**偏严**。判「零」的依据是源码注册表，不是跑起来的实例表。
2. **B2 判 `countermeasure_combo`「无产品面驱动」用的是 src 无调用方 + 无场景卡，这两条 grep 都看不见「字符串键分发」。** 该求解器在 `catalog.ts:120` 有完整 description/answersQuestions/tags，QOS 路径B 的 Agent **可能按目录描述自行选中它**再走通用 invoke —— 那条路是模型在运行期按语义挑 key，静态搜索一次都看不见。我没能排除这种间接调用。
3. **A1 的「两个模型互不相连」我验的是写入方，没有穷举派生/物化通路。** 我确认 `SopVersionRow` 唯一写入方是种子 `putAll`，且 `derivedProperties` 只有 `gap`；但我**没有**逐条读完 `runDerivations` / `timeseries` 物化的全部写入目标。若存在某条派生在播种后改写 `SopVersionRow.supply`，则「屏上的数来自写死弧」这一条会**偏严**（`step3` 那条计算链本身仍然成立，不受影响）。
