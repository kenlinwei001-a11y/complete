# RECHECK-G2 · 13 条判定复核（全程只读·未起任何服务）

## 报告头（自证在 PIN 上）

| 项 | 值 |
|---|---|
| 起步 HEAD | `778cc589`（旧树·`battery.ts` **1249** 行）⇒ **落后，已重钉** |
| 复核 HEAD | **`20b0a322`**（`origin/claude/inspiring-gates-aqczjg`） |
| 树龄探针 | `wc -l apps/datacore/src/synthetic/battery.ts` = **7053** ✅ |
| 取证时刻 | 2026-09-11 |
| `git status --porcelain` | **空**（一行未改·未 commit·未 push） |

**全局金丝雀（先自证量法，再报否定结论）**
- `grep -rn NETWORK_SCOPE_KEY apps/*/src packages/*/src` → **10** 命中（非 0 ⇒ glob 展开正常）
- `git ls-files -- 'apps/*/src/*'` → **692**；`git ls-files -- 'apps/*/test/*.test.ts'` → **580**（避开了「含通配 pathspec 当目录前缀」那个坑）

---

## 逐条复核

| 编号 | 我原来说的 | 实测 | 判定 | 证据（file + 符号 + 亲手跑出的数） | 金丝雀 |
|---|---|---|---|---|---|
| **1.1-d** | 工序排程最优化**默认部署**调用即抛「未接入最优化引擎」；可证最优族 11 个求解器前端零消费 | **两半都错。** ①「默认部署」= `docker compose up`，而 `docker-compose.yml:52` 有 `optimizer` 服务（`./services/optimizer`·CP-SAT），`:91` `OPTIMIZER_BASE_URL: ${OPTIMIZER_BASE_URL:-http://optimizer:4003}` 是**默认值**，datacore `depends_on optimizer(service_healthy)`；sidecar `services/optimizer/server.py:1056 MODELS` **13 个模型全在**（含 `job_shop_schedule`→`solve_job_shop_schedule:965`）⇒ 默认部署**不抛**。抛的只是**源码/内存模式**（`app.ts:531` 未配即 `optimizer=undefined`）。② 11 个里 **6 个有真前端消费方**：`views/OptimizeWhatifView.tsx:56-60` 的 family 下拉直接摆着 `facility_location / min_cost_flow / set_cover / independent_set / combinatorial_auction`，该页 `App.tsx:155` 有路由 `v/optimize-whatif`、`registry.ts:63` 有 renderer、`SandboxView.tsx:2514` 还内嵌（`mode==="optimize"`）；`multi_objective` 由 `console/useParetoFrontier.ts` + feature binding `opt.multiobj` 消费。③ 余下 5 个（selection/assignment/sequencing/packing/job_shop）**经 QOS 问答路可达**：`catalog.ts:192-195` 逐条带 `answersQuestions`/`tags`，`agent/live-capability-map.ts:134` 从 A 侧 `kind:"solver"` 检索建活目录，`agent/navigation-slice.ts:497` **`for (const key of Object.keys(cat)) candidateKeys.add(key)`** —— 活目录模式下**全部注册求解器**进候选，再由 `invoke_solver` 按**字符串键**分发（grep 一次都看不见）。 | **被推翻** | `docker-compose.yml:52/91`；`services/optimizer/server.py:1056`；`apps/frontend-shell/src/views/OptimizeWhatifView.tsx:56-60,726,857`；`App.tsx:155`；`views/registry.ts:63`；`apps/agentcore/src/agent/navigation-slice.ts:495-530`；`apps/agentcore/src/agent/live-capability-map.ts:109-165` | 逐 key 计数（frontend/agentcore）：facility_location 19/10 · min_cost_flow 5/7 · set_cover 5/1 · independent_set 5/1 · combinatorial_auction 5/1 · multi_objective 5/3 —— **无一为 0**；作为对照，`utilizationPct` 同命令全仓 **2** 命中且全在 datacore（见 2.5）⇒ 量法有鉴别力 |
| **2.1** | 集团产能「抬头数」走 13 个手写 gwh 求和；rollup 侧没有与型号无关的集团总量 | **前半机制说错，后半事实说错。** ① 抬头数**不是**前端把 13 个常数加起来：后端 `synthetic/service.ts:1766-1769` 下发的 widget 是 `query:{kind:"objects-aggregate", objectType:"Base", agg:"sum", prop:"gwh"}`，前端 `DashboardView.tsx:697-706` 走 **`POST /a/v1/objects/aggregate`** 服务端全量聚合（注释明写「要总量的 widget 走 objects-aggregate·服务端全量聚合」）。底层 `Base.gwh` 确实来自 `contracts/base-registry.ts:102-114` 的 **13 条**手写值，但「13 个手写值」≠「13 个手写求和」—— 加多加少一个基地对象，抬头数自己会变。② rollup 侧**有**与型号无关的集团总量原料：`solvers/capacity.ts:51 computeRollup(c)` 全函数**零次出现 `modelId`**（文件自己在 `:186` 写着「computeRollup 与 modelId 无关」），沿 设备→工序→产线→基地 金字塔算出逐基地 `dailyCells/weeklyWan`，且已注册为独立求解器 `capacity_rollup`（`catalog.ts:84`·`service.ts:6075` 返 `{bases, ruleRefs:["C01","C02"]}`）。 | **被推翻** | `apps/datacore/src/synthetic/service.ts:1766-1769`；`apps/frontend-shell/src/views/DashboardView.tsx:694-706`；`packages/contracts/src/base-registry.ts:102-114`；`apps/datacore/src/solvers/capacity.ts:51,186`；`apps/datacore/src/solvers/service.ts:6075-6078` | `grep -c "baseId:" packages/contracts/src/base-registry.ts` = **14**（1 行是类型声明 + **13** 条基地）⇒ 「13」这个数本身是对的，错的是它被怎么用 |
| **2.2** | 基地负荷率是 13 个手写常数，视图不刷新 | **一半对一半错。** ✅ `Base.util` 确实是常数：`base-registry.ts` 逐基地 `util: 88/85/82…`，且 `battery.ts:1223 baseDerived` 只有 3 条（`orderCount`/`committedQty`/`oeeIndex`）—— **`util` 不在其中**，而 `ontology.ts:1541 runDerivations` **只写 `type.derivedProperties`**（`:1553` `if (type.derivedProperties.length===0) continue`）⇒ 派生管线永远不碰它。❌ 但「没有别的屏用真时序」是错的：`views/sim/PhysicalTopologyView.tsx` 的车间格 util **走真 EquipmentOEE**——`views/sim/physicalTopology.ts:327-334` 对 `typeKey:"EquipmentOEE"` 做 `sum_actualProductionTime / sum_plannedProductionTime`（`:465-483` 真算、`:486` 取不到就诚实标 EMPTY 不补 0），台账 `REAL_DATA_ENTRYPOINTS` 标 `status:"connected"`、实测 **5460 行**。同屏**两个 util 是两个口径**（行首基地级=registry 常数；格内=计划工时利用率真值）。 | **部分推翻** | `apps/datacore/src/synthetic/battery.ts:1223-1230`；`apps/datacore/src/ontology.ts:1541-1580`；`apps/frontend-shell/src/views/sim/physicalTopology.ts:327-334,465-489,708`；`PhysicalTopologyView.tsx:435` | 反证 `runDerivations` 的写入目标是**可穷举**的（只有 `derivedProperties`）—— 这正是取证员自述「没穷举」的那一步，已穷举 |
| **2.3** | 跨基地调度腾出的产能恒落第一个基地；调剂杠杆是平标量 0.6 | **前半在一条路上成立、在另一条路上被推翻；后半成立但性质要改写。** ① `actions.ts:365` `homeBase = [...bases].map(String).sort()[0]` —— GlobalSim 采纳那条路的 `fromBase` 确实恒取排序首个；但**目的地不是固定的**：`:367` `toBase = base` 取自求解器选出的服务基地。② 真正做「腾产能」的那条路**完全不是恒定的**：`solvers/decision-info.ts:464 displacedOrdersFor()` 从**其他各基地**的窗内在手单里按「优先级低先让 → 交期晚先让 → 订单号」挑，逐张给 `{so, cust, baseId, baseName, displacedQty, delayDays}`，`delayDays = displacedQty ÷ 该单所属基地的 baseFreeDaily`（真派生 `Line.capacityDaily×(1−util/100)`），取不到就 `delayDays:null` + 原因。③ `0.6` 的性质：`base-outlook.ts:145 coeff("crossBaseAbsorbPct", 0.6)` 是**依赖注入**（`service.ts:3589-3590` 从 PUBLISHED RuleEntry `base_outlook_coeffs.params` 读），但该规则**全仓只有读方没有写方** —— 属「接了线没数据」，实际恒走代码兜底 0.6。且它只决定**吸收总量**，分摊到哪些单/哪些基地由 ②真算。④ 另有租户侧通路：`plan_change` 的 `payload.levers[{objectType,objectId,prop,value}]`（`actions.ts:44-49`）可点名任意对象写真值 —— 这条不是「调拨型 effect」，但它让「目标基地由调用方指定」在机制上已存在。 | **部分推翻** | `apps/datacore/src/actions.ts:363-383,44-56`；`apps/datacore/src/solvers/decision-info.ts:464-519,656-657`；`apps/datacore/src/solvers/base-outlook.ts:131-145,152-156`；`apps/datacore/src/solvers/service.ts:3589-3600` | 求「0.6 是不是可配」时先用**已确知被种下**的规则做对照：`C21`（`battery.ts:802`，`params:{balanceDeviationPct:0.1}`）能查到定义点 ⇒ 量法好的；`base_outlook_coeffs` / `portfolio_optimize_coeffs` 同法查**零个定义点** |
| **2.4** | 单→线：开关缺省关、零调用方开启、输出行类型无产线字段 | **第三条错，前两条按口径成立。** ✅ `lineGranularity` 缺省关（`portfolio.ts:278` `=== true`）；仓内**无 UI 调用方**（`grep lineGranularity apps/frontend-shell/src apps/agentcore/src` = **0**）。❌ 但「输出行类型无产线字段」是错的：`portfolio.ts:732 lineIdOfUnit(uid)` 把 `baseId#lineId` 拆开，`:965` 与 `:1049` 逐行写 **`{ orderId, base: baseIdOfUnit(al.base), line: lineIdOfUnit(al.base), window, qty }`**，契约 `packages/contracts/src/global-sim.ts:194` **`line: z.string().nullable().optional()`** 已定义。且 `unitCurrentModel`/`lineModelCompat`/线级换型小时（`:350-354` `coHoursTo(from,to,lineId)`）整条线粒度逻辑都已写完。开关也**不是只有 UI 能开**：`service.ts:3512 lineGranularity: asBool(args.lineGranularity)`，而 `args-schemas.ts` **没有 `global_sim_optimize` 条目**（args 原样透传）⇒ 任何 REST / QOS `invoke_solver` 调用方传 `{"lineGranularity":true}` 即生效。 | **部分推翻** | `apps/datacore/src/solvers/portfolio.ts:277-296,400-430,732,965,1049`；`packages/contracts/src/global-sim.ts:190-199`；`apps/datacore/src/solvers/service.ts:3512` | 报「零调用方」前先跑**兄弟参数**做对照：`frozenCapacityMode` 同一个 args 对象里，`GlobalSimView.tsx:527,566,574` **3 处**真传 ⇒ 量法能看见「有人传」，`lineGranularity` 的 0 是真 0 |
| **2.5** | 5 个目标里没有利用率；`capacity_ledger.utilizationPct` 前端零消费 | **前半成立，后半按「直接消费」成立、按「可达」被推翻。** ✅ 5 个目标确为 `ontime / delay / changeover / fgInventory / cost`（`inproc-optimizer.ts:74 objVal()` 五分支；`portfolio.ts:425` 逐格只算这五项），**无利用率**。✅ 直接 grep：`utilizationPct` 全仓 **2** 命中，都在 `solvers/capacity.ts:814,897`，前端 0。❌ 但间接可达：`capacity_ledger` 在 `catalog.ts:86` 有完整 `answersQuestions`（"这条产线还剩多少产能"）+ `tags`，故与 1.1-d 同一条链 —— QOS 活目录（`live-capability-map.ts:134`）→ `navigation-slice.ts:497` 全员候选 → `invoke_solver("capacity_ledger")` → 回包 `pools[].utilizationPct` 直接进 agent 上下文。屏上没人画它，但**问答路答得出**。 | **部分推翻** | `apps/datacore/src/solvers/inproc-optimizer.ts:74`；`apps/datacore/src/solvers/portfolio.ts:425`；`apps/datacore/src/solvers/capacity.ts:814,897,934-958`；`apps/datacore/src/catalog.ts:86` | 报 `utilizationPct` 前端 0 命中的同时给命中证据：同一条命令对 `capacity_forecast` 在 `mocks/handlers.ts` 命中 **10** 次 ⇒ 工具没坏 |
| **2.6** | 产销匹配两个模型互不相连；`step3` 结果从不写回 `SopVersionRow` | **前半被推翻，后半成立（且已穷举）。** ❌ 两个模型**是连的**：`sop.ts:197 step3()` 第一件事就是 `computeRollup(c)`（产能金字塔），再 `weekly×certFactor×curveMult(w, maintWeek)` 逐基地累加成 `sup`（`:202-222`）—— S&OP 的供给侧读的就是产能模型的输出，不是另一个孤立数。✅ 写回：`step3` 只 `v.steps.s3 = {...}` + **`repos.sopVersions.put(v)`**（`:234`），落的是 `SopVersion` 记录，**不碰 `SopVersionRow` 对象**。且 `runDerivations` 的写入目标已穷举 —— `battery.ts:2015 sopVersionRowDerived` **只有一条** `{propKey:"gap", formula:"demand - supply"}`，`supply` 不是派生属性；全仓对 `SopVersionRow` 的 `objects.put` 只有播种那一处（`synthetic/service.ts:812 putAll("SopVersionRow", …)`）。 | **部分推翻** | `apps/datacore/src/sop.ts:197-234`；`apps/datacore/src/synthetic/battery.ts:2015-2017`；`apps/datacore/src/synthetic/service.ts:812`；`apps/datacore/src/ontology.ts:1552-1585` | 「没穷举 runDerivations 写入目标」这一步**已穷举**：`ontology.ts:1553` 明写 `if (!type \|\| type.derivedProperties.length === 0) continue` ⇒ 写入面 = 全部 `derivedProperties`，可枚举，已枚举 |
| **2.7** | S&OP 五步只查功能开关不查角色，五步没挂职能册 | **成立但不完整。** ✅ 五个路由全部只有 `await requireFeatureTag(req,"apiTags","sop")`（`app.ts:7796,7804,7808,7812,7824,7833`），而 `requireFeatureTag`→`features.requireByBinding(tenantId, kind, value)`（`app.ts:1247-1251`）**只传 tenantId 不传 role** ⇒ `features.ts:526` 的 **L4 role narrowing 这条路上根本没走到**（role 分层机制本身是存在的：`layeredSet(tenantId, role)` / `resolveForUser(ctx)`）。❌ 但「五步只查功能开关」不准：**步①与步③另有一道按用户的 A6 列权限闸** —— `sop.ts:96` 与 `sop.ts:200` 各有 `assertContextColumnUnrestricted(ctx, "S&OP 步骤①产品评审" / "步骤③供应评审")`，实现 `solvers/service.ts:5709-5712` → `authz.columnRestrictedObjectTypes(ctx)`，其 `:141` 读的正是 **`ctx.roles`**（admin/platform_admin 直通，其余按列级 policy 逐类判定）。同族还有 `planviews.ts:50,216`、`simclock.ts:85`。 | **部分推翻** | `apps/datacore/src/app.ts:1247-1251,7796-7836`；`apps/datacore/src/features.ts:507-542,579-581`；`apps/datacore/src/sop.ts:96,200`；`apps/datacore/src/solvers/service.ts:5709-5712`；`apps/datacore/src/authz.ts:140-153` | 反向对照：同文件里 `resolveForUser(ctx)` 确实按角色收窄 ⇒ 「角色机制不存在」会是错结论，真相是「**这条路没接上已有的角色机制**」 |
| **2.10** | 61 条事件订阅里没有一条指向求解结果，求解从不自动重算 | **被推翻（两半）。** 订阅表实测 **60** 条（`grep -c "{ event:" event-subscriptions.ts`）。① **有**订阅指向求解结果：`event-subscriptions.ts:114` `sim.tick_completed → invalidates:["sim-world","sim-sessions"]`、`:122` `sim.perturbation_created → ["sim-perturbations","sim-world"]`；而前端 `store/eventInvalidation.ts:50` **`"sim-world": [["a","sim-world"], ["a","sim-pareto-assemble"]]`** —— `sim-pareto-assemble` 就是方案寻优的模型装配（`console/SandboxOptRoute.tsx:173-174` `useQuery(["a","sim-pareto-assemble", sessionId, worldVersion])`），它失效 → 新 `req` → `console/useParetoFrontier.ts:859-860` 的 `useQuery(["a","sim-optimize-pareto", JSON.stringify(req)])` 键变 → **`POST /a/v1/sim/optimize-pareto` 自动重解**。链路是活的：`useDomainEventStream.ts:36 invalidateForEvent(e.event)` → `eventInvalidation.ts:228 queryClient.invalidateQueries`。另有 `:35 ts.ingested→["solver-inputs"]`、`:55 calibration.applied→["solver-params"]`。② 「求解从不自动重算」更是错：`RiskBoardView.tsx` 的 **8 处 `invokeSolver` 全部包在 `useQuery` 里**且 queryKey 含实参（`:229 ["a","risk-timeline",{horizon}]`、`:1031 [...,horizon,baseFilter]`、`:1306 [...,baseIdForScope,rcFactor]`、`:2140 [...,base,factor,tightness]`）—— 杠杆一动键就变，TanStack 当场重取重解，不需要事件。 | **被推翻** | `apps/agentcore/src/event-subscriptions.ts:35,55,114,122`；`apps/frontend-shell/src/store/eventInvalidation.ts:50,139-215,228`；`store/useDomainEventStream.ts:36`；`views/sim/console/SandboxOptRoute.tsx:173`；`views/sim/console/useParetoFrontier.ts:859`；`views/RiskBoardView.tsx:229,237,1031,1306,2140` | 取证员自述「只逐行核了一个页面」—— 换 4 个页面数 useQuery/useMutation：RiskBoard 12/3 · Sandbox 10/0 · GlobalSim 4/0 · OptimizeWhatif 4/0 ⇒ **推演主路压倒性是 useQuery**，不是手动 mutation |
| **2.13** | 物料只做事后检查不进模型；运费加在解之外 | **前半成立，后半部分推翻。** ✅ 物料确是事后：`portfolio.ts:920-950`，`materialOn` 分支拿的是 **`out.occupancy`（已解出的分配）**，按窗口/单号排序后逐单扣 `avail`，扣不动就 `push({reason:"material"})` + `:985 r.status="material_blocked"` —— 一个变量都没进最优化模型。✅ portfolio 的目标函数里确实**没有运费**：`portfolio.ts:425` 每格 `cost = 延误罚 + 换型 + 成品持有 + 拆单固定`；运费 `:772-778 freightCost` 是解完之后在两阶段排产行上算的。❌ 但「没有任何路径让运费进目标函数」是错的：`optimize_whatif(family="min_cost_flow", autoBind=true)` 的自动装配（`solvers/service.ts:5384-5421`）会挑「有 ≥2 个 ref 指向决策类型的弧类型」当 arc，并把 `arc_cost` 绑到**其上命中成本词库的数值字段**；本租户 `InterBaseTransfer` 恰好 `fromBase`/`toBase` 两个 `refToTypeKey:"Base"`（`battery.ts:2127-2128`）+ `freightCost`（`:2132`），而词库 `field-role-lexicon.ts:29 cost: /成本\|cost\|费用\|…/i` 对 `freightCost` **命中** ⇒ 该族的目标函数**就是** Σ 运费。`facility_location` 同理（`Base.serveCost` 干线履约成本，`battery.ts:1221` 自陈是 `assign_cost` 系数源）。 | **部分推翻** | `apps/datacore/src/solvers/portfolio.ts:425,772-778,920-950,985`；`apps/datacore/src/solvers/service.ts:5384-5421`；`apps/datacore/src/solvers/field-role-lexicon.ts:29`；`apps/datacore/src/synthetic/battery.ts:2127-2132,1220-1221` | 词库命中逐条验：`freightCost` 含 `Cost`、正则带 `/i` ⇒ 命中；同词库对 `breachPenaltyWan` **不**命中（文件 `:33-44` 自陈并因此单开 `penalty` 词库）⇒ 词库不是「什么都命中」，判据有鉴别力 |
| **2.14** | 7 条经营目标无 baseId / 无 period，不能按基地或按期间设 | **「按基地」成立，「按期间」被推翻。** ✅ `GOAL_REGISTRY`（`base-registry.ts:383-392`）确为 **7** 条，`GoalTarget` 接口（`:368-381`）无 `baseId`，只有 `level:"op"\|"year"`；`Base` 对象属性（`battery.ts:1196-1221`）也无任何 target 格。❌ 但「不能按期间设」错了：`PlanTarget` 对象类型**就是按期间的目标**——`battery.ts:2665-2672` 有 **`period`（"2026" \| "2026-Q1" \| "2026-01"）+ `level`（year\|quarter\|month）+ `value` + `year` + `scenarioKey`**；`battery.ts:7013-7017` 真播种 **1 年 + 4 季 + 12 月 = 17 行**；`synthetic/service.ts:1325 putAll("PlanTarget", …)` 落库，`:1330` 连 `AnnualScenario`（`scenario_to_target`）、`:1353` 连 `Order`（按 `Order.dueMonth` 对 `PlanTarget.period`、`level=='month'`）、`:1365` 连 `Principal`（责任人）；消费方 `planviews.ts:31,202`（AOP 年→季→月分解）。 | **部分推翻** | `packages/contracts/src/base-registry.ts:368-392`；`apps/datacore/src/synthetic/battery.ts:1196-1221,2665-2672,7013-7017`；`apps/datacore/src/synthetic/service.ts:1325-1365`；`apps/datacore/src/planviews.ts:31,202` | 报「Base 上没有 target 格」前先确认能看见 Base 的属性表：同一段里数到 `openCost`/`serveCost`/`oeeIndex` 等 20+ 格 ⇒ 在读真属性表，不是读空 |
| **2.15** | 成本只有 4+1 项调度罚金，不含物料/工时/能耗/折旧 | **portfolio 内成立，「全平台没有含物料的成本」被推翻。** ✅ portfolio 确只有 4 项 + 未排罚（`portfolio.ts:425` / `:679` `cost:{delay,changeover,unserved,total}`）。❌ 但**别的求解器有真物料成本，只是没接进 portfolio**：`quote_margin`（`solvers/extended.ts:465-500`）`bomCost = Σ(单耗 × 现价 × (1+加工费率))`，且走的是**真 BOM**——`extended.ts:993` `selectEffectiveBom(c.bomHeaders, c.bomDetails, modelId)` × `Material.unitPrice`（注释 `:947` 还记着「原先取错表、恒 313.7452」那次错账已修）；毛利式 `(price − bomCost − mfgRate×price − logistics)/price` 含**制造费率与物流**。能耗侧另有 `carbon_footprint`（`extended.ts:603-615`）`Σ工序(单位能耗×电网因子)` 读真 `EnergyMeter`（`:1188` 取不到就诚实拒绝）。`margin_attribution`（`catalog.ts:188`）则接受任意 `costFields[]`。另有按件口径 `Model.unitCost`（`contracts/order-line.ts:38` 自陈「今天只含物料，不含人工/制造费用/物流」）。 | **部分推翻** | `apps/datacore/src/solvers/portfolio.ts:425,679`；`apps/datacore/src/solvers/extended.ts:465-500,603-615,993`；`apps/datacore/src/catalog.ts:116,188`；`packages/contracts/src/order-line.ts:38` | 「含物料成本」不靠 grep `materialCost`（全仓 0）下结论 —— 改追**语义等价物** `bomCost` / `BOMDetail` / `spotPrice`，命中真实现 |
| **2.16** | portfolio 回包里规则 key / 系数值 / 服务端耗时 / agentInvolved 全无 | **事实成立；「要从零造」被推翻。** ✅ `SOLVER_OUTPUT_FIELDS.portfolio`（`service.ts:598`）14 个键里**确实没有** disclosure/ruleRefs/coefficients/timings/agentInvolved；`ontology.ts:1451-1476 invokeSolver` 的信封只加 `snapshotVersion`，无耗时；`?disclose=1` 只挂在 `POST /a/v1/sim/sessions/:id/tick`（`app.ts:2533,2549-2550`），**不挂求解器 invoke**。❌ 但机制早已建成、且已有求解器在用：① 沙盘侧 `app.ts:2563 buildSimRunDisclosure({...ruleParams, ruleExpressions, firedRuleKeys, trace, timings: r.timer.timings(DISCLOSURE_PHASE_ORDER), adversaryEnabled…})`，`:2648` 自陈「六项一次给全 —— 引用的数据 / 走过的切片 / 命中的规则 / 约束 / agent 是否参与 / 各环节耗时」（正是铁律 1.5 判据二的六项）；② **求解器层已有先例**：`capacity_ledger` 的回包顶层就带 `disclosure{formula, edgeAmountSource, units, demandMultiplier, loadWorkOrders, **agentInvolved:false**, counts{...}, totals{...}}`（`solvers/capacity.ts:934-958`，且 `service.ts:530` 已把 `disclosure` 登记进输出字段册）；③ 系数披露器也在：`decision-info.ts:663-682 disclosedCoefficients()` 逐项给 `{key,value,basis:"RULE_PARAMS"\|"DEFAULT_FALLBACK",ruleKey,note}`，`risk.ts:1096` 已在用。只是它**写死** `RULE_KEY="base_outlook_coeffs"` 与固定两个键名，portfolio 复用需把 ruleKey/键集参数化。 | **部分推翻** | `apps/datacore/src/solvers/service.ts:530,598,3451`；`apps/datacore/src/ontology.ts:1451-1476`；`apps/datacore/src/app.ts:2533,2549-2550,2563-2594,2648`；`apps/datacore/src/solvers/capacity.ts:934-958`；`apps/datacore/src/solvers/decision-info.ts:663-682`；`apps/datacore/src/solvers/risk.ts:1096` | 报「portfolio 没有 disclosure」前先确认输出字段册读得出别人有：同一个 `SOLVER_OUTPUT_FIELDS` 里 `capacity_ledger:["pools","violations","disclosure","summary"]` ⇒ 册子读得对 |

---

## 「所以真实的工作量是 ____，而不是我说的 ____」

- **1.1-d** — 真实的工作量是**给 DEPLOY.md 那句「默认不设 OPTIMIZER_BASE_URL」补一句「compose 态默认已接」，外加给 5 个只走问答路的求解器（selection/assignment/sequencing/packing/job_shop）各配一个屏上入口**；而不是我说的「接 11 个求解器 + 修默认部署即抛」。
- **2.1** — 真实的工作量是**把 `capacity_rollup` 的 `Σ bases[].weeklyWan` 摆到抬头上（或把抬头的口径名从「铭牌 GWh」改成实话）**；而不是我说的「rollup 侧要新建一个与型号无关的集团总量」。
- **2.2** — 真实的工作量是**给 `Base.util` 加一条 `derivedProperties`（照 `oeeIndex` 的 `AVG(... BY baseId)` 写法），数据源 `EquipmentOEE` 的聚合口径已在 `physicalTopology.ts:465-483` 算过一遍**；而不是我说的「视图不刷新、要新建一条时序回写链」。
- **2.3** — 真实的工作量是**把 `crossBaseAbsorbPct` 这条 RuleEntry 在种子里真种下（今天零写方），并把 `actions.ts:365` 那个 `sort()[0]` 换成从 `displacedOrdersFor` 已有的逐基地结果里取**；而不是我说的「跨基地调度整条恒定、要重做目标基地选择」。
- **2.4** — 真实的工作量是**在 `GlobalSimLevers` 上加一个复选框把 `lineGranularity:true` 传下去**（求解、契约 `line` 字段、线级换型/兼容全已写完）；而不是我说的「输出行类型要加产线字段」。
- **2.5** — 真实的工作量是**给 `capacity_ledger` 配一个屏（或把 `utilizationPct` 挂进 portfolio 的第 6 个目标）**；而不是我说的「零消费 ⇒ 这条链没建」。
- **2.6** — 真实的工作量是**在 `sop.ts:234` 那行 `sopVersions.put(v)` 旁边补一次 `SopVersionRow` 回写**（供给侧算法已经读产能模型了）；而不是我说的「两个模型互不相连、要把它们接起来」。
- **2.7** — 真实的工作量是**把 `requireFeatureTag` 从 `requireByBinding(tenantId,…)` 改成带 role 的那条（`resolveForUser(ctx)` 已存在），再把 `assertContextColumnUnrestricted` 从步①③扩到②④⑤**；而不是我说的「五步没挂职能册、要新建角色层」。
- **2.10** — 真实的工作量是**给 portfolio / risk_timeline 这类结果补一个事件标签（照 `"sim-world"` 那条把 queryKey 登进 `LABEL_TO_KEYS`）**；而不是我说的「求解从不自动重算、要新建重算触发机制」——沙盘的 Pareto 路今天就是事件驱动自动重解的。
- **2.13** — 真实的工作量是**把物料从「解后扣减」改成「解内约束」（给 CP-SAT 模型加一组物料容量行）**；运费那一半则是**把 portfolio 的格成本里加一项 freight**（`freightCostMap` 入参与 `InterBaseTransfer.freightCost` 都已在手，`min_cost_flow` 已在目标函数里用它）；而不是我说的「运费根本没路进目标函数」。
- **2.14** — 真实的工作量是**给 `PlanTarget` 加一个 `baseId` 属性并逐基地播种**（`period`/`level`/年季月 17 行/四条链接全已在）；而不是我说的「7 条目标无 period，不能按期间设」。
- **2.15** — 真实的工作量是**把 `quote_margin` 的 BOM 成本口径接进 portfolio 的格成本**（真 BOM 读取 `selectEffectiveBom` 已存在）；而不是我说的「不含物料，成本模型要重建」。
- **2.16** — 真实的工作量是**照 `capacity_ledger` 的 `disclosure{}` 块给 portfolio 加同形状的一段 + 把 `disclosedCoefficients` 的 `RULE_KEY`/键集参数化 + 挂一个 phase timer**；而不是我说的「披露层全无、要从零设计」。

---

## 《我可能错在哪》

1. **「QOS 问答路可达」我只追到了「候选目录会收进这些 key」，没有真起服务发一个问句验证它真被 `invoke_solver` 调用过。** `navigation-slice.ts:517` 还有一道 `canInvokeSolvers(scope?.toolNames)` 的工具白名单闸，若 demo 租户的 agent scope 没给 `invoke_solver`，那 5 个求解器在真实会话里仍是 0 消费。本单禁起服务，这一层没验；判「1.1-d 被推翻」若要收窄，可以收窄成「机制可达、生产实参未验」。

2. **2.1 我说「抬头数是服务端聚合」，但没验证屏上那个 KPI 卡是否真用后端下发的 `DASH_LAYOUT`，而不是 `mocks/fixtures.ts:445-451` 那一份。** 两份 widget 定义 `featureKey` 还不一样（后端 `view.dash.widget.capacity` vs mock `view.dash.widget.gwh`）——若部署态某个分支读的是 mock 那份，结论不变（同样是 objects-aggregate），但「屏上取的哪一个」这句我答得不够硬。另：后端 util 卡的 provenance 写「**12** 基地利用率算术平均」，而册里是 **13** 条 —— 这个 12/13 不一致我没追，可能是又一处过期文案。

3. **2.13 的 `min_cost_flow` autoBind 我是按代码路径推的，没有真跑一次 `optimize_whatif`。** `rankRoleCandidates` 会硬过滤零实例的候选，且 arc 候选是「≥2 ref 指向决策类型」的**第一名**——若本租户还有别的类型同时满足（如某种 Link 对象），选中的可能不是 `InterBaseTransfer`，那运费就没进目标函数。要坐实需要一次真调用看回包里的 `arc_cost` 绑到了谁。
