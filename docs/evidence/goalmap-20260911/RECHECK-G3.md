# RECHECK-G3 · 16 条判定的反向复核（全程只读）

## 报告头（自证树在 PIN 上）

| 项 | 值 |
|---|---|
| 起步 HEAD（旧树） | `778cc589` · `battery.ts` **1249 行** ⇒ 确实落后，前 13 个 agent 撞的那个坑我也撞上了 |
| 移过去后 HEAD | **`20b0a322`**（`origin/claude/inspiring-gates-aqczjg`，提交信息「docs: 仓主裁决 3.10 集团层选 A」） |
| 树龄探针 | `wc -l apps/datacore/src/synthetic/battery.ts` = **7053** ✅（PIN 值） |
| 交单时 `git status --porcelain` | 空（RC=0） · 一行代码未改 · 未起任何服务 |

**金丝雀总表**（否定结论的前置自证，逐条都跑过）

| # | 金丝雀 | 期望 | 实测 | 结论 |
|---|---|---|---|---|
| C1 | `git grep -l "BATTERY_SOLVER_PARAMS" -- 'apps/*/src/*'` | 非 0 | 命中 ≥3 文件 | pathspec 写法可用（未踩 `apps/*/src` 恒 0 的坑） |
| C2 | `lever-meta.ts` 里 `"Order.` 计数 | 1 | **1** | 抽取式有鉴别力 ⇒ `"Customer.` = **0** 是真零 |
| C3 | `ScheduledJobKindSchema` 里 `"RULE_SCAN"` | 1 | **1** | ⇒ `CLOCK_TICK` = **0** 是真零 |
| C4 | `capacity.ts` 里 `disclosure:` | 1 | **1** | ⇒ 全 `solvers/` 目录 `disclosure:` 合计 **1** 是真值 |

---

## 逐条复核

| 编号 | 我原来说的 | 实测 | 判定 | 证据（file+符号+亲手跑出的数） | 金丝雀 |
|---|---|---|---|---|---|
| **3.1** | 链路 24 节点只有 4 个有节拍真值；驾驶舱 12 KPI 里交付段 0、回款段 0 | **节拍 4/8 成立但分母错**：`CADENCE_NODES` 登记的是 **8 个限流环节**（不是 24），其中 4 条标 `EMPTY`+`NO_CARRIER`（`order.review` / `material.mrp` / `order.settlement` / 另一条），4 条有真值 —— 其余链路节点**按设计不承载节拍**。**驾驶舱交付段不是 0**：`SEED_LIVED_IN=1` 时注入 4 个 widget（`trend-12m` / **`ontime-rate` 已交付准交率** / `executed-workorders` / `delivered-ledger` 已交付订单台账）。**回款段在别的屏上有**：决策台「应收（万）+ 逾期天数 + 超额度」、Console0828 money 面板「应收/逾期」、S&OP 平衡台现金垫公式含回款 | **部分推翻** | `apps/datacore/src/synthetic/cadence.ts:205 CADENCE_NODES`（8 条·注释自陈「7 个环节 + maint_window，含 4 个查不到证据的」）· `apps/datacore/src/synthetic/service.ts:2039-2060`（`...(opts?.livedIn ? [4 个 widget] : [])`，`ontime-rate` 标签「近 12 个月已交付订单按期率（51/60）」）· `apps/frontend-shell/src/views/sim/DecisionConsoleView.tsx:1060,1081` · `apps/frontend-shell/src/views/sim/SopBalanceView.tsx:121` · `apps/frontend-shell/src/locales/zh.ts:1123-1124` | C1 |
| **3.2** | 供需缺口求解器的消费入参集 = ∅，改任何杠杆这个数逐字节不动 | **前半成立**：`supplyDemandGapAttribution` 调 `ignoredSolverArgs(args, SDGA_IGNORED_ARG_REASONS, SDGA_GENERIC_IGNORE_REASON)` —— **第 4 参 `consumed` 不传**，默认 `new Set()` ⇒ `Object.keys(args).filter(k => !consumed.has(k) && args[k]!==undefined)` ⇒ **每一个非 undefined 的键都回成 ignored，没有例外**。**后半推翻**：它读 6 类本体对象（SopVersionRow/DemandSegment/Line/Equipment/MaterialBalance/Order），而 `test/supply-demand-gap-attribution.test.ts` 的 **C3/C4 两个用例专门断言**「改 `DemandSegment.demandWanPerYearP50` → 需求端占比变」「改一台 `Equipment.oee_current` → 供给端占比变」。准确命题是：**沙盘杠杆（R4 只读、无 `worldId` 入参）到不了它；真写本体值（审批链 Action）到得了** | **部分推翻** | `apps/datacore/src/solvers/service.ts:753 ignoredSolverArgs` / `:3261` 调用点（**无 consumed 实参**）· `:3229-3231` 方法头自陈 C3/C4 · `apps/datacore/test/supply-demand-gap-attribution.test.ts:77,89` | — |
| **3.3** | 13 条杠杆全是供给侧，`Customer` 在杠杆册命中 0 | **杠杆册本身成立**：`LEVER_PROP_SPECS` 恰 13 条，类型 = Equipment/Line/Process×5/MaterialBalance/Material×2/Order.outsourceRatio/ChangeoverMatrix/Shipment，`"Customer.` 命中 **0**。**但「没有需求侧机制」被推翻**：① 沙盘扰动引擎有 5 类 `PerturbationKind`，含 **`demand_shift` / `cost_shock`**，落点类型含 `Order` / `OrderPromise` / `DemandSegment` / `Model` / `CustomerLocation`；② 场景卡 11 件事里 **6 件是需求侧**（插单 `rush-order` / 改期 `due-change` / 砍单 `order-cancel` / **调价 `order-reprice`** / 预测偏差 `forecast-bias` / 改收货地 `ship-to-change`）；③ `Customer` 在传导图上**是个真节点**：`Customer.receivablePressure --customer_places_order--> Order.orderChurn`（1:N 扇出）；④ `quote_margin` 吃 `args.price` 报价入参 | **部分推翻** | `apps/datacore/src/solvers/lever-meta.ts:73-97 LEVER_PROP_SPECS`（13 条） · `apps/frontend-shell/src/views/sim/unified/console0828/eventCatalog.ts:93-235 BUSINESS_EVENTS`（11 条·`kind` 取自契约 `PerturbationKindSchema`） · `packages/contracts/src/sim.ts:1379` · `apps/datacore/src/sim/change-impact.ts:642` · `apps/datacore/src/sim/pair-weights.ts:404` · `apps/datacore/src/catalog.ts:116` | C2 |
| **3.4** | 四类产能策略只真支持换产；检修 `writable:false`；扩产求解器读 0 个本体对象 | **两条硬事实成立**：`CAPACITY_FACTOR_BINDINGS` 共 20 条，`⑱ 设备检修 MaintPlan.week writable:false`；`SOLVER_READ_TYPES.capex_scenario = []` 且注释自陈「纯读 params.capexScenario + args·**不读任何核心对象类型**」。**「只支持换产」被推翻**：① `capacity_forecast.whatIf` 有 **3 根真杠杆**（夜班 `nightShiftCoef` / 扩通道 `channelCoef` / 外协 `outsourceRatio`）+ C03 物理上限封顶 + C08 外协红线拒收；② 阻滞点枚举器的候选面 = **11 条 `writable:true` 的产能因子** × **8 条 `IMPEDIMENT_RULE_BINDINGS`**（3 类 `BOTTLENECK`/`CONGESTION`/`BREAK`，7 个落点类型 Process/Line/Order/MaterialBatch/MaterialBalance/DataSourceHealth/CONTENTION），每阻滞点 2–4 条候选。**`countermeasure_combo` 有间接调用方**：它在 `catalog.ts` 带 `answersQuestions`+`tags`，经 `agentcore/src/tools/datacore-http.ts:416` → `dril/resource-projector.ts:125` → `dril/search-engine.ts:266-278 semanticCandidates` 被**运行期语义检索选中** —— 静态 grep 一次都看不见 | **部分推翻** | `packages/contracts/src/capacity-factors.ts:49-78`（20 条·11 writable） · `apps/datacore/src/solvers/service.ts:509` · `apps/datacore/src/solvers/capacity.ts:596-635` · `apps/datacore/src/solvers/chain-impediment.ts:154-264` · `apps/datacore/src/solvers/impediment-options.ts:87-99,168,567` · `apps/agentcore/src/dril/search-engine.ts:270` | C2 |
| **3.7** | 5 个目标无利润轴；毛利轴因分母不一致被系统自行下架 | **成立，且恢复路比我说的窄得多**。`marginAvailable = currencyAligned && assignCostBound && denomCoherent`，`denomCoherent = !crossDenomRisk \|\| (revDenom!==undefined && costDenom!==undefined && **revDenom === costDenom**)`。**检查提示里那个假设是错的**：`DENOMINATED_CURRENCY_UNITS` 现算自 `PROPERTY_UNITS`，本仓只认 **元/kWh · 元/吨 · 元/kg · 元/个 · 元/L · 元/㎡** ——「**元/套**」与「**元/电芯**」是单位字典**刻意不收**的（`PROPERTY_UNITS` 注释原文「单价一律以 kWh 或物料自身单位作分母，**禁用「套」**」，出处 `docs/DECISION-unit-of-account.md §1.5`）。⇒ 照那个假设去声明会被**发布门直接拒掉**；就算收了，两个分母**不相等**（套≠电芯）照样 `denomCoherent:false`。轴序有后果：前端散点取 `objectives` 前两根当 X/Y，毛利在册时排第 1 | **成立**（但恢复路径的工作量被低估） | `apps/datacore/src/solvers/opt-assemble.ts:83 DENOMINATED_CURRENCY_UNITS` / `:489 denomCoherent` / `:526 marginAvailable` / `:631-660` 报缺文案 / `:782-805 objectives` · `apps/datacore/src/domain.ts:245-275 PROPERTY_UNITS` | — |
| **3.8** | `decision_play` 的 3 条方案标题是字面量写死 | **标题成立**（`"缩短备份供应商认证周期"` / `priceLinked ? "长协条款优化" : "长协加价格联动条款"` / `"上游自采矿+战略储备"` 三个字面量；数值 `closesGap/cost/cycleDays` 由真对象派生）。**但「枚举器候选没接进来」被推翻**：`enumerateImpedimentOptions` 的真候选**已经在同一个回包里**，两处：① `impedimentPlays`（`decisionImpedimentPlays`，走 `LOCUS_EXACT`/`LOCUS_TYPE`/`BASE_SCOPE` 三条 join 路，带 `gapClose` 折算或 null+理由）；② `locusPlay.candidates`（传 `locusType`/`locusId` 时）。**不合并是刻意的、已写成裁决**：`SolutionCandidate` 只有「哪个对象哪个属性从多少拨到多少 + 各维 KPI 前后值」，而 `DecisionOption` 要六维 —— `cost/cycleDays/risk/exposure/reversibility` **五维在候选里一个真值都没有**，合并就得编五个数，而它们会被比对矩阵 `bestByDim` 与贪心组合 `closesGap/cost` 当真用 | **部分推翻** | `apps/datacore/src/solvers/service.ts:4046-4056 allOptions`（3 条字面量）· `:4115-4117` 接枚举器 · `:3735-3830 decisionImpedimentPlays` · `:4195-4230 decisionPlayLocus` 方法头（「为什么不把候选塞进 options」整段裁决）· `:4316-4320 optionsNote` 屏上明说两者不是一回事 · `apps/datacore/test/decision-play-options.seam.test.ts` | — |
| **3.8注** | 60 个求解器里只有 1 个有 `disclosure:` 块 | **字面成立**：`catalog.ts` 60 个求解器；全 `apps/datacore/src/solvers/` 目录 `disclosure:` **1 处**（`capacity.ts:937`，`capacity_ledger`）。`disclosedCoefficients` 被**恰好 1 个**求解器复用（`risk.ts:1096`，风险时间线）。**但「按需下发路径被漏掉」部分成立**：另有 3 条独立披露路，只是不叫 `disclosure:` —— ① 沙盘 tick `?disclose=1`（或 body `disclose:true`）装配 `buildSimRunDisclosure`，默认不下发、回包形状逐字节同旧；② `assembleParetoModel` 在传 `sessionId` 时注入 `world.disclosure()`（`SimWorldReadDisclosure`）；③ `finance_world_projection` 回带 `FinanceWorldTurnDisclosure` | **部分推翻** | `apps/datacore/src/solvers/capacity.ts:937` · `apps/datacore/src/solvers/decision-info.ts:663` ← 唯一消费方 `risk.ts:1096` · `apps/datacore/src/app.ts:2549-2563,2648` · `apps/datacore/src/solvers/service.ts:5211` · `apps/datacore/src/sim/world-read.ts:143,293` · `packages/contracts/src/sim-disclosure.ts:31` | C4 |
| **3.9** | 年度情景只有 3 个预置档，未来期结果是查表不是推演 | **AnnualScenario 那一半成立**：`generatePlanDomain` 恰造 3 档（保守/基准/激进），且 `capex`/`irr`/`cashCushion` **确实是查表**（`BATTERY_SOLVER_PARAMS.planview.scenarios.finance[key]`）；只有 `revenue = demand × avgUnitPrice / 1e4` 是现算。**「没有任何一条决策变量→未来期损益的路」被推翻**，至少两条：① **`finance_world_projection`**（`solvers/finance-world.ts` · 536 行 · 已在 `service.ts:4630` 接线）—— 吃 `args.worldId`，把世界态扰动经**真 `PropagationRule` 的真 id 与真系数**折成金额：`金额 = 基线 ×(1 + 压力 ÷ divisor)`，基线取 `FinancePlan.{budget,rolling}` / `ARInvoice.amount` 真值，链路原样下发；文件头自陈它补的正是 `finance_pnl` 缺的那一半（`finance_pnl` 零世界态入参，施加任何扰动返回逐字节相同）；② `capex_scenario` 按季算项目现金流 IRR/NPV + C23 门槛，吃 `args.demand/projects/s0` | **部分推翻** | `apps/datacore/src/synthetic/battery.ts:6954-6988 generatePlanDomain`（3 档 + `fin[key]` 查表）· `apps/datacore/src/solvers/finance-world.ts:1-40`（文件头）/ `:170 projectFinanceWorld` · `apps/datacore/src/solvers/service.ts:4630` · `apps/datacore/src/solvers/capex.ts` ← `service.ts:6121` | — |
| **3.11** | 订单可追率至多 52%；状态机四处坏 | **52% 只是上界、方向还错**：`deriveFulfills` 是 `N:1`，260 张工单按 `hashString(woId) % n` 选一张订单 ⇒ distinct 订单数**严格 ≤ 260，且必然因碰撞更低** —— 真实值比 52% **更差**不是更好。**但「订单追不到产线」被推翻**：存在**两跳确定路径** `Order --order_for_model--> Model --model_certified_on--> Line`（1:N + N:N），对**每一张**有 model 的订单都成立，完全不依赖 `fulfills`；另有 `Order.bases` 直接落基地、`WorkOrder --wo_on_line--> Line`（N:1）与逆边 `line_runs_work_order` | **部分推翻**（且 52% 这个数本身偏乐观） | `apps/datacore/src/synthetic/battery.ts:5180-5205 deriveFulfills` / `:3598 order_for_model` / `:3600 model_certified_on` / `:3715 wo_on_line` / `:3739 fulfills` / `:3806 line_runs_work_order` · `apps/datacore/src/synthetic/service.ts:1396` putLink | — |
| **3.12** | 产出偏差两处量纲错（聚合口径 4.665 · 达成率 100× 显示错） | **两处量纲错都已修**：① `Line.actual_output_daily` 现声明 **`件/日`**（不是 `套/日`），battery.ts 就地写了完整对拍（中位 `actual/max_capacity_day` = **4.665** vs `actual/capacityDaily` = **447.8**，后者与同对象 `utilization` 中位 92.1% 不可能同时为真）；② 达成率 100× 错**由 `ratio` 声明修死**：后端 `synthetic/service.ts:1780` 无条件下发 `key:"attain", unit:"%", **ratio: true**`，前端 `KpiWidget` 的大数字 `formatKpiValue(value, unit, ratio)` 与进度条注脚 `pctVal` **共用同一个 `ratio`**，「两条路径各猜各的」在结构上不可能再发生。**残留的只有 4.665 这个聚合口径**（`formationCapDaily` 是基地级电芯产能而 `max_capacity_day` 是单线级，一基地约 10 条线），battery.ts 明写「不在本单范围」⇒ 仍未闭 | **部分推翻** | `apps/datacore/src/synthetic/battery.ts:1626-1645`（`actual_output_daily` 单位 + 对拍数 + 4.665 括注）· `apps/datacore/src/synthetic/service.ts:1776-1782` · `apps/frontend-shell/src/views/DashboardView.tsx:1199-1229 KpiWidget` 头注 + `:1224 pctVal` · `apps/frontend-shell/src/api/types.ts:124 ratio?: boolean` | — |
| **3.14** | 世界的今天写死 2026-06-10；业务视图零自动刷新；世界时钟只能手推 | **写死不止一处**（比我说的多）：datacore 正源 `BATTERY_SOLVER_PARAMS.forecastStart = "2026-06-10"`（`battery.ts:854`），**另有前端生产码第二份** `GlobalSimView.tsx:263 PORT_FORECAST_START = "2026-06-10"`，再加 mock 3 处。**「零自动刷新」被推翻**：① 后端 `SchedulerService` 是**真 30s 轮询**（`server.ts:133 services.scheduler.start()`），种子期注册 4 个定时作业（`DERIVATION_FULL` 每日 2:00 / `RULE_SCAN` 每小时 / `TS_AGGREGATE` 每小时 30 分 / `CALIBRATION_RUN` 每周一 3:00），另有 `SCHEDULED_FORECAST`/`SOP_AUTO_OPEN`/`APPROVAL_REMINDER`；② 前端 `useDomainEventStream` 在 `ShellLayout.tsx:852` **登录后常驻挂载**，**20 秒**双源轮询 `/a/v1/outbox` ⊕ `/b/v1/outbox` → `invalidateForEvent` → react-query 重取。**「世界时钟只能手推」成立**：`ScheduledJobKind` 9 个取值里**没有任何一个推 simclock**（`CLOCK_TICK` 命中 0，金丝雀 `RULE_SCAN` 命中 1），唯一入口是 `POST /a/v1/synthetic/clock/tick` | **部分推翻** | `apps/datacore/src/synthetic/battery.ts:854` · `apps/frontend-shell/src/views/sim/GlobalSimView.tsx:263` · `apps/datacore/src/scheduler.ts:34-53`（`intervalMs ?? 30_000`）· `apps/datacore/src/server.ts:133` · `apps/datacore/src/synthetic/service.ts:324-329` · `apps/datacore/src/app.ts:1217-1240` · `apps/frontend-shell/src/store/useDomainEventStream.ts:14 DEFAULT_POLL_MS=20_000` · `apps/frontend-shell/src/pages/ShellLayout.tsx:852` · `packages/contracts/src/actions.ts:432-446` · `apps/datacore/src/app.ts:8076` | C3 |
| **3.15a** | `tgt` 与 `demandWanPerYearP50` 逐项全等 ⇒ 目标与预测是同一个数 | **是设计如此、不是偶然**：`SEG_DEMAND_ANCHOR` 三行**手写把同一个数字打了两遍**（201.7/201.7 · 139.2/139.2 · 34.1/34.1），不是派生相等。**但「别处没有不同的目标值」被推翻**：① `PlanTarget`（年→季→月分解）的口径**完全另一条基**——`annualBase = weeklyTotalWan × 52`，取自 S1.1 供给侧 rollup，与 `DemandSegment.tgt` 无关；② `AnnualScenario.demand` 三档（保守 = annualBase×conservativeFactor / 基准 = annualBase / 激进 = ×aggressiveFactor）是第三套目标量；③ `Metric.target`（如 `kpi-attain` 的 `target:100`）是第四套 | **部分推翻** | `apps/datacore/src/synthetic/battery.ts:257-261 SEG_DEMAND_ANCHOR` · `:6963 annualBase = round(weeklyTotalWan*52,1)` · `:6985-6987` 三档 scenario · `:6409 goalMetric(target:100, actual: act/tgt*100)` | — |
| **3.17** | 回流只覆盖 3 个参数 / 1 个求解器；销售/利润/需求三类偏差零回流 | **成立，且无运行期扩路**。`calibration.params` 种子恰 **3 条**：`yield_baseline`（EMA · `ONTOLOGY_PROPERTY` · `Process.yield`）、`ramp_base`（`REPLAY_ATTRIBUTION` · `SOLVER_PARAMS` · `ramp.base`）、`p90_health`（`QUANTILE` · `health.normal`）—— 后两条同属产能预测求解器。`calibrationConfig()` 的 `DEFAULT_CONFIG.params = []`，只从 `solverParams.calibration` 读。**写路只有两条，都改不了这张表**：`SolverService.setParam(tenantId, path, value)` 只写**一个路径上的数值**；`rules.ts:147 projectRuleParams` 只投影 `RULE_PARAM_BINDINGS` 里写死绑定的规则阈值。**没有任何 API 能往 `calibration.params` 追加条目** —— 追加只能改场景包重新播种（注释自陈「认证系数等场景包可按需追加同方法条目」） | **成立** | `apps/datacore/src/synthetic/battery.ts:1167-1185` · `apps/datacore/src/calibration/config.ts:9-31` · `apps/datacore/src/calibration/service.ts:233-234,258` · `apps/datacore/src/solvers/service.ts:5620-5650 mutateParams` / `setParam` · `apps/datacore/src/rules.ts:147-163` | — |
| **3.18** | 决策台账只能按 id 查；`commit` 返回 COMMITTED 但零派工单 | **前半成立**：datacore 只有 4 条路由（`POST /a/v1/decisions` · `GET /:id` · `POST /:id/commit` · `POST /:id/outcome`），**无列表路由**；agentcore `/b/v1` 侧也只代理 create/commit 两个（`tools/datacore-http.ts:317,322`），**没有列表代理**（金丝雀：同一文件里 `app.get("/a/v1/rules"` 这类列表路由抽得出来，抽取式有鉴别力）。**后半的「根因」与我猜的不同**：commit 零草稿**不是 `dryRunMitigation` 跑不通这个技术故障，是一次有意的拒绝**——`decision_play.options` 是**公司级供应链战略**（`opt-backup-cert` / `opt-lta-clause` / `opt-insource`，`factorId` 是 `cf-*` 因果因子），而 `params.risk.mitigations` 的键是 **7 个基地级产能风险因子**、方案是 `early_stock`/`air_freight`/`reroute` 这类基地处置动作 —— **两个域没有任何真实映射**，硬挑一条会让台账写着「上游自采矿」而 Action 去执行「空运补料」。且失败被**逐条记名**（`undispatched[]` + `trace` 里写死因 + `decision.committed` 事件带 `undispatchedCount`），不是静默 | **部分推翻** | `apps/datacore/src/app.ts:6702-6723`（4 条路由，无 list）· `apps/agentcore/src/tools/datacore-http.ts:311-322` · `apps/datacore/src/decision/kernel.ts:113-152`（整段裁决）/ `:154-202 commit` 实现 | C2 式抽取金丝雀 |
| **3.19** | 三个告警事件只落 outbox，通知服务 5 个调用方全在审批流 | **「只落 outbox / 平台不主动送」被推翻**：`OutboxService.start()` 在 `server.ts:135` 真起，**每 5 秒**遍历租户 `processOnce` → 取 `status==="ACTIVE"` 的 webhook **真投递**，带指数退避重试 + `DEAD` 死信 + `redeliver` 手动重投。租户经 `POST /a/v1/webhooks` **自助注册任意 url**（`WebhookSchema` 不做白名单/不校验可达性）。**外加第二条被动路**：`GET /a/v1/outbox?since=` 被前端**全局 20 秒轮询**（见 3.14）。**准确的命题应当是**：「平台**会**把领域事件推给租户自注册的 webhook，并在前端 20s 内失效重取；缺的是**平台自带的人向通道**（邮件/短信/IM）与**默认订阅者**——`NotificationService.notify` 全仓生产调用方**恰 2 处，都在 `actions.ts` 审批流**（`:889` / `:931`），三类告警事件没有一个调它」 | **被推翻**（命题需重写） | `apps/datacore/src/outbox.ts:61,112,141,229-240 start(…, intervalMs=5000)` · `apps/datacore/src/server.ts:135` · `apps/datacore/src/app.ts:8103-8118` · `apps/datacore/src/actions.ts:889,931`（notify 的**全部**生产调用方）· `apps/frontend-shell/src/store/useDomainEventStream.ts` | C1 |
| **3.21** | 三条对象级溯源都造好了，一条都没挂到生产风险看板 | **整条推翻 —— 三条全挂着，而且真能下钻到对象 id**。① `RiskBoardView.tsx:1305-1313` 用 `useQuery` 调 `invokeSolver("gap_attribution", { scope: { baseId, factorId? } })`，`:1337` 交给 `gapAttributionToBaseRootCause(ga, card.base)`，`:1771` 渲染 `<ProvenanceDag data={dag} onNodeClick={setInspNode} />` + 节点检视面板；② 该投影函数**本身就产对象级证据节点**：L2 每个结构叶按 `provenance.drillType/drillField/drillValue` 生成 `kind:"evidence"` 叶，**且 depth-3 层直接用对象主键当节点 id** —— 引擎侧 `service.ts:2881` 造的是 **`capobj:${factorId}:${o.objId}`**，`factor` 文案是 `${o.objId} · ${drillField}=${o.value}`，`provenance.drillId = o.objId`；③ `allFactors` / `factorSeries` 两个 optional 字段**后端真产、前端真读**（`risk.ts:800,869` → `RiskBoardView.tsx:1390,1706`）；④ 连「未解释残差」都画上了，且取的是本基地那一条 `reconChecks`（depth=2）而非全局残差 | **被推翻** | `apps/frontend-shell/src/views/RiskBoardView.tsx:1305-1313,1337,1390,1706,1771` · `apps/frontend-shell/src/components/ProvenanceDag.tsx:198-290 gapAttributionToBaseRootCause` · `apps/datacore/src/solvers/service.ts:2866-2890`（`capfactor:`/`capobj:` 两层）· `apps/datacore/src/solvers/risk.ts:772-800,869` · `packages/contracts/src/solvers.ts:265-279` | C4 |

---

## 「所以真实的工作量是 ____，而不是我说的 ____」

- **3.1**：真实工作量是「**把 `livedIn` 那 4 个 widget 的门槛从 `SEED_LIVED_IN=1` 挪成常驻（或让标准种子铺已交付史）**，再把决策台已有的应收/逾期三列提到驾驶舱」，而不是我说的「交付段与回款段要从零建 KPI」。
- **3.2**：真实工作量是「**给 `supplyDemandGapAttribution` 加一个 `worldId` 入参并叠世界态视图**（`buildWorldReadView` 已存在、签名逐字兼容，`assembleParetoModel` 就是这么接的）」，而不是我说的「这个数根本不会动、要重造消费链」。
- **3.3**：真实工作量是「**把已有的 6 张需求侧场景卡与 `demand_shift` 扰动，接进杠杆册那张表的 UI**（两套机制今天各自能跑，只是不在同一个面板里）」，而不是我说的「需求侧杠杆整类缺失、要从零造」。
- **3.4**：真实工作量是「**给 `MaintPlan.week` 开 `writable` 并补一条检修窗改期的判据**」＋「把 `capacity_forecast.whatIf` 那 3 根杠杆端上屏」，而不是我说的「四类策略里三类要从零实现」。
- **3.8**：真实工作量是「**给 `SolutionCandidate` 补 cost/cycleDays/risk/exposure/reversibility 五维的真值来源**（候选与六维方案卡已在同一回包里并排显示）」，而不是我说的「让 `decision_play` 改用枚举器」——那件事已经做了，且刻意没合并。
- **3.9**：真实工作量是「**把 `finance_world_projection` 接到年度情景页**，让 AnnualScenario 的 capex/irr/cashCushion 从查表改成投影」，而不是我说的「决策变量→未来期损益整条链要新建」。
- **3.11**：真实工作量是「**在订单溯源里走 `order_for_model → model_certified_on` 两跳**（100% 覆盖）」，而不是我说的「要把 `fulfills` 的覆盖率从 52% 补上去」——后者既补不满，52% 本身还是个偏乐观的上界。
- **3.12**：真实工作量是「**只剩 `formationCapDaily`（基地级电芯）当 `output:line`（单线级）尺度锚这一处聚合口径**」，而不是我说的「两处量纲错」——另一处（套/日→件/日）与 100× 显示错都已闭。
- **3.14**：真实工作量是「**新增一种 `ScheduledJobKind`（如 `CLOCK_TICK`）并注册 handler**」＋「把 `GlobalSimView.tsx:263` 那份 `forecastStart` 副本收敛回后端」，而不是我说的「业务视图要从零建自动刷新」——20 秒双源轮询 + 30 秒后端调度器都已在跑。
- **3.15a**：真实工作量是「**在 `SEG_DEMAND_ANCHOR` 给 `tgt` 一个独立于 P50 的来源**（PlanTarget 的供给侧基、AnnualScenario 三档、Metric.target 已各有一套）」，而不是我说的「全系统只有一个目标值」。
- **3.18**：真实工作量是「**新立一个语义正确的动作类型（「采纳经营方案」），或让 `decision_play` 产出基地处置粒度的方案**」＋「补一条 `GET /a/v1/decisions` 列表路由」，而不是我说的「修一个条件」——kernel 里那段裁决明写：放宽解析就是把注定失败的草稿换个 key。
- **3.19**：真实工作量是「**补平台自带的人向通道（邮件/IM）与默认订阅者**」，而不是我说的「事件送不出去」——webhook 扇出带重试与死信、前端 20 秒轮询，两条路都通着。
- **3.21**：真实工作量是「**0**（这条挂着且能下钻到 `capobj:<factorId>:<对象主键>`）」，而不是我说的「三条溯源都没挂到看板」。

---

## 《我可能错在哪》

1. **数据面的数我一个都没亲手跑出来**——本单禁起服务，所以 `fulfills` 的真实 distinct 订单数、`CADENCE_NODES` 到底哪 4 条 EMPTY、`SEED_LIVED_IN=1` 下 `ontime-rate` 的真读数，全部是**读生成式推的**，不是实测的。3.11 我说「比 52% 更差」是按 `hashString % n` 必有碰撞推的，**碰撞率没算**——极端情况下 260 张工单可能恰好打散到 260 张不同订单，那时 52% 就是准确值而非上界。凡带「条数/比例」的结论都该按这条打折。
2. **3.4 的「四类产能策略」我没找到那张四类的原始清单**——我用 `CAPACITY_FACTOR_BINDINGS`(20) + `IMPEDIMENT_RULE_BINDINGS`(8) + `capacity_forecast.whatIf`(3 根) 三处证据反证「不止换产」，但如果「四类」指的是另一张我没找到的枚举（比如某个 `CapacityStrategy` union），我这条就答偏了题。
3. **3.3 / 3.9 的「别的机制」我只追到了接线，没追到屏**——`demand_shift` 扰动、`finance_world_projection` 都验到了「有生产调用方、有真系数、有契约」，但**没有验证用户在哪个页面、点几下能用到它**。铁律 0.5 第二形态（接了线没数据）与第三形态（接错地方）在这两条上我都只排除了第一形态。特别是 `finance_world_projection` 要 `args.worldId`——**哪个前端页面会传这个参数，我一次都没追**。
