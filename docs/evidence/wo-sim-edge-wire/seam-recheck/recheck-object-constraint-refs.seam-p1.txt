
 RUN  v3.2.6 /Users/apple/deploy/wo-edge-wire/apps/datacore

 ❯ test/object-constraint-refs.seam.test.ts (8 tests | 1 failed) 40833ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ① 存得住：配 1 条 → 回读 1 条（修前是 201 但整键被 zod 静默 strip 掉 ⇒ 回读 0 条）  6456ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ② 引用不存在的规则 / 不存在的属性 → 400（不许静默收下：静默丢弃比报错危险得多）  4737ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ③ 求解器读得到对象上挂的约束：挂之前 4 条、挂之后 5 条，且新增那条点名取证对象  4479ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ④ 对照实验：只换引用的规则（阈值 96 → 95），同一求解器同一入参的结论 PASS → BLOCK  4644ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ⑤ 全称约束取『最可能违规的那个实例』：上限型必须取 max，取平均会把超限稀释成 PASS  5452ms
   × WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ⑤b 非数字属性（日期/文本）上选实例必须**按值**比，不许塌给 id 字典序 5012ms
     → 应选中按 name 最大的 obj_line_LINE-WS-zigong-pack，而不是 id 最小的 obj_line_LINE-WS-changzhou-assembly: expected '通过（Line.utilization > params.utilCeil…' to contain 'obj_line_LINE-WS-zigong-pack'
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ⑥ 规则挂上之后被下线 → 诚实标 NOT_APPLICABLE 点名断链（不静默跳过）  5490ms
   ✓ WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ⑦ 向后兼容（R6）：一条约束都没配时，求解器输出与本单上线前逐字节一致  4559ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  test/object-constraint-refs.seam.test.ts > WO-CONSTRAINT-REFS · 对象约束 = 对规则库的引用（写入→存储→求解器读取 全链） > ⑤b 非数字属性（日期/文本）上选实例必须**按值**比，不许塌给 id 字典序
AssertionError: 应选中按 name 最大的 obj_line_LINE-WS-zigong-pack，而不是 id 最小的 obj_line_LINE-WS-changzhou-assembly: expected '通过（Line.utilization > params.utilCeil…' to contain 'obj_line_LINE-WS-zigong-pack'

Expected: [32m"obj_line_LINE-WS-zigong-pack"[39m
Received: [31m"通过（Line.utilization > params.utilCeiling）｜对象约束 Line.name（must_be_before）取证对象 obj_line_LINE-WS-jinhua-calendering"[39m

 ❯ test/object-constraint-refs.seam.test.ts:284:83
    282|     const r = (await evaluatedRules(t)).find((x) => x.key === "LINE_UT…
    283|     expect(r, "约束应被并入引用集").toBeDefined();
    284|     expect(r.evidence, `应选中按 name 最大的 ${maxByValue.id}，而不是 id 最小的 ${mi…
       |                                                                                   ^
    285|   });
    286| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 7 passed (8)
   Start at  13:51:48
   Duration  49.70s (transform 4.91s, setup 0ms, collect 8.24s, tests 40.83s, environment 0ms, prepare 163ms)

