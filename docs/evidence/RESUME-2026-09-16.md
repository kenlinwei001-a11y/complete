# 续跑单 · 2026-09-16（额度见底触发落盘，铁律 1.7）

## 坐标
- 集成分支 `claude/handoff-sim-world-single-source` @ **`3e3b2fca`**（已推）
- canonical = `claude/inspiring-gates-aqczjg`（**本轮未推 canonical**）
- 抢救分支 8 条 `claude/rescue-*`（14 条躺了 8 天的未推提交，已全部落远端）

## 已落地并收编（都经真浏览器/活服务复验）
1. **界面词三段式**（什么事·发生时间·调整了什么），「拍」降为括注；12 个事件分一次性/持续，改交付地点不再问「持续几拍」
2. **4.3 屏瀑布 → 一屏八页签**（`#main-content` 842/842）；受阻环节（真数据）排第一位，敞口降级带 `~` 记号
3. **推演世界首次有真业务数**：`measuredCells` **0 → 450**（7.07%）。活服务验过。金额差 11 倍的两张订单推演结果拉开 14677（= 两者 qty 之差，精确）
4. **推演沙盘 / EdgeActivePanel 改走后端播种**（原先前端自算全哈希世界 POST 上去）

## 在跑未收（额度触发时叫停，产出在各自分支）
- `claude/handoff-desat3` —— 传导标定（三个乘性病因一次做完）
- `claude/handoff-meter` —— 真实度仪表 + 一键对照

## 本轮最贵的实测结论（最容易随中断蒸发）
- **饱和是真病因，但种子扰动不是来源**：删掉整条种子扰动，`costPressure` 均值逐字节相同（93.39494165383555），差 0.07%
- **加 0 件 / 1 件 / 12 件扰动，四个数完全相同** ⇒ 那四个 KPI 量的是**触达面**不是**强度**，150 = 未完成单总数 = 结构上限
- **三个乘性病因**：① `Line.blockedPressure` 无界积分器（120 拍 30,831）② 系数按稳态增益写、引擎按每拍入流用（稳态 2.7×，23 组里 22 组越界）③ 未归一扇入（入流差 51 倍）
- **仓里有两台同名不同宗的派生引擎**：`runDerivations`（模板，round 6 位）vs `recompute`（§2 DSL，4 位）。`Order.value` 是前者算的 —— 曾据此误判「DerivationSpec 已物化」
- **播种期没有任何 `recompute`** ⇒ 加派生规格一格都不物化
- **DSL 不是瓶颈**：51 对分档，B 档（原料齐但 DSL 不够）= **0**；A 档 32 对/3446 格今天就能写
- **`DEMO_SIM_WORLD_TICKS` 拐点是 96 拍不是 12**；且世界收敛到一个**饱和的不动点**（12→160 拍逐字节不动）
- **48 条 `weightRef` 里 43 条是 null** ⇒ 大料小料对成本影响一样

## 下一步（判据 = 不做则后面白做）
1. 收编 `desat3`（传导标定）—— 不做则真数灌进去也看不见
2. 补播种期 `recompute`（1 行，必须排在 `seedDemoSimWorld` 之前）
3. 写 A 档 32 条换算式 —— 详见 `docs/WO-SIM-REAL-DATA.md`（含 10 个陷阱 + 五道测试臂）
4. 状态变量加 `valueRef` 绑定（今天靠名字撞上，失败静默回落哈希）
5. 边权 Σ=1 / 强度指标 / 每拍重算的求和算子 / 类别型扰动落点

## 待裁决
- 禁令 2：沙盘里新增的 `· 实测格 450/6363` 那个 span 算不算 UX 改动（我判为纯措辞，回退只需删一个块）
- 强度指标选哪个上屏（分位数 / 金额加权 / 过线条数）—— 四个 KPI 结构上答不了「12 件比 1 件严重多少」
- `fetchSimSessionBaseSnapshot` 恒返回 null 且静默 ⇒ 沙盘下区差分基线今天取不到，需单开一张单
