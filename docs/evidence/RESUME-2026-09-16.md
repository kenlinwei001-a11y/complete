# 续跑单 · 2026-09-16（额度见底触发落盘，铁律 1.7）

## 坐标
- 集成分支 `claude/handoff-sim-world-single-source` @ **`3e3b2fca`**（已推）
- canonical = `claude/inspiring-gates-aqczjg`（**本轮未推 canonical**）
- 抢救分支 8 条 `claude/rescue-*`（14 条躺了 8 天的未推提交，已全部落远端）

## 已落地并收编（都经真浏览器/活服务复验）
1. **界面词三段式**（什么事·发生时间·调整了什么），「拍」降为括注；12 个事件分一次性/持续，改交付地点不再问「持续几拍」
2. **4.3 屏瀑布 → 一屏八页签**（`#main-content` 842/842）；受阻环节（真数据）排第一位，敞口降级带 `~` 记号
3. **推演世界首次有真业务数**：`measuredCells` **0 → 450**（7.07%）。活服务验过。金额差 11 倍的两张订单推演结果拉开 14677（= 两者 qty 之差，精确）
4. **推演沙盘 / EdgeActivePanel 改走后端播种**（原先前端自算全哈希世界 POST 上去）

## 在跑未收（额度触发时叫停，产出在各自分支）

### `claude/handoff-desat3` @ **`f072c8dc`** —— 传导标定 ✅ **标定本身已成，卡在金值收尾**

**实测（改前 → 改后，脚本在 `docs/evidence/wo-sim-desat3/`）**
| 判据 | 改前 | 改后 |
|---|---|---|
| 越界格 | 2697/4807 (56.11%) | **0/4937 (0.00%)** |
| `Model.costPressure` 反算 raw | 3600.301 | **31.655**（域内） |
| `Order.costPressure` 因果贡献 | 4.37e-6 | **0.01605**（> 0.01 地板） |
| 过地板订单数 | 0 | **150** |
| 漂移格（动静没做没） | — | **3751**（≠0） |
| DC 增益 ≤0.75 的组 | 1/23 | **24/24** |
| **三臂强度读数 magP90** | 三臂相同 | **7.6e-3 / 1.4e-2 / 3.7e-1 各不相同** ✅ |
| 10 节点环 | 未核 | **已拆，L=3.91e-3 ≪1** |

⚠ 对抗边自身 `g×W=2.33 > 0.75` 预算 —— 开 `sim.propagation.adversary` flag 会让 `Order.orderChurn` 越预算（环仍不发散）。已登记，未动。

⚠ **「三件必须一次标定」有了实物证据**：11 条等份边这个数**只有 ① 落地后才成立** ——
① 之前实测只有 **10 条**合格。拆开做确实要重标两遍。

**它停在**：逐个修被新口径打红的金值，已修 7 个文件；datacore 全量套件跑到 **68/361** 被叫停。

**还差（下一轮直接接）**
1. 跑完 datacore 全量拿完整红名单 —— 剩 293 个文件**大概率还有金值红**（④ 把播种 3 拍改 96 拍，凡咬 demo 世界快照/计数的门都会动）。
   ⚠ **那次后台全量的日志在 `/tmp` scratchpad，不跨会话存活 ⇒ 下轮必须重跑**。
   🔴 **已知第 7 个红、它没看过也没修**：`sim-order-real-fields.seam.test.ts`（**6 tests / 3 failed**）——
   这正是 `WO-SIM-ORDER-REAL-FIELDS` 那 450 格真值的守门测试。**标定与真值这两条线在这里交汇，下轮先看它。**
2. **⚠ 要仓主裁决**：`sim-seed-world.seam.test.ts` ⑤e 唯一一条它**没动**的红。④ 让 `budgetTicks` 2→95，结构可达面 **222→3861（17×）**，而真跑只动 2445 格。改成「子集」判据属**放宽**（红线 2），它写不出「不比改前弱」故顶回来。三条路：**A** 保 96 改判据（需点头）／**B** 退回 3（判据 1/3 随之失守）／**C** 让排序键变成值感知（另开一单）
3. 补跑「只退回 ④、保留 ①②③」那一臂 —— 上面那个 17× **不等于**「①②③ 对 ⑤e 零影响」
4. 四包 gate（`typecheck` 已跑：只有 4 条前置红，零新增）


### `claude/handoff-meter` @ **`24e82377`** —— 真实度仪表 + 一键对照 ✅ **本单交付已全部完成并验过**

真浏览器验收做完（datacore `:14901` pid 自证 + vite `:15173` + `SEED_DEMO=1`，⛔ 无 `VITE_MOCK`，pageerror 0）。
仅剩：全量 vitest 只在 `6f3fd9a8` 上跑过（321 files / 2294 tests / RC=0），`24e82377` 之后只跑了 4 个门文件（那次只删了一个 scope 字段，无渲染方）。

**并排两列实测**（1 件扰动「原材料涨价·铝箔 delta 20」，3 拍；两个不同 sessionId，后端亲验 `measuredCells` 450 vs 0，`cells` 同为 6363）

| 读数 | 含真值(7.1%) | 纯占位(0%) | 差 |
|---|---|---|---|
| 变化格数 | 4,832 | 4,832 | **0** |
| 被推动订单 | 150 | 150 | **0** |
| 敞口(元) | 15,663,001,584 | 15,663,001,584 | **0** |
| 幅度中位数 / 最大值 | 75.00 / 99.00 | 75.00 / 99.00 | **0.00** |

差 0 如实上屏，未调参未换口径。

### 🔴 **下一轮最该先看的一条 —— 450 格真值是死胡同**

两臂终态真正不同的只有 **466 格** = 448 格真值自己 + 18 格
`Model.backlogQtyTop` / `backlogPriceTop` / `backlogHorizonDays`。
逐条核过已发布边集：**那三个量 `asSource: 0` —— 不是任何规则的源。**

⇒ **450 格真值的因果半径 = 1 跳 / 3 条边 / 死胡同。真数进去了，走一步就停。**

**这条直接改写 T2 的前提**：照 `docs/WO-SIM-REAL-DATA.md` 写完 A 档 32 条、把 `measuredCells`
推到 3,896，**如果那些变量在传导图里同样是死胡同，屏上的数一格都不会变**。
⇒ **写式子之前必须先查：该状态变量在已发布边集里 `asSource` 是不是 0。**
这应当作为 §5「每条式子必须带三样」的**第四样**加进工单。

🐤 金丝雀：孪生世界逐格比对 448 格不同（≠450 是因为 2 格 `Order.leadDays` 真值恰好撞上哈希值；权威计数一律用 `measuredCells`）。

### 它顺手修掉的三个真问题
1. **对照会把控制台切到占位世界上** —— `POST …/tick` 把新建会话翻成 `RUNNING`，而 `useConsoleSession` 选「最近一条 RUNNING」⇒ 刷新后第一层读数变 `0 / 6,363`。修法：两臂读完立刻迁 `ENDED`
2. **`c0828-verdict-origin` 浮层写死「5,895 格全部为派生值，真读数 0 格」** —— 自 `WO-SIM-ORDER-REAL-FIELDS` 落地当天起就是**印在用户屏上的假话**，已改现算
3. 它自己第一版把 `**` markdown 写进上屏串，靠真浏览器截图才发现（屏上字面 `**` 现为 0）

## 本轮最贵的实测结论（最容易随中断蒸发）
- **饱和是真病因，但种子扰动不是来源**：删掉整条种子扰动，`costPressure` 均值逐字节相同（93.39494165383555），差 0.07%
- **加 0 件 / 1 件 / 12 件扰动，四个数完全相同** ⇒ 那四个 KPI 量的是**触达面**不是**强度**，150 = 未完成单总数 = 结构上限
- **三个乘性病因**：① `Line.blockedPressure` 无界积分器（120 拍 30,831）② 系数按稳态增益写、引擎按每拍入流用（稳态 2.7×，23 组里 22 组越界）③ 未归一扇入（入流差 51 倍）
- **仓里有两台同名不同宗的派生引擎**：`runDerivations`（模板，round 6 位）vs `recompute`（§2 DSL，4 位）。`Order.value` 是前者算的 —— 曾据此误判「DerivationSpec 已物化」
- **播种期没有任何 `recompute`** ⇒ 加派生规格一格都不物化
- **DSL 不是瓶颈**：51 对分档，B 档（原料齐但 DSL 不够）= **0**；A 档 32 对/3446 格今天就能写
- **`DEMO_SIM_WORLD_TICKS` 拐点是 96 拍不是 12**；且世界收敛到一个**饱和的不动点**（12→160 拍逐字节不动）
- **48 条 `weightRef` 里 43 条是 null** ⇒ 大料小料对成本影响一样

## 下一步（判据 = 不做则后面白做）
1. 收编 `desat3`（传导标定）—— 不做则真数灌进去也看不见
2. 补播种期 `recompute`（1 行，必须排在 `seedDemoSimWorld` 之前）
3. 写 A 档 32 条换算式 —— 详见 `docs/WO-SIM-REAL-DATA.md`（含 10 个陷阱 + 五道测试臂）
4. 状态变量加 `valueRef` 绑定（今天靠名字撞上，失败静默回落哈希）
5. 边权 Σ=1 / 强度指标 / 每拍重算的求和算子 / 类别型扰动落点

## 待裁决
- 禁令 2：沙盘里新增的 `· 实测格 450/6363` 那个 span 算不算 UX 改动（我判为纯措辞，回退只需删一个块）
- 强度指标选哪个上屏（分位数 / 金额加权 / 过线条数）—— 四个 KPI 结构上答不了「12 件比 1 件严重多少」
- `fetchSimSessionBaseSnapshot` 恒返回 null 且静默 ⇒ 沙盘下区差分基线今天取不到，需单开一张单
