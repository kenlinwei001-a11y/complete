# WO-CAPSIM-THREE-SLOTS · 改前/改后实测

量法 = 门自己的那一份（`node scripts/check-ui-first-layer.mjs --explain <file>`，D1 `first` / D2b `prose`）。
门金丝雀先行：检测器 9/9 · 判据 15/15 · D5 3/3 · D7 5/5 · 扫描面 213 文件 ⇒ **量法有鉴别力**。

## 一、四数表（前端第一层密度）

| 文件 | first 前→后 | deferred 前→后 | prose(≥24字) 前→后 | 总量 前→后 | 原生 title 前→后 |
|---|---|---|---|---|---|
| `views/RiskBoardView.tsx` | 221→**221** | 93→**93** | 5→**5** | 314→**314** | 2→2 |
| `views/risk/DispositionOptionsPanel.tsx` | 80→**76** | 4→**16** | 4→**1** | 84→**92** | 1→1 |
| `views/risk/DoNothingPanel.tsx` | 49→**45** | 5→**14** | 4→**1** | 54→**59** | 1→**0** |
| `views/risk/ExposurePanel.tsx` | 51→**50** | 2→**6** | 2→**1** | 53→**56** | 0→0 |
| `views/risk/decisionInfoShared.tsx` | 20→**18** | 2→**24** | 2→**0** | 22→**42** | 0→0 |
| **合计** | **421→410** | **106→153** | **17→8** | **527→563** | **4→3** |
| **四块面板小计**（不含未动的 RiskBoardView） | **200→189** | **13→60** | **12→3** | **213→249** | **2→1** |

⚠ `RiskBoardView.tsx` 三槽尚未落地 ⇒ 它这一行**前后相同是诚实的**，不是没量到。

## 二、降层守恒证明（第四个数）

**总量 527 → 563（+36），deferred 106 → 153（+47），一条证据都没删。**

- 守恒的正确形态是 `first↓` 且 `deferred↑`、**总量不掉**（门 D4 原文）。实测 first −11 / deferred +47 / 总量 **+36**。
- 总量为什么会**涨**而不是持平：新增了「记号统一说明」`MarkLegend`（原本散在各处的
  「显示 0 等于断言不赔钱」被收成一份全屏共用的说明）+ 三槽原语。**只增不删。**
- 门 D4「内容变少」在我这 5 个文件上**零命中**（命中的 2 个文件是 `views/sim/**`，别的单在动）。

## 三、真后端回包普查（ground truth · 不是扫源码）

`SEED_DEMO=1` 起 datacore（4011 端口，**真去 bind 探的空口**，⛔ 未用 `ss`/`netstat`），
`POST /a/v1/solvers/risk_timeline/invoke`，8 张卡 / 17 条 planRows。

- 金丝雀「违约金」命中 **16 条** ⇒ 抓法有鉴别力。
- 回包里 **≥24 字的串共 710 条 / 34 种字段路径**。最长 **784 字**
  （`planRows[].options.options[].sideEffects[].detail`，48 条），其次 316 字（`penalty.reason`，8 条）。

## 四、⚠ 「32 个空 `*`」的病因订正（派单里的诊断未复现）

派单说「32 行只有 `*` 没有内容 ⇒ 空列表渲染漏了」。**照它修会修错地方。**

实测（复现步骤 = 上面那条 invoke，然后逐串扫）：

| 命题 | 实测 |
|---|---|
| `penalty.checked[]` 有空项 | ❌ **33 条，无一为空** |
| 全回包有空串 / 纯 `*` 项 | ❌ **0 个** |
| 屏上真有 32 个 `*` | ✅ **有，但成因是字面 markdown 泄漏** |

真病因：这批文案**按 markdown 写**（`违约金/罚则**当前本体无承载**：…`），前端一路当纯文本渲染
⇒ **星号原样印在屏上**。全回包 **151 个串 / 318 个 `**`**，其中 `cards[].doNothing.penalty.reason`
一项贡献 **32 个**（4 个/卡 × 8 卡）—— **这就是仓主数到的那 32 个。**

修法：`RichText`（`decisionInfoShared.tsx`）把 `**x**` 还原成 `<b>x</b>` ——
**保住强调意图**，不是把星号删掉了事；只认成对 `**`，落单星号原样留着（`Order.*` 这类字段通配符不能被吃掉）。

## 五、门状态（RC=1，但 68 条里只有 5 条是我的）

| 归属 | 条数 | 说明 |
|---|---|---|
| 我改的 5 个文件 | **5** | 其中 **4 条是 D7「棘轮松弛」= 基线高于实测 = 我改好了**；1 条 D3 字号 1→2 是我引入的真回归，**已修**（13px→12px，现 sizes=1） |
| 别人的文件（`views/sim/**` · `components/QueryDock/**` 等） | **63** | 另有单在动，本单不碰 |

**我的文件现在零个真超标。**

⚠ **刻意不跑 `--tighten`**：它是**全局**的，会一并改写 60+ 条别的 agent 正在动的文件条目
（`views/sim/**` 明令不许碰）。收紧动作应由收编方在四条分支并完之后**统一跑一次**。
