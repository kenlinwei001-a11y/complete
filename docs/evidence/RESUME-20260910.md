# RESUME · 2026-09-10 · 预算见底落盘交接（铁律 1.7）

> **canonical = `0ef5f67d`**（`claude/inspiring-gates-aqczjg`）
> 触发原因：仓主预告配额将尽 + 「先落盘，其他任务暂停」。
> ⛔ 下一轮照**铁律 1.6**：**只跑剩下那 10%，别重读已核过的坐标、别重跑已经绿的实验、别开新战线。**

## 一、分支落盘表（按**内容**判，不按分支存在）

| 分支 | tip | 状态 | 内容差 |
|---|---|---|---|
| `wo-sim-unified-restore` | `f7354047` | **未并入** | 2 files changed, 685 insertions(+), 36 deletions(-) |
| `wo-sim-wire4` | `1fc1d76b` | **未并入** | 8 files changed, 1209 insertions(+), 52 deletions(-) |
| `wo-onto-wire4` | `c75fd6f5` | **未并入** | 2 files changed, 115 insertions(+), 2 deletions(-) |
| `wo-sim-0828` | `64998c83` | **未并入** | 11 files changed, 3167 insertions(+), 42 deletions(-) |
| `wo-onto-spine` | `095530e3` | **未并入** | 12 files changed, 678 insertions(+), 8 deletions(-) |
| `wo-coef-finish` | `be3a37ab` | 已并入 | 无差异 |
| `wo-simtai-verify` | `ae512bfe` | **未并入** | 1 file changed, 147 insertions(+) |
| `wo-onto-verify` | `86864c95` | **未并入** | 2 files changed, 781 insertions(+) |

### ⚠ 表里「未并入」有两种，别混（否则会重做已并入的活）

- **`wo-simtai-verify` / `wo-onto-verify` 的内容其实已在 canonical 上** ——
  我是用 `git checkout <branch> -- <file>` 取文件再提交的，**分支本身没被 merge**，
  所以祖先关系不成立。**⛔ 别再 merge 它们**（会得到重复内容）。
  这正是铁律 0.6 第 2 条那个病：**「我用『分支不是祖先』当作『内容没并进来』的证据。」**
  复核方式：`git show 0ef5f67d:docs/evidence/WO-SIMTAI-DESIGN-VERIFY.md | head -3`
- **其余五条是真未并入**，需要复验后收编。

## 二、五张单各停在哪 · 还差什么

| 单 | 停在哪 | 还差什么（下一轮直接做这些） |
|---|---|---|
| **`wo-sim-unified-restore`**（08-26 复刻） | 已交付。落点类型下拉 1→8、左栏 6 业务子页、真后端对照实验 PASS | ① 复验收编。②**已被 08-28 取代一部分** —— 它的 `businessFaces.ts`（类型枚举+中文名+真实例数）要保留给 0828 复用，6 业务面的分组会被 12 件事替换 |
| **`wo-sim-wire4`**（四条接线） | 已交付。①挂 PerturbationTimeline ②业务量投影 ③披露层 open ④复用导出 ⑤棘轮 4→1、全门 64→61 | ① 复验收编。② **顶回来一条待裁决**：`ui-first-layer` 残留 D7 要求跑 `--tighten` 改基线 —— 属禁令 3 冻结的基线 JSON，**未动，需仓主裁决** |
| **`wo-onto-wire4`**（本体四条） | 已交付 ④（类型下线真相源收敛，含变异反证）；①②③ 三条顶回来 | ① 复验收编 ④。② ①②③ 的顶回结论**已写进本文件第三节**，别再照原单派 |
| **`wo-sim-0828`**（08-28 新屏） | tip **`64998c83`**。**六区代码全部写完并推上去**（2,010 行 / 4 个新文件 + `UnifiedSimShell.tsx` 接线）。**卡在取证**：真浏览器动线只跑完第 1 步（登录页渲染 OK），CDP 驱动在 load 38–46 下没跑完 | ① 跑完动线取证：`scratchpad/drive.mjs` **已写好**（登录→导航→点开事件→选落点→加两件→算一下→专家模式往返，逐步截图+四数），**等 load < 10 再跑**。② 复跑 `pnpm --filter frontend-shell exec tsc --noEmit` 确认那 2 处修干净（`mainCause` 漏返回 + `noUncheckedIndexedAccess` 链式下标，已修已推、未及复跑）。③ `check-ui-first-layer --tighten` 把 `Console0828.tsx` 落进 `unlisted` 段 —— **会写基线 JSON，撞冻结令，需仓主裁决** |
| **`wo-onto-spine`**（11 步脊） | tip **`3acacde5`**。**11 步全部映射完成，无遗漏**，表在 `docs/evidence/WO-ONTO-SPINE-11.md`（五列）；代码已落地并经真浏览器验过 | ① **跑四包 gate**（本单只跑了 `tsc --noEmit`，`TSC_RC=0`）。② **第 05 格「事件」要么建面要么删格 —— 这是产品裁决不是工程题**：3 条静态种子事件该不该有管理面？不裁就永远虚着。③ 第 02 步新建入口（= 本体底本批注 #2 那条接线项），补上后动线第 7 步卡点即消失。④ `wo-onto-wire4` 修完后可撤掉脊图例里那条 `status` 诚实位 |

## 三、本轮已知、但尚未落进任何门/台账的结论（最容易随中断蒸发）

1. **`merge-base --is-ancestor` 这条判据不够用**（我写在 CLAUDE.md 铁律 0.6 第 2 条里的那条机制）。
   它只测得出「**已被 canonical 完全包含**」，测不出「**canonical 往前走了**」。
   实测：`wo-coef-finish` 停在一棵**落后 38 个提交**的树上而该判据说「不落后」，
   其中两个提交的标题就是 `ontology-anchors:check 转绿` / `ontology-descriptions:check 转绿`；
   rebase 后 `stale-claims:check` 由红转全绿、治理门红 12→11。
   **修法**：判据换成 `git rev-list --count HEAD..$CANON`，>0 即落后。**⛔ 此条尚未回写 CLAUDE.md。**
2. **`utilization` 口径「统一」会制造 100× bug，不是修它。** 真后端实测值域：
   `Line.utilization` ∈ **[89.77, 94.13]** ⇒ `%` 正确；`Process.utilization` ∈ **[0.882, 1.0]** ⇒ `dimensionless` 正确。
   两个声明各自如实。设计稿把**同名**当成了**同量**。真要做的是**改名**（`utilizationPct`/`utilizationRatio`，动 42 处读点），另开单。
3. **`GET /a/v1/boundary/impact?node=X` 的 `node` 是假参数** —— 带参与不带参回包**逐字节相同**（都 2187 字符），
   数据源是 `packages/contracts/src/base-registry.ts:426` 的静态常量。⛔ 别再拿它当「波及面已实装只差上屏」的证据。
   四桶波及面是另一个端点 `POST /a/v1/sim/change-impact-preview`，其 `ChangeFocus` 判别联合**没有 objectType**。
4. **`coefficientRef` 0/47 不是接线缺口，是没有可引用的业务量。** 30 条已发布规则里只有 6 条带数值参数，
   没有一条是「传导强度」。按数值巧合去绑（10 条 0.6 撞 `pendingCertFactor`）就是「拿数字相同当同一个量的证据」。
   机制本身已用四个数验活：内联 0.5 → 引用 0.1 → 只改规则参数 0.9（边一字未动）→ 对照臂始终 0.7。
   附带订正：姊妹字段 **`decayRef` 已在用**（`battery.ts:3376/3385` → C35），「引用机制形同虚设」今天只对 `coefficientRef` 成立。
5. **`ontology-anchors` 的 ±40 容差是静音地雷** —— 三个 `battery.ts` 锚点同时卡在 40，
   任何人在该文件上方加 ≥1 行就一次引爆 5 条 `LINE_DRIFT`，而加行的人完全不知道自己碰了本体。
6. **本机有 chromium：`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`**（`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`）。
   有 dev 因 `playwright` 包 unresolvable 报「本机没有任何浏览器引擎」并放弃动线取证 —— **那个结论是错的**，
   形态：「我用『npm 包解析不到』当作『浏览器不存在』的证据。」不需要 playwright 包，`--headless --screenshot` 直接能出图。
   已知副作用：**Google Fonts 被容器拦**，字体走系统回退（版面内容不受影响）。
7. **`scratchpad` 是跨 agent 共享的** —— 两个 dev 的通用文件名（`start-datacore.sh`/`explore.mjs`/`start-dc.sh`）
   互相覆写过。**以后文件名一律带单号前缀。**
8. **`docs/prd-*-index.json` 的 `generatedAt` 会被门链改** —— 纯日期戳、零实质内容。
   半截运行的产物**不要**提交（本轮已 `git checkout --` 丢弃）。

## 三之二、08-28 那张单实测钉死的四个数（已可当口径用）

1. **订单簿总额 = `Σ Order.value` = 45,464,327,004 元 = 454.64 亿**（500 张单全有 `value`，缺值 0 张，与服务端 `total=500` 对账）。
   ⇒ **设计稿 `UI-sim-console-20260828.html` 里那句「订单簿 427.11 亿」是错的，不采用**；
   与 CLAUDE.md 铁律 3 立的 454.64 亿**逐元相同**。此前我标为「待核」的那条**已结案**。
2. **65 实体六分类实测 = 基地 13 · 客户 20 · 供应商 15 · 物料 8 · 型号 6 · 需求段 3 = 65**，与设计稿逐个相符。
   **12 件事全部 12/12 落到真实状态变量。**
3. **对照实验通过，残差 0.000000000**（每臂跑在刚重启的干净 datacore 上，全 130 线 Σutil）：
   `①都不加 11247.171798 · ②只加A 11098.263743 · ③只加B 11075.365848 · ④A+B 10926.457794`
   ⇒ `ΔA=−148.908054`、`ΔB=−171.805950`、`ΔA+ΔB = ΔAB = −320.714004`。
   交叉核对：A 只动常州（Δ金华=0）、B 只动金华（Δ常州=0）。
4. **饱和陷阱独立复现**：源格只动 **0.008248**，下游动 **148.908054**，**差 18,053 倍**。
   ⇒ 再次确认：**⛔ 别拿源格读数判断扰动生没生效，去看下游。**

### 08-28 那张单撞出来的三个新坑（下一轮别再踩）

- **`rollback` 只还原世界态，不清扰动清单**；且**在检查点那一拍写扰动会污染检查点本身**
  ⇒「干净基线」臂里源格已是目标值、Δ=0，读起来像「引擎不响应」。**干净基线只能靠重启服务拿。**
- **世界逐拍强烈振荡**（同一基地 Σ 相邻两拍 729→858）⇒ **各臂必须推同样拍数才可比**，否则差值全是噪声。
- 区③的毛利/成本/应收**已按设计稿页脚授权降级成「这次算不出来」删除线** ——
  推演层 500 张单 2000 格全是 0–100 压力数，换算成钱需要一个**全平台没登记的系数**。
  上屏大数改成**可核对的订单敞口**（Σ 真金额）。⚠ 这是有意的诚实降级，**不是没做完**。

### `PerturbRail.tsx` 那两条红门的归属（别派错单）

`ui-first-layer` 的 D4 守恒（5→4）与 D7 棘轮松弛，**来自 `wo-sim-unified-restore`，不是 0828 引入的**。
0828 的 `Console0828.tsx` 只被记为「新文件未落账」（first=115 / deferred=75，**降层率 39.5%，远高于全仓 15.6%**），
D2 口径、D3 字号两条硬上限**都没红**。⇒ 要判的是那条 restore 分支：**回写基线，还是把内容搬回浮层。**

## 三之三、11 步脊那张单钉死的事实（下一轮别再照设计稿开工）

1. **设计稿页面一步骤条那 11 个计数，只有「域 15」对得上。** 实测：
   `02 对象 12/70` → **100/878** · `03 关系 0/110` → **127** · `06 规则 0/29` → **30** ·
   `08 因果 0/45` → **47** · `05 事件 11 型` → **3**。⇒ 代码里一个都没写死，全部现算。
2. **「11 个面」≠「11 步」，这是本轮最重要的结构订正。**
   **03/04/07/08/11 五步挤在同一页** `OntologyRelationsPage`（**1892 行**），而 **05 事件零落点**。
   ⇒ 准确说法是「**一页塞五步，另一步没有页**」，不是「一页一个 API 资源」。**我原先那句描述是错的。**
3. **两个诚实标记，未拿别的面凑数**：
   · **05 事件 = `gap`**（金丝雀：同一条路由正则 `ontology` 命中 **53**、`event` 命中 **0**；
     `ActionsPage.events` 是审批审计流水，**不是**本体事件）
   · **04 状态 = 派生**（`view-config` 里 `[...new Set(rules.flatMap(...))]`，是第 08 步的投影，无独立建模面）
4. **对照实验四数齐**：08 因果 **47 → 48 → 47**，四个对照格（域15/对象100/关系127/状态41）**同刻全不动**。
5. **动线四数：8 步 / 7 点击 / 6 跳转 / 卡在第 7 步。**
   卡点 = **对象类型页新建按钮 0 个**；追一层确认 `createObjectType` **零 UI 调用方**
   （金丝雀 `createLinkType` 命中 3 处）。唯一造类型的路是「建模草案 → publishDraft」，需先有 dataset。
   **与本体底本批注 #2 一致，该缺口仍开着。**

### ⚠ 我在那张派单里给错的两处（我的错，记账）

- WO 里写的路径 **`apps/frontend-shell/src/views/admin/adminRegistry.ts` 不存在** ——
  真实路径是 **`pages/adminRegistry.ts`**，且 **`views/admin/` 整个目录不存在**。
  （我在多张单的范围边界里都写了 `views/admin/**`，下一轮派单前先核路径。）
- **`adminRegistry.ADMIN_NAV_GROUPS` 不渲染左导航** —— 真正渲染的是 `ShellLayout.NAV_GROUPS`，
  该文件注释明写「只改前者会掉进『其它』兜底桶」。dev 因此没动分组，改用挂载式步骤条，判断是对的。

### ✅ 第三节第 1 条（`merge-base --is-ancestor` 判据不够用）已被**第二次独立复现**

该 dev 开工时实测：**worktree 落后 canonical 4570 个提交，而 `merge-base --is-ancestor` RC=0**（判「不落后」）。
加上 `wo-coef-finish` 那次的 38 个提交，**同一缺陷两次独立命中** ——
照铁律 0.6 三级处置，**第 2 次必须当场建机制**。修法已写在第三节第 1 条：换成 `git rev-list --count HEAD..$CANON`。
**⛔ 这一条下一轮开工第一件事就回写 CLAUDE.md，别再拖。**

### 那位 dev 自己抓出并修掉的一个 bug（值得学）

真浏览器走查时发现：读端未返回时 `ready === undefined` 落进 `todo` 分支，
**把「没读到」画成了「没建成」** —— 正是它自己文件头注在防的那条。已加第五态 `unknown`（虚点边框），
不参与 `now`、不落 `todo`。**形态：「我用『没读到』当作『不存在』的证据。」**

⚠ 另记一条差点误报的：探针给**无 body 的 DELETE 带了 JSON header** ⇒ 400 `FST_ERR_CTP_EMPTY_JSON_BODY`，
**差一点报成「删除路坏了」**。是探针的错，不是应用缺陷。

## 四、⛔ 明确未完成、下一轮必须做的两件

1. **12 道红门与 canonical 基线的逐条比对没做完。** 我起的基线跑被自己造的负载拖垮后被我杀掉。
   ⚠ **⛔ 不许拿「本来就红」当挡箭牌放行**，必须真比一次。
   已知线索：`wo-coef-finish` 那位在 rebase 后实测治理门红 **12 → 11**，且逐条追到了点名文件
   （`factlock-anchor`→`decision-console-model.test.ts:548` · `claim-strength`→`OptimizeWhatifView.tsx:631/764` ·
   `coverage-blind` 报 141 条 `apps/datacore/test` 命中 · `fact-usage` 前端读取位棘轮 462→447）。
2. **上一次四包 gate 的结果作废，需重跑。** 两个理由：
   ① datacore 不是断言红，是 `ERR_IPC_CHANNEL_CLOSED`（8 个 vitest 并发、load 71.98 下 worker 被挤掉）；
   ② gate 自己改了两个 index JSON 导致工作区不干净 —— 按判据「HEAD 未变 **且** 工作区干净」两条任一不成立即作废。
   **重跑前先探并发**：数「父进程不是 vitest 的 vitest 进程」，≥1 就等。

## 五、本轮的调度教训（我自己的错，记账防复发）

- **同时开 5 张单、每张都写着「跑 gate」** ⇒ load **71.98**（CLAUDE.md 记的自伤线是 35）。
  派单模板里那句「跑 vitest 前先避让」**是让每个 dev 各自判断**，
  而**每个 dev 各自判断的结果就是大家一起上** —— 避让规则解决不了「谁先跑」这个协调问题。
  形态：**「我用『每张单里都写了避让』当作『不会撞车』的证据。」**
  **修法（下一轮照做）**：四包 gate **由收编方统一跑一次**，派单里直接写死「⛔ 不跑 gate、不跑全量 vitest」。
- **我自己那次 gate 留下 3 棵僵尸 vitest 树** —— 外层壳报完了、进程还在吃 CPU，占了当时负载的一半。
  杀掉后 load 72 → 46。起 gate 后**必须回收自己的后代**。
