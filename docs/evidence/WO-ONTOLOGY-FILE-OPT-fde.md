# WO-ONTOLOGY-QUICKINDEX（R21）· FDE 真跑证据 — 系统本体 §0.5 快查索引

> 目标：`docs/SYSTEM-ONTOLOGY.md` 已 591 行，read-first 协议要求先定位对象类型/链路/不变量/门禁却无跳转锚。
> 在 §0（read-first 协议）与 §1（顶层地图）之间插入 **§0.5 快查索引**（纯人读导航层·零新事实），
> **硬约束**：不得污染两个下游正则解析器（meta/parse.ts 投影 + check-system-ontology / check-meta-sync 门），
> 否则会撑破 meta-ontology.test 的确定性计数断言（R6）。

## §1 改动（唯一文件·只增不改）

- `docs/SYSTEM-ONTOLOGY.md`：在旧 §0 结尾 `---` 之后、`## 1. 顶层地图` 之前，插入 `## 0.5 快查索引（read-first 导航层 · 指路不承载真相）`。
- `git diff --stat` = **1 file changed, 35 insertions(+)**（**零 deletions** → §2–§10 既有正文一字未动）。
- 内容：定位声明 + 主表「我要找 X → 去哪」（14 行诉求→主节/相关节/相关不变量·断点）+ 高频锚点速查（纯文本要点）+ 节末给机器读者的"本节不含可解析模式"声明。

## §2 硬约束核验（成败线 · 全绿不变）

### 2.1 解析器计数：施工前 = 施工后（§0.5 对投影不可见）
```
events 92 · slices 15 · domains 11 · objTypes 32   （§0.5 插入前后完全一致）
```
§0.5 刻意规避的可解析形态（全部未引入）：反引号事件形 `x.y`、切片键 `sys.x.y`、加粗域形 `**Dn 名**`、
行首加粗制品 `- **Name**`、反引号真实文件锚 —— 表内 R/G/D 引用一律纯文本指路，节号用 §N 文本。
断点行劫持风险规避：§0.5 表所有行首列为"诉求"（`| 我要找… |`），无任一行以 `| G-n |` 开头 →
parseBreakpointRow 的 `^\|\s*G-x\s*\|` 仍只命中 §8 正文行。

### 2.2 三机器门（WO §3.B 成败线）
- `pnpm ontology:check` → ✓ 系统本体与代码一致（事件 / 求解器 / 文件锚点）；断点动态抽取不漂。
- `pnpm meta:sync` → ✓ 断点 15 · 不变量 17 · 元投影可解析校验（markdown 结构稳定可确定性投影）。
  - 注：断点 15 / 不变量 17 是机器索引 `prd-ontology-index.json` 权威计数（DR-AUDIT 增 G-DR-1 后为 15），
    **非本单 markdown 计数**；本单 §0.5 未使该计数变化（parse.ts 不变量/断点走 index 非 markdown 扫描）。
- `vitest run test/meta-ontology.test.ts` → **7 passed**（含 SystemInvariant/SystemBreakpoint 计数=index 断言、
  两次解析字节一致 R6 断言、Entitlement 先于 authz 404 门）。

### 2.3 green→red→green 自证（守卫非空转）
```
真实文件 events = 92 （绿·未污染）
向 §0.5 注入一处反引号事件 `scenario.launch` → events = 93 （红·门会挡）
```
证明：检测器确会因污染而涨计数 → 真实文件 92 未涨 = §0.5 确未引入可解析模式（非侥幸绿）。

## §3 DoD 对照（用户视角·带真实问题走一遍）

1. 打开 `docs/SYSTEM-ONTOLOGY.md`，§0 与 §1 之间出现 `## 0.5 快查索引`。✅
2. 真实问题"我要改求解器输出形状先看哪" → §0.5 主表命中行
   `求解器 / 校准 / 仿真 / S&OP / 多源融合 → §2.E 求解推演域 · §3（求解器↔计划）· §10.4 · R6 · G-2`。✅
3. 随手点验跳转目标节号真实存在：§2.E（L45 起 §2 对象类型目录内子节）、§3（L168）、§8（G-2 断点登记）—— 均对得上。✅
4. 关键判据：三机器门全绿不变（见 §2.2）→ 导航节未污染机器投影。✅

## §4 诚实边界（不在本次范围）

- 未改任何代码/脚本/契约（parse.ts / check-*.mjs / meta-ontology.test.ts 一字未动）——纯文档工程。
- 未动 §2–§10 既有正文（不重排/不删改/不新增对象类型·链路·事件·不变量·断点）。
- 未生成机器可读 TOC / YAML front-matter / 锚点 JSON（避免第二真相源；保持单一 markdown）。
- 未做文件拆分/瘦身（591 行不拆——拆分会动所有正则解析器路径，另开工单）。
- 未校准正文里随代码漂移的 file:line 锚点（独立维护动作）。

## §5 距北极星

- ✅ 真做到：read-first 有了导航层，带真实问题可一眼定位到节号而不必通读 591 行；机器投影零污染（R6 守住）。
- 📏 距北极星：§0.5 是人读导航；若要机器可跳转（GitHub 锚 / 目录 JSON）需另权衡"第二真相源"风险，本单诚实不做。
- ⚠️ 无合成/happy-path 成分（纯文档 + 确定性计数核验 + green→red→green）。

## 本体引用与影响

- 对象类型/链路/事件/不变量/门禁：**零触碰**（§0.5 是人读导航层，只指路不承载真相）。
- 回写：依 §9 维护铁律，仅改接线才回写 §3/§4/§8 —— 本单纯导航层，**无需回写**；§0.5 自身即本体常驻章节。
- 受 R6（确定性投影）直接约束，已用"计数施工前后一致 + 三门绿 + red-proof"证明守住。
