# WO · 「产能推演」页 1:1 复刻黑曜石 HTML · 接真求解器(禁 mock) · 平台唯一推演 surface

> ⚠ **本文来源说明**：完整施工单基线在用户 commit `ef05c80`（**未推入本仓**）。本仓当前落地的是：① 1:1 目标 HTML（`docs/reference/capacity-sim-obsidian-target.html`）；② 用户指令三铁律 + 测试标准 + 四项增强（下文）；③ **§5 统一 surface 全文**（来自 patch 增量·权威）。**§2 逐行求解器绑定表、§3 多方案细节、§4 面板细节、§7.5 JS→SQL 5 条验收的逐字规格**仍需用户同步 `ef05c80` 基线后补全——**dev 建 §5 IA 可立即开工，像素复刻 §1-4 建议等基线同步**。

> **这是什么**：把 `docs/reference/capacity-sim-obsidian-target.html`（锂电产能预测·黑曜石版）的"产能推演"看板，在平台 **1:1 像素复刻**（布局/交互/间距照抄，颜色/字体换平台 `tokens.css`），**每个元素接平台已有真求解器**（禁复制 HTML 的 `hashN`/`riskVal` mock），从左侧导航**移除**、改挂到"产能推演"每个基地卡片『开推演决策』下一级看板。**「推演沙盘」独立页/导航项彻底取消·统一并入本看板**。含四项增强。
> **一句话**：前端照抄 HTML 是易事；真活是"让照抄出的每个数字都来自真求解器+真数据·无真源诚实标缺不造假红"——平台后端 90% 已就绪，本单接全 + 补 UI + **收敛为唯一推演 surface**。
> **状态**：待派单（§5 IA 可立即建；§1-4 像素复刻待 `ef05c80` 基线补 §2/§7.5）。

---

## §0 本体引用与影响（铁律 0）
- **对象类型/链路**（母体 §2/§3）：`Base`/`Order`/`Model` + 决策派生量；不新增对象类型（复用既有真求解器输出）。
- **不变量**（母体 §5）：**G-DM-1 诚信红线**（禁 mockTightness/hash 造决策级红·平台已治本删·禁复活）· **R6 同源**（对话态/嵌入场景态走同一批求解器同源出结果·同输入字节一致）· **dataMode 诚实**（无真源 → `dataMode!=LIVE` → 灰/缺件面板·绝不假红）· **R14**（不搬 HTML 静态数据入平台）。
- **断点**（母体 §8）：收敛推演 surface（消除双入口）；`SandboxView` 独立路由退役。
- **回写**：母体 §2.I 沙盘域改述"沙盘 = 产能推演看板下钻态·非独立导航/路由"；导航信息架构删沙盘项；`SandboxView` 独立路由退役登记。`pnpm ontology:slices`。

---

## §1 三条铁律（最高优先·用户亲定）

1. **"1:1" 的准确含义**：结构/间距/交互**严格照抄** HTML；颜色/字体换平台 `tokens.css`（同一设计语言·近零工）；**数据值全新且必须真——绝不搬 HTML 的 `hashN`/`riskVal`/`RISG_SOL`/`AOP_SCEN` 任何静态数据**。
2. **数据来自真求解器 + Postgres 真行（"JS 假→SQL 真"）**：每个元素接哪个真 solver、落哪张表见 §2 绑定表（`risk_timeline`/`mitigation_select`/`affected_orders`/`capex_scenario`/`quote_margin`/`kit_readiness`/`credit_exposure`/… 平台 **90% 已就绪**）。无真源 → 走既有 `dataMode!=LIVE` → 灰/缺件面板诚实空态。
3. **诚信红线 G-DM-1**：禁止用 `mockTightness`/hash 造决策级红（平台已治本删除·禁复活）。**green→red**：把 `demandCapacityTightness` 换回 `mockTightness` → `risk-board-kill-mock-red` 测试必须红。

## §2 求解器绑定（元素 → 真 solver → 真表）

平台 90% 已就绪的真求解器（元素逐一绑定·**完整逐行表待 `ef05c80` 基线**）：
`risk_timeline`（风险时间轴）· `mitigation_select`（缓解方案选择）· `affected_orders`（受影响订单）· `capex_scenario`（资本开支情景）· `quote_margin`（报价毛利）· `kit_readiness`（齐套就绪）· `credit_exposure`（信用敞口）· …
- 无真源元素 → **既有 `dataMode!=LIVE → 灰/noDataReason` 诚实空态**，绝不假红。锚点：`RiskBoardView.tsx`/`risk.ts`/`solver-registry.ts`/`tokens.css`。

## §3/§4 四项增强
① **多方案**（数量可选 topN）+ 并排比较表；② **缺失信息分类面板**（缺主数据/缺时序/缺派生规则/缺求解器绑定）；③ **全元素悬浮溯源**（hover 见来源 SQL 行/solver）；④ **推演过程图接真 provenance**。

## §5 统一为一个推演 surface + 两种模式（「推演沙盘」独立页取消）

> **信息架构定案（唯一 surface 原则）**：平台**不再有两个推演入口**。"推演沙盘"作为**独立页 / 左导航一级项彻底取消**，一切收敛到唯一 surface = 本"产能推演"看板。原沙盘能力**不丢**，并入看板对应位置。

- **一个 surface · 两种模式**（两态都在同一看板内）：
  | 模式 | 入口 | 形态 |
  |---|---|---|
  | **嵌入场景态**（主） | 基地卡片『开推演决策』/『风险传播』→ 选 X 天 | 产能推演看板（时间轴/方案/过程图）·**非对话** |
  | **对话态**（辅·同一看板内） | 看板内"提问"框 / AI 指挥台 | 自然语言问 → 同一批求解器 → 同源答案 |
- **重定位（彻底）**：
  1. **左侧导航删除"推演沙盘"一级项**——不再是独立菜单/独立页。
  2. 唯一入口 = **"产能推演"页 → 每个基地卡片『开推演决策』→ 下一级看板**（scope 聚焦该基地）。
  3. 原 `SandboxView`（对话沙盘）**不再作为独立路由**；`/sandbox` 若保留则 **302 重定向**到产能推演。其对话/what-if 能力迁入看板"对话态"提问框（复用同一批求解器）。
- **并入既有两份沙盘 WO（以本单为准·杜绝双 surface）**：
  | 此前文档 | 原设定 | 并入本看板后 |
  |---|---|---|
  | `WO-SANDBOX-READINESS-UX` | 沙盘就绪认证进抽屉 | → 看板顶部**一行信任条 + [查看完整体检] 抽屉**（L0-L4/三雷达进抽屉·守 RL3 不动投影） |
  | "问→答→因→行"对话稿 | 独立对话沙盘页 | → 看板**对话态提问框**（问=提问框·答/因/行=看板结果/过程图/采纳→工单） |
- **验收**：①左导航无"推演沙盘"；②无独立 `/sandbox` 路由（或 302 重定向）；③基地卡片『开推演决策』→ 进看板 scope=该基地；④对话/嵌入两态走同一求解器同源（R6）；⑤就绪认证呈现为**信任条+抽屉**（非独立页）。

## §7.5 测试标准（三层分治·用户亲定）
- **L1 UI 几何 diff**：遮蔽颜色字体·别做色彩 diff（换 tokens 天然不同色）。
- **L2 UX 交互逐个 replay**：交互行为逐个复现。
- **L3 数据层"反直觉"**：**绝不断言"数字==HTML"**（那是逼你抄 mock）；而是断言"**可溯 SQL 行 / 无 JS 常量路径 / 无源诚实空**"。JS→SQL 5 条验收照做，**复用既有 `capacity-basecards-realdata` / `risk-board-kill-mock-red` 测试门**。

## §8 与既有沙盘 WO 的关系（审核方并单裁定）
- **S1-S6 机制（已复验 DONE/BUILT）= 本看板"嵌入场景态"的引擎**（渲染/时序接地/徽标/分支/派生）——**不废弃·成为看板下的求解器/推演内核**。
- **RC-1（前向闭合门）** → 就绪认证移入看板信任条·仍需修（canEnter 决定信任条状态）。
- **RC-UX-KPI-CARRIER / RC-UX-DOOR-TEXT** → 目标从"独立沙盘页"改为"本看板"（磁贴/门文案坑在看板内同样要治）。
- **RC2-DEFAULT-FEED / CAPFORECAST-DATAMODE-HONEST** → 本看板数据必须真 solver·同 G-DM-1 红线。
- **`SandboxView` 独立路由退役** = 本单 §5 交付项。
