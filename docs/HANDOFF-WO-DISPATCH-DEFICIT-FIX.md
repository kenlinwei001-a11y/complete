# HANDOFF · WO-DISPATCH-DEFICIT-FIX（2026-08-17）

⚠️ 本文通篇用「该符号 / 未派标记」指代台账里的未派图标，**故意不写字面量** ——
本仓刚有 dev 在订正文字里写了字面标记，被自己的抽取器数进去，计数不降（14≠13 当场抓出）。

## ① 修前/修后「待派」实数对照

T2 基线：merge-base = `2a1a412b`（本分支即从它长出，集成线 `origin/claude/verify-reclaim-6` 自分叉点 0 提交）。
旧脚本取自 `git show 2a1a412b:scripts/dispatch-deficit.sh`，与 HEAD 新脚本跑**同一份** `docs/REQUIREMENTS-TRACE.md`、同一个仓库：

| | 旧脚本（merge-base） | 新脚本（HEAD） |
|---|---|---|
| 待派 | **3**（TRACE 含未派标记的行） | **6**（TRACE 未派章节·未划掉项） |
| 待复验 | 5 | 5 |
| 待写WO | 13 | 13（判据改限定 §8，现 13 条命中本就在 §8 内，数值不变） |
| 合计 | 21 | 24 |

旧判据数的 3 行逐条核对（`grep -n`）：第 14 行 = 状态口径图例行 · 第 41 行 = B4 正文提及 · 第 151 行 = 章节标题。**一行都不是单。**

**与工单预估不符，如实报**：工单写「真正可派的条目 = 0（5 条全部已划掉）」。实测当前基线上该章节里躺着
**两份编号清单**：第一份 5 条中 4 条已划掉、第 1 条（步骤模板层）未划；第二份 6 条中只有第 1 条划掉、
2–6 条未划。新判据数到 **6** 条未划掉列表项，逐条看全是**陈旧记账回声**（内容与别处已闭环条目重复，
如 B4 行与章节内另一条已划掉记录均载「步骤模板层已交付」；第 6 条自述「只报不动」）。
⇒ 判据本身修对了（它不再把图例行/标题/正文当单），它如实暴露的是**台账自身两份清单删除线没回写齐**。
清理台账超出本单边界（`docs/REQUIREMENTS-TRACE.md` 对我是只读），登记为界外发现，见 ⑥。

## ② 判据新形态

「待派」= 台账中同时满足三条的行（`count_pending_dispatches()`，awk 单趟）：
a. **在「未派」章节内** —— 含该符号的 `## ` 标题起、下一个 `## ` 标题止；
b. **是列表项** —— `N.` / `-` / `*` 起头；
c. **未划掉** —— 行内无 `~~删除线~~`。
图例行、章节标题、正文提及被 a/b 天然排除。判据与金丝雀符号一律变量 `UNASSIGNED_MARK` 拼构造。

## ③ 另两队列复核结论

- **待复验**：判据 = 非集成分支祖先 **且** tip 不旧于集成分支尖端（时间闸）。两个盲区都在输出里明说：
  ① 时间闸**必要不充分** —— 推得早、至今没并的分支它看不见（漏报）；
  ② 反方向 —— 已 cherry-pick/squash 收编、但 tip 比集成分支尖端新的分支会被**多算**（祖先判据对摘并天然不成立）。
  顺手补的一处真洞：旧版 `ls-remote` 失败被 `2>/dev/null` 静默吞掉 ⇒ 网络一断队列就当 0。
  现改为失败即显式打印「待复验**未评估**（不代表为 0）」。现值 5 条（含本单自己的 handoff 分支）。
- **待写WO**：判据从「全文 grep 状态标记」改为**限定 §8 章节**（别章提到同样状态词会混入 —— 与判据 ① 同一个病）。
  **盲区（本单不修，工单明确要求明说）**：§8 现 190 编号行（172 唯一编号），其中 **27 行（27 个唯一编号）无任何状态标记**，
  队列永远看不见它们 —— 「待写WO 13」≠「全部未闭断点 13」。回填归 WO-ONTO-STATUS-BACKFILL（另有 dev）。
  脚本每次运行**现算**这两个数打出来，不写死。
  ⚠️ 工单写「104 行（95 唯一编号）无标记」，我实测是 27/27（判据：行内无 🔴◑✅🟡⚪🔶🟢 任一）。以实测为准。

## ④ 金丝雀证据（判据级，每次运行自证，与主逻辑共用同一函数）

夹具运行时生成即焚，样例**全部取自台账实物**：图例行（状态口径引用行）· 正文提及（B4 表行）·
章节标题 · 已划掉项（章节内第一条带删除线的列表项）· 必中样例（已划掉项剥掉删除线的派生物）·
章节外未划掉列表项。期望恒 **1**。
- **必中**：正常跑过金丝雀才出数 ⇒ 新判据数到夹具里那 1 条未划掉项（否则脚本 RC=2 根本走不到打印）。
- **必不咬**：同一跑里图例行/正文提及/章节标题/已划掉项/章节外项 5 条全没被数（期望 1 实测 1）。
- 取样失败（哪类实物取不到）同样 RC=2，不许读成 0。

## ⑤ 变异反证

把 `count_pending_dispatches()` 改回旧判据「数所有含该符号的行」：
脚本 **RC=2**，报「待派判据金丝雀不中 ⇒ 夹具应数 1 条、实测 **3** 条」——
多出的正是图例行 + 正文提及 + 章节标题，必不咬那侧精确开火。
`git checkout --` 还原后复跑：待派 6 · 待复验 5 · 待写WO 13 = 24；`git status --porcelain` 为空。

前置门：`node scripts/check-branch-base.mjs wo-dispatch-deficit-fix --onto=origin/claude/verify-reclaim-6` **RC=0**（分叉点落后 0）·
`node scripts/check-merge-conflict-markers.mjs` **RC=0**（金丝雀 7/7）。

## ⑥ 界外发现（只登记，未顺手修）

1. **台账「未派」章节有两份编号清单、删除线回写不齐**（见 ①）：6 条未划掉项全是陈旧回声。
   该文件归台账维护方，本单只读。新判据上线后这 6 条会持续被报为待派 —— 这不是探针误报，
   是台账真欠回写；清掉后探针自然归零。
2. **CLAUDE.md 铁律 2 机制段**仍写「① 待派 = 台账里标（该符号）的行」—— 判据已改，文档口径未跟。
   建议由有权限改 CLAUDE.md 的人同步成「未派章节里未划掉的列表项」。
3. 旧脚本在 macOS 上因缺 `nproc` 直接 RC=2、因缺 `/proc/loadavg` 把载荷读成 0 ⇒ 假「欠派 21 张」。
   新脚本已加 `sysctl` 回退（顺带交付，同文件内）。
