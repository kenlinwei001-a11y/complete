# WO-ONTOLOGY-QUICKINDEX · 系统本体加 §0.5 快查索引（不破坏单一真相源）

> 由来：需求 R21——`docs/SYSTEM-ONTOLOGY.md` 已达 **567 行**（`wc -l` 实测），read-first 协议（§0 L10-20）要求每次改动前定位对象类型/链路/不变量/门禁，但全靠人肉滚动无跳转锚，检索成本随文件膨胀。加一节 **§0.5 快查索引**（人读导航层），**保持 markdown、不动任何既有章节内容、不改代码**。依赖：仅 `docs/SYSTEM-ONTOLOGY.md` 本身；**硬约束**是不破坏它作为机器可读单一真相源的两个下游解析器。

## §0 目标 + DoD-as-experience（用户视角·亲手走一遍能用）

**目标**：在 §0（read-first 协议）与 §1（顶层地图）之间插入 **§0.5 快查索引**——一张"我要找 X → 跳第几节/搜什么关键词"的导航表 + 高频锚点速查，让读者不必通读 567 行即可定位。**纯导航层，零新事实**（所有真相仍在 §2–§10，索引只指路）。

**DoD（FDE 亲手走一遍，非测试绿）**：
1. 打开 `docs/SYSTEM-ONTOLOGY.md`，第 ~22 行（§0 之后、§1 之前）出现 `## 0.5 快查索引`。
2. 带着一个真实问题读它，例如"**我要改求解器输出形状，先看哪**"——在 §0.5 表里一眼看到 `求解器/Solver → §2.E · §3(G-2) · §5 R6 · 断点 G-2(§8)`，据此直接跳到对应节，**不用滚全文**。
3. 表里每个"跳转目标"都是**真实存在的节号**（§2.A–§2.J / §3 / §4 / §5 / §7 / §8 / §10.2–§10.5），随手点验 3 条都对得上。
4. 关键判据：`pnpm ontology:check`、`pnpm meta:sync`、`pnpm --filter datacore test`（meta-ontology 用例）**全绿不变**——证明新增的导航节**没有污染机器投影**（这是本单的成败线，见 §3 门）。

## §1 现状盘点（钉真实 file:line，grep/read 核实）

| 事实 | 锚点（真实） | 状态 |
|---|---|---|
| 文件行数 567 行（需优化的由来） | `wc -l docs/SYSTEM-ONTOLOGY.md` = 567 | 🔴 无快查索引 |
| read-first 协议要求先定位对象类型/链路/不变量/门禁 | `SYSTEM-ONTOLOGY.md:10-20`（§0 五步） | ✅ 有协议 / 🔴 无跳转锚 |
| 现有章节结构 §0…§10（含子节 §2.A–J / §10.1–10.5） | `SYSTEM-ONTOLOGY.md` 标题：§0=L10 §1=L24 §2=L45 §3=L164 §4=L294 §5=L368 §6=L398 §7=L404 §8=L460 §9=L486 §10=L495 | ✅ 结构清晰 |
| 已有任何"快查/索引/TOC/跳转" | grep `快查\|索引\|目录\|TOC\|跳转` — 仅 §2 KbDoc `kb.ts` 的"索引/检索"及 §2 标题"对象类型目录"，**无导航索引** | 🔴 缺 |
| 本体是**机器可读**单一真相源（下游解析器①） | `apps/datacore/src/meta/parse.ts:41-89` `parseMetaOntology` 用**正则**抽 8 类元对象 | ✅ 在用（约束源） |
| 下游解析器② 漂移门 | `scripts/check-system-ontology.mjs:30/32/54`（事件反引号 / 文件锚点正则）、`scripts/check-meta-sync.mjs:17-29` | ✅ 在用（约束源） |
| 解析器正则**当前命中量**（新节不得增此集） | `parse.ts` 实测：events **86** · slices **15**（`sys.x.y`）· domains **11**（`**Dn 名**`）· objectTypes **32**（`- **Name**`）· 文件锚点 `path/file.ts` | ✅ 已量化 |
| 确定性计数断言（会因污染而红） | `apps/datacore/test/meta-ontology.test.ts:26-27`：SystemInvariant 数 = index.invariants(**17**)、SystemBreakpoint 数 = index.breakpoints(**14**) | ✅ 硬门 |
| R/G 权威集来源（index 而非 markdown 计数） | `docs/prd-ontology-index.json`：invariants 17 个、breakpoints 14 个 | ✅ 参考 |
| §9 维护铁律：改接线才需回写；本单不改接线 | `SYSTEM-ONTOLOGY.md:488` | ✅ 本单纯导航→**无需回写 §3/§4/§8** |

**核心约束（钉死）**：`SYSTEM-ONTOLOGY.md` 是**人读文档 + 机器投影源**双重身份。§0.5 是给人的导航，但正则解析器不分"导航节 vs 正文节"——它扫全文。因此 §0.5 **绝不能**引入这些模式，否则 `parse.ts` 会把导航里的字样当成真元对象，撑破 `meta-ontology.test.ts:26-27` 的计数断言、触发 `check-system-ontology.mjs` 文件锚点门：
- 反引号事件形 `` `x.y` ``（除已存在于正文的）；
- 反引号切片键 `` `sys.x.y` ``；
- 加粗域形 `**Dn 名称**`；
- 行首加粗制品 `- **Name**`（首字母大写、2–40 字符）；
- 反引号真实文件锚 `` `apps/...ts` `` / `` `scripts/...mjs` ``（`check-system-ontology.mjs:56` 校验存在性）。

## §2 施工范围（dev 可直接照做）

**唯一改动文件**：`docs/SYSTEM-ONTOLOGY.md`。**只增不改**：在 `SYSTEM-ONTOLOGY.md:22`（现有 `---` 分隔线，§0 与 §1 之间）**之后、§1 之前**插入新节 §0.5。既有 §0–§10 一字不动。

**§0.5 内容规格**（照此写，遵守上面"绝不引入"清单）：

1. 节标题：`## 0.5 快查索引（read-first 导航层 · 指路不承载真相）`
2. 一句定位声明：说明本节只是导航，真相在 §2–§10；改接线回写仍改对应正文节，不改本节。
3. **主表「我要找 X → 去哪」**（列：诉求 / 主节 / 相关节 / 相关不变量·断点）。用**纯文本描述**指路，**不要**用会被正则捕获的形态。示例行（关键词用普通文本，不加反引号成 `x.y`；节号用 §N 文本）：

   | 我要找… | 主节 | 相关 | 不变量/断点 |
   |---|---|---|---|
   | 数据怎么进系统（连接器/原始表/合成/构建发动机） | §2.A 数据接入域 | §3 关系图 · §10.2 D1 | R2 · G-8 |
   | 对象类型/实例/派生/切片 | §2.B 本体对象域 | §10.2 D2 · §10.3 type_lineage | R6 · R11 |
   | 规则/约束/规则文档抽取 | §2.C 规则约束域 | §10.2 D3 | — |
   | Action 审批 / 真值写回 | §2.D 行动权限域 | §6 行动 · §10.2 D5 | R4 |
   | 求解器 / 校准 / 仿真 / S&OP | §2.E 求解推演域 | §3(Solver↔Plan) · §10.4 | R6 · **G-2** |
   | 时序 / 模拟时钟 / 回放 / 达成率下钻 | §2.F 时序运营域 | §10.2 D10 | R13 · R14 |
   | 租户隔离 / 权限行级过滤 / Entitlement | §2.G 治理平台域 | §5 · §10.2 D6 | R2 · R3 |
   | 意图 / 执行计划 / QOS / Agent / Skill / MCP | §2.H 交互编排域 | §3 中枢链 · §10.3 query_to_answer | **G-1 · G-3 · G-4** |
   | 场景包 / 场景卡 / 场景入口 / 视图 | §2.H · §10.2 D8 | §10.3 scenario.launch | — |
   | 事件 / 失效 / 通知 / 订阅刷新 | §4 数据流事件图 | §10.2 D9 | D-29 |
   | 系统不变量清单（改动铁律） | §5 系统不变量 | — | R1–R17 |
   | 检测 / 门禁（改完必过） | §7 检测门禁 | §10.5 | R11 闭包 |
   | 已知断点登记 | §8 断点登记 | §10.4 跨域节点 | G-1…G-15 |
   | 系统自我域 / 域内切片 / 跨域接缝 | §10 | §10.2/10.3/10.4 | — |

4. **高频锚点速查**（一小段要点清单，纯文本）：如"中枢链在 §10.3、构建链在 §10.3、断点几乎全在跨域节点（§10.4）、'绿测试≠能用'见 §0 结尾"。**不要**在此粘贴真实 `path/file.ts` 反引号锚点（会进 `check-system-ontology.mjs:56` 校验集，且属正文职责）。

5. **给机器读者的一行提示**（放节末）：`> 本节为人读导航，非机器投影源；meta/parse.ts 的元对象仍从 §2–§10 正文抽取，本节刻意不含 sys.* 切片键 / 事件反引号 / 加粗域 / 加粗制品 / 文件锚点等可解析模式（防投影计数漂移）。`

**dev 自查清单（写完立刻跑，不达标即改回）**：
```bash
# 新节不得给 parse.ts 增计数（下列四数须与施工前一致：86/15/11/32）
node -e 'const fs=require("fs"),md=fs.readFileSync("docs/SYSTEM-ONTOLOGY.md","utf8"),X=/\.(ts|tsx|js|mjs|cjs|json|md|sql|csv|txt|html|yaml|yml|sh|conf)$/;
console.log("events",[...new Set([...md.matchAll(/`([a-z0-9_]+\.[a-z0-9_]+)`/g)].map(m=>m[1]))].filter(e=>!X.test(e)).length,
"slices",[...new Set([...md.matchAll(/`(sys\.[a-z_]+\.[a-z_]+)`/g)].map(m=>m[1]))].length,
"domains",[...new Set([...md.matchAll(/\*\*(D\d+)\s+([^*（(]+?)\*\*/g)].map(m=>m[1]))].length,
"objTypes",[...new Set([...md.matchAll(/^- \*\*([A-Z][A-Za-z0-9 \/]+?)\*\*/gm)].map(m=>m[1].split("/")[0].trim()))].filter(k=>k.length>=2&&k.length<=40).length)'
```
若任一数变大 → §0.5 用词命中了正则，替换该处写法（去反引号 / 改行首非 `- **` / 避免 `**Dn `）。

## §3 验收（FDE 亲手·真门·非测试绿）

**A. 人读体验（亲手走一遍）**
1. `git diff docs/SYSTEM-ONTOLOGY.md` → 仅在 §0 与 §1 间新增 §0.5，**别处零改动**。
2. 在编辑器/GitHub 预览打开文件，带真实问题"改求解器输出形状先看哪" → §0.5 主表定位到 `§2.E · §3(G-2) · §5 R6 · G-2`，跳过去内容对得上。随机再验 3 行跳转目标节号真实存在。

**B. 机器门（成败线，全绿不变）**
```bash
pnpm ontology:check        # 事件/求解器/文件锚点漂移门（check-system-ontology.mjs）→ ✓
pnpm meta:sync             # markdown 结构可投影门（check-meta-sync.mjs）→ ✓
pnpm --filter datacore test -- meta-ontology   # 计数断言 SystemInvariant=17 / SystemBreakpoint=14 不变 → 全绿
```
三者全绿 = §0.5 是纯导航、未污染机器投影。**任一红 = §0.5 引入了可解析模式，回 §2 dev 自查清单修**。

**C. 门（交付底线）**
- `pnpm -r build && pnpm -r test` 维持 4 包全绿基线（datacore 69 / agentcore 66 / frontend 25+）——本单不改代码，理应零影响，用于兜底证明。

## §4 不在本次范围（诚实边界）

- **不改任何代码/脚本/契约**：`parse.ts`、`check-*.mjs`、`meta-ontology.test.ts` 一律不动（本单是文档工程）。
- **不动 §2–§10 既有正文**：不重排、不删改、不新增对象类型/链路/事件/不变量/断点——那些是回写职责，本单只加导航层。
- **不生成机器可读 TOC / YAML front-matter / 锚点 JSON**：需求明确"保持 markdown（非 YAML）"。§0.5 是人读表格，不引第二份索引产物（避免第二真相源）。
- **不做文件拆分/归档/瘦身**：567 行不拆成多文件（拆分会动"单一真相源 + 所有正则解析器路径"，风险与工作量远超本简短单；若确需，另开工单）。
- **不校准既有 file:line 锚点**：正文里随代码漂移的锚点校准是独立维护动作，不在此单。

## 本体引用与影响（链路/对象类型/不变量/断点/回写）

- **对象类型/链路/事件**：本单**零触碰**。§0.5 是人读导航层，不新增/不改任何对象类型、链路、领域事件。
- **不变量**：受 **R6（确定性）** 直接约束——`parse.ts` 是确定性纯函数投影，`meta-ontology.test.ts:20` 断言两次解析字节一致、L26-27 断言节点计数=index 权威集；§0.5 必须"对解析器不可见"（不增 events/slices/domains/objectTypes 计数）方能守住 R6 投影稳定。间接服务 **read-first 协议（§0）** 的可用性。
- **断点**：不新增、不改状态；§0.5 主表**引用** G-1/G-2/G-3/G-4/G-8 等作为"去哪找"的指路，不改其 §8 登记行（`meta:sync` 门守 §8 行完整）。
- **门禁**：产出即受 `ontology:check` + `meta:sync` + `meta-ontology` 单测三门约束（见 §3.B），这三门正是"本体作单一真相源不被污染"的守护。
- **回写**：依 §9（`SYSTEM-ONTOLOGY.md:488`），仅当改动新增/改变对象类型/链路/事件/不变量/门禁才需回写 §3/§4/§8——**本单纯导航层，无需回写**。§0.5 自身即写在本体内，是"文档对自身可读性的增强"，落地后成为本体常驻章节。

*审核方自包含施工单(design+review·铁律0.5·钉真实file:line)· 仅推 claude/vigilant-knuth-b1nmxn · 模型标识不入任何提交物*
