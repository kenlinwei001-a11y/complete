# LOOP10 · 怀疑者（第 1 次发言）

base `3408572c` · 取证 2026-08-29 13:34 UTC · `wc -l apps/datacore/src/synthetic/battery.ts` = **5357**
真后端 `:4331` + 真 chromium，禁 `VITE_MOCK`。截图 `docs/assets/loop10-skeptic/`。

## 论点

**不支撑。因为能扰的量一个都不值钱。** 屏上「今天扰不动的量（20）」列的正是
需求 `Order.qty`、物料齐套 `onHand`、设备 OEE、瓶颈工序、换型损失 —— CEO/COO 的场景全在这 20 条里；
能扰的 40 个全是 `xxPressure / xxRisk`，屏上自认「量纲 无」。
故多数未兑现的承诺**该删**，只有两条该补。

## 论据（屏上原文 → 实测 → 补/删）

1. 「世界态出处：DERIVED…**实测格 0/7204**」＋「今天扰不动的量（20）」。
   → **补数据**：500 单 / **507.26 亿元**的订单簿是唯一有金额的场景，而它的 `qty` 恰恰扰不动。
2. 「**示例数据 · 这一页的数不是本次推演算出来的**」（传导识别档顶）；同档「传导方向 常州→扬州／常州→合肥」改值 = **0 请求 0 字变化**。→ **删**（挂不上任何场景）。
3. 「方案寻优」目标下拉「`OrderLine.unitPrice` 最大／`Base.serveCost` 最小」改值 = **0 请求 0 字变化** —— CEO 唯一的取舍旋钮不转。→ **补**。
4. 「应用到本会话」→ `PATCH` **409**，全页零错误提示。→ **补**（静默失败的写操作）。
5. 「钉到对照／追这条链」**0 请求**，只往屏底日志写「动作 `pin:supplyRisk`（**本单不落写操作**）」——工单术语上屏。→ **删**。
6. 页签写「**38** 条因果边」，同屏面板写「**42** 条边」。→ **删那句 38**。
7. `?type=Order&limit=1000` 静默回落 50、零警告；`EquipmentOEE` 自报 `total=1000`（＝硬顶），真实 780 台 × 7 天 = **5460**。`total` 是调用方唯一能察觉截断的字段，它自己被截断了（指标墙有红字自认截断，这个接口没有）。→ **补**。

## 假控件（一行数）

`/v/sim-unified` 8 档 ＋ `/v/decision-play`：可见未禁用 **103 · 真 95 · 假 8 · 报错无提示 1**，禁用 **14**（「损失归因」独占 13）。
金丝雀：传导边开关 **24 次全部真**（最大屏文 +52,713 字）；复核推翻我自己 5 条误判（`<details>`／`<select>` 要 change 事件，不是 click）。

## 「传播和后果」兑现几成

**路径有**（上游一跳／下游一跳＋系数＋延迟拍数），**人话解释无**，**后果不是经营语言**：
屏上是「供应风险 **5496.22** Δ+280」，归因档只给天与百分比（订单回款 71% · 780.00D），
决策页方案比对给「**代价 248**」—— 无单位。全程零金额。→ **这句承诺今天该撤。**

## 只有我会说的那一条

**这台控制台最诚实的两句话，正是判它今天不能用的两句** ——「实测格 0/7204」和「今天扰不动的量（20）」。
它没骗人：它把「我做不到」写在了屏上，然后照样摆了 8 个页签、117 个控件。
**该删的不是那两句话，是那几档挂不上场景的页签。**
