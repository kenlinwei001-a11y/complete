# 仓主要求 ↔ WO 追溯台账

> **为什么有这份文件**：2026-08-14 容器重启把 9 个后台任务一起杀掉，其中 2 张单的产出因未 push 而丢失。
> 口头追踪的要求同样会随上下文丢失。此表是**唯一台账**，新要求进来即追加，状态变化即回写。
>
> **状态口径**（不许含糊）：
> - ✅ **已交付并收编** —— 代码已在 `claude/verify-reclaim-6`，且我亲手复验过
> - 🔵 **在跑** —— 有 agent 正在做
> - 🟡 **部分** —— 做了一半，缺口写明
> - ⛔ **未派** —— 还没开工
> - 🔶 **等仓主裁决** —— 属产品/信息架构决策，我不擅自定

---

## A · 推演沙盘前端

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| A1 | 「推演沙盘前端开发完毕了吗」「何时可以上线最新的」 | 多张（v3/v4/metro/candidates…） | ✅ 已收编 |
| A2 | 「没有看到类似**地铁线路**的 UX」 | WO-R9-METRO-UX | ✅ 已收编（含三皮肤截图证据 `docs/shots/`） |
| A3 | 「文字都看不清楚，文字与背景色反差太低」 | WO-R9-CONTRAST | ✅ 对比度 184/141/318 → **0/0/0**（三套皮肤各 422 元素） |
| A4 | 「关系边（本体图谱结构），为何目前系统里没有这个功能？」 | WO-ACTIVE-EDGE-UX | ✅ 9 个推演页挂 `EdgeActivePanel` + 专门的挂载点门 |
| A5 | 「目前系统的页面都参照这个做了调整吗？」 | WO-ACTIVE-EDGE-UX | ✅ 9/9 页，且门咬「必须挂在默认导出主组件里」 |
| A6 | 「输入扰动因素后会出现一个『求解』的 icon，很难看」 | 审核方直接改 | ✅ 改为 `inflightStrip`（无跳动 + 呼吸点 + 文字取消） |
| A7 | 「这些都是什么数据？对客户的价值是？」「没价值则展示汇总数据，点击后再展示详细数据」 | 审核方直接改 | ✅ 汇总条 + `<details>` 折叠；**刻意不报「缺口 X→Y」**（面板拿不到 gap，编造就是造数） |

## B · 后端有、前端没有（接缝断链）

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| B1 | 「**彻查**所有后端有功能、前端没有的情况还有哪些」 | WO-GATE-BEFE-SEAM 建门 + BEFE-A…G | ✅ 零调用 **196 → 128** |
| B2 | 「都派」 | BEFE-A/B/C/D/E/F/G 七张 | ✅ 全部收编 |
| B3 | 「不局限在推演沙盘，而是**整体系统**」 | 同上 + REFERENCES-FAMILY | ✅ 引用族 9 条 → 一个客户端 + 一块共享面板 |
| B4 | 剩余 1 条 `POST /a/v1/process-instances` | WO-BEFE-CLEANUP 分诊 | 🟡 **诚实挂账不接**：契约要求 `tasks.min(1)`，而流程定义**零步骤字段** ⇒ 前端无数据源可填，接按钮就得凭空发明步骤。前置是**步骤模板层**，⛔ 未派 |

## C · 数据构建发动机 / 逆向数据推演

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| C1 | 「逆向数据推演…你解决了吗」 | v4 反推 PRD 三个缺口 | ✅ 2.1 哈希占位诚实位 · 2.2 `coverage` 死杠杆（**判定为派生不存储**，避免同一自由度存第三份）· 2.3 流程运行时两半 |
| C2 | 「输入故事脚本 → 显示缺什么 → 人工触发创建 → 完成创建 → 人工确定入库或下载」 | WO-DBUI-FLOW | ✅ 主流程排第一 + 建门 `check-dbui-flow-order` 咬死顺序 |
| C3 | 「你目前数据构建发动机的前端 UX 逻辑，我看不懂」「这些都是我看不懂的功能」 | WO-DBUI-FLOW + `check-dev-jargon-onscreen` | ✅ 屏上禁「区2/区4」「三页归一」「厂商中立施工」等开发话，建门机检 |
| C4 | 「模拟的数据是否可以入库，需要系统**再次复验自检**（人不清楚系统里数据现状、是否有冲突）」 | WO-DBUI-FLOW 的 promote-precheck | 🟡 干跑回执有 5 类，**13 类卡片在 step② 做不到**（那是 pre-build，`BuildPlan` 无 by-id 读端点）。⛔ 完整自检未派 |
| C5 | 「需要在前端展示**具体创建了哪些数据详情**（表格形式），而不只是说创建了 XX 数据」 | WO-DBUI-FLOW | 🟡 部分。⛔ 完整明细表未派 |

## D · 决策质量（"传统 BI"那条）

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| D1 | 「典型的传统 BI，只展示发生了什么，**不提供根因分析，不提供解决推演后的方案**」 | WO-DECISION-PLAY-OPTIONS | ✅ 查实**真病比报的重**：不是"数值写死"，是**方案身份与根因语义无关**（现金域根因是应收账龄，照样拿到"正极供应链战略"）。已改为**依据可核对才下发** |
| D2 | 同上·下半场 | WO-DECISION-PLAY-FE-CONSUME | ✅ 屏上三种态说**三句不同的话**；`gapClose=null` 时**一个 0 都不出现** |
| D3 | 同族矛盾（门抖出来的） | WO-LTA-EVIDENCE-CONFLICT | ✅ 「因为 A 家违约 ⇒ 给 B 家加条款」治根；顺带查出屏上那个「160 万」**从头到尾指错人** |
| D4 | 同族·更狠的一条 | WO-ORDER-DEPENDENT-PICK | ⛔ **重启丢失，待重派**：归因树入口因子按 `factorId` **字典序**取第一个 ⇒ 新增一个字母靠前的因子，根因整个换人，**零测试变红** |

## E · 导航与信息架构

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| E1 | 「导航栏里面的『决策推演』不应该在这个位置，而是**嵌入到每个需要决策的点**」 | WO-ORDER-JOURNEY | ✅ 抽成 `DecisionPlayPanel` 唯一实现，壳与嵌入同一份 |
| E2 | 是否**删掉**导航项 | WO-ORDER-JOURNEY 已测连坐面 | 🔶 **等裁决**：删了会连坐 4 处（导航覆盖门 / DashboardView 跳转 + 其测试 / imp2plan 挂 route / 深链 query 契约）。dev 现改为保留 route 改标签 |
| E3 | 「『流程等待态』是干嘛用的？客户什么场景需要进入这个页面？」（问了两次） | 已回答 + WO-PROCESS-CANVAS-LIVE | ✅ 答：模板层「这**类**流程通常等什么」+ 实例层「**这一张单**卡在第几站」 |
| E4 | 「它应该是在**推演沙盘里面**，且是**动态的数据变化**，基于某个时间 screenshot 有个总结」 | WO-PROCESS-CANVAS-LIVE | ✅ 第五档接上推演节拍，三档分类（随节拍/无承载对象/引擎够不着）各说不同的话 |
| E5 | 两页是否合并 / 移组 | 同上 | 🔶 **等裁决**：dev 建议两页都留（答的问题正交），并指出真要合该合 `process-wait` 与 `process-stuck` |
| E6 | 「订单全链条推演与项目推演，是否部分功能重复？」 | WO-ORDER-JOURNEY | 🔶 **等裁决**：改名提案 订单全链条→**订单进展与卡因** · 项目推演→**接单可行性** · 全局项目推演→**全域接单组合最优**（第一个已改，后两个待你点头） |
| E7 | 「是否修改为**订单状态**，且需要类似地铁线路图的 UX 展示项目进展？」 | WO-ORDER-JOURNEY | ✅ 单订单地铁图已接（`ChainLineMapView` 零改动复用，按 `data.so` 锚真实订单） |

## F · 产能推演可读性

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| F1 | 「看不懂『产能推演』这个 UX，你希望用户看到这个做什么？」 | WO-UI-LAYERING-BURNDOWN | 🟡 6 层产能金字塔已按 `role` 公式可读化；**分层欠账仍剩 21 条**，WO-UI-BURNDOWN-21 在跑 |

## G · Agent 系统换心（deepseek-harness）

| # | 仓主原话 | 对应 WO | 状态 |
|---|---|---|---|
| G1 | 「期望用它替换掉目前系统的 agent 系统，是否可行」 | WO-HARNESS-FEASIBILITY | ✅ 已出 `docs/REPORT-harness-migration-feasibility.md` |
| G2 | 「所有推演的功能都需要借鉴这个设计 UX」 | 融入 metro / 分层 / 诚实位各单 | 🟡 部分 |
| G3 | 「关于换心，是否需要做一个 POC 验证一下？」「你发我提示词，我转发给做 POC 的 dev」 | 提示词已交付仓主 | 🔶 **在仓主手上**，我不掌握进度 |
| G4 | 「你看一下 POC 测试报告」（`dshpocplan.md`） | 已阅并反馈 | ✅ |

## H · 流程与纪律（仓主对我的要求）

| # | 仓主原话 | 落地形态 |
|---|---|---|
| H1 | 「该项目你自己不断推动，形成 LOOP，无需我确认」 | 铁律 2 §0 + `scripts/dispatch-deficit.sh` |
| H2 | 「找到问题后自己建 WO 解决…**解决根源，不是打补丁**」 | 每张 WO 顶部硬要求 + 「不许为消红而放宽判据」红线 |
| H3 | 「**多 agent 并行**」「火力全开」「为何不是 7 个 agents 跑」 | 按 CPU 画像分层派（重 ≤1 · 中 2–3 · 轻不限） |
| H4 | 「为何你不即时 push？」「沙箱定期重启，需要有应对机制」 | 铁律 1 §5 每单元即 push；**本次重启验证：4/6 分支幸存、2 条丢失** |
| H5 | 「我的决策维度只有考虑是否完整，不是风险高低」 | 不用"风险高"当不做的理由；不完整就说清缺口 |
| H6 | 「**输出中文**…只需要论点和论据，不要思考过程」 | 全部报告与 WO 均中文，只给判据与实测 |
| H7 | 「完成了 PRD 就派单，无需我确认」 | 铁律 0.6 §3「不许为派活请示」 |

---

## 现在最该看的三件事

1. **⛔ 未派**：步骤模板层（B4 的前置）· 数据构建完整自检与明细表（C4/C5）· 归因树字典序入口因子（D4，重启丢失需重派）
2. **🔶 等你裁决**：删不删导航项（E2）· 两页合不合（E5）· 后两个改名（E6）
3. **正线**：`canonical` 仍停在 `c87656a1`，集成分支已积 **300+ 提交 / 500+ 文件**，四包 `build` 已 RC=0，`test` 段待重启后的单收完再起
