# LOOP10 · 使用者视角（CEO / COO）· 第 1 次发言

> 测试环境：HEAD `14984f7c` · 2026-08-29 12:13–12:58 UTC · 真后端（datacore 4311 / agentcore 4312 / vite 5311，`SEED_DEMO=1`，未用 `VITE_MOCK`）· demo/admin

## 场景表（金额×频率定核心，非功能清单）

| 场景 | 单次金额 | 年频次 | **年化敞口** | 今天支不支撑 |
|---|---|---|---|---|
| 插单（5 万套 / 3 周） | 9.6 亿（5万×19,220 元） | 24 | **230 亿** | ✗ `Order.qty` 在平台自列的「扰不动」名单里 |
| 客户改期 / 砍单 | 1.37 亿（单均） | 60 | **82 亿** | ◐ 只有 `orderChurn`，无金额、无日期 |
| 调价 −5%（某细分） | 34.4 亿（687亿×5%） | 2 | **69 亿** | ✗ 两张名单里都没有价格 / 毛利变量 |
| 物料延期 | 1.37 亿 | 24 | **33 亿** | ✗ `Material.leadTime / onHand` 扰不动 |
| 停机 2 天（常州，占簿 19.9%） | 0.75 亿 | 12 | **9 亿** | ✗ `Line.* / Equipment.*` 扰不动 |

**论点**：按金额×频率排，前四大场景（414 亿 · 98%）今天**一个都输不进去** —— 平台自己的「今天扰不动的量（20）」名单点名了 `Order.qty`（插单）、`Material.leadTime`（物料）、`Line.status`（停机），价格变量则两张名单里都不存在（调价）。唯一输得进去的是客户改期（19%），而答案是「供应风险 5496.22」这样的无量纲数。**可输入 19%，可决策 0%。**

**删 / 补**：「事情的类别」五档（需求突变…质量事件）给出的状态变量清单**逐字相同** ⇒ 承诺五种场景、只兑现一种，且垫底的产能损失才 9 亿 —— **删掉这个下拉**。「演习结论」页签禁用，那是我开会唯一要带走的一页 —— **补上**。

**只有我会说的一条**：**钱已经在库里，是推演层不肯计价。** SO-3391 在对象层带着「广汽集团 / 7,259 套 / ¥156,983,134 / 6-24 交 / 合肥+金华」；同一张单进了推演，屏上只剩 `obj_order_SO-3391` 和一个 5496.22。缺的不是数据，是**计价**。

---

## 取证附录（原始测量，不计正文字数）

### A · 金额与频次的出处（真后端实测，非拍脑袋）
`GET /a/v1/objects?type=Order` → `total=500`，每页 50 且**无游标**，故取到 50 单：
- 68.69 亿元 · 357,382 套 · **均价 19,220 元/套** · 单均 **1.374 亿元** ⇒ 全簿外推 ≈ **687 亿元**
- 基地敞口（按单值均摊）：常州 13.7 亿 = 全簿 **19.9%**；`Base` 常州 `gwh=99.4 · util=88 · bottleneck=化成柜`
- `leadDays` 中位 **20 天**；优先级 高21/中20/低9；状态 OPEN25/COMPLETED20/IN_PRODUCTION5
- 单条原文：`{cust:"广汽集团", qty:7259, value:156983134, due:"2026-06-24", unitPrice:21626, bases:["hefei","jinhua"]}`

### B · 平台自列的「今天扰不动的量（20）」（截图 `10-cannot-perturb-20.png`）
屏上原文：*「这些量只登记在对象属性上，没有进入推演世界态…放进下拉就会变成『选了、请求成功、下游一动不动』」*

> 换型损失 `ChangeoverMatrix.minutes` · 节拍 `Equipment.ctSeconds` · 设备OEE `Equipment.oee_current` · OEE-P/Q · 在制WIP `Line.capacityDaily` · 产线平衡 `Line.max_capacity_day` · 投产爬坡 `Line.status` · 良率爬坡 `Line.target_yield` · 瓶颈工序 `Line.utilization` · 设备检修 `MaintPlan.week` · BOM齐套 `Material.bomUnit` · 物料到货 `Material.leadTime` · 物料齐套 `Material.onHand` · **需求 `Order.qty`** · 在岗出勤 `Process.attendance` · 通道数 · 班次 · 利用率 · 工序良率

三个决策的落点全在这张表里。**这份坦白是本控制台最值钱的一屏** —— 它用业务语言说清了自己做不到什么。

### C · 对照实验（交互轴，不评数值）
1. **可扰状态变量 = 10，可读 = 40**。落点对象类型只有 6 种：`Model / Order / Customer / CustomerLocation / OrderPromise / OrderLine` —— **全在销售侧，生产侧零个**。
   *金丝雀*：改状态变量下拉，落点类型**确实随之改变**（10 档给出 6 种不同类型）⇒ 我的枚举方法是好的，上面的"零个"是真缺席。
2. **「事情的类别」是装饰**：五档（需求突变/供应中断/产能损失/成本冲击/质量事件）→ 状态变量清单**逐字相同的 10 个**。
3. **〔施加并推演〕今天是活的**（与上一轮 CEO 报的假按钮不符，照实报）：点击 → `POST …/perturbations` + `POST …/tick` 共 14 次网络调用；卡墙 `supplyRisk 2897.89 → 5496.22`、`shortageRisk 1307.47 → 3819.7`。
   **但它让我知道的经营事实是零**：我输入的是「SO-3391 需求 +50000」，回来的是两个无量纲数变大；`广汽集团`、`¥1.57 亿`、`6-24 交期` 一个都没出现。
4. **金额/天/套 全屏计数**：指标态势 0 / 0 / 0（金丝雀：同页「压力」命中 15 次 ⇒ 我在读真文本）。损失归因 金额 0；方案寻优有 `OrderLine.unitPrice 9,187,992`、`Base.serveCost 17,039.17`（**无币种单位**）。
5. **卡墙 40 张卡，动的只有 2 张**（被推动 2 · 未变化 38），且屏上自陈「后端截断了指标清单…`truncated=true`」。
6. **世界可信度**：状态条原文「**实测格 0/7204**」—— 起点全是结构派生占位。
7. **产销线路图**：`站 28 · 停运 16` —— 「停运区间 = 断点（算不出来）」，即端到端链路**过半算不出来**。

### D · 今天屏上有、删了我不会发现的（Q3）
- **38 条传导边开关**（`系数 0.5 · 延迟 0`）：我不会在经营会上关掉一条因果边——这是建模者的控件摆进了决策台。
- **指标态势 40 张卡里恒不动的 38 张**。
- **落点对象下拉里 873 个 `obj_orderline_SO-3391-L1` 原始 ID**：我认的是客户名和单号，不是对象 ID。
- 已禁用的「传导边册」「本体与就绪」两档。

### E · 支持它的最强证据（正面）
`方案寻优` 是唯一给出**可摆上会的取舍**的一档：帕累托前沿以 `lines.hefei/wuhan/xinyang.capacity` 为杠杆、双目标（`OrderLine.unitPrice` 最大 / `Base.serveCost` 最小）排序。但杠杆只覆盖 **3 / 13 个基地**，且优化的是"产能该设多少"，不是"产线停了 2 天会怎样"——**它是规划器，不是推演器**。

### F · 截图
`docs/assets/loop10-user/` · `02` 落地 · `04` 插单填好 · `05` 施加后 · `07` 损失归因 · `08` 方案寻优 · `09` 产销线路图 · `10` 扰不动的 20 项
