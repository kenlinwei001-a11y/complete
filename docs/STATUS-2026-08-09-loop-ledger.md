# 进度实测账 · 2026-08-09

> 回答仓主四问：**有开发完未复验的代码吗 / 4209 行需求完成多少 / 进行中多少 / 未生成 WO 多少 / 生成了 WO 未派发多少。**
> 基线 `claude/inspiring-gates-aqczjg` @ `69804185`（`git status --porcelain` 空，`git ls-remote` sha 与 `HEAD` 一致）。
> **本文每个数字都是本次亲手跑出来的**，不是从旧报告抄的；抄旧数正是本仓 2026-08-08 连犯 5 次的那个病。

---

## 0 · 先自证工具（否则下面的数字一律不许当结论）

照铁律 0.6：**扫描类结论必须先跑「已知必中」的金丝雀**。本次三件工具、三组金丝雀：

| 工具 | 金丝雀 | 结果 |
|---|---|---|
| 分支「已并入」判据 = `git merge-base --is-ancestor` | canonical 自己必须判为已并 | ✅ |
| 文件存在判据 = `git cat-file -e <rev>:<path>` | `package.json` on canonical 必须 PRESENT | ✅ |
| 判定表提取器（四份 CHECK 文档） | 13 条已知行的档位必须逐条对上 | ✅ 13/13 |

**提取器写到第三版才对**，前两版都被金丝雀当场抓翻 —— 这两次是本文最有价值的部分：

| 版本 | bug | 若信了会报出 |
|---|---|---|
| v1 | 直接 `grep '\| ✅'` 数全文符号 | SPEC-AUT 的 ⚠️ 报 **0**（真值 13）—— 度量的是「全文出现过的符号」，不是「逐条判定列」 |
| v2 | ID 正则强制要连字符 + 判定列写死第 3 列 | `D009` 这类 ID **全漏**；`⛔本期/❌` 这类复合判定**全落空**（558 行未定档） |
| v2.1 | 裸 `split("\|")` 不认 markdown 的 `\|` 转义 | 含转义管道的行**整列错位**，`RT-020` 抽成 null |
| v3 | 按**表头**定位「档」列 + 认转义 + 复合判定取首符号 | 13/13 金丝雀过 |

> **形态**（照铁律 0.6 的句式）：v1 是「我用『全文符号出现次数』当作『逐条判定分布』的证据，而前者并不度量后者」。
> 这与 2026-08-08 那 5 次是**同一个形态**。区别只在于：这次是机器先说话，不是人先想起来 —— 机制起作用了。

---

## 1 · 有开发完未复验的代码吗？ → **有，2 条带真代码 + 8 条纯文档**

判据不是「分支是不是 canonical 的祖先」（cherry-pick 之后祖先关系必然不成立，会把已并的误报成未并），
而是**逐文件比对内容**：ABSENT（canonical 上没有）/ DIFFER（有但不同）/ SAME（一致）。

### 1.1 带真代码、确实未并（🔴 需要我复验）

| 分支 | 提交 | 行 | ABSENT | 具体缺什么 |
|---|---|---|---|---|
| `handoff-skill-compiler-s1` | 4 | 1513 | **4** | `apps/agentcore/src/skill-compiler.ts` · `packages/contracts/src/skill-compile.ts` + 两份测试 |
| `handoff-skill-partial-a` | 3 | 381 | **1** | `apps/agentcore/test/skill-partial-a-seam.test.ts` + 6 文件 DIFFER |

两条都卡在**冲突**（都改 `server.ts` 与 `contracts/index.ts`）。这就是 §6.2 S0 单里预告过的
「五条之间很可能互相冲突，必须串行并」。已并 3/5（orchestrator-s1 · partial-b · refclosure-a），**剩这 2 条**。

### 1.2 老分支 `verify-skill3`（2026-08-02）

2 个文件 ABSENT（`zz-adversary-adopt.test.ts` · `zz-adversary-golden-probe.test.ts`）+ 16 DIFFER。
**与欠账 #82 相关**（对抗测试坐实了 `affected_orders` 与 `risk_timeline` 报不同 peak/crossDay，现挂 `it.skip`）。
DIFFER 16 个文件说明它落后很多，**不能直接并**，要么重做要么只摘那两个对抗测试。定性：**待处置，不是待并**。

### 1.3 纯文档、未并（🟡 无代码风险，但内容有价值）

| 分支 | 缺的文档 |
|---|---|
| `handoff-prd-coverage-full` | `AUDIT-prd-coverage-full-2026-08-09.md`（575 行） |
| `handoff-skill-migration-scope` | `WO-SUITE-skill-migration.md`（507 行） |
| `handoff-skill-agent-reconcile` | `RECONCILE-skill-agent-prd-2026-08-09.md`（689 行） |
| `handoff-sandbox-metro-prd` | `PRD-sandbox-metro-ops.md`（616 行·对应欠账 #147 地铁大屏） |
| `handoff-sandbox-field-inventory` | `INVENTORY-sandbox-backend-fields.md` |
| `handoff-sandbox-a6-audit` | `AUDIT-sandbox-a6-businesstypes.md`（欠账 #146） |
| `handoff-sandbox-a10-audit` | `AUDIT-sandbox-events-a10.md`（欠账 #145） |
| `handoff-wo-sandbox-a10` | 5 份 AUDIT/PRD |

### 1.4 一处**我原先记错了、现予订正**

欠账 #148 记「12 提交 / 3397 行零远端分支」。**实测：那 12 个提交的内容今天已在 canonical 上**
（`check-chain-scan-honesty.mjs` PRESENT · `SolutionCandidate` 3 文件 · `sim.scenario_saved` 4 文件，
金丝雀 `SimSession` 17 文件证明 grep 有效）。四条分支之所以显示「未并」，是因为**内容被 cherry-pick 而非 merge**，
祖先关系不成立而已。**真正未并的只是各分支顶上那 1–3 个审计文档提交。**

> 这本身就是判据 #5 的又一个实例：**「分支不是祖先」不度量「内容没进主线」**。

---

## 2 · 4209 行需求完成多少？ → **实提 1671 条，真满足 34.3%**

四条复验路线逐条勾选，**无一条跳过**（提取口径是超集：一张表的一行 / 一个 YAML 键 / 一条不变量各计一条）。

| 文档 | 条目 | ✅ | 🔗 | ⚠️/◐ | ❌ | ⛔ | 复合档 | 未定档 |
|---|---|---|---|---|---|---|---|---|
| SPEC-AUT | 289 | 65 | 79 | 0 | 88 | 29 | 2 | 28 |
| DSL-CMP | 469 | 123 | 61 | 5 | 211 | 55 | 26 | 14 |
| RT-GOV | 644 | 147 | 33 | 17 | 309 | 125 | 9 | 13 |
| MIG-XR | 269 | 105 | 17 | 1 | 20 | 126 | 115 | 0 |
| **合计** | **1671** | **440** | **190** | **23** | **628** | **335** | 152 | 55 |

**有效判定基数 = 1671 − ⛔335 − 未定档55 = 1281**

| 档 | 数 | 占比 | 含义 |
|---|---|---|---|
| ✅ **真满足** | 440 | **34.3%** | 承载物在该在的对象上 + 有 `src/` 生产调用方 + 已追到触发条件 |
| 🔗 接线不全 | 190 | 14.8% | 有实现，但挂错地方 / 数据恒空 / 半条成立 |
| ⚠️/◐ 只有 test 引用或部分 | 23 | 1.8% | **已排练，不是已实现** |
| ❌ 无承载物 | 628 | **49.0%** | 真没做 |

**55 条未定档不是漏勾**，是四份文档末尾的**缺口清单表**（F1–F14 · X1 · X-01–X-12 · N-01/N-02 · E1–E3 · F-1–F-10 = 42 条）
加 SPEC-AUT 里 13 条判定列缺失的行。缺口清单表本身就是产出，不是需求条目。

### 2.1 三条必须说清的口径

1. **⛔335 条不是「没做」**，是 §0.25 定的非目标三分：绝对不做 / 本期不做 / 不改不新造。
   其中「不改不新造」是**反向断言 —— 做了反而是缺陷**。把它们算进分母会低估完成度，算进分子会虚报。
2. **✅440 混了三种东西**：真交付的、盘点正确的、以及「文档写对了」。
   四条路线自己都警告过这点。举例：GOV 320 条里 13 条 ✅ **全部是既有资产**，
   **P0–P4 五期的交付量是 0**。所以按域看比按总数看诚实：
   - DSL 215 条 → 实现层真成立 **28 条（13.0%）**
   - CMP 240 条 → canonical 上真成立 **34 条（14.2%）**
   - GOV P0–P4 → **0**
3. **26 条「宣称做了其实没做」是最毒的一类**（F1–F14 + X-01–X-12）。
   它们不在 ❌ 里 —— 因为文档正面陈述已生效，读的人不会去查。其中 **5 条是反向过期**
   （文档说「这做不了」，实际已做完），照它排期会**重复造一道已存在的门**。
   ⇒ 已派 `WO-DOCFIX-SKILL-CLAIMS` 逐条回写。

---

## 3 · 进行中多少？ → **6 张 WO 已闭 + 1 张在途（S0 · 3/5）**

按 canonical 上的实测 commit（不是按我的记忆）：

| WO | 内容 | 落地 commit | 关闭的欠账 |
|---|---|---|---|
| **P0** | 扰动升格一等公民（契约+表+路由+前端入口+`/act` pending 修） | `56d273b2` `30357b11` `8ab0cd3a` → `ee40d3ba` | #150 #151 · REQ060 |
| **P1** | 传导边 3 → 13（六方向） | `50c17468` | #158 · REQ143 |
| **P2** | `propagateTick` 接扰动 + 到期回退 + 溯源 | `f3b605aa` | — |
| **Q0** | 业务流程层 13 域 × 65 流程 | `a7050bb7` | — |
| **S0.5** | solver 类 precondition 被静默丢弃 + 不可执行声明 lint 门 | `ed83c700` `45493bdf` `ba968908` | #154 |
| **L4-b** | `sim.*` 事件补真订阅方（1/5 → 4/5） | `0c07f804` `1bf4f5fc` | #145（部分） |

**S0（收编五条 Skill 分支）在途：3/5 已并**（`23e01e45`），剩 `compiler-s1` + `partial-a` 卡冲突 —— 即 §1.1。
另有 `WO-LEVER-FIX` 返回零改动，**这是正确的**（我把 dist 过期误诊成源码缺陷，dev 没有跟着我错）。

**本轮新派 5 张**（本文写作同时派出）：`WO-SIM-SCOPE-TRIAL`(P3+L3-a) · `WO-REFGATE-ENT`(N-01+X-09+F14) ·
`WO-D6` · `WO-L7A` · `WO-DOCFIX-SKILL-CLAIMS`。画像分层 3 中 + 2 轻，符合 4 核机的并发红线。

---

## 4 · 未生成 WO 多少 / 生成了未派发多少？

### 4.1 🔴 先订正我自己 PRD 里的一个错

`PRD-UPGRADE-decision-sandbox-v2.md` §6.2.1 写「**全量 WO 共 30 张**（P×4 · Q×3 · S×6 · L×14 · D×3）」。
**逐条数：L 是 15 不是 14，D 是 5 不是 3，总数 33 不是 30。**

| 族 | 逐条 | 数 |
|---|---|---|
| P | P0 P1 P2 P3 | 4 |
| Q | Q0 Q1 Q2 | 3 |
| S | S0 S0.5 S1 S2 S3 S4 | 6 |
| L | L1-a/b/c · L3-a/b · L4-a/b/c/d/e/f · L6-a/b · L7-a/b | **15** |
| D | D0 D1 D3 D5 D6（D2 已撤单 · D4 并入 Q2） | **5** |
| **合计** | | **33** |

**形态**：「我用『分族时脑子里的那个数』当作『逐条数出来的数』的证据」。同族第 N 次，照 0.6 记账。

### 4.2 账

| 类 | 数 | 明细 |
|---|---|---|
| **已生成 WO** | **33** | 全部写在 PRD §6.2 / §6.2.1 / §6.3 |
| ├ 已派发且已闭 | 6 | P0 P1 P2 Q0 S0.5 L4-b |
| ├ 在途 | 1 | S0（3/5） |
| ├ **本轮新派** | **5** | P3+L3-a 合单 · N-01/X-09/F14 合单 · D6 · L7-a · DOCFIX |
| └ **已生成·未派发** | **22** | Q1 Q2 · S1 S2 S3 S4 · L1-a/b/c · L4-a/c/d/e/f · L6-a/b · L7-b · D0 D1 D3 D5 |
| **未生成 WO** | 见下 | |

### 4.3 未生成 WO 的部分（**这才是真缺口**）

33 张 WO 覆盖的是**沙盘**需求线。**4209 行 Skill 需求的 628 条 ❌ + 190 条 🔗，
今天只有 S 族 6 张 WO 触及** —— 也就是说：

> **Skill 侧约 818 条未满足的需求里，绝大多数没有任何 WO 承载。**

具体地，四条路线新发现、**PRD 写作时还不存在、因而不可能被 33 张 WO 覆盖**的缺口：

| 编号 | 缺口 | 处置 |
|---|---|---|
| N-01 | 引用门对 rule 只查「存在」不查「状态」⇒ 可引用 DRAFT 规则 | **本轮已派**（WO-REFGATE-ENT） |
| X-09 / N-02 | `POST /b/v1/skills/:id/compile` 无 entitlement 门（违反 R3） | **本轮已派**（同上） |
| F14 | 出厂 Skill 走旁门直插仓储，**一次没过发布门** | **本轮已派**（同上） |
| F1/F2/F3 | 三个门名在 `package.json` 与 `scripts/` **0 命中**（`skill-lint:check` / `skill-eval:check` / `ontology-writeback:check` 未上链） | **未生成 WO** |
| F4 | `maxBudgetRounds` 零消费方零数据 · `resources` 7/7 空 | 部分在 partial-a（未并） |
| F5/F6/F7 | `skill_quality` 用例 0 条 · 出厂范例 Skill 不存在 ⇒ **SA2/SA3 两条验收的被测对象不存在** | **未生成 WO** |
| X1 | summary 长度三处口径不一（PRD 200 / 契约 400 / 前端 400）| **待仓主裁决**，dev 不许自裁 |
| #159 / #162 | `Skill.execution` 名不副实 · `execution.mode` 声明红线却零读点 | **未生成 WO** |
| #163 | GOV P0 两项硬前置都没闭，且 PRD 漏写一个坑：只改 handler 无效 | **未生成 WO** |
| GOV P0–P4 | 五期交付量 **0** | **未生成 WO**（量最大的一块） |

---

## 5 · 一句话结论

- **未复验代码**：2 条带真代码（`compiler-s1` 1513 行 · `partial-a` 381 行，都卡冲突）+ 1 条待处置（`verify-skill3`）+ 8 条纯文档。
- **4209 行**：实提 1671 条，扣掉 335 条非目标与 55 条清单行，**真满足 440/1281 = 34.3%**，
  **真没做 628 = 49.0%**；但按域看更诚实 —— **DSL 13.0% · CMP 14.2% · GOV P0–P4 = 0**。
- **进行中**：6 张已闭 + 1 张在途 + 本轮新派 5 张。
- **已生成未派发 22 张**；**未生成 WO 的是 Skill 侧那 818 条**（尤其 GOV 五期，交付量为 0）。

> 全文最该记住的一句：**这次是机器先说话** —— 判定表提取器被自己的金丝雀连翻两次，
> 才没有把「SPEC-AUT 的 ⚠️ 是 0」和「RT-020 未定档」这两个假数字报给你。
