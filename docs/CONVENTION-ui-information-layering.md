# UI 信息分层规范（决策页第一层只放结论）

> **来历**：2026-08-10 仓主逐页实测后给出的通例判断 ——
> 「**太多信息在第一层，密密麻麻，无法看到重点指标信息**」。
> 已具名的现场：推演沙盘主屏、产能推演「可用产能派生诊断（自下而上 6 层）」、
> Skill 库 / Agent 注册表 / 意图目录（窄列 + 单层清单）。
>
> 本规范的目的是**让这件事不必逐页重新争论**。新页面按此做，旧页面渐进对齐。

---

## 0 · 一句话

**第一层回答「怎么样」，第二层回答「具体是什么」，浮层回答「凭什么」。**

把三者混在同一层，用户就得自己做视觉降噪——而决策者恰恰是最没耐心做这件事的人。

---

## 1 · 三层的准入清单（超出即降层）

| 层 | 交互成本 | **只允许放** | 明确不许放 |
|---|---|---|---|
| **第一层** | 零（不点就看见） | ① **数值本身** ② **它的状态**（好 / 警 / 危） ③ **它是什么的一个名字** | 公式 · 口径说明 · 逐项明细 · 参数 · 诊断信息 · 调试信息 |
| **第二层** | 一次点击 | 明细表 · 逐项拆解 · 可操作项 · 对比 · 下钻 | —— |
| **浮层** | 悬停 / 聚焦 `?` | 口径 · 公式 · 为什么这么算 · 诚实位说明 · 数据来源 | 任何**结论性数字**（数字属于第一层，不许只藏在浮层里） |

**诚实位的特殊规定**：诚实位（「引擎未取到数」「未对到因子」「合成数据」「未按该因子细分」）
**允许**降到浮层，**绝不允许删除**。降层时第一层必须留一个可见的记号（角标 / 变色 / 图标），
让人知道「这里有话要说」——**静默降层等于删除**。

---

## 2 · 四条可核对的硬判据

写页面时自查，复审时按此退单。

> ⚠ **2026-08-17 从三条加到四条**（WO-SCREEN-PLAINSPEAK）。原来三条管的都是「**同一句话放在哪一层**」，
> 一句都没管「**这句话本身该不该给用户看**」—— 于是一段印着 `curl` 和 `X-Debug-User` 的文字，
> 只要它待在浮层里，前三条判据**逐条都过**。R-UI-4 补的就是这个洞。

### R-UI-1 · 视线距离
**一个数值与解释它是什么的标签，视线距离 ≤ 一张卡的宽度。**

反例（2026-08-10 实测）：产能推演「6 层派生诊断」每层占满 ~2000px，
左端层名、右端数字、中间 1800px 是公式与空白 —— 眼睛要横跨整屏才能把
「第 3 层 产线产能」和「张力 96/100」连起来。

判据：**若要把标签和数值同时读进眼里必须移动视线超过一张卡宽，就该改成卡片。**

### R-UI-2 · 字号层级 ≤ 3 级，最大一级只给本页的那个数
一屏之内最多三级字号（**主数值 / 标题与标签 / 辅助说明**）。
**最大的那一级，只给「这一页要回答的那个数」**，其余一律降级。

反例：6 层里 `可用产能数 18,819 套` 与 `节拍×OEE×通道×班次`
视觉权重相同 —— 一个是结论，一个是口径，不该平起平坐。

### R-UI-3 · 公式与口径不在第一层
凡形如 `A × B ÷ C`、`min(...)`、`∩ ...`、「口径差」「联动口径」「按 X 显示不按 Y 措辞」
的文字，**一律进 `?` 浮层**。它们是**解释**，不是**结论**。

浮层规格（统一，别每页各做一套）：
- 触发：`?` / `ⓘ` icon，**悬停显示 · 移开立即消失**
- 键盘可达：focus 显示 / blur 消失 / `Esc` 关闭
- `max-width` 约 380px，超长内可滚动
- **禁止用 HTML `title` 属性或 SVG `<title>` 元素**充当浮层 ——
  那是浏览器原生 tooltip：由操作系统绘制、不受组件控制、**永远画在最上层**、移开后会滞留。
  2026-08-10 实测事故：`ChainLineMapView.tsx` 的两个 SVG `<title>`
  在环形图上滞留并遮挡图形本身。

### R-UI-4 · 开发的话不上屏（**任何一层都不许**）

**判据一句话：「这句话用户读了能做什么决定？」答不出 ⇒ 它不该在屏上 —— 浮层也不行。**

> **来历**：2026-08-17 仓主指着「流程等待态」页问
> **「这些功能描述型的文字之前也要求你调整，为何没有调整」**。
> 那一句话里同时印着：`curl` 命令 · `X-Debug-User` 调试头 · `127.0.0.1:4001` 本机端口 ·
> `jq` 表达式 · `docs/xxx.md` 仓库路径 · `battery/S/seed=42` 种子口径，
> 外加一串**没被渲染的 markdown 星号**。

⚠ **R-UI-4 与前三条的关系是正交，不是细化。** R-UI-1/2/3 全在回答「放哪一层」，
所以它们对上面那句话**逐条都放行** —— 它待在浮层里，位置完全合规。
**位置对了不等于内容该给用户看。**

**十三类形态**（每一条都取自真印在屏上的字，不是想象；机械化实现见 §6）：

| 形态 | 例（实测原文） | 为什么不该上屏 |
|---|---|---|
| shell 命令 | `curl -s -H …` · `jq '{available}'` · `pnpm …` | 决策者不会开终端 |
| 本机 URL / 端口 | `127.0.0.1:4001` · `localhost` | 用户浏览器里根本不存在这个地址，印上去只会误导 |
| 调试请求头 | `X-Debug-User: demo:admin:admin` | 生产走 JWT；印它等于叫用户自己拼 header |
| 仓库文件路径 | `docs/WO-FLOWTIME-feasibility.md` | 用户没有这个仓库 |
| 接口路径 | `/a/v1/…` · `/b/v1/…` | 读了做不出任何决定 |
| 种子 / 画像口径 | `seed=42` · `battery/S` | 内部复现参数，不是业务事实 |
| 部署期环境变量名 | `OPTIMIZER_BASE_URL` · `SEED_DEMO` | 运维配置，用户改不了也不该知道 |
| 内部常量 / 注册表名 | `SEG_REGISTRY` · `SOLVER_FIELD_ROLES` | 只存在于源码里的容器名 |
| 契约类型名 | `ChainImpedimentSchema` | 用户不需要知道契约长什么样 |
| zod / 实现细节标识符 | `strictObject` · `useQuery` · `msw` | 同上 |
| 工单编号 · PRD 区号 | `WO-FLOWTIME` · `区4` | 内部排期与章节编号 |
| 内部机制名 | 「三页归一」「厂商中立施工」 | 开发内部叫法 |
| **未渲染的 markdown 星号** | 屏上原样印着 `**标准工期**` | 见下 —— 这一条性质不同 |

**⚠ 最后一条不是「用错了词」，是「渲染方式不对」。** 这些文案**按 markdown 写、按纯文本渲染**，
所以 `**` 原样印在屏上。两个病叠在一起：难看 + **作者以为自己做了强调，读者看到的是乱码**。
**全仓统一取一种修法**（不许一半一半）：**去掉星号，要强调就用真正的 DOM 强调**（`<b>` / `<strong>`），
**不引行内 markdown 渲染器** —— 本仓不许无理由加依赖，而那层依赖换不来别的东西。

#### R-UI-4 与 §1「诚实位绝不允许删除」怎么并存

这是本条最容易做错的地方：**为了让屏上变干净，把诚实边界整段删掉** —— 那是拿一条红线换另一条。

**分开处理**，一句话里通常两样东西都有：

| 成分 | 例 | 去处 |
|---|---|---|
| **结论**（用户读了能做决定） | 「65 条流程里今天只有 9 条反推得出停留时长，其余 56 条会逐条告诉你缺哪种单据 —— **不会拿 0 冒充「没卡」**」 | **留在原层**，只把话换成人话 |
| **复验配方**（给工程师的） | `curl` 探针 · 端点 · 仓库路径 · 种子口径 | **移进代码注释** |
| **保质期标记** | 「2026-08-13 实测」 | **压成小字角标，不是删** —— 删了 `stale-claims:check` 会反过来咬红 |

⛔ **复验配方不许挪进 `title=` 悬浮**（2026-08-17 实测的坑）：
`title` 也是**屏上的字**，§6 那道门的文案型属性表正咬 `title` ——
挪进去只是把同一句开发话**换个地方印**，门照红，用户照样看得见。
真正的去处是**代码注释**（注释不进 AST，机器和用户都不会把它当屏上文案）。

---

## 3 · 结构本身也要表达（不只是密度问题）

信息分层不等于「把东西藏起来」。**若数据本身有结构，第一层要把结构画出来。**

反例：那 6 层是一条**自下而上的推导链**（设备 → 工序 → 产线 → 可投 → 预测 → 缺口），
但行式布局把它画成六个并列条目 —— **链的方向和递进关系一点都没表达**。
改成卡片时若只是把六行变六格而不体现递进，等于没改。

判据：**画完之后，不看文字能不能看出「谁推出谁」？** 看不出就没做完。

---

## 4 · 适用与豁免

- **适用**：一切决策页与管理页的第一屏。
- **豁免**：明细表页（用户来这里就是为了看全量行）、日志/审计视图。豁免要在代码注释里写明理由。
- **渐进**：不要求一次重排全部旧页面。**新页面天然遵循；改动到的旧页面顺手对齐。**

### 4.1 豁免 · **结构性口径**（2026-08-14 判例，仓主当场推翻原判定）

> **判据**：当一段口径/公式片段是某一层、某一步、某一行的**唯一身份标识** ——
> 即**去掉它，两个不同的东西在屏上无法分辨** —— 它是**名字**（§1 第一层准入清单第 ③ 条），
> 不是**解释**（R-UI-3），**允许留在第一层**。此时完整口径仍须在 `?` 浮层里给全。

**判例来历**：产能推演页「可用产能派生诊断（自下而上 6 层）」。
按 R-UI-3 原判定，各层的推导式（`views/capacity/factorOntology.ts` 的 `ONTO_LAYERS[].role`）
被降进 `?` 浮层，并由 `test/capacity-card-layout.test.tsx` 断言「第一层零公式」。
仓主逐页实测后原话：

> 「**看不懂**『产能推演』的这个 UX，你希望用户看到这个做什么？或你认为客户看到这个希望了解什么？」

复核结论：**模型是对的，屏上没把它讲出来。** 具体地——
- 第 3 层（`min(瓶颈工序) · WIP · 平衡`）与第 4 层（`∩ 物料齐套 · 到货约束`）
  在屏上**逐字相同**：都是「张力 95/100 · ◆危」。模型里差得很清楚，屏上分不出。
- 这一页要回答的是「**该去修哪一层**」（修 OEE？修良率？还是催料？），
  而「这层在算什么」正是那个问题的唯一答案。

⇒ 原判定被推翻，本节豁免入册；测试断言同步反转（**反转本身要在测试注释里记账，不许静默改绿**）。

**这条豁免不是口子，它有两道闸**：
1. **必须真的无法分辨**才用。「显示出来更方便」不构成豁免 —— 判据是「去掉之后两个东西是否逐字相同」。
2. **浮层里必须仍有完整口径**。豁免的是「不许出现在第一层」，不是「可以不给全」。

⚠ **门看不见这一条，别把「门没红」读成「门批准了」**：
`scripts/check-ui-first-layer.mjs` 的 `formula` 判据只扫**字符串字面量**，
而 `role` 是经 `ONTO_LAYERS[].role` 取来的**数据引用** —— 同样的文字上屏，门测出来仍是 `formula=0`。
真正守这条的是 `capacity-card-layout.test.tsx` 里那条**可见性**断言，不是门。
（本仓 §0.6 句式：「我用『门没报』当作『合规』的证据，而门根本看不见这个形态。」）

### 4.2 诚实位可以**埋得太低** —— 那要**升层**，方向与降层相反

§1 的诚实位规定只说了一个方向（「允许降到浮层，绝不允许删除」），
实测发现它答不了另一个方向：**诚实位放在了比它该在的位置更深处**。

**判例**（同上，2026-08-14）：「物料齐套 = 张力95/100（**合成·未接实测**）」这句
原先只出现在**第 4 层展开明细**里。而第 4 层正是主瓶颈那一层 ——
这句话真正的含义是「**整条链的结论都建立在合成数据上**」，
它不是第 4 层的脚注，是**整页的前提**。等用户点开明细才发现，等于没说。

**判据**：问一句「**这条诚实位若为真，用户会不会重新解读第一层的那个结论？**」
- 会 ⇒ 它属于第一层，**不管它现在在哪一层**。
- 不会 ⇒ 按 §1 降到浮层，第一层留记号。

**配套**：数据出身标注（实测 / 合成 / 估算）应当**恒显**，不是「只在坏的时候才出标注」——
只在坏的时候标，等于逼用户从「没标注」里自己推断，那不算诚实位。

---

## 5 · 复审判据（审核方按此退单）

1. 第一屏截图，逐个元素问：**它属于三层里的哪一层？** 有一个放错层即退。
2. **数一数第一屏有几个信息块。** 说不出「这一页要回答的那个数是哪一个」即退。
3. R-UI-1 / R-UI-2 / R-UI-3 逐条过。
4. 诚实位：降层了吗？降层后第一层还有记号吗？**删掉了即退，无条件。**
5. 结构：不看文字能看出递进关系吗？
6. **R-UI-4 逐句过：这句话用户读了能做什么决定？** 答不出即退 ——
   **浮层里的也要问**（R-UI-4 与前五条正交，位置对了不代表内容该给用户看）。
   ⚠ 复审时**不许拿源码 grep 自证**：文案多经 `locales/` 变量间接上屏，
   源码里没写 `curl` **不代表**屏上没印出 `curl`。判据要落在**渲染结果**上
   （`screen.getByTestId(…).textContent`），或直接看 §6 那道门的 `--list`。

---

## 6 · 本体引用与影响

- **对象类型**：不新增。本规范约束的是既有「视图（View）」这一类制品的呈现层。
- **链路**：不改。不涉及求解器 / 意图 / 计划的接线。
- **不变量**：与 **R14（应用层无业务常数）** 协同 —— 浮层文案一律走 `locales/`，不内联；
  与 **R17（决策单页）** 同向 —— R17 要求「一页看全数据→推演→溯源→动作→AI」，
  本规范补充的是**这一页内部怎么分层**，两者不冲突：R17 管「不跳页」，本规范管「不糊成一片」。
- **门禁**：⚠ **原文「暂未机械化」已过期**（本节 2026-08-14 回写；留着过期措辞比没有更糟——
  它写在最容易被引用的地方）。现状分三档，**不许合并读**：

  | 判据 | 谁在守 | 状态 |
  |---|---|---|
  | §1 三层准入的**可数部分**、§2 R-UI-2 字号 ≤3、R-UI-3 口径公式不在第一层、「绝不允许删除」 | `ui-first-layer:check`（`scripts/check-ui-first-layer.mjs`，`pnpm ui-first-layer:check`） | **已机械化** · 棘轮 |
  | §2 **R-UI-4 开发的话不上屏**（十三类形态） | `dev-jargon:check`（`scripts/check-dev-jargon-onscreen.mjs`） | **已机械化** · 棘轮（见下「R-UI-4 的门」） |
  | §2 **R-UI-1 视线距离** | 无门（**判不了，也不假装能判**：那是渲染后的几何量，源码里不存在这个量；硬编「full-width flex」之类的代理指标度量的是**写法**不是**视线距离**） | **仍由 §5 人工复审** |
  | 「降下去的东西还在、还调得出来、第一层还留着记号」 | 接缝测试（真 hover，非查 DOM） | **静态门证明不了**，只有运行时测得到 |

  门的**七条判据**：D1 第一层信息块数棘轮（**§3 结构化改造豁免**：first↑ 仅在 deferred 同时↑ 时放行）·
  D2 第一层口径/公式（R-UI-3）· D2b 第一层长说明串（§1）· D3 字号层级数（R-UI-2，新文件硬上限 3）·
  D4 **单文件守恒**（反「删内容冒充分层」）· D5 **全局守恒** · D6 **未登记文件也进棘轮** · D7 **棘轮松弛反向遍历**。

  ⚠ **D5/D6/D7 的来历（2026-08-14 · WO-RATCHET-NEWFILE-BLINDSPOT，合成沙盘实测复现，不是推想）**：
  D1–D4 全都以「基线里登记的**那个文件**」为度量单位，**而文件不是内容的归属单位** ——
  搬一次家归属就变了。两个盲区各自复现过：
  ① 内容整体搬进**未登记的新文件**后，那个文件从 20 块堆到 120 块，门 RC=0、零红
     （搬家**那一步** D4 其实咬到了；要害在**搬完之后永久免检**）；
  ② 新文件里一整块 `InfoPopover` **删光、不留可见记号**（deferred 8→0），门同样零红 ——
     **这直接违反本规范 §1 的「绝不允许删除」红线，而门一声不吭。**
  形态：**「我用『基线里那一条的数字没涨』当作『屏上第一层没变多』的证据，而前者并不度量后者。」**
  修法**不是**把那个新文件加进基线（只堵一个，下一个照旧免检），而是咬**规则**：
  D5 把守恒对象上提到「扫描面全体这个**内容集合**」（搬家总量不变 ⇒ 绿，删除 ⇒ 红）·
  D6 新文件**未落账即红**（免检期从「永远」缩到「第一次进仓那一次提交」，落账后受同一套棘轮 + 硬上限）·
  D7 反向遍历基线（基线**高于**实测 = 免检名额，正向遍历发现不了）。

  **门的诚实边界（写规范的人尤其要知道，别把「门绿了」读成「这一页合规」）**：
  · 棘轮只答「**有没有变差**」，永远不答「**现在有多差**」—— 后者看门每次打印的**存量榜**；
  · D5 守的是「总量不掉」⇒ **「这里删、那里补」净额为零就是绿**：拦得住只删不补，拦不住边删边补；
  · `formula` 判据只扫**字符串字面量**，经变量/`locales/` 间接上屏的口径**门看不见**
    —— **那是门看不见，不是它同意**；这条只有接缝测试守得住；
  · `first` 是**静态模板**计数（`.map()` 行模板计一次），不等于屏上最终块数；
  · 「第二层」靠披露式条件变量名与浮层组件名判定，别的命名法写的折叠会被误判成第一层（**偏保守，宁可多报**）。
- **断点**：本规范落地前的存量页面统一记为 `G-UI-FIRSTLAYER-OVERLOAD`（信息第一层过载）。
