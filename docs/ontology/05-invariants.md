# 本体切片 §5 · 系统不变量（规则 = 改动不可违反的铁律）

<!-- 自动生成·勿手改 -->
> ⚠ **本文件由 `scripts/build-ontology-slices.mjs` 从母体 `docs/SYSTEM-ONTOLOGY.md §5` 派生**（本体克隆切片·层 2）。
> **改接线改母体 §5，再跑 `node scripts/build-ontology-slices.mjs` 同步**（勿直接改本文·门 `ontology-slices:check` 守漂移）。母体 hash `22ab249e415e16c3`。

---

## 5. 系统不变量（规则 = 改动不可违反的铁律）

> 来源：根 `CLAUDE.md` + 闭包/审核。违反即返工。

| # | 不变量 | 检测点 |
|---|---|---|
| R1 | **contracts-only-shared**：跨包只依赖 `@platform/contracts`；前端不重定义契约类型 | 构建/评审 |
| R2 | **tenant_id everywhere**：所有读写/事件/缓存键带 tenantId；跨租户 403/404 | 仓储层 |
| R3 | **entitlement 先于 authz**：功能关 = 不存在 → 404 `FEATURE_NOT_FOUND` | `features.ts`/`gate.ts` |
| R4 | **真值写入经 Action 审批**：对象物化/本体变更经 `domainExecutor`（Phase9B），EXECUTED 才落。**职责分离为默认策略、可配置留痕例外（SA，有意放宽）**：发起人自批默认硬阻断（STRICT=现行为）；按租户策略 `selfApprovePolicy`（env `SELF_APPROVE_POLICY` 覆盖；demo 默认 ALLOW_ADMIN）或类型 `ActionType.selfApproveAllowed` 可放行发起人自审，但**必显式留痕 `ApprovalStep.selfApproved=true`**（R13 透明可审计，杜绝悄绕）——解锁单 admin/演示租户下 provisional→governed / SOP 定稿 / gap-fill 收尾等 R4 收尾闭环。**WO-ACTUATE 出站口径（G-14）**：EXECUTED 后经**可插拔写回适配器**出站（`ActionExecutor`=适配器接口·config `WRITEBACK_TARGET` 选 mock/erp_rest）——MockWritebackAdapter 自动落 writeback-echo→reconcile（ECHO_SUPPRESSED/writeback.divergence 两路）·ErpRestWritebackAdapter 未配端点诚实 `WRITEBACK_NOT_CONFIGURED`（不假装写成功 R13）；Action 记 `writebackTarget` + 前端徽标诚实标「MOCK 非真 ERP」（R13）；沙盘采纳模拟态不直写（RL4），经本适配器走正门 | `app.ts:290` · `actions.ts`（`tenantSelfApprovePolicy`/`selfApproveAllowedFor`·`ActionExecutor`）· `writeback.ts`（Mock/ErpRest 适配器） |
| R5 | **no-secrets-echo**：凭据 AES-GCM 落库，响应仅 credentialRef | 连接器/LLM/MCP |
| R6 | **确定性**：同 (industry,scale,seed) 字节级一致；求解器同输入同输出；测试不依赖网络/时钟/随机；LLM mock | 合成/求解器/构建 freezePlan |
| R7 | **错误信封统一** `{error:{code,message,requestId}}` | 两系统 |
| R8 | **认证**：生产 Bearer JWT（A 签发，B 经 JWKS 验签）；开发 `X-Debug-User`；服务间 `SERVICE_TOKEN`。**深链续期（WO-11.4）**：前端 access token 仅驻内存，refresh token 走 httpOnly cookie（Path=/a/v1/auth）；F5 刷新任意深链时 `ShellLayout` 启动守卫先 `silentRefresh()`（POST /a/v1/auth/refresh，单飞与 401 重试共享）再判跳登录——续期成功 setState 触发重渲染使 `useWorkspace`（enabled=内存有 token）真拉取，续期失败才跳 /login，消除"刷新深链必掉登录、丢所在位置"。 | `auth.ts` · `apiClient.silentRefresh` · `ShellLayout` |
| R9 | **仓储双实现**：memory(测试)+pg(DATABASE_URL)；新表四处同改(migrations+pg+memory+repo接口) | `repo/` |
| R10 | **D-29 数据流闭环**：产出操作必发事件、下游必订阅（§4） | `event-subscriptions.ts` |
| **R11** | **全链闭包（审核新增，当前部分违反）**：每个 ScenarioCard 必须 Intent+Plan+Solver(输出形状匹配渲染模板)+render 全接通，否则不可上架 | ⚠ 16/20 违反；缺构建时门禁 |
| R12 | **双向闭包（数据构建）**：对象必落切片(反向-对象 HARD)、字段必被消费(反向-data SOFT)、求解器入参必存在(正向 HARD) | `closure.ts` |
| **R13** | **结论可溯源（信任 = 出处 + 推导可当场亮出）**：凡推演结论里的数字必为可溯源对象——悬浮即出 `{来源系统·新鲜度·推导公式·输入因子·关联规则·备注}`（参考 PRD §1.2/§4，与 R12 输入侧"字段全建模"对称的输出侧纪律）。源系统降级时，依赖它的派生数字自动标降级、置信度(P90)随之下调(C09)。覆盖优先级见 `docs/REFERENCE-HTML-INVENTORY.md` 信任章。**真推演 not 假推演（轨M 增量1·R13 延伸到推演红/黄状态）**：推演输出（红/黄/财务数字）**绝不裸渲染当真值**——`risk_timeline` 逐卡透 `dataMode(LIVE/MOCK)` + `currentTightness{value,live}`（`solvers/risk.ts riskTimeline`：LIVE=实测真 OEE/利用率/良率，MOCK=无真数据源），前端 `RiskBoardView` 逐卡显"估算（实测当前 N）"/"实测当前 N"（红被诚实标或锚定真测量值）。修法抄典范 `capex_scenario`（缺数抛错不造假）+ `LedgerView`（逐格 Provenance）。**门 `genuine-sim:check`（已落·入 pnpm gates，轨M 增量1c）**：`scripts/check-genuine-sim.mjs` 保守哨兵静态扫"推演 schema(risk_timeline/capacity_forecast/bottleneck_matrix)有 dataMode + 前端 RiskBoardView/ProjectSimView 消费 dataMode/live 显估算/实测 + bottleneck 前端传 LIVE"防回潮（假3/假4 修后扩入）。**置信度三维（WO-FRESHNESS·R13 从一维诚实位扩到三维）**：诚实位从"实测↔估算"(LIVE/MOCK/PARTIAL)扩为三维 `真实↔合成 × 新鲜↔陈旧 × 实测↔估算`——`SolverDataModeSchema` 追加 `STALE`(关键源 `dataHealth.critical` 滞后·C09 跨求解器化)/`SYNTHETIC`(纯合成对象决策·`origin=SYNTHETIC`·与 `domainTrustLevel=UNVERIFIED` 同源)，`SolverConfidenceSchema` 落三维 structured；`solvers/service.ts invoke` 统一叠加 `applyConfidenceDimensions`(确定性 R6)，前端 `DataModeBadge` 标「基于 N 小时前 / 合成数据(非真实接入)」、`RiskBoardView` 置信度横幅显三维。门 `pipeline-freshness:check`（§7·关键源 dataHealth 接进决策置信度·缺即红）+ `solvers.test` WO-FRESHNESS①②③ 真跑守。**角标真值闭合（PROV-REF-INTEGRITY·R13 的答案角标面）**：答案里的 `⟦ref⟧` 上标必须**点得出真溯源条目**——数字索引作者位在答案组装口统一解析成真实 provId（`agentcore/util/prov-refs.ts resolveNumericRefs`·Path A/B/场景/降级四口全接）、`solver_summary` 逐 KPI 独立 provId + 字段级 outputPath（悬停出该字段真路径非泛化 $.data）、越界索引诚实摘除转「未溯源」示警；死角标（悬停恒『加载中…』）=R13 违例。详 §8 G-3 PROV-REF-INTEGRITY 段。 | `<Provenance>` + lineage 端点；前端 `provenance.test`；`solvers.test` risk dataMode + WO-FRESHNESS①②③；`agentcore/test/prov-ref-integrity.test.ts` |
| R-一致 | **一个事实一个出处**：同一指标在驾驶舱/S&OP/体检口径一致（同一对象库派生），跨视图同值 | 单一对象库 + 聚合下推 |
| **R14** | **应用层无业务常数（多租户）**：前端组件不得内联业务数据/结构/租户专属文案；一律来自本体/WorkspaceConfig/ViewConfig.layout/i18n。换租户=换配置不改代码。守护 G-5 不回潮。 | ✅ `debattery:check`（基线 0：无未声明业务常数；兜底逐行 `// debattery-allow`）；标杆 `DashboardView`/`LedgerView` |
| **R-PACK** | **换行业 = 加一个 pack 文件·0 码改（INDUSTRY-PACK-CONVERGE·P1 北极星·R14 姊妹律）**：行业专属栈（派发/结构/阈值/时间/实体 5 层 + viewLayouts + 决策场景）必须收敛为单一声明式 `IndustryPack`，外置为**目录约定** `apps/datacore/src/synthetic/packs/<industryKey>.pack.{ts,json}`；`PACK_REGISTRY` 由运行期扫描该目录 auto-discover 构建（非手工登记数组）。**新增一个行业 = 往 `packs/**` 放一个文件，既有引擎/视图/求解器/加载器零改**（`git diff --name-only` 仅含该 pack 文件）。battery 收编须 byte-identical（identity 引用既有常量·现有断言不变即证 R6）；非电池行业由其 pack 的 `views`/`scenarios` 驱动**自有**视图（引擎零 `if(industry===)`）。守 R14 从"换租户=换配置"升到"换行业=加一个文件"。 | ✅ `industry-pack:check`（§7·`scripts/check-industry-pack.mjs`：PACK_REGISTRY===目录扫描·源码无手工数组·文件名===industryKey·battery/logistics 合法·0 码发现活证）；teeth `test/industry-pack.test.ts`（C2 目录派生 + 探针·C3 logistics 非电池视图/场景·byte-identical） |
| **R15** | **CLI 对等（A15）**：每个对外模块能力必须有 CLI 等价命令（注册 `OPERATION_CATALOG`），经同一 REST + R3 + R4 + 事件触发——CLI 与 GUI 平行同源、无功能洼地；不宜 CLI 内联的（求解器上传/复杂可视化）须登记 GUI 深链（`uiDeepLink`）。新增对外能力无 CLI 命令/深链 = 功能洼地，返工。 | ✅ `cli-parity:check`（棘轮基线 `cli-parity-baseline.json`；OPERATION_CATALOG 每条须 cliCommand 或 uiDeepLink，新增不可达即红）；`POST /b/v1/operations/classify` + `platform do` 万能路由 |
| **R16** | **发育闭环（system-ontogenesis 总纲）**：系统是个体发生的有机体——**倒序发育**（从场景/需求倒推长出数据/对象/规则/求解器/Agent/工作流）⊕ **正序运作**（QOS 问句→答案沿已长成管线）是同一有机体两相（`StoryBuildRun⊕GrowthLedgerEntry by runId` 认两面）。每次发育（建域/补缺/scaffold）须自动闭合**三环**：①数据（build-to-verify 真能在正序跑通，A10）②本体（新对象/链路/事件进活体本体，dogfooding §9，非手抄）③能力（目录从注册表自动派生 `deriveOperationCatalog`/`FEATURE_REGISTRY`/`SOLVER_CATALOG`，非手维护）；产物**二分处置**（AUTO-DERIVE 自动生成 / NEEDS-HUMAN 自动开 `GrowthTicket`+通知+收件箱+深链，**绝不静默残缺**）；发育过程**透明可视**（FDE 节点图/模块同步矩阵/覆盖度）；成熟**分相位** PROVISIONAL→ADVISORY→GOVERNED（只 GOVERNED 计真值，A18）。正序 `GapReport`=生长信号自动触发倒序生长——越用越大。复用 R4/R6/R11/R12/R13/R14/R-一致。 | ◐ 机制散落已具雏形（runStory/growth LOOP/A10/A18/dogfooding/CL.1–CL.7）；`ontogenesis:check` 门（三环+二分声明性校验，已并入 `pnpm gates`）；活体本体落库 + 自动派生目录分相位演进。事件 `ontogenesis.organ_matured`（L-onto，产物 GOVERNED 转正，可选）·切片 `sys.meta.ontogenesis_loop` |
| **R17** | **决策单页（Decision-on-one-page · 前端宪法 · 推演沙盘 HANDOFF 增量 0 立）**：决策页一页看全 **数据→推演→溯源→动作→AI**，就地下钻不跳页、配置驱动密度。新决策页（沙盘主屏/数据管道建模/就绪认证）天然遵循；改动到的现有决策页对齐（不主动重排全部，渐进）。竞品沙盘屏三栏（图谱 · Runtime Health/Trust 雷达 · AI 指挥台）= R17.2 的成品参照（`GROUNDING-MAP §F.1`）。 | 🚧 拟立（增量 0 入本体即生效）；配套门 `decision-page:check`（待建·增量 4 随 UI 落）；新决策页须遵循，旧页渐进对齐 |
| **R-RETENTION** | **每张无界增长表有留存上限（WO-RETENTION ⑤·防长跑爆库 + 合规留存）**：`outbox_events`/`ts_points`/`scheduler_runs` 等按时间无限追加的表，必须在留存策略注册表（`RETENTION_DEFAULTS`/`RETENTION_TABLES`，平台默认 + 租户覆盖·R2）有项，并由 `RETENTION_SWEEP` 每日 cron 清理 **过期 + 已处理** 行——删 `createdAt(/ts/scheduledAt) < now − keepDays`；**outbox 仅删已 DISPATCHED（DELIVERED/DEAD），PENDING/FAILED 永不删**（防丢未投递事件）；`scheduler_runs` 仅删终态（RUNNING 不删）；一律 tenantId 限定（勿跨租户删）；未配 policy → 平台默认，PAUSED → 跳过该表（向后兼容不误删）。清理计数 metric `dc_retention_purged_total{table}`。新增此类表须同登记进注册表 + `sweepTable` 删除分支。 | ✅ `retention-coverage:check`（§7·注册表覆盖+清理分支+RETENTION_SWEEP 接线）；`retention.ts RetentionService` · `app.ts GET/PUT /a/v1/retention-policies` · `test/retention.test.ts` |
| **R-AUDIT** | **统一审计·每写路径带 actor（WO-AUDIT-OBS）**：每条 admin/写路径变更必经统一审计写入器（`apps/datacore/src/audit.ts AuditService.record(ctx,…)`），落**专用 append-only `audit_log`**（`{id,tenantId,actorId,action,targetKind,targetId,before?,after?,at,requestId}`·只插不改不删·R13 子律），actor=ctx.userId、before/after 取关键字段（非全 doc 避膨胀）、requestId 跨两系统贯穿。只读消费 `GET /a/v1/audit-log`（platform_admin / 新增 `auditor` 只读角色·无 PUT/DELETE 写路由·append-only 结构性成立·不破 A6 行级·R2 租户隔离）。`features.ts saveConfig` 与 `adminplatform.ts` 全 admin 变更路径统一经此。**跨服务 requestId 透传语义**：AgentCore 出站调 DataCore（`tools/datacore-http.ts call()`）透传 `x-request-id`（无则生成）；DataCore `app.ts genReqId` 优先取入站 `x-request-id` 再回退 `req.id` → 同一 requestId 贯穿 AgentCore 日志 → DataCore 日志 → 错误信封（R7 信封已含 requestId），一线可追排障/合规。 | 门 `audit-actor:check`（`scripts/check-audit-actor.mjs`·并入 `pnpm gates`）：静态守 admin 变更经统一 audit(actor) + append-only 无写路由 + R9 audit_log 四处 + 跨服务 x-request-id 透传；运行期由 `apps/datacore/test/audit-log.test.ts` 守（改 feature→审计现该条带 actor+before/after+requestId·只增·R2·角色·outbox 向后兼容） |
| **R-DR** | **每个持久化 pg 库有逻辑备份脚本覆盖 + 恢复演练路径（WO-ENTERPRISE-DR-AUDIT·sub-A·灾备）**：平台两库（`postgres-a`=datacore·`postgres-b`=agentcore）都由 `scripts/backup-pg.sh`（`pg_dump \| gzip` → 宿主机 `<name>-<ts>.sql.gz` 逻辑备份·可 ls 见）覆盖 + `scripts/restore-pg.sh`（导入一次性 scratch pg·打印核对表行数供人核"导得回"·行数与源一致）演练路径；备份=时间点快照·非连续 PITR（**R13 诚实叠加**：连续恢复需 pg WAL 归档·当前 `docker-compose.yml` 未启用·`DEPLOY.md §9.3` 给升级样例·不假装已有）。新增持久化 pg 库须同扩备份脚本两库列表。**审计外送 sub-B 与本条互补**：sub-A 保业务数据可恢复·sub-B 保审计证据外流合规（两者都是"数据不因单机/单系统损毁而丢/不可查"的灾备维度）。 | 🚧 runbook 门（`DEPLOY.md §9`）+ FDE 真跑证据 `docs/evidence/WO-ENTERPRISE-DR-AUDIT-fde.md`（备份文件 ls + restore 行数一致：datacore audit_log 7/7·outbox_events 12/12·agentcore outbox_events 5/5）；脚本 `scripts/backup-pg.sh` / `scripts/restore-pg.sh` |
| **R-NO-ORPHAN-SOURCE** | **无凭空源（WO-SOURCE-TRANSPARENCY · 消灭"走捷径"·治本立不变量）**：凡来源为合成/物化（`origin.type ∈ {SYNTHETIC, MATERIALIZED}`）的 `ObjectInstance` **必有可解析的 `rawDatasetId`**（SYNTHETIC.rawDatasetId / MATERIALIZED.datasetId），且该 RawDataset 在本租户真实存在——即每一个业务对象的源数据都能溯回"数据连接器"页可见的一张原始表（进而 `GET /a/v1/raw-datasets/:id/export?format=xlsx\|csv` 可下载 Excel 审计）。凭空对象（无 rawDatasetId 或指向不存在数据集）= 走捷径，判红。**诚实豁免（非漏洞）**：MANUAL（人工录入）/ META（系统本体自反投影·源=文档锚点）/ 时序 TsSeries/TsPoint（**诚实归类为派生指标非源数据**·派生自 Base/Line/Process 随时间·非 ObjectInstance·D1 分类）。**R13 诚实叠加**：合成源导出文件名带 `.synthetic` 段（不冒充真实上传）、前端 SYNTHETIC 徽标常驻（透明 ≠ 洗成真实）。R6：审计纯遍历排序无时钟/随机。R2：仅本租户。 | ✅ `no-orphan-source:check`（§7·`scripts/check-no-orphan-source.mjs` → `test/no-orphan-source.test.ts`：真种合成→审计 0 凭空 + **green→red 自证**[植入无 rawDatasetId 对象→门红] + 导出端点真 .xlsx/.csv + R2 隔离）；审计逻辑 `apps/datacore/src/no-orphan-source.ts auditNoOrphanSource` |
| **R-NO-FAKE-DONE** | **自我账目完成即真（PRD-trustworthy-self-accounting WO-1·堵谎根 P1·命名式不占 R18）**：任何标为**完成/已修/已验证**的自我账目（work-queue DONE · meta 断点 FIXED · Capability.verifiedStatus）必须挂 **reality-derived 证据**——其 `acceptance` 引用的制品**运行时真存在**（当前静态口径：求解器 key ∈ DataCore `SOLVER_REGISTRY` 单一来源）且非空验收（有 `criteria`）；引用幽灵制品 → **FAKE**，空验收 → **UNVERIFIABLE**，二者皆 = 诈 → 自动打回**开放缺口**持续暴露，**不得手打状态字蒙混**（`collab-queue.mjs done` 只写 status、从不查制品即根）。守 §2.E 求解器真实性 + §10.5.4 元层自陈「尚未接规则引擎校验不变量为真」。**诚实边界**：本律当前由静态门守"求解器 key 存在性"；"代表问经 QOS NL 真跑答出"的运行时 E2E 判据属 WO-2（Capability 派生 verifiedStatus）。 | `no-fake-done:check`（§7·`scripts/check-no-fake-done.mjs`：DONE 的 acceptance 抽取求解器 key→存在性静态校验·棘轮基线 `no-fake-done.baseline.json` 诚实记存量诈账待 WO-6 回炉·green→red 自证·新 DONE 引幽灵/空验收即红·闭 G-16） |
| **R-QUANT** | **下发量化（视觉/样式/尺寸必给精确值·禁定性·WO/PRD 铁律·用户 2026-07-02 亲立）**：凡 WO/PRD/任务里的视觉·样式·尺寸·阈值类要求，必须给**精确量化值**——颜色=`#RRGGBB` 色号（禁"深/浅/更暗/深蓝调"）、尺寸/间距/圆角/字号=`px`/`rem` 数值、对比度=WCAG 比值、透明度=%、时长=ms。严禁"深浅/大小/更X/大概/合适/清爽/有质感"等定性词代替数值。验收必可测（真浏览器 `getComputedStyle` 逐值比对目标值）。**审核方对含定性视觉词的 WO 拒收**（返工重写为量化后方可下发）。由来：主题重设计三次返工（8040049→THEME-CONTRAST-FIX），根因是任务给"深浅"未给色号，dev 无量化靶。 | 评审/WO 下发硬判据（审核方拒收定性视觉描述）；验收 `getComputedStyle` 逐值比对 |
| **R-PRD** | **重构配整页 PRD（页面/布局/换肤类任务必附逐元素 PRD·禁大概·WO/PRD 铁律·用户 2026-07-02 亲立）**：凡页面/布局/重构/换肤/新页类任务，WO 必须附**整页 PRD**——逐区块（位置/尺寸/层级/占比）+ 逐组件（形态/状态/交互/数据源/引用哪个求解器）+ 栅格·间距·断点 + 空态/加载/错误态 + 配色引用（走 R-QUANT 色号）。严禁用"大概描述/重组一下/更清爽/参考某图"这类笼统措辞代替 PRD。**审核方对无整页 PRD 的重构类 WO 拒收**。由来：沙盘布局重构（用户亲选 Option A）我只给"大概描述"未出整页 PRD，dev 无逐元素靶 → 需求悬空。 | 评审/WO 下发硬判据（审核方拒收无整页 PRD 的重构/换肤任务） |
| **十红线（推演沙盘落地纪律 · `docs/HANDOFF-sandbox-build-and-review-contract.md` §4 · 越线即停）** | RL1 本体先行(改接线先回写本体过 `ontology:check`) · RL2 暗发(新模块 `defaultOn:false` 不动现有租户) · RL3 单一来源(不出双份；就绪=投影既有 closure 零新校验；单源门复用 `boundary-singlesource:check` 勿造 `ia-single-source:check`) · RL4 走正门(沙盘 act 模拟态，采纳才经 Action R4 写真值) · RL5 零业务常数(传导核/表无行业实体名，两行业验收 R14) · RL6 确定性(传导核纯函数，无 Date.now/随机，R6) · RL7 CLI 先于 UI(R15) · RL8 倒序长出(世界态经连接器/合成/runStory，禁硬编码 seed，R16) · RL9 additive 可回退(迁移有 down，entitlement 关=404，旧路径在) · RL10 不与在建分叉(复用 sim-views/A8/recompute/replay/ontogenesis/closure，不平行造第二套)。 | 大多复用既有不变量（RL1=R16本体先行·RL3=R-一致·RL4=R4·RL5=R14·RL6=R6·RL7=R15·RL8=R16·RL10=不分叉）；逐 PR 评审硬判据（HANDOFF §5） |

---
