# 本体切片 §7 · 检测/门禁（改动必须过）

<!-- 自动生成·勿手改 -->
> ⚠ **本文件由 `scripts/build-ontology-slices.mjs` 从母体 `docs/SYSTEM-ONTOLOGY.md §7` 派生**（本体克隆切片·层 2）。
> **改接线改母体 §7，再跑 `node scripts/build-ontology-slices.mjs` 同步**（勿直接改本文·门 `ontology-slices:check` 守漂移）。母体 hash `cc8c84c9c392d414`。

---

## 7. 检测/门禁（改动必须过）

- **闭包门**（数据构建）：object/data/forward 三向，HARD 失败拒发布 · `closure.ts`。
- **validate**（工作流/本体）：DAG 环 / 类型 / render 末步 / storageMode 一致性。
- **准备度评分**（实体/子图成熟度）。
- **entitlement 门**：FEATURE_NOT_FOUND（先于 authz）。**feature 退役声明门（GRAPH-PANORAMA-ONLY 起）**：静态注册键删除必须同步入 `RETIRED_FEATURE_KEYS`/`RETIRED_VIEW_KEYS`（datacore `features.ts`·agentcore `features/registry.ts` 镜像）防幽灵 entitlement——残留 override 忽略、新 override 400、退役视图键禁动态注册/不下发 workspace；teeth：datacore `planviews.test.ts`（400+解析零残留）· agentcore `qos-f-entitlement.test.ts`（注册表零残留）· frontend `f25.graph-panorama.test.tsx`（退役键重现即红）。**导航 IA 退役同范式（GROWTH-TICKET-MERGE 2026-07-06）**：前端管理页/导航项退役（非 datacore feature 键）同走「声明退役非静默删」——`/admin/growth` 页删后 `App.tsx` 留 tombstone 302→`/admin/tickets`（防死链）、`adminRegistry.ts` ADMIN_PAGES 撤 growth 登记 + 「构建与成长」组去重 + 角色门取并集（growth∪tickets=admin/data_admin/catalog_admin）、`ShellLayout.tsx` NAV_GROUPS 撤 growth、内链改指存活页；teeth：frontend `f63.growth-ticket-merge.test.tsx`（导航无 growth 项/302 一致/角色门并集/独有功能承接·revert 任一即红）。
- **规则 BLOCK 短路**（工作流步骤遇 BLOCK 终止）。
- **A6 行级过滤**（query/slice/solver 读出）。
- **VLE 闭环验证引擎**（七段断言 + 三覆盖率，独立参照预言机双算）· `validation` · `apps/datacore/src/vle.ts`。七段全覆盖：①接入(GenSpec 行数守恒) · ②对象化(产出>0 + 引用完整性) · ③聚合派生(聚合==明细差分,经 query 路径) · ④规则查全查准(独立 plain-JS 谓词:字段齐备查全 + 植入越线行 C03 查准) · ⑤求解器执行(**参照实现双算 V5**：被测 capacity_forecast P50/P90 vs 独立参照预言机 `vle-oracle.ts`[第二套独立代码,设备→工序→产线→基地→周曲线累计,**零 import 被测**]逐字段比对容差 1e-6,改坏系数即⑤红+diff 下钻——VL2 真捕获人造 bug) · ⑥行动终态(R4:已注册 ActionType 审批链非空,无直写后门) · ⑦校准注入(提案 simulatedMapeAfter<mapeBefore,无反校准)。`assertionCov = 已覆盖规范段/7`（非硬编码 1）；VL7/V9 静态独立性:vle.ts 与 `vle-oracle.ts` 均不 import `solvers/service`/`solvers/capacity`/`ruledsl`——参照预言机独立于被测，杜绝"用被测算被测"。**门 `validation:check`（轨D 新建 · real-backend 聚合外独立门，同 `ui-smoke:*`）**：V10 真跑 SMOKE（pass + 工程验证度≥基线 + ⑤含 reference 双算断言才过；植入坏系数→门红 exit1）+ V9 静态扫零被测 import；· `scripts/check-validation.mjs`，`pnpm validation:check`。**已并入 `pnpm gates` 聚合**（评审打回校正：VLE 不进 gates=CI 拦不住=形同虚设；gates 链内前置 `pnpm --filter datacore build` 确保对当前源码跑 SMOKE → 改坏求解器 `pnpm gates` 真红）。
- **断链审计 DL1–DL12**（§4，每个产出环必须有事件+订阅）。
- **`ontology:check` 本体漂移门禁**（治理新增）：事件/求解器/文件锚点/钩子不漂 即 build 红 · `scripts/check-system-ontology.mjs`，`pnpm ontology:check`。
- **`genuine-sim:check` 真推演门（轨M 增量1c·守 R13 推演侧 + 真推演not假推演红线）**：保守哨兵静态扫——推演求解器输出 schema（risk_timeline/capacity_forecast/bottleneck_matrix）必带 `dataMode`；risk.ts 逐卡透 dataMode/currentTightness（liveTightness 实测当前张力）、capacity.ts 用 liveTightness 不裸 import mockTightness；前端 RiskBoardView/ProjectSimView 消费 dataMode/live 显"估算/实测"、bottleneck 前端传 LIVE → 防"mock/哈希/写死冒充真算"回潮（假3 OrderChainView/假4 PropagationTimeline 修后扩入）· `scripts/check-genuine-sim.mjs`，已并入 `pnpm gates`。**A0 空洞数据冰山结构性根因修（诚实位推广全族·门 ⑦）**：`SolverDataModeSchema(LIVE/MOCK/PARTIAL)` 立为契约层单一来源；`audit_timeline`（逐日曲线 kind 名哈希派生→PARTIAL/MOCK·**已由 FAKEDATA-SWEEP F1 `FILL-AUDIT-TIMELINE-REAL` 治本：产销/齐套映射需求驱动瓶颈因素→真基线聚合真曲线 measurement=LIVE·无真源口径退诚实空态 series:[]/hasData:false/MOCK·绝不哈希造曲线**）+ 13 个 extended 求解器（`extendedDataMode` 据真对象 vs 魔数/硬编码兜底分类：yield_diagnosis/quarterly_gap=MOCK·quote_margin/credit_exposure/maintenance_stagger/countermeasure_combo=PARTIAL·其余真对象派生=LIVE）经 `service.ts` 分发统一附 `dataMode`；前端共用 `DataModeBadge` 组件，`PlanAuditView` 审计时序标徽章。门 ⑦ 断言 audit_timeline/extended 透 dataMode + 契约枚举 + 徽章消费防回潮。**A★ 洛阳红色死路修**（`RiskBoardView` 点格→渲染卡已带 `affectedOrders` 真受影响订单·删 broken searchObjects·MOCK 诚实横幅·禁裸暂无数据）。**WO-FORECAST-SIM（合并 A★·推演接需求-产能真源）**：`risk_timeline` 紧张度从 `mockTightness` 哈希/`tensionSeries` baseline 改由**真需求-产能缺口**派生——需求侧 `DemandSegment(p50/p90/tgt)`+`SopVersionRow(demand/supply)`，供给侧 `computeRollup` 真产能；`solvers/risk.ts demandCapacityTightness`（负载比=真需求÷真产能·量纲无关·R6·无随机/时钟）喂需求驱动因素（瓶颈工序/人力工时/物料齐套）基线，`liveTightness` 优先用之（有真预测→LIVE）、否则回落 mockTightness（无真源→MOCK 诚实不冒充）。`loadContext` 注入 DemandSegment/SopVersionRow（types.ts SolverContext +demandSegments/sopVersions）；卡附 `demandGap{gapWan,source}`（缺口=预测需求−产能·R13 可溯）；契约 RiskCardSchema +demandGap。前端 A★ 升级：AffectedOrdersModal 对 LIVE 需求驱动卡显绿色"真需求-产能缺口"溯源面板（`affected-orders-demand-gap`·缺口万套可溯），MOCK 卡保留黄色诚实横幅、空列表诚实解释（禁裸 `zh.common.none`）。门 `solvers.test` V5d（改 DemandSegment.p50→张力曲线上抬·非哈希恒定·R6 字节一致）+ V5（基线改读 currentTightness 真张力）。FDE 真跑（内存 SEED_DEMO=1）：洛阳·物料齐套 dataMode=LIVE·curTight=68·demandGap 可溯·affectedOrders 非空（SO-3470 电网公司F）·R6 byte-identical。**H 修**（`plan_generate` outcome 下发 `shareDelta/revGrowth` 与 meets 闸门同源·前端删 -17/-100 魔数）。**WO-DM keystone（dataMode 推广全求解器 + no-silent-mock 门）**：契约层 `SolverDataMode` 单一来源已立；plan_audit/plan_generate zod schema 补 `dataMode`；`SOLVER_OUTPUT_SHAPES` 运行期为**每个 SOLVER_KEY** 追加 `dataMode`（46 全覆盖）；`SolverService.invoke` 统一 wrapper 保证每次输出真带 `dataMode`（hollow audit/extended 自置 MOCK/PARTIAL·PROVISIONAL 沙箱 PARTIAL·其余读真对象/config 默认 LIVE）；前端 `DataModeBadge` 铺到 audit_timeline + plan_generate 落点。门 `no-silent-mock:check`（`scripts/check-no-silent-mock.mjs`·并入 `pnpm gates`）导入编译产物断言每 SOLVER_KEY 输出形状含 dataMode（green→red 自证）。根除"求解器静默哈希/魔数冒充真算"整类。**WO-RISK-TRAJECTORY-DEFAKE（audit `docs/AUDIT-fake-value-remnants.md §2 簇B/§簇G`·「量化具体值残留层」修·同 G-DM-1 KILL-MOCK-RED 红线）**：KILL-MOCK-RED 修了**基线**（红/不红只在真数据 live=true），但 `risk_timeline` 的**轨迹目标 + 事件具体值**在 LIVE 卡上仍 hash 造、且主动标 EAM/CMMS·S&OP/ERP·WMS/ERP **假源归因**（谎称来自真系统·比无披露更坏）→ 本轮治本：**B1** 删 `riskTarget`（名首码+因子首码 hash 爬坡目标 → 制造假 `crossDay` 穿越日）+ `tensionSeries` 基线**改水平**（真张力 `liveTightness`），轨迹只由真基线 + 真事件脉冲（事件发生日=真本体：MaintPlan.week/订单交期/到货周期/延误在途）决定，越线由真数据顶过阈值才出（真检修 week5-8→day32+ 落窗内才越·30 天窗内无排检修+真张力 82-83 未及 85=诚实不越·crossDay 不能编·counterfactual 窗内无越线时回退取真张力最高 base×factor 为最坏潜伏风险）；**B2/B3/B4** 删 `EVENT_SRC`+`hn()`——事件 desc 只叙真锚点（qty 真/Shipment.coverageDays 真/在途批真计数），无实测量化一律标「需接入…实测·当前无实测值」或「估算」，**不再标 src 真源**；**B5** 信用占用比删 `creditBase+hash(cust|so)%creditMod/100`（编造占用比驱动信用阻断裁决）→ 取真 Customer `(应收+在制未开票)÷信用额度` 或 null（无真信用数据不分类 credit）·override 越限用真实敞口叙述(`why`)；**B6/B8/B9** 工时系数 1.6/severity 阈 92·78·加成12/需求张力常数 62·70·0.6·0.8·40 全入 `params.risk.{deliveryLaborPerWan,caseSeverity,demandTension}`（可校准·非内联魔数 R14·`caseSeverity` 默认=`CASE_SEVERITY_DEFAULT` 命名常量单源）；**B7** 删 per-order delay 的 `hash(so)%jitterMod` 抖动（延误=真峰值超阈幅度÷delayDiv）；**G1** `SEG_PRICE ?? 0.6` 假单价兜底（不可达）→ `segPriceWan()` 失败响亮（抛错·非静默假值）；**G2** extended `outsourcing_split` 缺省 gap=`totalDemand×DEFAULT_GAP_FRACTION` 由 LIVE 降 PARTIAL（诚实标估算）；**G3** `yield_diagnosis` 合成默认序列断点 `source:"MES"`（假源）→ `source:"SYNTHETIC"`+`synthetic:true`（mode 已 MOCK）。FDE 真起（内存 SEED_DEMO=1）逐值对照（`docs/evidence/RISK-TRAJECTORY-DEFAKE.md`）：h=90 自扫 4 张真 LIVE 卡越线绑真检修周·41 事件 src 全空·0 谎称真源·CREDIT 判定用真敞口叙述·洛阳/换型损失无真源诚实空态不出红。牙齿 `test/risk-trajectory-defake.test.ts`（回退 src/hash/MES/LIVE 即红）+ `solvers.test.ts` V5(水平基线曲线)/V5b(h90 真越线)/V5c(无假源归因·删 EVENT_SRC)。**WO-FAKE-05 堵根 C2（audit `docs/AUDIT-solver-fake-residues.md` 门盲区②·「dataMode 值诚实」无门可守的根）**：§⑧b 新增**行为断言**——真调 dist `riskTimeline`，构造两份**仅业务输入不同**的 SolverContext（设备OEE 改 `Equipment.oee_current`·良率波动 改 `Process.yield_baseline`），断言 measured 因子的峰值张力 `peak` **随真输入变**；出了非空决策峰值却两份相同 = 与真输入零耦合的扁平冒充（审计 R1/R2 设备OEE/良率扁平标 LIVE 的病）→ 红。补上"声称 measured/LIVE 的值必须随真输入变，否则须读真属性或诚实标 PARTIAL"这道语义门。牙齿实证：把 OEE 分支改成忽略 `oee_current` 返回常量 78 → 门红「峰值 78 不随真输入 Equipment.oee_current 变」·revert → 绿（FAKE-01 后 OEE/良率读真属性·峰值随输入变·honest-LIVE 通过）。
- **`seed-demo-smoke:check` 真启动冒烟门（WO-0·防"绿测试≠能用"复发·已并入 `pnpm gates`）**：`scripts/check-seed-demo-smoke.mjs` 内存模式真起 `apps/datacore/dist/server.js`（`SEED_DEMO=1`·无 DATABASE_URL），断言进程在 90s 内打印 `datacore listening`（= 种子链全过、未在 seed 期 Exit 1），否则非零退出贴最后日志。**由来**：本轮两次"vitest 绿但真东西红"——WO-4 归域门收紧后 `SEED_DEMO=1` 真启动崩 Exit 1（demo 整站起不来）但单测 778 绿（走 A 路绕发布门、从不跑真种子链）；`pnpm -r build`+`pnpm -r test` 都过却从不真起一次带 SEED_DEMO 的 datacore。本门是 gates/CI 里唯一**真把服务起起来**的门，挡 WO-4 类"种子/启动期崩"回归。**连带（修 WO-7 类 stale-dist 坑）**：`pnpm gates` 链首前置 `pnpm --filter @platform/contracts build`（本地 `pnpm gates` 与 CI `pnpm -r build` 对齐 → datacore tsc 不再撞旧 contracts `.d.ts` 报 TS2353）。
- **`chain:check` 全链闭包门（第一块砖，R11）**：跨系统静态校验"场景声明的求解器 DataCore 必须注册"，否则路径A 全链断（SOLVER_NOT_FOUND）即红 · `scripts/check-chain-closure.mjs`，`pnpm chain:check`。
- **`debattery:check` 去电池锁死门（R14）**：静态扫描前端视图/页内联的业务常数（基地名/型号/工序/产品段）；棘轮基线 `scripts/debattery-baseline.json` 防回潮——命中超基线即红 · `scripts/check-debattery.mjs`，`pnpm debattery:check`。`// debattery-allow` 豁免必要兜底。
- **`boundary-singlesource:check` 业务实例册单一来源门（DF.1–DF.3，G-5/R14/R6）**：静态断言三 BASE 消费端（datacore `battery.ts` BASES · 前端 `fixtures.ts` BASES · `simSolvers.ts` MOCK_BASES）均从 `@platform/contracts` BASE_REGISTRY **派生**（含 `.map`）且无内联 12 基地字面量回潮（baseId 字面量 ≥6 即红），四 SEG 消费端（`battery.ts`/`risk.ts`/`OrderChainView.tsx`/`simSolvers.ts`）均引用 SEG_REGISTRY、三 PLAN_GOAL 消费端（`battery.ts`/`PlanGenerateView.tsx`/`fixtures.ts`）均引用 PLAN_GOAL_TARGETS（DF.4）——防"改一处业务常数须手改多处、漏改即前后端崩不同步"漂移复发 · `scripts/check-boundary-singlesource.mjs`，`pnpm boundary-singlesource:check`。已并入 `pnpm gates`。
- **`cockpit-widgets:check` 门A · 经营驾驶舱 PRD↔widget 覆盖对账（防结构遗漏 + 后端/mock 两套 DASH_LAYOUT 漂移）**：静态断言 PRD §2.1/§3.3 必备 widget type（kpi/metric-strip/dag/counterfactual/version-toggle/order-ledger/plan-drill）在后端 `service.ts` DASH_LAYOUT + 前端 `fixtures.ts` DASH_LAYOUT + `DashboardView` 渲染分支**三处皆有**，必备组件区块（dash-problems/feedback-chain/modules/export）在 DashboardView——任一缺即红。由来：曾判"经营驾驶舱完成"却漏 order-ledger/plan-drill 整块（jsdom 只测"渲染了的"，测不出"该有却没有的"）· `scripts/check-cockpit-widgets.mjs`，已并入 `pnpm gates`。落地即抓出 mock 缺 metric-strip/dag 真漂移。
- **`ui-smoke` 门B · UI 交互冒烟（真浏览器 Playwright，治"渲染绿但点不动/下钻断"）**：起真 dev server(VITE_MOCK :5199) + Playwright 以用户身份点经营驾驶舱关键交互（问题卡下钻 order-chain / 台账行下钻 / 规划推演去体检 / 导出 / 模块直达），断言可达；死按钮/断链即红。补 fde-delivery 真前端验收欠债（jsdom 对 div/button 都触发合成 click，测不出纯 div 死按钮——曾漏问题卡不可点 bug，218 绿测试全过）· `scripts/ui-smoke.mjs`，`pnpm ui-smoke`（环境无 chromium 则 SKIP；CI 启用需 `npx playwright install chromium`，独立 job 非 `pnpm gates` 内以免环境依赖致不稳）。
- **`cli-parity:check` CLI 对等门（R15，A15）**：静态校验 `OPERATION_CATALOG`（`contracts/operation-intent.ts`）每条都有 `cliCommand` 或 `uiDeepLink`，且 cliCommand 经 CLI 调度（`platform-cli.mjs` run{} 或 `do` 万能路由）可达；棘轮基线 `scripts/cli-parity-baseline.json` 防回潮——新增对外能力无 CLI 命令/深链即红 · `scripts/check-cli-parity.mjs`，`pnpm cli-parity:check`。已并入 `pnpm gates`。
- **`provisional-honesty:check` 未审核诚实门（A18）**：test-backed（`a18-provisional-closure.test.ts`）——PROVISIONAL 域须 `domainTrustLevel=UNVERIFIED` + 终态 `PROVISIONAL_ANSWER`（绝不 VERIFIED/answerable）+ 闭包缺口全 `ADVISORY` + 不 `blocked`；`checkProvisionalHonesty` 纯函数守"未审核态绝不谎报"（R13）· `pnpm provisional-honesty:check`。
- **`prd:check` PRD 库结构化门（治理 #2）**：解析每篇 PRD 的《本体引用与影响》§0 → 写机器可读索引 `docs/prd-ontology-index.json`（PRD↔不变量/断点，需求↔制品↔缺口可查）；校验引用的 R/G 在本体真实存在（悬空引用即红），报告断点 PRD 覆盖与缺口、遗留 PRD 缺 §0（告警） · `scripts/check-prd-ontology.mjs`，`pnpm prd:check`。
- **`rule-closure:check` 规则引用闭包门（G-10 规则即引用 P1，R11 输出侧）**：静态断言 `⋃(SOLVER_RULE_REFS, SCENARIO_CATALOG.rules) ⊆ battery.ts rules[] 定义`——求解器/场景引用的规则码必须有一等定义（否则"关联规则半空 / 改规则不改推演"），只匹配带引号 `'Cxx'`（避颜色码 `#4C90F0` 误报）· `scripts/check-rule-closure.mjs`，`pnpm rule-closure:check`。已并入 `pnpm gates`。
- **`traceability:check` 可信溯源门（轨N 增量3 + 跟进2 · 守 R13 结论可溯源在交互层不回潮 · G-10）**：保守静态哨兵两维——① **规则号维（穷尽可靠）**：扫前端 `*.tsx` 禁"规则号裸渲染成 JSX 文本子节点"（`>{...ruleRef(s)...}<` 未包 `<RuleRef>`，悬浮无溯源信息；由来：轨N 增量0 铁证 `OrderChainView:464-466` `{...ruleRefs.join("/")}` 裸文本→增量1 全换 `<RuleRef>`；不误报 `code={...}` 属性内/注释/testid）。② **KPI 维（presence 哨兵）**：9 个财务/规划决策视图（Dashboard/Ledger/OrderChain/AnnualScenario/QuarterlyRolling/PlanAudit/PlanGenerate/SopBalance/ProjectSim）必须接入 `Provenance` 能力（import 在位）——逐数字穷尽 wrap 不可静态可靠（任意数字非皆 KPI），故守"决策视图须具备溯源能力 + 高价值 KPI 已逐视图 wrap"，防"新决策视图裸渲染 KPI"回潮（RiskBoard 风险峰值经 RiskPopover+bottleneck 详情另一机制溯源，不在此列）· `scripts/check-traceability.mjs`，`pnpm traceability:check`。已并入 `pnpm gates`。 **WO-PROVENANCE-SWEEP（R13 决策数字接溯源·收 §T5 缺口·闭 G-死交互）**：规划/推演 6 页此前裸奔的高价值决策数字逐个接六要素 `Provenance` 悬浮（真派生·非编·逐值对后端 curl）——`PlanAudit` 体检评分（`plan_audit` score=clamp(100−scoreH·|H|−scoreM·|M|,0,100)·实测 score=29 由 22/7 系数派生）、`PlanGenerate` 综合分（`plan_generate` total=round(Σ五维/5)−惩罚·违规数）、`ProjectSim` 产能裁决/缺口（`capacity_forecast` gap=累计需求−P90·MC 分位）、`OrderChain` 统一裁决（`order_fullchain` 三判聚合·真规则 C02/C03·C06/C16·C15/C18）、`AnnualScenario` 情景需求/营收·CAPEX·IRR（`capex_scenario`·C23）、`QuarterlyRolling` 季度缺口（`quarterly` gap=需求−供给·C03/C27·curl 5.87=58.27−52.4）、`SopBalance` ③产销缺口/④滚动毛利·现金垫/⑤最终缺口（`sop_balance`·C21/C15/C18）。provenance 的 src/formula/inputs/rule 均指向真求解器输出与真 ruleKey（无真源诚实标·绝不编假源归因），`dataMode` 诚实位透入备注（合成/估算即标"非真实决策依据"）。ⓘ 死交互经既有 `MarginLedgerTable`/`Dashboard` 六要素弹已闭合。真浏览器（VITE_MOCK·planner·in-app nav）逐页 hover 实拍六要素弹出（`docs/evidence/prov-*.png` 6 张），非 ⓘ 空占位。
- **`no-hardcoded-rules:check` 求解器阈值去硬编码门（G-10 规则即引用 P2，R14 规则维度）**：保守哨兵——与已登记规则同义的业务阈值（C09 数据健康降级系数/时延、C04 认证产能系数）必须从参数读（`capacity.ts` 须含 `p.health.staleHours`/`p.health.degraded`/`p.certFactors`，且无 0.93/0.90 裸字面量），防"改规则不改推演"回潮；新求解器引入裸业务阈值需扩断言 · `scripts/check-no-hardcoded-rules.mjs`，`pnpm no-hardcoded-rules:check`。已并入 `pnpm gates`。
- **`ontology-writeback:check` 本体回写完整性门（治理 · 反向守"代码改接线却漏回写本体"）**：静态断言**每个并入 `pnpm gates` 的 `check-*.mjs` 门都在本体 §7 登记**（门名/脚本名/pnpm 别名出现在 §7）——补 `check-prd-ontology` 只查"PRD→本体悬空引用"（正向）的反向缺口（代码新增门，本体漏登记，正是 P2 漏回写 no-hardcoded 的根因）· `scripts/check-ontology-writeback.mjs`，`pnpm ontology-writeback:check`。已并入 `pnpm gates`。
- **`prd:coverage` PRD 覆盖对账门（治理）**：解析 PRD↔实现文件双向覆盖，写 `docs/prd-coverage-index.json`（covered/uncovered/coverage 比），报告哪些 PRD 缺实现锚点 · `scripts/check-prd-coverage.mjs`，`pnpm prd:coverage`。已并入 `pnpm gates`。
- **`meta:sync` 元本体自反落库门（Dogfooding P1）**：把本体 markdown 确定性投影为元对象物化进元租户 `__platform__`（八类节点：对象/不变量/断点/事件/域/切片/门/链路），R2 隔离（业务租户查不到元对象）+ 影响分析 BFS · `scripts/check-meta-sync.mjs`，`pnpm meta:sync`。已并入 `pnpm gates`。
- **`meta-runtime-truth` 元层运行时真相跑批器（META-RUNTIME-TRUTH·WO-4·§3.5·报告态非 gate）**：把每个**声称 FIXED** 的断点交叉核对其 §8 行内引用的现成 reality-judge（`check-*.mjs`/`xxx:check`）——判据真跑不通→**DRIFT**（本体谎言曝光）、通过→FIXED、无可运行判据→诚实 **UNCHECKED** · `scripts/meta-runtime-truth.mjs`（`--strict` 查出 DRIFT 即 EXIT=1·`--json`）。**report-only 不入 `pnpm gates`**（判据白名单只纳静态可跑门，需活服务的门归 UNCHECKED，避免 CI 误红）；派生纯函数 `deriveRuntimeStatus`（parse.ts·R6）由 `datacore/test/meta-ontology.test.ts` 齿守（revert→红）。
- **`ontogenesis-runtime:check` 发育闭环收口门（R16，G-9·WO ONTO-SCEN-GATE-WRITEBACK·真跑）**：gates 链唯一**真起 agentcore 逐卡 grow** 的场景门（scripts/check-scenario-ontogenesis-runtime.mjs·内存态 buildServer+mock DataCore 合成世界·与 seed-demo-smoke 同"真启动"范式）——补上静态门测不了的 §6 运行期内核：§6.1 每 GOVERNED 卡 VERIFIED+rings.data+承载数据块(kpi/table/action_draft/⟦ref⟧) · §6.5 非 GOVERNED 卡必带 gaps[] 且 disposition∈{AUTO_DERIVE,NEEDS_HUMAN}(NEEDS_HUMAN 必带 ticketId·无静默残缺) · 工作流地板 13 张 WORKFLOW_FIRST 确定性 GOVERNED(revert 渲染投影即红·亲验) · §2.4 launch S01 deterministic:scenario-bind+Path A+承载数据块(点卡真决策视图)。实测 20/20 GOVERNED · `pnpm ontogenesis-runtime:check`，已并入 `pnpm gates`。真验 docs/evidence/ONTO-SCEN-GATE-WRITEBACK-fde.md。
- **`ontogenesis:check` 发育闭环门（R16，G-9·静态内核）**：声明性校验 R16 发育闭环不变量在本体钉牢（倒序⊕正序两相、三环自动闭合、二分处置、分相位成熟）+ §6 静态逐卡断言（每卡 plan 有 render 步 / 卡 solver ∈ SOLVER_OUTPUT_SHAPES / 卡 rules ⊆ 已发布规则 / 卡 intentKey 有意图计划；§6.1/6.4/6.5 运行期内核现由上条 `ontogenesis-runtime:check` 真跑守住，本静态门专司契约不漂）+ **§2.5 零死答 grep 断言（ONTO-SCEN-LAUNCH-DET）**：apps/*/src、packages/*/src 五根零「未能产出回答」死答串（缺口一律结构化 GapReport/诚实发育卡，回潮即红）**+ WO RENDER-PROJ §6.2 渲染投影绑定齿**：契约登记 `SOLVER_RENDER_BINDINGS` ⊆ `SOLVER_OUTPUT_SHAPES`（逐求解器逐字段·bogus 字段亲测红）· 20 计划**实际**渲染绑定（模板引用⊕solver_summary bindings，`deriveRenderBindings` 运行期同一派生）⊆ 形状 · 派生循环 bindings 驱动+`injectScenarioRuleStep` 烘焙在位 · 静态占位文案「…推演结果：」回潮红 · growScenario `deriveSliceTargetCandidates` 切片自动派生在位；配套**真值齿** `apps/datacore/test/render-bindings-real-fields.test.ts`（真合成世界真 invoke 逐绑定字段在场）与 `datadep-manifest:check` 增 `DATADEP_ROLE_CANONICAL`↔`ROLE_CANONICAL` 逐键对账（漂移红）· `scripts/check-ontogenesis.mjs`，`pnpm ontogenesis:check`。已并入 `pnpm gates`。
- **跨服务联调冒烟**（守护 G-2 + 挡 mock 漂移）：真实 AgentCore HTTP 客户端 ↔ 真实 DataCore · `apps/datacore/test/xservice-smoke.test.ts`。
- **场景接线回归**（守护 G-1）：20 场景全有意图+计划+求解器 · `apps/agentcore/test/scenarios-wiring.test.ts`。
- **本体必读强制**（治理）：CLAUDE.md 铁律 0 + SessionStart 钩子（从 §8 动态注入未修断点，结构上不漂）+ `/ontology` skill。
- **全链闭包门（R11）**：`chain:check` 覆盖"场景↔求解器注册" + SHAPE 输出形状覆盖报告；`validateClosure` 焊进 **CHAIN**（求解器注册）+ **SHAPE**（求解器输出形状↔渲染绑定 `renderBindings`，挡 G-2 跨服务形状）两维。**余**：补齐其余求解器输出形状声明 + BuildPlan 渲染契约自动生成。**2026-06-28 收尾（只读专调坐实多根已闭）**：① publish 前置 HARD 门已实装（`databuilder/service.ts` publish_build：`aOk&&bOk` 不过拒发布、数据不落库——本体"记于终态"系 stale）② SHAPE 完整性 chain:check 已守 `SOLVER_KEYS⊆SOLVER_OUTPUT_SHAPES`(46/46) ③ **新增字段级漂移运行时门 `test/shape-drift.test.ts`**：真 invoke 核心求解器断言**实现返回顶层键 ⊆ 声明形状 + 规则信封**（捕"实现新增字段忘同步 SHAPE"的 G-2 死灰复燃；首跑即真抓出 capacity_rollup/bottleneck_matrix 的 evaluatedRules/ruleSetVersion 信封并正确归类白名单）。**G-8 实质闭合**。**2026-06-28 优化收口：BuildPlan 渲染契约自动生成**——`comprehend.ts deriveBStack/comprehend` planNeeds.renderBindings 此前出厂 `[]`（接缝，建出的计划无渲染契约），改为 `defaultRenderBindings(solverKey)` 从 `SOLVER_OUTPUT_SHAPES` 派生（排除 error/规则裁决信封键）→ **renderBindings ⊆ SHAPE 天然成立**（closure SHAPE 维零破·彻底去 G-2 接缝），显式声明仍优先；测 `render-contract-autogen.test`（派生⊆SHAPE/排信封/未声明回退[]·100 databuilder 测无回归）。详 `docs/PRD-unified-build-engine.md`。
- **推演沙盘门（G-11 · 增量 0 登记契约 · 脚本待各增量新建并入 `pnpm gates`，本步不写未存在脚本路径以免文件锚点门红）**：
  - **`sim:check`（增量 1 已建·并入 `pnpm gates`）**——静态守会话层结构不变量：migration026 四表+down(R9 可回退)、R9 四处同改(SimRepo 接口+memory+pg)、sim.* 暗发 defaultOn:false 不回潮、端点过 requireSim entitlement 门(R3)、sim 路由无 Math.random(确定性 R6)；运行时由 `apps/datacore/test/sim-session.test.ts` 覆盖(init/tick/checkpoint/rollback/branch/确定性/R2) · `scripts/check-sim.mjs`，`pnpm sim:check`。
  - **`propagation:check`（增量 3 已建·并入 `pnpm gates`）**——test-backed 守传导核：系数×延迟正确性 + 改系数即改果 + Temporal Trust（tick 只读 ≤t 态，不窥未来）+ 确定性 R6（无 Date.now/Math.random）+ 零业务常数 R14 · `scripts/check-propagation.mjs`，运行时见 `apps/datacore/test/sim-propagation.test.ts`（propagateTick 贡献/延迟/确定性/coefficientRef）。
  - **`sim-readiness:check`（增量 2 已建·并入 `pnpm gates`）**——就绪认证 = 投影既有 closure（RL3 单源）：静态扫 `deriveCertification` 不 import closure 以外校验器 + L4 三子项全真才 CERTIFIED + `canEnterSimulation` 含 trialTick.passed∧L4∧gatePassed + 缺件入 `gaps[]` 诚实 + 全局/局部同一函数 · `scripts/check-sim-readiness.mjs`，运行时见 `apps/datacore/test/sim-certification.test.ts`（详 `docs/SPEC-sandbox-readiness-certification.md §9`）。
  - **`ui-smoke:sandbox`（增量 4 已建·门B 真后端真浏览器，聚合外独立门同 `ui-smoke`）**——起真 datacore+vite + chromium，开通 sim.sandbox → 导航 `/v/sim-sandbox` → init 会话 → 点「推进 tick」→ 断言拓扑节点数=view-config nodeTypes + curTick 推进 + 就绪面板 L0-L4/诚实 gaps；无 chromium→SKIP(exit0) · `scripts/ui-smoke-sandbox.mjs`，`pnpm ui-smoke:sandbox`。
  - `decision-page:check`（R17 配套·增量 4 建）——决策页一页看全 数据→推演→溯源→动作→AI。
  - **沙盘一页布局重构（WO-SANDBOX-LAYOUT-REWORK·Option A·治拥挤·遵 R-PRD `docs/PRD-frontend-visual-redesign.md §5`·纯前端视觉重组零链路/事件/对象改动）**：`SandboxView` 从「~12 面板等权平铺贴挤」重构为 **主体 + 渐进披露卡片栈**——整页栅格 `heroGrid` 主 7fr（hero 焦点：全局态大数 30px/700 顶栏 + 命令条[推进/存档/分支/采纳+tick heat] + 主视觉 DAG `min-height 420px` + AI 指挥台底栏）/ 右 5fr（`CollapsibleCard` 折叠卡栈：就绪认证默认展开·双雷达/运行态·风险TOP3/Schema派生/运行台Console/历史推演记录默认折叠·分支对比出现即展开）。**不删任何功能**——次要面板收进折叠卡（折叠态 `hidden` 保留 DOM·点标题真展开·所有既有入口仍可达）。密度从「12 面板」降为「1 主体 DAG + 1 展开卡 + 5 折叠标题」·信息层级清晰·留白充足（卡内 16–20 · 卡间 16 · 主右栏间 20）。牙齿 `apps/frontend-shell/test/sandbox-layout.test.tsx`（栅格 7fr/5fr + DAG 落左主区 + 渐进披露默认态 1 展开 5 折叠 + 折叠交互真展开 + 功能入口全在 DOM）· 真浏览器像素取证 `docs/evidence/SANDBOX-LAYOUT-REWORK-fde.md`（C1 密度下降/C2 功能逐项点验/C3 层级+折叠展开）。复用 f88189c 已落 CollapsibleCard/DecisionSummaryCard/WhatIfCalculatorCard（决策卡视觉规范 §2/§3·R17 锚点），不重做。
  - **单源门复用现存 `boundary-singlesource:check`**（沙盘 BASE/SEG/系数单一来源），**不新造 `ia-single-source:check`**（GROUNDING-MAP §A.2 裁决：重叠即违 RL3/RL10）。
  - **零业务常数**：沙盘 `sim/` 目录纳入 `debattery:check` 扫描（出现行业实体名即红，两行业验收 R14）。
- **优化融合门（G-12 · 增量 0 登记 · 许可证/不训练）**：
  - **`solver-license:check`（增量 0 已建·并入 `pnpm gates`）**——静态守 `THIRD-PARTY-NOTICES.md` 四红线：LIC1 不训练（优化作用域文件无 `import torch`/`tensorflow`/`from sklearn`/`.fit(`/`finetune` 训练管线调用）、LIC2 Gurobi 不碰（代码库无 `gurobipy`/`GRB.`/`grb.Model` 指纹）、LIC3 MIT 署名（NOTICES 含 MIT License 段）、LIC4 CDLA 取派生（OptModelTemplate 字面量须带 `provenance.license`）· `scripts/check-solver-license.mjs`，`pnpm solver-license:check`。已并入 `pnpm gates`。
  - **`opt-template:check`（增量 1-5 已建·并入 `pnpm gates`）**——静态守模板池/绑定层零行业实体名（5 核心抽象引擎）+ 5 核心 requiredRoles 齐 + LIC4 派生留痕在 · `scripts/check-opt-template.mjs`，`pnpm opt-template:check`。
  - **`opt-determinism:check`（增量 1-5 已建·并入 `pnpm gates`）**——静态守 CP-SAT seed+单线程（R6 字节一致）+ 绑定层稳定输入序 + embedding advisory 不入确定性求解路径（FUS2 隔离）· `scripts/check-opt-determinism.mjs`，`pnpm opt-determinism:check`。**`pnpm gates` 聚合 17→19**。
  - **`method-determinism:check`（METHOD-MC-STOCHASTIC 已建·并入 `pnpm gates`）**——平行 `opt-determinism:check`：静态守随机模拟族 MC 作用域「只吃 seeded rng（`method-mc.ts` mulberry32/`rngFromInput`）· 无非确定来源（Math.random/Date.now/new Date）· 样本 `.sort` 后取 type-7 分位」+ **根因收口断言**（`capacity.ts` P90/批次/what-if 处 `p50*healthFactor` 伪分位式零命中 + `service.ts recordCalibrationForecasts predictedP90` 不再 `*healthFactor`）· `scripts/check-method-determinism.mjs`，`pnpm method-determinism:check`（有牙齿：seed 行改 `Math.random()` → 门红）。
- **`repo-pg-notnull:check` PG 通用仓储 NOT-NULL 覆盖门（WO-P0-LOCK 根因解·并入 `pnpm gates`）**：根除一整类 P0——建表加 NOT-NULL-无默认列却让通用仓储（`PgStore` 写 `{id,tenant_id,doc}∪extraColumns`／`PgDataColStore` 写 `{id,tenant_id,data}`）去写它 → PG INSERT NOT NULL 违例直抛（内存仓储无约束故单测漏），正是 `execution_locks`（漏 extraColumns）+ `merge_candidates`/`object_merges`（用错列）翻车根因。纯静态：扫 migrations 取每表 NOT-NULL-无默认列，扫 `pg.ts` 取经通用写入的表及覆盖列集，`required ⊄ covered` 即红（自带 INSERT SQL 的自定义 store 不在范围）· `scripts/check-repo-pg-notnull.mjs`，`pnpm repo-pg-notnull:check`。
- **`css-vars:check` CSS 变量引用闭合门（WO-CSS·并入 `pnpm gates`）**：防"引用未定义 CSS 变量 → 静默回落空值/黑字"复发（由来：`InferenceProcessDag.module.css` 写 `fill:var(--text)`[全仓零定义·正确 `--txt`]→ 解析空 → 回落浏览器默认黑字 → 深底 DAG 标签不可读·typo 编译/测试期都不报只肉眼可见）。扫所有 `.css` 的**无 fallback** `var(--X)`，X 必须 ∈ 全仓自定义属性定义集（tokens.css + 各组件局部并集）；带 fallback 的 `var(--X, …)` 放行（语义明确）· `scripts/check-css-vars.mjs`，`pnpm css-vars:check`。
- **`scene-agent-config:check` 场景 agent 配置完整性门 → LLM 功能全绑定链门（WO-SCENE-C/D Phase D → WO-INTENT-MATERIALIZE-BINDING-COMPLETE 扩·防半截上架·G-3/G-9）**：让"人机对话入口配置完整"结构性可保证（详 §8 G-3）——A. 对每个出厂 SceneEntry 校验 ① mode≠WORKFLOW_ONLY（防 WO-SCENE-A 收口回潮）② AGENT_FIRST/AGENT_ONLY 必有 defaultAgentId ③ 设了 defaultAgentId → agent 须存在且 PUBLISHED ④ agent BUILTIN 工具∈注册表 + ruleBindings 合法形态；**B. 对每个一等 Intent 校验全绑定链 6 项齐**：⑤ mode 合法且=审核方钉死表 `INTENT_MODE`·status=PUBLISHED ⑥ {agent|workflow} 按 mode 存在且 PUBLISHED（workflow-first→plan PUBLISHED·agent-first→agent PUBLISHED）⑦ ontologySlice 存在（∈一等切片注册表）⑧ ≥1 evaluation 规则 ⑨ solver∈注册表 ⑩ skill 存在，且总数=20；半截配置（缺任一绑定/mode 漂移/指向缺失/草稿资产）门红——缺项跑 `POST /b/v1/intents/reconcile` 自动 scaffold DRAFT（导入 agentcore 编译产物，gates 链 `pnpm -r build` 在前）· `scripts/check-scene-agent-config.mjs`，`pnpm scene-agent-config:check`。
- **`skill-integrity:check` 方法论引用完整性 + 目录卡显式映射门（SKILL-LIB-POLISH①②·守 SKILL-LIBRARY-EVERYWHERE 引用闭合不回潮·并入 `pnpm gates`）**：补齐此前 engine.ts/seed.ts 注释宣称却不存在的命名门（实际把关仅 `skill-library.test.ts`）。导入 agentcore 编译产物静态校验两维——① **孤儿引用**：`agent.skills`/`workflow.skillRefs`/`plan.skillRefs`/`materializedIntent.bindings.skillId` 引用的每个 skillId 必存在且 `status=PUBLISHED`（指向缺失/草稿 skill 即孤儿→红）· ② **目录卡显式映射（防静默漏配）**：每张 `SCENARIO_CATALOG` 卡的 intentKey 必在 `intents/materialize.ts INTENT_SKILL` **显式**挂对口方法论——未映射即"将来新增卡静默回落 `skl_seed_capacity` 而非报错"隐患→红（`skillIdForIntent` 运行期亦对目录卡漏配抛错·非静默回落·非目录键 hardcoded 计划保留 `DEFAULT_SKILL_ID` 真实默认）。齿 `apps/agentcore/test/skill-library.test.ts`（引用完整性 4 维 + ② 漏配保护齿·revert 注入孤儿 skillRef→红/移除目录卡映射→运行期抛错+门红·亲验）· `scripts/check-skill-integrity.mjs`，`pnpm skill-integrity:check`。
- **`clarify-humanized:check` 澄清人话化门（WO-SLOT-CLARIFY-HUMANIZE·治 G-3 澄清人话化·铁律 0.4 零裸内部 key）**：遍历**全部 PUBLISHED 一等 Intent + 出厂种子 Intent 的必填槽**断言 ① 有 clarifyPrompt 或非空 description（缺→红·防新增槽回潮裸 key）② `clarifyPromptFor(slot)` 不落裸名兜底 `请提供${name}`（= 泄漏内部参数名如 demandDelta）③ 面向用户文案非整体 camelCase 内部 key。守「用户点场景卡改写问句时澄清弹裸英文参数名」整类不回潮（详 §8 G-3）。**CLARIFY-CHAIN-FIX 扩传输链段**：④ 编译产物级 `toClarificationSlot` 输出过契约 `ClarificationSlotSchema` 且携带 enumValues/objectType；⑤ 源码级两端字段对齐（orchestrator SLOT_FILLING payload 必经 toClarificationSlot·禁旧 `prompt:` 错位字段回潮；前端 Clarification.tsx 读 clarifyPrompt/渲 enumValues；taskStreamReducer 引契约 ClarificationRequiredPayload 不 fork）——守「服务端有的人话到不了用户」整类传输断点。导入 agentcore 编译产物静态校验（gates 链 `pnpm -r build` 在前·green→red 自证）· `scripts/check-clarify-humanized.mjs`，`pnpm clarify-humanized:check`。齿 `apps/agentcore/test/clarify-humanize.test.ts`（6 例·澄清优先级 clarifyPrompt??description??name + 卡改写自由路径 presetSlots 继承·revert→red）+ `clarify-transport.test.ts`（5 例·传输契约/enum/多轮/业务主键归一）+ 前端 `clarify-transport-render.test.tsx`（4 例·人话渲染/enum 真可选/多轮重渲）。
- **`solver-label-coverage:check` 答案投影人话化门（WO-ANSWER-PROJECTION-HUMANIZE·治 G-3 答案投影劣质·铁律 0.4 零裸 KPI key）**：遍历 datacore 派发单一来源 `SOLVER_REGISTRY` 的**全部 outputShape 顶层字段（47 求解器/304 字段）**断言每字段要么在 agentcore 投影登记 `FIELD_LABELS`（人话 label+单位·可 `${solverKey}:${field}` 消歧）、要么在 `META_FIELDS`（脚注非 KPI）/`STRUCTURAL_FIELDS`（投表/展开非标量）——缺即红（基线 ratchet·防新增求解器字段裸英文 key 直当 KPI label 回潮，如 S/G/s0）。守断点 G-3「答案投影人话化」：`summarizeSolverOutput`(`workflow/executor.ts`) 消费 `solver-field-labels.ts` 把裸字段键→人话 label+单位、元字段（dataMode/ruleSetVersion/confidence.*）移出 KPI 区→口径脚注、判断类问句出确定性结论叙事（`deriveConclusion`·只引输出真值不编造）、内部 id/`infeasible` 内部术语人话化。导入两服务编译产物静态校验（gates 链 `pnpm -r build` 在前·green→red 自证）· `scripts/check-solver-label-coverage.mjs`，`pnpm solver-label-coverage:check`。齿 `apps/agentcore/test/answer-projection-humanize.test.ts`（11 例·裸 key revert→red·元字段不进 KPI·结论叙事在·数字红线）。
- **`no-silent-mock:check` 求解器诚实位全覆盖门（WO-DM keystone·并入 `pnpm gates`）**：导入编译产物断言**每个 SOLVER_KEY** 输出形状含 `dataMode`（LIVE/MOCK/PARTIAL），根除"求解器静默哈希/魔数冒充真算"整类（green→red 自证）· `scripts/check-no-silent-mock.mjs`，`pnpm no-silent-mock:check`。
- **`no-fake-data:check` 决策路径假数据残口门（FAKEDATA-SWEEP·守 R-KILL-MOCK-RED/铁律0.6·并入 `pnpm gates`）**：确定性纯静态扫决策路径（`apps/datacore/src/solvers/**` + `apps/frontend-shell/src/views/**`）中 `hashString(...)` 派生**业务数值**（`%N` 造数/入算式）的残口——合法（结果进 id/version/bucket 非业务量）放行；诚实标注合成（±30 行内带 `dataMode/诚实/无实测/合成/MOCK/PARTIAL` 等诚实位）计入棘轮基线 `scripts/no-fake-data.baseline.json`（补真源子 WO 在办·非红）；裸冒充（既非合法又无诚实标）= SUSPECT 红。门只在**基线外新 SUSPECT/LABELED**时红（防新增哈希冒充真决策值回潮·守断点 G-DM-1）。F1 `FILL-AUDIT-TIMELINE-REAL` 治本后 `auditTimeline` 不再 hashString 造曲线 → 基线清空（0 LABELED）· `scripts/check-no-fake-data.mjs`，`pnpm no-fake-data:check`（`--update` 诚实更新基线）。**WO-FAKE-05 堵根 C1（audit `docs/AUDIT-solver-fake-residues.md` 门盲区①）**：SMELL 信号从只守全局 `hashString(` 扩为**任意 hash 命名函数**（`\w*[Hh]ash\w*(`）参与派生业务数值——逮**本地 hash**（如 R4 已删的 `riskHashN(base)%N` 造"谁负责"/占用比·FAKE-03 已治→防复发）；`createHash/digest`（crypto 去重非业务量）入 LEGIT 豁免·注释行不算残口；内置 gate自证（`mustCatch` 本地 hash 造数、`mustPass` crypto/分流 bucket·牙齿钝则门自 exit2）。牙齿实证：注入 `riskHashN(base)%N` 造业务值 → 门红·revert → 绿。
- **`experiment-determinism:check` 决策 A/B 分流确定性门（WO-EXPERIMENT ④·并入 `pnpm gates`·守 R6/R2）**：静态哨兵 + test-backed 守冠军-挑战者实验分流核（`solvers/service.ts chooseArm/routeExperiment`）：① **同请求键恒同臂（R6）**——chooseArm 必须用 `hashString(...)` × `canonicalJson(args)` × `splitPct` 选臂（hash·非随机），实验路由方法体内**无 Math.random/Date.now**；② **关实验=零影响既有**——routeExperiment 仅取 `status==="RUNNING"` 实验、无 RUNNING 返 undefined（不分流）；③ **R2 租户隔离**——以 tenantId 维度读 `solverExperiments`；④ 行为背书跑 `apps/datacore/test/experiment.test.ts`（两臂各约半 + 同请求键恒同臂 + conclude 落胜方 + 关实验不分流 + 403/400）· `scripts/check-experiment-determinism.mjs`，`pnpm experiment-determinism:check`。已并入 `pnpm gates`。
- **`retention-coverage:check` 留存覆盖门（WO-RETENTION ⑤·守 R-RETENTION 不变量不回潮·并入 `pnpm gates`）**：守"每张无界增长表都在留存策略注册表有项 + 由 RETENTION_SWEEP 真清理"——否则长跑爆库 / 漏配某表永久增长（正是 `outbox_events`/`ts_points`/`scheduler_runs` 此前无上限的根因·断点 G-RET）。纯静态：① 契约 `RETENTION_DEFAULTS`/`RETENTION_TABLES`（`contracts/actions.ts`）单一来源覆盖所有受治理增长表 ② 已知增长表清单 `GROWTH_TABLES` ⊆ 注册表（新增增长表漏登即红）③ `retention.ts sweepTable` 对每张注册表都有删除分支（注册了却不清理 = 假覆盖）④ `app.ts` 已 `.on("RETENTION_SWEEP", …)` 接线 + boot 注册每日 cron · `scripts/check-retention-coverage.mjs`，`pnpm retention-coverage:check`。
- **`audit-actor:check` 统一审计 + 跨服务追踪门（WO-AUDIT-OBS·守 R-AUDIT/append-only/R9·并入 `pnpm gates`）**：保守静态哨兵——① `adminplatform.ts` 每条 admin 变更经统一 `audit(c,…)` 写入器（首参为 ctx，带 actor=c.userId）且经 `AuditService.record`，无旧裸 `outbox.emit(tenantId,event,payload)` 审计形态复活；`features.ts saveConfig` 经 `auditSvc.record(ctx,…)`；`audit.ts` 同时 `auditLog.put`（append-only）+ `outbox.emit`（向后兼容）② 审计日志 append-only：仅 `GET /a/v1/audit-log`，无 PUT/DELETE/PATCH/POST 该路径（结构性 R13）③ R9 `audit_log` 四处同改（migration029 + repo.ts 接口 + memory.ts + pg.ts）④ 跨服务追踪：agentcore `tools/datacore-http.ts` 透传 `x-request-id: ctx.requestId`，datacore `app.ts genReqId` 优先取入站 `x-request-id` ⑤【WO-ENTERPRISE-DR-AUDIT·sub-B】`audit-sink.ts` **只读消费** `audit_log`（仅 `auditLog.list`·**禁 `auditLog.put/remove`**）——SIEM sink 不得新增绕过 `AuditService` 的审计写路径（保 append-only 单一来源不被 sink 破坏）。运行期由 `apps/datacore/test/audit-log.test.ts` + `test/audit-sink.test.ts` 守 · `scripts/check-audit-actor.mjs`，`pnpm audit-actor:check`。
- **`tracing:check` 全链 OTel span 树门（WO-OBSERVABILITY OBS-2·守分布式追踪不回潮·并入 `pnpm gates`）**：在 WO-AUDIT-OBS 的 requestId spine 之上加 W3C `traceparent` 分布式 trace（互补·非替换），守断点 **G-15「可观测性 span 树」**。保守静态哨兵：① 两服务各有 `apps/{datacore,agentcore}/src/tracing.ts`（`NodeSDK`+auto-instrument http/fastify/pg），含 **no-op 诚实降级**分支——**未配 `OTEL_EXPORTER_OTLP_ENDPOINT` → `exporting=false` 不接 exporter 不导出**（不假装），`service.name`=datacore/agentcore · ② `tracing.ts` 在 bootstrap（`server.ts`/`main.ts`）被**第一个 import**（先于 http/fastify/pg 被 require 完成 monkey-patch）· ③ 出站 OBO（`tools/datacore-http.ts`）**双轨**：保留 `x-request-id: ctx.requestId`（不破 AUDIT-OBS）+ `injectTraceContext` 注入 traceparent；开 `obo.datacore` 自定义 span · ④ 自定义业务 span 在位：`solvers/service.ts invoke`→`solver.invoke`（attr solverKey/dataMode/tenantId R2）+ `outbox.ts emit`→`outbox.emit`（attr event/tenantId）· ⑤ **no-secrets-echo R5**：span attr 禁凭据明文（tracing.ts 注释钉牢 + OBO/solver/outbox span attr 抽查无 token/apiKey）· ⑥ 采样 dev `ParentBased(AlwaysOn)`/生产 env ratio。运行期由 `apps/datacore/test/tracing.test.ts`（no-op 分支 + in-memory span 树真证 root→solver.invoke→outbox.emit parent 链 + attr 关联 + 无凭据）与 `apps/agentcore/test/tracing.test.ts`（no-op + root→obo.datacore + traceparent 双轨）真跑守 · `scripts/check-tracing.mjs`，`pnpm tracing:check`。**边界**：仅 traces 信号（OTel metrics/logs 不做·现 `Metrics` 类够用）；docker-compose `otel-collector`+`jaeger` 为 profile-gated（`--profile observability`·不影响默认 up）。
- **`solver-binding-determinism:check` SolverBinding 绑定层门（WO-SOLVER-ONTOLOGY-BINDING·守 B3/G-17 命门不回潮·并入 `pnpm gates`）**：守「canonical 业务求解器 role→租户真实类型/字段经 SolverBinding 解析·无绑定回退 canonical 默认·DF.8 接地」。保守静态哨兵：① 回退一致性——`solvers/solver-binding.ts` 的 `resolveSolverType` 必须有「无绑定→canonical 默认」回退分支（否则 demo 零绑定即回归·P0 红线），`SOLVER_CANONICAL_TYPES` 须含 `order_fullchain.order='Order'` + `context`（loadContext 拓扑回退）· ② DF.8 接地——导出 `assertSolverBindingGrounded`（落库前挡本体外类型/字段）· ③ 无残留硬编码——`service.ts` 的 `orderFullchain` 方法体内不得再出现 `listByType(…,"Order"/"Model"/"DemandSegment"/"MaterialBalance")` 字面量（须经 `resolveSolverType`/`T(role)`），且 `loadContext`/`probeTypes` 经 `resolveSolverType(idx,"context",…)`· ④ 缓存失效——`service.ts` 有 `invalidateBindingCache`（绑定写路径清缓存避 stale）。运行期由 `apps/datacore/test/solver-binding.test.ts` 8 用例真跑守（realco 真上传出真答案/R6 确定性/demo 零绑定回退不回归/DF.8×2/回退一致性/R14 两行业/RL4 自动草案）· `scripts/check-solver-binding-determinism.mjs`，`pnpm solver-binding-determinism:check`。
- **`no-orphan-source:check` 无凭空源门（WO-SOURCE-TRANSPARENCY·守 R-NO-ORPHAN-SOURCE·并入 `pnpm gates`·G-13 收口）**：立"每个合成/物化对象必可溯回一张 RawDataset（数据连接器页可见、可下载 Excel）"的结构不变量，根除"合成对象凭空落库、绕过可见源"的走捷径。门体 = 真种电池合成数据（走"合成源→RawDataset→物化"正门）跑 `auditNoOrphanSource` 断言 0 凭空 + **green→red 自证**（测试内故意植入无 `rawDatasetId` 的凭空对象 → 审计必抓出 `MISSING_RAW_DATASET_ID`；rawDatasetId 指向不存在数据集 → `DANGLING_RAW_DATASET`）+ 导出端点 `GET /a/v1/raw-datasets/:id/export?format=xlsx|csv` 真 .xlsx（node-xlsx 解回见表头+真业务行·行数=数据集行数）/.csv（真行非模版）回归 + 合成源文件名带 `.synthetic`（R13 诚实）+ R2 跨租户 404。诚实豁免 MANUAL/META/时序（派生非源）· `scripts/check-no-orphan-source.mjs`（→ `apps/datacore/test/no-orphan-source.test.ts`·审计 `apps/datacore/src/no-orphan-source.ts`），`pnpm no-orphan-source:check`。前端「数据连接器」页（`ConnectionsPage`）露每连接**全部**数据集 + 逐张行预览 + 「下载 Excel/CSV」+ 合成源 SYNTHETIC 徽标常驻（透明 ≠ 冒充真实）。
- **`pipeline-freshness:check` 决策置信度三维贯通门（WO-FRESHNESS·并入 `pnpm gates`·守 R13 三维不回潮）**：把 no-silent-mock 的二维诚实位（实测↔估算 LIVE/MOCK/PARTIAL）扩到**三维**——守"关键数据源新鲜度 + 真实↔合成 必须接进决策置信度，缺即红"。保守静态哨兵：① 契约 `SolverDataModeSchema` 含追加枚举 `STALE`/`SYNTHETIC`（不破既有 LIVE/MOCK/PARTIAL 消费）+ `SolverConfidenceSchema` 三维（synthetic/stale/measurement）· ② `service.ts invoke` 统一叠加 `applyConfidenceDimensions`：关键源 `dataHealth.critical` 新鲜度 `lagHours>staleHours`（C09 跨求解器化）→ **STALE**；纯合成对象决策（`isSyntheticDecision`·origin=SYNTHETIC）→ **SYNTHETIC**（与 `domainTrustLevel=UNVERIFIED` 同源）；MOCK 不被横切维洗白（最审慎优先）· ③ 前端 `DataModeBadge` 标 STALE/SYNTHETIC（「基于 N 小时前 / 合成数据(非真实接入)」）+ `RiskBoardView` 消费 `confidence` 显三维。运行时行为由 datacore `solvers.test` WO-FRESHNESS①②③（合成→SYNTHETIC·关键源滞后→STALE·MOCK 不洗白·R6）真跑覆盖 · `scripts/check-pipeline-freshness.mjs`，`pnpm pipeline-freshness:check`。
- **`datadep-manifest:check` 推演入口数据依赖清单门（DATADEP-MANIFEST-READINESS 脊·数据补齐拱心石·输入侧覆盖门·并入 `pnpm gates`·守 R6/R11/R14·镜像 DF.6 到输入侧）**：立"**每个推演入口必须声明声明式数据依赖清单（DataDependency）**"的结构不变量（precondition-first），与输出侧 `no-silent-mock`（`SOLVER_OUTPUT_SHAPES` 覆盖每 SOLVER_KEY）对偶——输出侧有覆盖门、输入侧此前一无。四道断言：① **输入侧覆盖**——每个 `SOLVER_KEY` 要么在 `SOLVER_DATADEP`（`@platform/contracts` datadep.ts）声明清单（读对象图的 context 求解器），要么在 EXEMPT（arg-only 优化族/净室通用/读非 SolverContext 对象·文档化豁免），两者皆非即红（防漏声明·run-first 回潮）· ② **无孤儿/悬空**——清单 key ∈ SOLVER_KEYS、`roleType` ∈ `ROLE_CANONICAL`（可解析真类型）、`minRows≥1` · ③ **loadContext 治本自证**——`uncoveredContextRoles()==[]`（每个 `CONTEXT_ROLES` 上下文角色被某清单覆盖）→ loadContext 迭代 **CONTEXT_ROLES ∩ ⋃(清单.requires.roleType)** 加载对象（**读清单并集·非写死 22 类**·杀 Hα/Hγ 硬编码），并集==全 22 角色故字节一致（R6）；删掉某上下文角色的唯一清单覆盖 → 此断言红（= loadContext 会漏加载该角色·求解器读空 → 治本非再加一层写死的牙齿）· ④ **R14 零业务常数**——`roleType` 全为抽象角色（经 SolverBinding「context」域解析真类型·非基地/细分业务实例名·换行业 0 码改·清单入 IndustryPack）。运行期由 `apps/datacore/test/datadep-manifest.test.ts`（loadContext 读清单并集 + R6·checkReadiness 空租户诚实缺口/已播种就绪·present-vs-needed）真跑守 · `scripts/check-datadep-manifest.mjs`（导入 datacore + contracts dist），`pnpm datadep-manifest:check`。green→red 自证：摘一个入口清单 → 红。
- **`industry-pack:check` 行业包目录约定门（INDUSTRY-PACK-CONVERGE·P1 北极星·并入 `pnpm gates`·守 R-PACK/R14/R6）**：立"**换行业=加一个 `packs/**` 文件·0 码改**"的结构不变量。`scripts/check-industry-pack.mjs`（导入 datacore dist + contracts dist）五道断言：① **目录派生**——`PACK_REGISTRY` 键集 === `packs/` 目录扫描键集（`scanPackFiles`·运行期 `readdirSync`+动态 `import()`/`readFileSync`+TLA 构建·非手工数组）· ② **源码无手工登记**——`industry-pack.ts` 无 `PACK_REGISTRY=[…]` 字面数组且确用 `buildRegistry`/`readdirSync` · ③ **文件名===industryKey**（目录约定·无映射漂移）· ④ **battery/logistics 存在且过 `IndustryPackSchema`** · ⑤ **0 码发现活证**——落一个 `.pack.json` 到 packs 目录 → 同一 `scanPackFiles` 立即发现 → 复位（证加一个文件即被 auto-discover·既有代码零改·非纸面）。运行期由 `apps/datacore/test/industry-pack.test.ts`（C2 目录派生 + 探针 + battery byte-identical·C3 logistics 非电池视图/场景 pack 驱动）真跑守。
- **`no-fake-done:check` 自我账本无诈门（PRD-trustworthy-self-accounting WO-1·并入 `pnpm gates`·守 R-NO-FAKE-DONE·闭 G-16）**：让"完成"构造即真——平台对外卖"真实数据→本体→规则→求解器→答案，缺了诚实报缺口"，对自己却用**手打状态字**账本（`docs/work-queue.json status:"DONE"`，`collab-queue.mjs done` 只 `set(status)`、从不跑 acceptance / 查制品）。`scripts/check-no-fake-done.mjs`（纯静态·无需起服务·R6）遍历每张 DONE 自带的 `acceptance`——assert 是**自然语言散文**非可执行码，故门不直接 curl 跑，而是**从 assert/run 抽取被引用制品锚**（当前口径：求解器 key·正则 `/a/v1/solvers/<key>/invoke`）对**真实制品集** DataCore `SOLVER_REGISTRY`（单一来源·静态读源码）做**存在性校验**：引用幽灵求解器（如 `MULTISRC-FUSION` 6 条 curl 全指零命中的 `source_conflict`，真 key 为 `multisource_fusion`）→ 标 **FAKE**；DONE 但 `acceptance` 缺失/`criteria` 空 → 标 **UNVERIFIABLE**（同 FAKE 处置）。**棘轮基线**（仿 `debattery:check`）`scripts/no-fake-done.baseline.json` 诚实记当前已知诈账/无验证 DONE（非洗白·每条注明待 WO-6 回炉重置为开放缺口），门只在**基线外新 FAKE/UNVERIFIABLE**（或基线该缩未缩）时红——**新 DONE 引幽灵制品/空验收即红**（不得进基线·green→red 自证）。查出 FAKE 报 `selfaccount.fake_detected`（§4 L15·NOTIFY）。**诚实边界**：本门静态守求解器 key 存在性；更广端点/契约引用 + "代表问经 QOS NL 真跑答出"运行时 E2E 判据属 WO-2（Capability 一等对象 + verifiedStatus 派生）。
- **`solver-coverage:check` 求解器覆盖矩阵门（WO UPG-L0-SOLVER-COVERAGE·PRD-upstream-classify-precision §5.1·治「没有对的求解器」·并入 `pnpm gates`·对齐 no-fake-done 精神防幽灵·R14）**：`SOLVER_COVERAGE`（`@platform/contracts` solver-coverage.ts·「问题类目→path-A 求解器」诊断矩阵·纯数据·problemClass 键为**抽象分析原型**归因/趋势/对比/约束求解/…非业务字面量）声明覆盖关系；本门读 contracts + datacore 两侧编译产物断言 ① 矩阵引用的**每个 solverKey 必 ∈ datacore `SOLVER_REGISTRY`**（防幽灵 key·green→red 自证：植入未注册 key 即红）② 已覆盖类目 ∩ `UNCOVERED_PROBLEM_CLASSES`（诚实缺口）=∅ ③ **未覆盖类目显式列出**（剩 temporal_trend_decomposition/anomaly_detection/text_semantic_extraction·非静默落 Path B）驱动 A3 覆盖补齐优先级。**WO UPG-L0-COVERAGE-FILL 已补齐高频缺口 general_causal_attribution**：原 PRD §2.2「全表无通用因果归因 path-A」→ 现由通用 `causal_attribution`（route=graph·上文 §3.6）覆盖（`SOLVER_COVERAGE.general_causal_attribution=[causal_attribution]`·从缺口清单移出·NL 经 **S27 及 S03 原痛点问句**path-A 真跑答归因链）。datacore `apps/datacore/test/solver-coverage.test.ts`（C1·可同时 import 两侧）双守 key∈registry。`scripts/check-solver-coverage.mjs`，`pnpm solver-coverage:check`。**WO UPG-L0-SOLVER-COVERAGE C3（覆盖矩阵接进运行时·可观测缺口）**：`INTENT_PROBLEM_CLASS`（solver-coverage.ts·意图键→分析型问题类目·值必 ∈ `SOLVER_COVERAGE` 键∪`UNCOVERED_PROBLEM_CLASSES`·未登记回退 `unknown_intent`）+ 运行时指标 **`qos_pathb_by_problemclass{class}`**（`metrics.ts` Counter·在 `orchestrator.runPathB` 单一收口 `.inc({class})`）——把「哪些**问题类目**在往未验证 Path B 兜底落」可观测；未覆盖类目（`isProblemClassCovered=false`）额外发 `step.completed{type:coverage-gap}` 事件显式报缺口（非静默兜底·驱动 A3 补齐）。注：AGENT_FIRST 意图经其绑定 agent 直达（有意为之·非缺口）不计入此计数。牙齿：`test/coverage-fill-classify-fuse-rework.test.ts`（live Path B → 计数出数 + `/metrics` 文本导出）+ `solver-coverage.test.ts`（INTENT_PROBLEM_CLASS 值域/回退/归口）。
- **`hidden-req-keys:check` 隐藏需求不造假 key 门（WO UPG-L0-HIDDENREQ·PRD-gap-analysis-engine §8·并入 `pnpm gates`·守 R6/R14·闭「隐藏需求造假 key」）**：立「**隐藏需求发现绝不造假 key**」结构不变量——`expandHiddenRequirements`（`@platform/contracts` databuilder.ts·纯函数 R6·三白名单 by-construction）**能产出的每个 key 必 ∈ 三张真实白名单**（本体图真类型 `graph.nodes` / 真链路 `graph.edges.label` / `SOLVER_DATADEP` 真求解器 key），弃 PRD 早期草案「Order→BOM 硬编码 + `capacity_loss` 幽灵 key」；改读系统已有 `SOLVER_DATADEP`（§2·脊）依赖闭包（B-Phase1）+ 沿租户真实本体图一跳（B-Phase2·`GET /a/v1/ontology/graph`），每 key 过 `filter(has)` 白名单外进不来（无真扩展路径→诚实空集·不编）。三道断言：① **存在性对账（真跑）**——真 `SOLVER_DATADEP` + 合成图 + **对抗性显式需求（塞 `capacity_loss`/`production_line`/`GHOST_TYPE_ø` 幽灵）** 跑真函数，输出（objectTypes/solvers/links + trace.added）逐个 ∈ 对应真注册表·幽灵被剔除；② **测谎自证（green→red 有牙齿）**——往输出副本注入幽灵 `capacity_loss` → 同一校验器 `keysOutsideRegistries` 必抓出（否则校验器无牙→门红）·亲验：往函数体注 `outTypes.add("GHOST_LEAK")` 绕 filter→门红 exit1、revert→绿；③ **源码守卫**——函数体三白名单守卫（`typeKeys.has`/`solverKeys.has`/`linkKeys.has`）在位 + 无被弃用幽灵字面量。暗发 `growth.hidden_req`（datacore `features.ts` + agentcore `features/registry.ts` 同键 defaultOn:false·RL2）——关 = `preAnalyzeQuery` 只诊断显式需求（== 不做·回退演练 C3）。整合入既有预分析流（`agentcore/src/growth/pre-analyze.ts foldHiddenRequirements`·非并行引擎）·齿 `packages/contracts/test/hidden-req.test.ts`（V8·每 key∈注册表·R6 字节一致·幽灵过滤）+ `apps/agentcore/test/pre-analyze.test.ts`（C3 flag OFF=仅显式·ON 折叠真类型）· `scripts/check-hidden-req-keys.mjs`，`pnpm hidden-req-keys:check`。
- **`workflow-dag:check` ExecutionGraph 契约不变量门（L1-B WO-L1B-1·PRD-L1B-execution-planner-workflow-runtime §0.6/§3/§7·并入 `pnpm gates`·守 R1/R6/R13）**：立「**执行图 IR（`ExecutionGraph`）契约不变量 + 线性 lift 往返无损**」结构不变量——`@platform/contracts` execution-graph.ts 落 `ExecutionGraph`（一等执行图 IR·Task Graph+Dependency Graph·`TaskNode` **内嵌复用 `PlanStepSchema`**·执行器派发零改）+ `Transition`/`Gateway`（一等边模型·补"现线性 steps 数组无边"SUPPLEMENT_Ch48:50）+ `CompensationAction` + `WorkflowDagRun`/`WorkflowDagNodeState`（durable 运行态·镜像 `BuildWorkflowRun`/`BuildWorkflowStep`）+ 纯函数 `fromLinearPlan`/`toLinearSteps`（线性 lift·R6 可逆·`generatedAt`/`plannerVersion` 注入·内部无时钟/随机）+ 校验器 `validateExecutionGraph`。五道断言（牙齿自证·green→red）：① **DAG 无环**（Kahn·检出环即红）② **`TaskNode.step.type` ∈ `PLAN_STEP_TYPES`**（不引幽灵步·qos.ts:105 单一来源）③ **引用完整 + `invoke_solver` solverKey ∈ `SOLVER_REGISTRY`**（复用 chain:check 同源白名单·datacore dist）④ **线性 lift 往返无损**（`toLinearSteps∘fromLinearPlan ≡ steps`·单步/多步边界·R6 双跑字节一致）⑤ **契约漂移守**（合法 lift 产物必过 `ExecutionGraphSchema.parse` + 校验器·非法图必被逮·校验器钝则门自 exit1）。门内自造合法/非法夹具（环/幽灵步/幽灵solver/非纯链扇入/悬空前驱）用被测校验器跑（同 hidden-req-keys:check 牙齿范式）。**暗发双闸**（本 WO 仅落契约 + 开关·尚未接线执行器/编排·additive 可回退）：agentcore config.ts `QOS_WORKFLOW_DAG`（=1 走 DAG 执行器·WO-2 起接线）+ `QOS_EXEC_PLANNER`（shadow/serve·规划器影子·WO-4 起接线），缺省全 OFF = 改造前系统。牙齿实证：中和 `validateExecutionGraph` 环检测 → 门红「牙齿钝：注入环未被逮」·revert → 绿。齿 `packages/contracts/test/execution-graph.test.ts`（契约形状 + round-trip 单步/多步/R6 + 校验器环/幽灵步/幽灵solver/悬空/入度0·16 例）· `scripts/check-workflow-dag.mjs`，`pnpm workflow-dag:check`。
  - **WO-L1B-2 已落（DAG 执行器 + 执行器派发接线·暗发·§2.1）**：`apps/agentcore/src/workflow/dag-executor.ts` `runWorkflowDag`——**Kahn 拓扑并行**（就绪队列按 nodeId 稳定序·波次 `Promise.all` 并发·扇入等全前驱 settled）+ **条件 Gateway 确定性择支**（EXCLUSIVE 首个命中支激活·未命中支 SKIPPED 沿子树剪枝传播；PARALLEL/INCLUSIVE·守卫纯值比较无 LLM·R6）+ **步级重试幂等守卫**（`create_action_draft` 出站非幂等一律不重试·防二次出站·R4）。复用 `executor.ts` 抽出的共享执行核 `executeStep`/`finalizeAnswer`/`mergeStepEffects`（步派发/render 投影/规则拦截零重造）——中枢链 `Plan→Step*` 段的**串行 for-loop（executor.ts:117 簇① 根病）旁**加 DAG 执行器（旧 `runWorkflow` 永不删·additive）。`engine.ts runWorkflowSteps` 加派发（`QOS_WORKFLOW_DAG=1`→纯线性 lift 或显式图走 `runWorkflowDag`·缺省 OFF 走串行·**纯线性图逐字节 parity**·真起服务真跑证 DAG-on↔串行答案字节一致）。**durable checkpoint 续跑 / 补偿回滚留 WO-L1B-3**（本 WO 只做内存态并行执行器）。齿 `apps/agentcore/test/workflow-dag.test.ts`（C1 并行扇入 / C2 Gateway 择支 + 重试幂等 / C3 影子 parity / C4 R6 双跑字节一致）。
  - **WO-L1B-3 已落（durable checkpoint 续跑 + 补偿反向序·移植 build 侧 `BuildWorkflowEngine`·G-8⑤/G-11·暗发·§2.3/§4.4/§4.6）**：`workflow/checkpoint.ts` 加 `DurableWorkflowCheckpointStore`（**保留 `NoopWorkflowCheckpointStore`/新 `NoopWorkflowDagCheckpointStore`·关闸回退**）；`workflow_dag_runs` 表（**migration 015 + pg.ts + memory.ts + repos.ts `WorkflowDagRunRow` 接口·R9 四处**·R2 tenant 谓词·`check-repo-pg-notnull` 绿）；`dag-executor.ts runWorkflowDag` 每波次 DONE 落 `WorkflowDagRun` 快照（per-node checkpoint=步 effects + stepOutputs 全局快照）+ `resumeWorkflowDag`（跳过 DONE 节点从就绪集重驱动·`resumedCount++`·**续跑答案逐字节等价**·R6·真崩溃 `stopAfterNode` 镜像 `stopAfterStep`）+ **补偿反向拓扑序**（NOOP 跳过·REVERSING_ACTION 反向步经 **S2 Action 审批**·R4·不可逆步 `COMPENSATED=false` 诚实记录不伪装·KILL-MOCK）；`ops/sweep.ts sweepResumableDagRuns` 续跑分支（flag 开走 resume·关=空表·`INTERRUPTED_BY_RESTART` 不变·C3）；端点 `GET /b/v1/workflow-dag/runs/:runId`·`POST …/:runId/resume`（admin·R-AUDIT）·`GET /api/v1/queries/:taskId/execution-graph`（暗发·`qos.workflow_dag` entitlement 门·关=404·先于 authz·datacore features + agentcore `features/registry.ts` 双注册·`defaultOn:false`）。**内部域事件**（非 SSE·经 outbox·镜像 `buildworkflow.*`）：`workflow_dag.checkpoint_saved`/`.run_resumed`/`.step_retry`/`.compensating`/`.compensated`。engine.ts 派发处注入 durable store + 补偿钩子（QOS_WORKFLOW_DAG=1·缺省 Noop·可回退）。真起服务真跑证（`docs/evidence/WO-L1B-3-fde.md`）：真崩溃续跑逐字节 + 补偿反向序 + 回退两态 + migration down/up 幂等 + R2 404。齿 `apps/agentcore/test/workflow-dag-durable.test.ts`（C1–C4）+ `workflow-dag-endpoints.test.ts`（真 inject·门 404/R2/续跑）。
  - **WO-L1B-SAGA 已落（跨系统 Saga 一致性·外部幂等键 + 对账补偿 + 部分失败重放·簇① V2-7-142/V3-1-039·NG2/R4·暗发·§8）**：承 WO-L1B-3 补偿（本系统内可逆步）之上，为**出站到外部系统（MES/ERP/WMS）的步**加跨系统三件套（对象详 §2.H「跨系统 Saga」）——`workflow/saga.ts`：① `deriveIdempotencyKey`（R6 纯派生·`tenantId+taskId+nodeId+净荷 canonical 哈希`·同键重放绝不二次出站）；② `runOutboundSaga`（顺序出站·**先对账后 submit**·真失败即停 `PARTIAL_FAILURE`·记 `failedAtNode`·重放跳过已落地 `SKIPPED_ALREADY_APPLIED`·`resumedCount++`）；③ `reconcileAndCompensate`（**反向拓扑序·只补偿确证落地步**·未落地/已反转不盲反转·反向动作经 **S2 `CompensateHook`·R4·`applyReversal` 标外部账本 REVERSED**·不可逆/UNCERTAIN 诚实 `compensated=false` + run→`RECONCILIATION_PENDING`·不伪装）。**真外部沙盘 `ExternalSystemSandbox`**（**非 mock 回显**·真有状态账本按键真去重[第二次同键 submit 返首次回执·`applyCount` 不增]·`injectSubmitFault` 驱真部分失败·`injectReconcileFault` 驱真对账不可达·真 `reconcile` 查落地态·`activeKeys`/`reversedKeys`/`appliedCount` 供断言账本一致·承 WO-ACTUATE "真 stub·诚实 NOT_CONFIGURED" 范式）。契约 `@platform/contracts` saga.ts（`ExternalSystem`/`OutboundSagaStep`/`IdempotencyRecord`/`ReconciliationOutcome`/`SagaStepOutcome`/`SagaCompensationOutcome`/`SagaRunState`·R2 tenant·index 追加）。**暗发双闸**（关闸=saga path 不启用=现行行为字节一致·NG6·additive 新模块不接热路径·可证回退）：env `QOS_WORKFLOW_SAGA`（config.ts·镜像 QOS_* 命名）+ entitlement `qos.workflow_saga`（**datacore features.ts + agentcore `features/registry.ts` 双注册·`defaultOn:false`**·防"未注册键恒真陷阱"·L1B-3/L1B-4 都栽过此坑）。内部域事件（非 SSE·审计）：`saga.step_submitted`/`.step_skipped`/`.step_failed`/`.run_settled`/`.compensated`。真跑证（`docs/evidence/WO-L1B-SAGA-fde.md`）：真部分失败→幂等重放→对账补偿一致·外部账本零双落地·不可逆诚实 false·对账 UNCERTAIN 挂起。齿 `apps/agentcore/test/workflow-saga.test.ts`（R6 幂等键确定性·C1 真部分失败→幂等重放→零双落地+真去重·C2 对账补偿反向序+S2 REVERSED/不可逆 false/UNCERTAIN 挂起·C3 契约对账·7 例全绿）。
- **`requirement-graph:check` 需求图不造假节点门（WO-L1A-2·PRD-L1A-requirement-graph-engine §4.2/§7·并入 `pnpm gates`·守 R6/R11/R14·闭「需求图造假节点」）**：立「**Graph Builder（`buildRequirementGraph`·agentcore `growth/requirement-graph.ts`·纯函数 R6·AST→RequirementGraph 八段 Pipeline）产出的每个 object/solver/data 节点必 ∈ 三张真实白名单**」结构不变量（by-construction·越界节点丢弃+记 gap·零幽灵）：① `object.ontologyType ∈ 发布类型`（本体图 `graph.nodes`·独立于 AST·防「AST 自证幽灵」穿透）② `solver.solverKey ∈ datacore SOLVER_REGISTRY`（`REGISTRY_SOLVER_KEYS`·门层真牙齿）③ `data.roleType ∈ DATADEP_ROLE_CANONICAL`。**复用不重造**（NG1）：求解器候选←`SOLVER_COVERAGE[problemClass]`、数据依赖←`SOLVER_DATADEP.requires`、角色→类型←`DATADEP_ROLE_CANONICAL`、隐性需求←L0 `expandHiddenRequirements`（不新造引擎）、切片目标←`deriveSliceTargetCandidates`、本体边←`HiddenReqGraph.edges` 真 LinkType（断链止步·kind 取 label 语义·真 label 存 `reason` R13）。下游 I/O 投影（§4.3·G3·喂 L1-B/slice/solver）：`solverCandidates`/`dataRequirements`/`sliceTargets` 纯派生。四道断言：① **存在性对账（真跑 builder）**——真本体图+**对抗性 AST（塞幽灵 `GHOST_TYPE_ø` ontologyType）**跑真 builder，节点逐个 ∈ 三真白名单·幽灵被剔除+记 gap；② **测谎自证（green→red 有牙齿）**——往输出副本注入幽灵 solver/type/role → 同一校验器 `nodesOutsideRegistries` 必全抓出（否则门红）·亲验：把 builder `publishedTypes.has` filter 改 `if(false)` 绕过→门红 exit1、revert→绿；③ **契约层白名单 ⊆ 真 registry**——`VALID_SOLVER_KEYS`（`coveredSolverKeys ∪ SOLVER_DATADEP` 键）全 ∈ `REGISTRY_SOLVER_KEYS`（防契约层白名单自身含幽灵）；④ **源码守卫**——builder 函数体三白名单 filter（`publishedTypes.has`/`VALID_SOLVER_KEYS.has`/`VALID_ROLE_TYPES.has`）+ `expandHiddenRequirements` 复用在位·契约字段（ontologyType/roleType/solverKey/solverCandidates/dataRequirements/sliceTargets/coverageScore）无漂移。暗发（WO-L1A-3 接线）`QOS_REQUIREMENT_GRAPH`（env·热路径构图）+ `growth.requirement_graph`（entitlement·读端点·defaultOn:false·RL2）——WO-L1A-2 只落 builder 纯函数+门·不接线（旁路挂载归 WO-L1A-3）。**WO-L1A-3 已接线（观察态·暗发·可回退）**：orchestrator `buildRequirementGraphSideband`（classify 落库后、τ 决策前旁路·`try/catch` 全吞·不改判决/路由/answer·emit `step.completed{stepId:"requirement-graph"}`）+ `requirementGraphs` repo（R9 四处：repos.ts/memory.ts/pg.ts/migration `014_requirement_graphs.sql`·独立表非搭车 PreAnalysisReport 避 lost-update）+ 读端点 `GET /b/v1/queries/:taskId/requirement-graph`（entitlement `growth.requirement_graph` **双注册** agentcore `features/registry.ts` + datacore `features.ts`·defaultOn:false·R2 跨租户 404）。双闸独立回退：关 `QOS_REQUIREMENT_GRAPH`=旁路不执行·pipeline 字节一致；关 `growth.requirement_graph`=端点 404。齿 `apps/agentcore/test/requirement-graph-builder.test.ts`（C1 三白名单·C2 幽灵剔除·C3 下游投影逐值对账·C4 R6 双跑字节一致+隐性需求复用 L0）· `scripts/check-requirement-graph.mjs`，`pnpm requirement-graph:check`。
- **`execution-planner:check` 计划综合不造假门（WO-L1B-4·PRD-L1B-execution-planner-workflow-runtime §4.1/§7/§8·并入 `pnpm gates`·守 R6/R11/R13·闭「计划综合造假 / 非法综合图 / 非确定性择优」）**：立「**`synthesizePlan`（agentcore `growth/execution-planner.ts`·纯函数·消费 L1-A 下游投影）综合的每个节点 ∈ 真注册表、图 DAG 无环、Skill/Agent 择优确定性、覆盖门<0.8 诚实回落模板（绝不产非法图）**」结构不变量——门层坐实 + 自证有牙齿（green→red）。五道断言：A· **真需求图→综合图**：`validateExecutionGraph.ok`（DAG 无环 + 每 `invoke_solver.solverKey ∈ datacore REGISTRY_SOLVER_KEYS` 真牙齿 + `resolve_slice.sliceKey ∈ 白名单`）· solver 节点串成管线（Ch10.13·后一 depends_on 前一）· slice 入度0 并行前沿（Ch10.12）· 非回落；B· **R6 双跑字节一致**（同 reqGraph+注册表+`generatedAt` → 同 JSON·无时钟/随机/迭代序不稳）；C· **覆盖门 <0.8 → 诚实回落模板**（`fromLinearPlan`·`isFallbackGraph`·合法图·往返无损·`diffPlannerShadow.fellBackToTemplate`·绝不产非法图·永不误红）；D· **Skill Match/Agent Assign 确定性**（`scoreSkill` 历史/成本因子中性 **1.0**·诚实不伪造·NG5 · bound skill scenarioMatch 择优胜出 · Agent 能力交集匹配 · 双跑同选）；E· **牙齿测谎**：①幽灵 solverKey 被 filter 剔除（不入图）②综合图注入环→`validateExecutionGraph` 红③无可综合节点→回落（非非法空图）。门内自造合法/非法夹具用被测函数跑（同 requirement-graph:check 牙齿范式）·contracts 不可 import datacore 故 solverKey∈registry 真牙齿在门层读 datacore dist 对账。**影子接线（STAGE-0 shadow ONLY·暗发 `QOS_EXEC_PLANNER=shadow`·additive 可回退）**：orchestrator `runPathA` 内 `resolvePlanForIntent` 后 `runWorkflowSteps` 前旁路影子跑（判决/路由/answer 地位不换手·全 try/catch 吞·不发 SSE 帧·不设 graph=synthesized·serve 归 WO-L1B-5）·`PlannerShadowRecord` 落 in-process sideband（`getPlannerShadow`）+ best-effort 搭车既存 `PreAnalysisReport.planner`（不新建报告·零迁移）。关 `QOS_EXEC_PLANNER`=连影子都不跑·同问句 answer 与改造前逐字节一致（NG6·可证回退）。**serve 翻闸接线（WO-L1B-5·`QOS_EXEC_PLANNER="serve"`·暗发 additive 可回退）**：orchestrator serve 分支——**STAGE-1**（无模板 fall-through intent·综合出真可执行图·覆盖门≥0.8·非回落 → 服务综合图跑 `runWorkflowDag`/线性 parity·留痕 `resolvedRefs:[{kind:"plan",key:served.graphId}]`·**graphId=eg_ 非模板 plan_**）+ **STAGE-2**（`QOS_PLANNER_WHITELIST` 白名单 intent·综合图替换模板·有模板则 parity·摘除白名单=秒级回退）·`trySynthesizeServeGraph` 覆盖门<0.8/回落/异常→保持模板/fall-through（诚实·不 degrade·全 try/catch）。**判决地位不换手**（NG6·`resolvePlanForIntent` 仍是判决态·serve 只在其后 additive 翻图）·缺省 OFF/shadow=不 serve（== 改造前）。齿 `apps/agentcore/test/execution-planner.test.ts`（C1 综合图节点∈注册表+拓扑无环+管线+并行·C2 R6 双跑·覆盖门回落·Skill/Agent 择优确定性+历史中性1.0·C3 影子期 answer 逐字节一致·C4 divergence 按 intent 聚合·关 env 无影子）+ `execution-planner-serve.test.ts`（WO-L1B-5·C1 serve ON 真翻闸=COMPLETED+综合图 graphId(eg_)·C2 关闸=PLAN_NOT_FOUND·C3 shadow 不翻闸·真编排真求解器派发）· 证据 `docs/evidence/WO-L1B-4-fde.md` + `docs/evidence/WO-L1B-5-fde.md`· `scripts/check-execution-planner.mjs`，`pnpm execution-planner:check`。
- **`decision-kernel:check` 决策内核不造假门（L2 决策内核·KILL-MOCK-RED·R6·PRD-L2 §7·并入 `pnpm gates`）**：守决策制品 `DecisionPackage`「每数字有 `provId`·方案键 ∈ 真求解器（`SCENARIO_SOLVER_KEYS`）·反事实引擎 ∈ 白名单（`CF_ENGINES`/契约 `CfEngine`）·热路径无 `Math.random`/`Date.now`/`new Date`（R6）·双跑字节一致·entitlement 注册·env 暗发闸」。静态哨兵：① `kernel.ts` 有 `assembleDecisionPackage`/`validateDecisionPackage`·契约 `DecisionPackageSchema` 在位；② 热路径无非确定来源；③ prov 铸用 `hashString(canonicalJson)`（非 ULID·R6）；④ 白名单常量与契约对齐；⑤ 暗发双闸 datacore `config.ts` `QOS_DECISION_KERNEL` + `features.ts` `decision.kernel`（defaultOn:false·RL2）；⑥ migration 带 down（RL9）。动态测谎（真跑 dist·green→red）：⑦ 真组 `DecisionPackage`→`validateDecisionPackage` 零违例；⑧ 注入幽灵方案/无 provId 数字/越白名单引擎→必报（门有牙）；⑨ R6 双跑字节一致。断点 §8 G-2/G-3。`scripts/check-decision-kernel.mjs`，`pnpm decision-kernel:check`。
- **`decision-case:check` 企业记忆案例不造假门（L1.5 CBR·KILL-MOCK-RED·R6·PRD-L1.5 §0.6/§7·并入 `pnpm gates`）**：守案例投影「特征键 ∈ 真实注册表（零幽灵）·检索/投影确定性·SEED 诚实标·契约漂移」。静态哨兵：① `memory/decision-case.ts` 有 `projectCase`/`extractFeatures`/`validateCaseFeatures`/`pseudoEmbed`；② 热路径无非确定来源（now 必注入·R6）；③ `projectCase` PROBLEM_CLASS 经 `problemClassForIntent`（∈ `INTENT_PROBLEM_CLASS`·零幽灵）；④ 契约 `cbr.ts` 有 `DecisionCaseSchema`/`SimilarityQuerySchema`/`FeedbackSignalSchema`/`DecisionPatternSchema`/`AdaptationProposalSchema`；⑤ 暗发 env 闸 datacore `config.ts` `QOS_CBR_INGEST`/`QOS_CBR_RETRIEVAL`/`QOS_CBR_RERANK`；⑥ migration 带 down（WO-L1.5-2 后生效）。动态测谎（dist·green→red）：⑦ 真造案例→`validateCaseFeatures` 零违例 + 每 PROBLEM_CLASS 键 ∈ 注册表；⑧ 注入幽灵 PROBLEM_CLASS→必报（门有牙）；⑨ R6 同 `DecisionArtifact`+注入 now 双跑字节一致。断点 §8 G-3b。`scripts/check-decision-case.mjs`，`pnpm decision-case:check`。
- **`feature-parity:check` 跨系统暗发键双注册一致门（堵根门·防"未注册键恒真陷阱"·系统性根治 L2-1/L1B-3 同类复发·并入 `pnpm gates`）**：守**跨系统暗发 feature 键**在 DataCore `features.ts`（权威源）↔ AgentCore `registry.ts`（镜像）双注册一致——任一侧缺注册、或 `level`/`defaultOn` 不一致 → 门红 EXIT=1。**根因**：`agentcore/src/features/registry.ts` `featureEnabled` 的 `if (!def) return true`——BY_KEY 查不到该键即**恒真 fail-open**（暗发门恒开关不掉）；反向只注册 AgentCore 而漏 DataCore 权威源 → resolved set 永不含键 → 端点恒 404。两类此前只靠人肉复验抓（L2-1 缺 agentcore 侧 `decision.kernel`·L1B-3 缺 datacore 侧 `qos.workflow_dag`）。**比对集派生·非手工策展**（复验根因：手工清单正是漏键根源——曾漏 `growth.requirement_graph`/`growth.hidden_req`/`sim.sandbox_render`/`qos.workflow_saga` 等 fail-open 键）：`enforced = ① AgentCore registry 里 BLOCK/defaultOn:false 键（镜像集）∪ ② AgentCore 源码真消费键（`isEnabled`/`featureEnabled` 第二实参字面）∩ DataCore BLOCK/off 键`。①对齐每个镜像键与 DataCore 权威；②即便 agentcore **删注册但仍消费**（真 fail-open）也被逮。pending 天然处理：DataCore-only 的 BLOCK/off 键（agentcore 既未注册也未消费）不入 enforced → 不误红；agentcore 注册或消费即自动纳硬校验。判红：enforced 键任一侧缺注册、或 `level`/`defaultOn` ≠ {BLOCK,false} → EXIT=1。有牙（KILL-MOCK-RED·green→red·内建自测含"删 agentcore 消费键注册仍消费必红"根治齿 + 真删验证）：删任一侧注册 / 篡改 `defaultOn`·`level` → 门必红；datacore-only 本地键删不误红；补回 → 绿。断点 §8（堵未注册恒真陷阱类回归）。`scripts/check-feature-parity.mjs`，`pnpm feature-parity:check`。
- **`closure-honest:check` 闭包/verify 据实不空壳判绿门（WO-DB-CLOSURE-HARDEN·KILL-MOCK-RED 核心形态·堵 §8 G-BUILD-SHELL/G-BUILD-VERIFY·并入 `pnpm gates`）**：守数据构建发动机两洞——**洞C(G-BUILD-SHELL)**：`closure.ts validateClosure` 旧仅读 `domain` 存在=空壳判绿（domain 已分配但切片真跑解析 0 节点仍 gatePassed=true）→ 硬化：调用方（`service.ts run()` 物化后·仅纳"应物化"类型）传 `resolvedCounts(typeKey→真实节点数)`·domain 有值但解析 0 节点 → `OBJECT/FAILED`→gatePassed=false（物化前 dry gate / counts 缺省→行为不变·未知不误红·向后兼容）。**洞D(G-BUILD-VERIFY)**：`service.ts buildStoryValidationTrace` NUMERIC_PROVENANCE 旧写死 PASS·从不看 answer/evidence → 硬化：终态 `verifyBuild` 带 `evidence/answer` 重算·`BUILD_STATIC`（未过 QOS 运行时·兜底直调）或答案无数字 → NUMERIC_PROVENANCE **FAIL**·verdict 非 ALL_PASS（RUNTIME_PROBE+含数字→PASS）。有牙（green→red·内建动态测谎）：真跑 `validateClosure` domain 有值+counts{0}→gatePassed=false+OBJECT/FAILED（回潮即门红·已真删验证）·counts{5}/缺省→true。齿 `datacore/test/closure-slice-hardening.test.ts`（洞C 4 例）+ `a10-build-verify.test.ts`（洞D BUILD_STATIC→NUMERIC FAIL / RUNTIME_PROBE→PASS）。**诚实边界**：crossValidation 同义反复（自值对自值恒 CONSISTENT）+ "按 LLM 链路真生成 paths" 部分依赖 WO-DB-LINK-STABILIZE·本单先落 link-无关据实硬化（洞C 非空判 + 洞D NUMERIC 据实）·crossValidation 语义反复留后续（需真实结论断言源·耦合链路稳定）。`scripts/check-closure-honest.mjs`，`pnpm closure-honest:check`。
- **`comprehend-nofloor:check` 建域硬依赖 LLM·不静默地板降级门（WO-DB-LLM-REQUIRED-NO-FLOOR·KILL-MOCK-RED·堵 §8 G-COMPREHEND-FLOOR·并入 `pnpm gates`）**：守 `comprehendPlanBody`（service.ts）三条静默降级路（无 LLM / catch 吞错 / 0 对象 → 落 7 词电池词表 `comprehendScript` 地板·把冷链故事硬套成电池味 Order/Process/Base）**全断**——改诚实结构化报错：无 LLM→`LLM_PURPOSE_UNBOUND`、0 对象→`COMPREHEND_NOT_UNDERSTOOD`、瞬时失败重试仍败→`LLM_UNAVAILABLE`（经 dry_build→record 诚实 surface 为 BLOCKED gapReport 缺口·不产任何电池味垃圾域）。地板降为**显式开关** `DC_COMPREHEND_DETERMINISTIC=1`（仅 CI/离线信创·产物标 `comprehendedBy:"FLOOR"` 诚实非真理解）·真 LLM 理解标 `comprehendedBy:"LLM"`。有牙（green→red·真删验证）：把「静默 catch 吞错 → return comprehendScript 地板」旧模式改回 → 门红。齿 `datacore/test/nofloor-comprehend.test.ts`（FLOOR/LLM 标注·0 对象→NOT_UNDERSTOOD·LLM 失败→不产电池域）+ 6 个测 LLM 路用例置 `DC_COMPREHEND_DETERMINISTIC:"0"`（makeApp 默认 "1" 走地板保测试确定性绿）。**诚实边界（代价·用户已择"诚实>可用"）**：CI/demo 须绑 LLM（seed KIMI_API_KEY 已自动绑 comprehend）或置 flag·离线信创无网丧失建域能力（诚实报错·非静默）。`scripts/check-comprehend-nofloor.mjs`，`pnpm comprehend-nofloor:check`。
- **`link-stabilize:check` 链路派生稳定·切片非空 hops 门（WO-DB-LINK-STABILIZE·洞E·KILL-MOCK-RED·R6·堵 §8 G-BUILD-LINK·并入 `pnpm gates`）**：守建域 comprehend 切片 `hops` **不再恒 `[]`**（旧单根空切片·多跳偶发断链·下游结构恒成立比随机更严重）——`comprehend.ts deriveSliceHops` 从对象 `refToTypeKey`（+ **FK 名启发兜底**补 LLM 偶发丢的链·rows-free 结构+命名·非值域）建链图 → 经现成 `slice-planner.planSlice`（真 BFS 多跳·确定性·H3 复用非重写）派生真实 hops·LLM 路 `deriveBStack` 与地板路 `comprehendScript` **共享**（R6 一致）·供 CLOSURE-HARDEN 的"按真链路生成非空 paths"收口。有牙（green→red·内建动态测谎）：显式 ref→非空 hops；LLM 丢链 + FK 兜底开→补回非空（门绿）；**关兜底 + 丢链→空**（门红·证兜底真生效非死板）。齿 `datacore/test/link-stabilize.test.ts`（① 显式 ② FK 补回 ③ 关兜底空 ④ R6 多跳）。**诚实边界**：comprehend 期 rows 未生成故用**结构+命名启发** FK（非 `detectFkCandidates` 值域 uniqueRate≥0.95·那需 rows·属数据先行/上传路后续增量）；覆盖度=切片 hops 非空判（空 hops 经 CLOSURE-HARDEN 洞C 物化后再据实翻红·二门互补）。`scripts/check-link-stabilize.mjs`，`pnpm link-stabilize:check`。
- **`modeling-wire:check` 故事发动机接 A3·数据先行门（WO-DB-MODELING-WIRE·KILL-MOCK-RED·堵 §8 G-6·并入 `pnpm gates`）**：守故事发动机 `run()` 给 `fromDatasetIds`（已上传 rawDataset）时 objectTypes/链路从**真实列/FK** 经 A3 `deriveModelingSuggestion`(+`detectFkCandidates` uniqueRate≥0.95/containment≥0.9·无 LLM·R6)派生（非 LLM 凭空造类型·此前"故事路零引用 A3·只 /admin/modeling 独立用"）；`refToTypeKey` 真链随即经 `deriveSliceHops` 派生 hops（与 LINK-STABILIZE 一致）。契约 `BuildRunBody`/`StoryRunRequest`+`fromDatasetIds`·端点透传·`service.deriveObjectTypesFromDatasets`。齿 `datacore/test/modeling-wire.test.ts`（真上传 FK-CSV→objectTypes/主键/`refToTypeKey` 从真列/FK·不给 fromDatasetIds 对照走脚本地板·green→red）。缺省=故事先行（旧行为不变）。`scripts/check-modeling-wire.mjs`，`pnpm modeling-wire:check`。
- **`bstack-derive:check` B 栈按故事真派生门（WO-DB-BSTACK-DERIVE·洞B·KILL-MOCK-RED·R6·堵 §8 G-8·并入 `pnpm gates`）**：守 comprehend B 栈（workflow/agent/skill）**随故事复杂度真派生**（非模板 fan-out）——`comprehend.ts deriveWorkflowSteps`（多作用域类型→加 `resolve_slice`·有入参→加 `bind_args`）/`deriveAgentPrompt`（systemPrompt 含 script 摘要 + 作用域对象·非纯 solverKey 模板）/`deriveSkillResources`（真作用域切片·非 `[]`），LLM 路 `deriveBStack` 与地板路 `comprehendScript` **共享**（R6 一致）。**沙盘划界（钉死·防双写 contracts/databuilder.ts:437）**：绝不派生 `propagation_rule`/`state_var`（S0/RequirementGraph/S1 的活）。有牙（green→red·动态测谎）：两复杂度悬殊故事→workflow.steps/agent.systemPrompt 字节相同→红。齿 `datacore/test/bstack-derive.test.ts`（① 悬殊两故事字节不同 ② resolve_slice/skill 资源 ③ prompt 含故事 ④ 沙盘划界空 ⑤ R6）。`scripts/check-bstack-derive.mjs`，`pnpm bstack-derive:check`。
---
