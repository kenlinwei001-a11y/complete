# 开发提示词 · 统一推演控制台页签式重做

> 配套 `docs/PRD-sim-console-tabbed-20260911.md`。
> **每张单可直接整段复制给 dev。** 已按本仓踩过的坑内置全部前置与止损。

---

## ⛔ 派发纪律 · 一个 dev 只做一张（本节先于所有单）

**⛔ 不许把本文件整份交给一个 dev 让他从头做到尾。** 收到本文件的 dev，
**只做派单人点名的那一张**，其余一律不碰。三条理由，都是本仓真实踩过的：

1. **有两张明令不许开工** —— WO-4（待前置 A · R17 裁决）与 WO-7（待前置 B · 禁令 3 豁免）。
   把它们和可开工的单放在同一份文件里交给同一个人，**本身就是在制造越界的机会**。
2. **有两张是续跑单，全套提示词会诱导重开** —— WO-1 与 WO-6 的代码**已落盘**
   （8 文件 +420 / 5 文件 +316，零未提交），只差把测试跑完。故它们只在文末「附」里各三行。
   铁律 1.6 原话：**「续跑不是重开」**，而续跑最贵的不是剩下那 10%，
   是**它回去重新确认一遍前面那 90%**。
3. **画像冲突** —— WO-2 是**重画像**（跑 datacore vitest，约 1 小时），
   CLAUDE.md 硬约束「跑 datacore vitest 的单同时 **≤1**」。
   一个 dev 串着做完全部，光测试就是大半天；而本仓刚因负载 94 + 磁盘满，
   让三张单的全量各挂了三次。

### 建议分派（文件边界已核，零重叠，可同时开工）

| 派给谁 | 单 | 画像 | 🚦 文件边界 |
|---|---|---|---|
| Dev A | **WO-2** 推演金额诚实位 | 重 | `datacore/src/solvers/finance-world.ts` · `datacore/src/sim/` · `packages/contracts/src/` |
| Dev B | **WO-3** 热力图 | 中 | `frontend-shell/src/views/sim/unified/` |
| Dev C | **WO-5** 投料比例 | 轻 | 只读（大概率只产出一份缺口文档） |
| 审核方自收 | WO-1 · WO-6 | — | 只差复验，**⛔ 不要另派** |
| **不派** | WO-4 · WO-7 | — | **等仓主裁决** |

### 给每个 dev 的话（一句，随本文件一起给）

> 两份 MD 都读。**只做 WO-X 这一张**，§0 共用头对你同样适用。
> 其余几张别碰 —— 有两张正在别人手上，有两张没拿到开工批准。

---

## 0 · 所有单共用的头（复制时一并带上）

```
## ⚠ 开工第一段（不许跳，写不出来别动手）

我给的 file:line 与状态是**线索不是结论**。开工第一件事是把那几行的**原文读出来**，
各写成一句「**今天的行为是 X，应该是 Y**」。**写不出来别动手。**
若实测发现**已经做了** —— **停手，把证据（file:line + 触发条件）顶回来，改台账不改代码。**

⚠ 这一条不是客套。本仓真实发生过：判某道门今天做不了，实际那个函数早已存在且接了两处，
把工作量从「接一条线」错报成「造一道门」，直接歪掉排期。
同族刚发生过两次：① 前端注释写「压力→金额的系数全平台没有登记册」，实测
`finance_world_projection` 一直在算；② 屏上写「一拍等于几天全平台没有登记」，
实测 `tickDays`/`createdAt` 就在同一个 schema 上。**先查再说「没有」。**

## 开工第一条命令（铁律 1 · 本仓因未推丢过整天产出，容器一天回收四五轮）

CANON=origin/claude/inspiring-gates-aqczjg
git fetch origin
git rev-list --count HEAD..$CANON      # >0 就是落后，必须重开（⚠ 不许用「某文件在不在」判断）
git checkout -B <wo-branch> $CANON
git commit --allow-empty -m "WIP" && git push -u origin HEAD:refs/heads/claude/handoff-<wo>

之后**每改完一个文件就 commit + push 一次** —— 不是「写完一个功能」，更不是「测试跑绿了再推」。

## 环境前置（都被 dev 顶回来过）

pnpm install --prefer-offline
pnpm --filter @platform/contracts build && pnpm --filter @platform/llm-adapters build   # 两个都要

不装会报 `Failed to resolve entry for package "@platform/contracts"` 或 `vitest: not found`
这类**与本单无关的假红**。跑单文件用 `npx vitest run test/<file>`
（⚠ `pnpm --filter <pkg> test -- <pattern>` 的 `--` **不过滤，会跑全量**）。

## CPU 避让（4 核机，本仓因并发跑崩过）

数**「父进程不是 vitest 的 vitest 进程」** —— 每棵进程树只算它的根。
**双向金丝雀必须都过**才准拿它做决策：空闲报 **0**、单 run 报 **1**。
任一不过 ⇒ 报「量法坏了」，⛔ 别拿它避让。
（错法示范：数进程表里含 vitest 的行数 —— 实测 14 行 = 2 个 run，差 7 倍。）

## 端口（若要起服务）

本机**没有 `ss`/`netstat`**。端口可用性**唯一可靠判法是真去 bind**；
起完用 `lsof` 取属主 pid 并核 `/proc/<pid>/cwd` 是你的 worktree ——
⛔ 别读了别的 agent 的旧服务再对自己的代码下结论（本仓踩过）。
**不信「HTTP 有响应」= 种子灌完**：探到订单簿 `total=500` 才算
（`GET /a/v1/objects?type=Order&page=1&pageSize=1`，⚠ **该端点不认 `limit`，只认 `page`+`pageSize`**），
金丝雀客户 `total=20`。浏览器用 `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` 走 CDP，
**别装 playwright**；**从 `/login` 走起**填 `demo / admin / demo1234`，⛔ 不许手敲 URL。

## 退出码纪律

⛔ 禁止 `cmd | tail -n; echo "EXIT=$?"` —— `$?` 取的是管道末端的码，**恒 0**。
用 `RC=$?` 紧跟命令本身。本仓曾据此把一个编译失败的 commit 判为「BUILD 通过」并入正线。

## 通用止损

⛔ 别重读已核过的坐标、别重跑已经绿的实验、别开新战线。
做不成的**如实写「未做 / 未取到 + 为什么」**，⛔ 不许为了交差把没做的写成做了。
⛔ 不许新增门 / 棘轮 / 基线 JSON（仓主 2026-08-20 禁令 3）。
⛔ 不碰 `docs/SYSTEM-ONTOLOGY.md` §8 与 `docs/REQUIREMENTS-TRACE.md`（禁令 1 停工中）——
   该回写的内容**写进报告**让审核方判。
```

---

## WO-2 · 推演金额的诚实位　★最高优先

**分支**：`wo-sim-money-honesty`　**画像**：重（跑 datacore）

```
本单：关闭 G-DATAMODE-PROV 在推演金额侧的那一半 ——
**让屏上不可能印出一个看起来像真数的假毛利。**

## 今天的行为是 X（实测，非读码）

种子世界 t0 的加权 `costPressure` = **49.62 是哈希占位**（回包 `measuredCells: 0`），
tick3 = 93.10 ⇒ `finance_world_projection` 的毛利绝对投影 = **−422.12（−455%）**。
而**回包没有一格披露这个基线是派生占位** —— 实测 `measured` / `derived` / `hash` / `占位`
四个词在整个 payload 里**零命中**。

## 应该是 Y

1. 回包**逐项透出基线来源**（实测 / 派生 / 占位），字段名与形状你定，但必须能被前端逐格读到。
2. 占位基线时，**绝对投影不可用** —— 由后端明确标记，不是让前端自己猜。
3. **Δ（相对基线的变化）在两种情况下都可用** —— 它不受基线绝对值影响。

## 为什么这是最高优先

前端若直接摆绝对值，**屏上会印出一个 −422 亿的毛利，而它看起来和真数一模一样**。
这比「算不出来」坏得多 —— 后者不说谎。
本体 R13 原文：「推演输出绝不裸渲染当真值」；断点 `G-DATAMODE-PROV` 自述
「**合成数据冒充 LIVE/实测** · KILL-MOCK-RED 命门」。本单是它在金额侧的实例。

## ⛔ 对照实验（铁律 1.5 判据一，写不出这条本单没法验收）

> **同一次推演，基线为「占位」与基线为「实测」两种情况下，回包必须不同：
> 占位时绝对投影被标为不可用，实测时可用；而两种情况下的 Δ 必须相同。**

跑出**四个数**（两种基线各给「绝对投影是否可用」与「Δ 的值」），缺一个不算交付。
⚠ 第二半（Δ 相同）同样重要：只验前一半的话，把 Δ 也一起禁掉也会通过。

## 顺带必查一条

`apps/datacore/src/sim/drill-orchestrator.ts:283` 那个热力图载荷的形状里带着
`dataMode: "MOCK"`。**确认它在生产路径上是什么值**（断点 `G-MOCK-OVERCLAIM`）。
查到什么写什么，⛔ 不许推断。

## 交付判据（缺一条不算交付）

1. 对照实验四个数（真后端 `SEED_DEMO=1`，⛔ 禁 mock）
2. **接缝测试**（SEAM-GATE）：跨「基线来源标注 + 金额投影」两半的组合测试，
   在集成态断言端到端行为，⛔ 不许只测各半 unit
3. `pnpm --filter datacore test` **RC=0**（对照：356 文件 / 2462 用例）+ `typecheck` RC=0
4. **变异反证**：把基线来源标注改成恒「实测」⇒ 测试必须当场红；改回 ⇒ 绿。两次 RC 写进报告
5. 报告头回显 `base commit` + 取证时刻 + `wc -l apps/datacore/src/synthetic/battery.ts`
6. 报任何否定结论时**同时给金丝雀的命中证据**

## 🚦 范围边界

只许改 `apps/datacore/src/solvers/finance-world.ts` · `apps/datacore/src/sim/` ·
`packages/contracts/src/`，测试放 `apps/datacore/test/`。
⛔ **一行前端都不许改** —— 前端消费那一半是下一张单。
```

---

## WO-3 · 热力图接线 + 因子诚实标注

**分支**：`wo-heatmap-factors`　**画像**：中

```
本单：把「基地 × 影响因子」热力图接上真数据，**并在屏上如实标出哪几个因子能动、哪几个只能看**。

## 今天的行为是 X

设计稿画了 13 基地 × **9** 维度 —— **那 9 是我编的**。
实测因子是 **7** 个（设备OEE / 物料齐套 / 良率波动 / 人力工时 / 瓶颈工序 / 物流时长 / 换型损失），
「交付」「成本」两列是画稿时加的。

而本体 §8 断点 `G-CAPACITY-FACTOR-SHALLOW` 说的比这更狠（**读它的原文**）：
> 20 因素本体**宽而浅** —— `factorOntology.ts:47-74` 无 object.property 绑定；
> 仅 `LEVER_FACTOR_PROPS` **4 个因子**有可写属性、`bottleneck_matrix` **7 个因子**有真张力，
> **~13/20 是纯展示徽章**。

## 应该是 Y

1. 热力图**只画有真张力的那几个因子**（数目现算，⛔ 不许写死 7 或 9）
2. **逐列标出「可动 / 仅可观察」** —— 可动 = 在 `LEVER_FACTOR_PROPS` 里有可写属性
3. 数据源是 `risk_timeline.factorSeries`（`apps/datacore/src/solvers/risk.ts:773`），
   逐基地逐因子 30 天序列

## ⛔ 明令

**不许为了凑满一行把纯展示徽章也画成一列。**
那会让 COO 以为那些维度也能动 —— 而它们连可写属性都没有。
少画几列是诚实，画满是说谎。

## 判据

- 报「共 N 个因子」之前**跑金丝雀**：拿一个你确定有真张力的因子，验证抽取法能命中它；
  命不中就报「**量法坏了**」，⛔ 不许报「只有 N 个」
- **对照实验**：把某个因子的 `LEVER_FACTOR_PROPS` 绑定去掉 ⇒ 它在屏上必须从「可动」变「仅可观察」；
  加回来 ⇒ 变回去。四个数（改前/改后各给「该因子标记」与「可动因子总数」）

## 交付判据

1. 真后端 + 真浏览器实测截图，证明列数与标注**真在屏上**
2. `pnpm --filter frontend-shell test` RC=0 + `typecheck` RC=0（对照：317 文件 / 2267 用例）
3. 门不比基线多咬红：`check-text-legibility` · `check-ui-first-layer` · `check-dev-jargon-onscreen`
   （基线名单自己先跑一遍取，⛔ 别抄别人给的数）

## 🚦 范围边界

前端：`apps/frontend-shell/src/views/sim/unified/`；后端只读，若需要后端改动 ⇒ **顶回来**。
```

---

## WO-5 · 在产订单「已投料比例」缺口登记

**分支**：`wo-wip-ratio-gap`　**画像**：轻（只读 + 写文档）

```
本单大概率**做不成，那也是合格交付** —— 要的是把缺口钉死，不是把它填上。

## 今天的行为是 X

订单状态闸门已落地（`isOnHandOrderStatus`），已完成单不再被涨价推动。
但 `IN_PRODUCTION` 的 **100 张单仍按全额推** —— 而它们已投料的那部分成本同样锁定了。

## 应该是 Y

按「已投料比例」分档。

## ⛔ 但先查清楚这个数存不存在

前一个 dev 实测：「已投料比例」**全仓零出处**（6 个词 0 命中；
金丝雀 `outsourceRatio` 20 命中 ⇒ 搜法是活的）。

**你要做的第一件事是复核这条**，换你自己的词表再扫一遍：
投料 / 领料 / 已耗 / WIP / 在制 / issuedQty / consumedQty / pickedQty …
⚠ 扫法必须**类型名命中 ∪ 字段名命中**两条并集 —— 本仓有过教训：
`ChangeoverMatrix` 的数值格叫 `minutes`/`hours`，名字里一个「换型」都没有，
只扫 propKey 会得出「全本体没有任何换型格」这个与事实相反的结论。

## 两种结局，都要给

- **找得到** ⇒ 接上去，并给对照实验（同一张在产单，已投料 0% vs 80%，
  `costPressure` 必须按比例拉开；四个数）
- **找不到** ⇒ **如实登记缺口**：缺哪个字段、该挂在哪个对象上、谁能补、
  补之前这 100 张单怎么办。⛔ **不许编 0.5** —— 那是 R14/RL5 明禁的内联业务常数。

## 🚦 范围边界

找得到才改 `apps/datacore/src/`；找不到就**只产出一份缺口登记文档**。
```

---

## WO-4 · 补审批出口　⚠ 待前置 A 裁决

**分支**：`wo-sim-approval-exit`　**画像**：中

```
⛔ **本单依赖 PRD §0 前置 A（页签式与 R17 的冲突裁决）** —— 出口放在哪个页签取决于它。
   未裁决前**不得开工**。

## 今天的行为是 X（实测）

`Console0828.tsx` 与 `UnifiedSimShell.tsx` 的 `useActionDraft` / `/a/v1/decisions` /
`ActionDraft` **三项全 0**；金丝雀 `SandboxView.tsx` 同项为 **2 / 2** ⇒ 搜法有鉴别力，那些 0 是真 0。
⇒ 四个推演页里，**唯独统一推演控制台算完了没有出口** ——
用户看完 18 处受阻环节、4 套改法，**没有任何一个按钮能把决定落下去**。

## 应该是 Y

选定对策 → 生成 `ActionDraft` → 走 S2 审批链。

## 本体依据（这不是新增能力）

不变量 **R4-sim** 原文：
> 仿真结论要生效**必须**生成 Action 提案走 R4 正门 —— **「采纳」是唯一出口**。

⇒ **本单是补上不变量已经要求的东西，不是加功能。**

## ⛔ 三条边界一条都不许越（R4-sim 原文）

① 仿真世界**不得回写真实世界**（fork 必产生新行，真实世界那一行逐字节不变）
② 仿真结论要生效**必须**生成 Action 提案走 R4 正门
③ 仿真快照的每条指标 `source.kind` 必须标 `FORKED`，**不许自称「从对象库现场数出来的」**
   —— 世界隔离的头号风险是**出处串**，不是数据串

## 对照实验

> 在控制台采纳一条对策 ⇒ ① 产生一条 `ActionDraft`（状态非 EXECUTED）
> ② **真实世界那一行逐字节不变** ③ 审批通过后才落真值。

四个数：采纳前/后各给「ActionDraft 条数」与「真实世界行的 hash」。
⚠ 第 ② 半最要紧：只验「产生了草案」的话，直接写真值也会通过。
```

---

## WO-7 · `sim.*` 门控解耦　⚠ 待前置 B 豁免

**分支**：`wo-sim-gate-decouple`　**画像**：中

```
⛔ **本单新增 2 个 entitlement 键，落在仓主 2026-08-20 禁令 3 的字面范围内。
   未获定向豁免不得开工。**

依据：`docs/evidence/SIM-ENTITLEMENT-DECOUPLE-20260911.md` **方案 A**
（能力闸留原名 `sim.sandbox` + 新增两个 `view.*` 页面闸）。

## 为什么是 A 不是 B（改名）

A 拿到 B 的全部收益，却**完全不碰「键名字符串」这唯一一处不可逆的东西**。
B 要动 **111 处字面量 / 53 个测试文件 / 13 个门脚本**，而 typecheck 一个都看不见；
更要命的是**静默反转线上租户开关** —— 写路拒未知键、读路不校验，
刻意关掉的租户会被 L2 模板悄悄重新打开。

## 必须同时修的一个洞（否则拆了门也没用）

实测：`App.tsx:160` 的 `v/sim-unified` **一道 Guard 都没有**，
`ShellLayout.tsx:384` 的导航条目**不带 `feature`**（`:379–383` 注释说明这是刻意的，
因为页面侧没 Guard）。两个决定各自有道理，**合起来 = 控制台在 entitlement 上完全无保护**。
⇒ 关掉开关时，用户看到的不是「未开通」，是**一个坏掉的页面**。

## 对照实验

> 关掉新的页面闸 ⇒ 沙盘那一页 404，**而控制台照常工作**；
> 关掉能力闸 `sim.sandbox` ⇒ 两者都不可用，**且控制台显示「未开通」而不是报错**。

四个数：两种关法各给「沙盘可达性」与「控制台可达性」。

## ⛔ 不可逆风险

`featureConfigs` 存的是**键名字符串**。改键名会打掉既有租户的 override ——
方案里必须有**迁移路径**，⛔ 不许只说「改个名」。
```

---

## 附 · 两张在途单的续跑提示词（只差复验）

```
⛔ 这是续跑单，不是新单。**别重开、别重读已核过的坐标、别重跑已经绿的实验。**

WO-1 客户集团层：
  git checkout -B wo-customer-group origin/claude/handoff-wo-customer-group   # tip 88993fc0
  停在哪：datacore 全量跑到 42/359 时被配额 429 掐断，**零断言失败**
  还差：① rebase 到最新 canonical ② 补跑 `pnpm --filter datacore test` ③ 写报告
  对照实验（必须有）：改广汽埃安敞口 X ⇒ 广汽集团同步变 X，**且小鹏所在集团纹丝不动**（四个数）
    ⚠ 只验前一半不够 —— 全连成一个集团时前一半也会通过

WO-6 导航命名 + 衰减上屏：
  git checkout -B wo-nav-decay-honesty origin/claude/handoff-wo-nav-decay-honesty   # tip f90b2734
  停在哪：等自己的前端全量跑完时被配额 429 掐断
  还差：① rebase ② 补跑 `pnpm --filter frontend-shell test` ③ 写报告
```
