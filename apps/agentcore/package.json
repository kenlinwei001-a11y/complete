{
  "name": "agentcore",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:test": "tsc -p tsconfig.typecheck.json --noEmit",
    "typecheck:src": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run",
    "start": "node dist/main.js",
    "migrate": "node dist/migrate-cli.js",
    "smoke:llm": "pnpm run build && node dist/scripts/smoke-llm.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@fastify/cors": "^11.2.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@platform/contracts": "workspace:*",
    "fastify": "^5.3.0",
    "openai": "^6.42.0",
    "pg": "^8.16.0",
    "pino": "^9.6.0",
    "zod": "^4.0.0",
    "@platform/llm-adapters": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
